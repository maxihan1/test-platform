# spec-review — 체크리스트 A~C

## 체크리스트

### A. 계약 위반 — 치명

한 곳이라도 깨지면 병렬 작업 전체가 어긋난다.

| # | 검사 | 통과 조건 |
|---|------|----------|
| A1 | `packages/kit/src/types.ts` 변경이 **SPEC §5.1에 적힌 것과 일치**하는가 | 일치 (또는 변경 없음) |
| A2 | `db/migrations/` 변경이 **SPEC §6에 적힌 것과 일치**하는가 | 일치 (또는 변경 없음) |
| A3 | `docker-compose.yml` **과 `apps/*/Dockerfile`** 변경이 **SPEC §9에 적힌 것과 일치**하는가 | 일치 (또는 변경 없음) |
| A4 | 러너 HTTP 요청·응답이 `ExecuteRequest` / `ExecuteResponse`와 필드까지 일치하는가 | 일치 |
| A5 | Admin API 경로와 응답 형태가 SPEC §7과 일치하는가 | 일치 |
| A6 | DB 컬럼명이 SPEC §6과 일치하는가 | 일치 |

**A1~A3은 2026-09-17부터 "변경 없음"이 아니라 "SPEC 대로인가"를 본다.** 개정 SPEC 이 세 파일의 변경을
이미 승인했고 훅의 잠금 목록(`guard.mjs`의 `LOCKED`)도 그날 비웠다. 이제 변경되는 것이 정상이다.

`git diff origin/main...HEAD -- packages/kit/src/types.ts db/migrations/ docker-compose.yml apps/*/Dockerfile` 로 무엇이 바뀌었는지 뽑아
SPEC 해당 절과 **한 줄씩 대조한다. SPEC 에 없는 변경이 하나라도 있으면 치명이다.**
잠금이 사라졌으므로 이 검사가 유일한 방어선이다 — 변경이 없으면 "변경 없음"으로 적고 넘어간다.

### B. 도메인 불변식 — 치명

SPEC §3에 적힌 불변식. 깨지면 데이터가 조용히 거짓이 된다.

| # | 검사 | 통과 조건 |
|---|------|----------|
| B1 | `run_item`의 `params`/`expected`가 실행 시점 **스냅샷 복사**인가, `param_set` 참조인가 | 스냅샷 복사 |
| B2 | `run_item`의 `tc_name`·`precondition`도 스냅샷인가 | 스냅샷 |
| B3 | 러너 코드가 DB에 접근하는가 (DB 클라이언트 import, 커넥션 문자열, SQL) | 접근 없음 |
| B4 | 스캔 시 `tcId` 중복을 검출해 실패시키는가 | 검출함 |
| B5 | 코드에서 사라진 케이스를 삭제하는가, `is_active=false`로 두는가 | false 처리 |
| B6 | 동시 실행 수가 설정값(기본 2)으로 제한되는가 | 제한됨 |
| B7 | Reporting 컨텍스트가 `evidence_document` 외 테이블에 write 하는가 | write 없음 |
| B8 | `platforms` 배열 길이만큼 `run_item`이 생성되는가 | 생성됨 |
| B9 | **작성 자료** (도메인/작성 §7 「자료」 · 공통/4-데이터모델 §6 「작성 자료」) — `authoring_asset` 표의 `CHECK` 가 SPEC SQL 과 같은가 · `DRAFT→PENDING` 은 줄에 세우기 한 문장(`WHERE status='DRAFT'`)만 하고 집기는 `PENDING` 만 집는가 · 자료 통로 셋이 `auth/scope.ts` 라우트표와 `auth/gate.ts` 등급표 **둘 다에** 있는가 · 피그마 주소를 받은 그대로가 아니라 **정규화해 다시 조립한 값**으로 저장하는가 · **작성 에이전트 경로**는 자식이 `/tpx` 가 아니라 `tpx-author` 를 타고 git·PR 은 에이전트 스크립트가 하는가(작성 §3.6) · 테스트만 바뀐 PR·push 는 `.claude/scripts/cases-only.mjs` 판정으로만 가벼운 길을 고르는가 · **`cases-only.mjs` 가 다른 파일을 import 하지 않는가**(작성 에이전트가 그 파일 하나만 복사해 병합 판정에 쓴다) · 훅·CI 의 차선은 `lane.mjs` 한 곳에서 오는가(정본 docs/HOOKS.md 「차선」) | 전부 그렇다 |
| B10 | **미확정 꼬리표** (도메인/작성 §3.6 「★ 역방향 — 화면과 대조한다」 · 공통/2-명세선언 §4 K11) — 화면에서 읽은 값을 기대값으로 쓴 케이스에 `unconfirmed` 꼬리표가 붙어 있는가. **꼬리표 없이 화면 값이 기대값이면 치명이다** — 화면의 버그가 기대값으로 박혀 통과율에 섞이고 회귀 세트가 그 버그를 초록으로 지킨다. 반대로 정방향 작성·사람이 쓴 케이스에 꼬리표가 붙었는가도 본다(K11 은 역방향 작성만 단다). **이미 있는 케이스에 `unconfirmed` 를 새로 다는 diff 는 게이트 2 요약에 싣는다** — 확정 실패를 미확정 묶음으로 옮겨 숨기는 길이다(도메인/실행 §3.2). K11 은 모양만 본다. 이 항목은 늘 사람이 본다 | 화면 값을 기대값으로 쓴 케이스는 전부 미확정이다 · 미확정이 아닌 케이스의 기대값은 기획서나 사람의 판정에서 왔다 |
| B11 | **역방향 비밀값 검사 자리** (도메인/작성 §3.6 「★ 역방향」 「남는 한계」 · LEARNINGS 2026-09-26 두 번 승격) — 작성 에이전트가 밖으로 보내는 것(케이스·PR 본문·`result.diffs`·`outputs` 산출물·피그마 댓글)에서 테스트 계정 비밀번호·피그마 토큰을 찾는 검사가 **push 전에** 있는가 · **마지막 변환 뒤 실제로 나가는 모양**(푼 JSON 값 · 되읽은 문서 구조 · 다시 묶은 docx 의 XML 파트)을 보는가 · 새면 PR 없이 `FAILED` 인가. PR 뒤에 처음 보는 검사가 있으면 **치명** — 새는 것을 알아도 명세대로 FAILED 로 못 끝낸다 | 전부 push 전 · 나가는 모양 · 새면 FAILED |

B1이 가장 자주 깨지고 가장 늦게 발견된다. **매번 최우선으로 검사한다.**

### C. 경계 침범 — 중대

| # | 검사 | 통과 조건 |
|---|------|----------|
| C1 | 이번 작업 범위 밖 폴더를 건드렸는가. 건드렸으면 **계획이나 게이트 2 요약에** 이유가 적혀 있는가 | 이유가 있음 (계획이 없는 0·1등급은 게이트 2 요약만 본다) |
| C2 | Catalog / Execution / Reporting이 서로의 모듈을 직접 import 하는가 | 직접 import 없음 |
| C3 | 러너가 공유 볼륨(`artifacts/`) 외 경로에 파일을 쓰는가 | 쓰지 않음 |
| C4 | 주석이 **다른 파일이 막아 준다**를 근거로 위험을 안 막았으면, 그 파일을 열어 확인했는가 | 확인함 (또는 그런 주석 없음) |
| C5 | 계획의 `files` 에 적힌 파일이 **실제 diff 에 전부 있는가.** 없으면 왜 안 끝났는지 게이트 2 요약에 적혀 있는가 | 전부 있음 (또는 이유가 적혀 있음) |
| C6 | `[계약 변경 필요]` 블록의 `상태:` 를 닫았으면, **그 블록을 아직 `대기` 라 부르는 다른 문서**도 같이 닫았는가 (`grep -rn "상태: 대기" docs/` 와 앞 계획들의 「범위 밖」 표) | 같이 닫음 (또는 그렇게 부르는 곳 없음) |

C1은 `git diff --name-only origin/main...HEAD` 로 변경 파일 목록을 뽑아
계획의 `files` 와 대조한다.

**C5 는 2026-09-21 에 생겼다.** C1 은 **밖으로 나간 것**만 본다 — 안 한 것은 안 본다.
「실행상태」 PR 의 중대 셋이 **전부 같은 모양**이었다: 계획의 `files` 에 있는데 바뀐 파일 목록에 없다.
`design-mockup.html` 의 옛 토큰 · 앞 계획의 사실 아닌 문장 · 킥오프의 「그 파일이 없다」.
셋 다 계획이 「고치겠다」고 적어 둔 자리이고 **아무도 안 고쳤는데 검사가 전부 초록이었다.**

**바뀐 파일 목록을 뽑아 계획의 `files` 와 대조한다.** 빠진 파일마다 이유를 묻는다 —
필요 없어졌으면 계획을 고치고, 잊은 것이면 그 자리에서 드러난다.
**문서는 특히 잘 빠진다** — 코드가 안 돌아 깨지는 것이 아니라 조용히 낡을 뿐이다.

**C6 은 2026-09-21 에 생겼다 (C5 와 같은 유형 2회차).** C5 는 **계획이 「고치겠다」고 적은 자리**만 본다 —
`files` 에 아예 안 올라온 문서는 **C5 의 그물 밖으로 빠진다.**

「절차단위진행」 PR 이 계약 블록 둘을 `철회됨` 으로 닫았는데, **앞 계획**(`실행상태.md` 의 「범위 밖」 표)이
여전히 「그 블록 둘이 `상태: 대기` 로 남아 있다. 다음 PR」이라고 적고 있었다.
그 파일은 이번 계획의 `files` 에 없어 아무도 안 열었다.

**그대로 뒀으면 다음 세션이 「아직 대기구나」로 읽고 이미 접은 설계를 다시 집는다.**
`상태:` 를 닫는 것은 한 파일의 일이 아니다 — **그 블록을 가리키는 모든 문서의 일**이다.
`grep -rn "상태: 대기" docs/` 와 앞 계획들의 「범위 밖」·「다음 PR」 표를 같이 훑는다.

**C4 는 2026-09-19 에 생겼다** (LEARNINGS 「문서가 규정한 것이 코드에 안 걸려 있다」 3회차).
러너의 `abort()` 주석이 "admin이 닫는 UPDATE마다 `finished_at IS NULL`을 붙여 이미 막고 있다"를 근거로
경합을 안 막았는데, 그 가드는 `store.ts` 의 `CLOSE_UNFINISHED` 에만 있고 `finishItem()` 에는 **없었다.**
근거로 든 자리 바로 위 주석이 「언제나 붙인다」라고 규정하고 있어서 그 말을 확인 없이 믿었다.
**같은 파일의 다른 주석이 그렇게 말한다는 것은 근거가 아니다.** 해당 SQL·조건을 눈으로 센다.

**2026-09-18 부터 기계가 안 본다.** `ownership` 훅을 걷어냈다 — 구현은 멀쩡했지만
켜는 자리(`settings.local.json` 의 `env`)를 아무도 안 채워 한 번도 안 돌았고, 쓰지 않기로 정했다.

그래서 이 항목은 「막았는가」가 아니라 **「범위를 벗어난 이유가 어딘가에 적혀 있는가」**를 본다.
벗어나는 것 자체는 금지가 아니다 (`CLAUDE.md §1.1`).

**어디를 보는지는 등급에 따라 다르다.** 계획 파일은 2등급 이상에만 있다.

| 등급 | 어디를 보나 |
|---|---|
| 0 · 1 | 게이트 2 요약 (계획 파일이 없다) |
| 2 · 3 | 계획의 `files` — 구현 중에 늘었으면 게이트 2 요약 |
| 명세만 바뀐 일(`spec` 차선) | 게이트 2 요약 (계획 파일이 없다 — `/tpx` §차선) |
