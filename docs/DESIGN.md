# DESIGN.md — 어드민 디자인 기준

> `docs/design-mockup.html`이 이 문서의 시각적 기준이다.
> WS-E는 목업을 먼저 열어보고 시작한다. 토큰 값은 여기서 그대로 가져다 쓴다.

---

## 컨셉

**대시보드가 아니라 검사 기록지.**

이 화면이 하는 일의 본질은 지표 모니터링이 아니라 **판정**이고,
그 판정이 그대로 제출 문서가 된다. 그래서 시험성적서의 구조 논리
— 항목번호 / 사전조건 / 시험절차 / 판정 — 를 화면 구조로 가져온다.

결과 상세 화면(§화면 3)과 증적 문서 PDF는 **같은 레이아웃**이어야 한다.
화면을 보던 사람이 문서를 받았을 때 다시 배울 것이 없어야 한다.

---

## 원칙 3가지

### 1. 색은 판정만 갖는다
통과·실패·미실행에만 색을 쓴다. 나머지는 전부 잉크색과 종이색이다.
버튼도, 링크도, 강조도 색을 쓰지 않는다. 화면에서 색이 보이면 그건 판정이다.

### 2. 카드 대신 괘선
테스트 결과는 나열이지 독립된 객체가 아니다.
둥근 카드에 그림자를 넣어 쪼개지 말고, 얇은 괘선으로 구분한다.
`border-radius`는 컨트롤에만 2px. 카드 radius와 box-shadow는 쓰지 않는다.

### 3. 숫자는 세로로 맞춘다
TC ID, 소요시간, 응답 코드가 세로로 정렬돼야 훑을 수 있다.
`font-feature-settings: "tnum"`을 body에 건다.

---

## 토큰

```css
:root{
  /* 바탕과 면 */
  --paper:      #E6E9E2;  /* 페이지 바탕. 방안지 느낌의 옅은 회녹색 */
  --sheet:      #F8F9F5;  /* 기록지 면 */
  --sheet-2:    #EFF1EA;  /* 행 hover */

  /* 잉크 */
  --ink:        #17201B;  /* 본문 */
  --ink-muted:  #626C64;  /* 라벨, 보조 */
  --ink-faint:  #8B948B;  /* 힌트, 비활성 */

  /* 괘선 */
  --rule:       #C8CEC3;  /* 구획 경계 */
  --rule-soft:  #DCE0D7;  /* 행 구분, 바탕 격자 */

  /* 판정 */
  --pass:       #0D6A46;  --pass-bg: #E2EDE6;
  --fail:       #A62B21;  --fail-bg: #F6E4E1;
  --na:         #8A7742;  --na-bg:   #F1EBDC;
}
```

바탕에는 28px 격자를 `--rule-soft`로 아주 옅게 깐다. 기록지 면에는 깔지 않는다.

---

## 타이포

**Pretendard 한 종류.** 별도 제목용 서체를 쓰지 않는다.

| 용도 | 크기 | 굵기 |
|------|------|------|
| RUN 번호 | 30px | 800 |
| 케이스명 (상세 헤더) | 19px | 600 |
| 집계 숫자 | 20px | 800 |
| 본문 / 케이스명 (목록) | 14.5px | 400 |
| 입력값, 검증 문장 | 13.5px | 400 |
| TC ID, 라벨, 구획 제목 | 12–12.5px | 600 |
| 판정 배지 | 12px | 800 |

- **대문자 라벨 금지.** 구획 제목도 한국어 문장으로 쓴다 (`사전조건`, `시험 절차`)
- **가운뎃점 나열 금지** (`A · B · C`) — 헤더 메타 한 줄은 예외로 허용
- 버튼 문구에 화살표를 붙이지 않는다
- 버튼은 동사로 끝난다: `실행하기`, `입력값 세트로 저장`

---

## 판정 표기

| 상태 | 배지 | 행 좌측 거터 |
|------|------|------------|
| 통과 | `--pass-bg` 바탕 + `--pass` 글자 | `--pass` 4px |
| 실패 | `--fail` 바탕 + 흰 글자 (유일한 반전) | `--fail` 4px |
| 미실행 | `--na-bg` 바탕 + `--na` 글자 | `--na` 4px |

**과감함은 실패 한 곳에만 쓴다.** 실패 배지만 색 반전이고,
실패한 검증 문장 줄만 `--fail-bg` 배경을 갖는다. 나머지는 전부 조용하게.

목록에서 실패 행은 케이스명 아래에 실패 지점을 한 줄로 덧붙인다.
`2번 절차에서 실패 — 토큰이 발급된다`

## 환경 표기

케이스 1건 = 1행. 판정 칸에 `PC` / `모바일` 두 배지를 나란히 둔다.
지원하지 않는 환경은 `—`로 비우고 배지를 그리지 않는다.
환경 이름은 화면에서 `PC` / `모바일`로 쓴다 (`desktop`/`mobile`은 코드 안에서만).

---

## 폼 자동 생성 규칙

`paramSchema`를 읽어 만든다. 라벨은 반드시 zod의 `.describe()` 값을 쓴다.

| 스키마 | 컨트롤 |
|--------|--------|
| `z.string()` | 텍스트 입력 |
| `z.number()` | 텍스트 입력, 너비 120px |
| `z.enum([...])` | 셀렉트 |
| `z.boolean()` | 셀렉트 (의미 있는 두 단어로. `발급됨 / 발급 안 됨`) |
| `.optional()` | 라벨 옆에 `선택` 표기 |

- 라벨 열 너비 132px 고정, 값 열이 나머지를 채운다
- 파라미터가 8개를 넘으면 구획을 나눈다. 접기는 쓰지 않는다 (증적에 다 나와야 함)
- 검증 실패 시 해당 칸 아래에 `--fail` 색 12px 한 줄로 이유를 쓴다.
  버튼을 비활성화하지 말고 누르면 오류를 보여준다

---

## 금지

- box-shadow (focus ring 제외)
- gradient
- 카드 radius (12px 등). 컨트롤 2px만
- 판정 외의 색 사용
- JSON 원문을 목록 화면에 노출
- 아이콘으로 판정을 표시 (글자로 쓴다. `통과` / `실패` / `미실행`)

---

## 반응형

620px 미만에서 목록 행은 2단으로 접힌다 (거터 + 내용).
입력 폼은 라벨이 값 위로 올라간다.
증적 문서 출력은 A4 고정폭이므로 반응형 대상이 아니다.

---

## 접근성 기준

- 포커스 링은 `--ink` 2px, `outline-offset: -1px`. 버튼은 `--fail` 2px로 구분
- 판정은 색만으로 전달하지 않는다. 항상 글자를 같이 쓴다
- `prefers-reduced-motion`을 존중한다. 애초에 전환 효과를 거의 쓰지 않는다
