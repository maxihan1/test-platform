# LEARNINGS.md — 겪은 것들

> 모든 세션이 시작할 때 읽고, 끝날 때 덧붙인다.
> Claude Code 세션은 서로 기억을 공유하지 않는다. 이 파일이 유일한 통로다.
> 여기 적히지 않은 삽질은 다음 세션이 처음부터 다시 한다.

---

## 적는 기준

**적을 것** — 다음 세션이 몰라서 시간을 쓸 것

- 이 환경에서만 걸리는 것 (도커, EC2, Playwright 버전, 노드 버전)
- 처음에 틀리게 만들었다가 고친 것 — **왜 틀렸는지**까지
- SPEC이 애매해서 판단이 필요했던 것과 그 판단
- 특정 라이브러리의 예상 밖 동작
- 되돌린 결정 — 무엇을 왜 되돌렸는지

**적지 않을 것**

- SPEC에 이미 있는 내용 (중복되면 둘이 어긋난다)
- 잘 된 작업의 경과 보고 (`docs/progress/WS-X.md` 담당)
- 일반적인 프로그래밍 지식
- 한 번만 일어나고 재현되지 않는 것

---

## 형식

시간 역순으로 위에 쌓는다. 항목 하나는 5줄을 넘기지 않는다.

```
## [WS-C] 2026-10-02 · Playwright 리포터에서 줄 번호가 안 나옴
증상:  StepResult.line이 항상 undefined
원인:  onTestEnd의 result.error.location이 아니라 result.errors[0].location에 있음
해법:  errors 배열의 첫 항목에서 꺼낸다
주의:  에러가 없는 스텝은 errors가 빈 배열이므로 옵셔널 체이닝 필수
```

태그는 `[WS-A]`~`[WS-E]`, `[Phase0]`, `[병합]`, `[환경]` 중 하나.
여러 갈래에 해당하면 `[공통]`.

---

## 승격 규칙

같은 유형의 실수가 **두 번 이상** 나오면 여기 적는 것으로 끝내지 않는다.

| 반복된 것 | 옮길 곳 |
|-----------|--------|
| SPEC을 잘못 읽어서 생긴 어긋남 | `SPEC.md` 해당 절을 명확하게 고친다 |
| 작업 방식 때문에 생긴 문제 | `CLAUDE.md`에 규칙으로 추가 |
| 검사에서 못 걸러낸 위반 | `spec-review` 체크리스트에 항목 추가 |
| 환경 설정 삽질 | Phase 0 킥오프 프롬프트에 미리 반영 |

**기록은 쌓이기만 하면 소음이 된다.** 반복되는 것은 규칙으로 올려서
다음 세션이 읽지 않아도 안 틀리게 만든다. 그게 이 파일의 목적이다.

승격한 항목은 여기서 지우지 말고 `→ CLAUDE.md §3으로 승격`을 덧붙인다.
나중에 왜 그 규칙이 생겼는지 추적할 수 있어야 한다.

**어디까지 직접 고쳐도 되는지는 CLAUDE.md §2.5 표에 있다.** 검사기·훅·안내 문서는 직접 고치고,
SPEC과 잠긴 파일은 승인을 받는다. 승인이 필요한 것은 `제안:` 줄로 남기고, 처리되면 그 줄을 승격 표시로 바꾼다.

---

## 분량 관리

**분량은 목표가 아니라 결과다.** 두 번째 실수를 기계가 막도록 옮기면(CLAUDE.md §2.5) 그 항목은 한 줄로 줄어든다.
줄지 않고 늘기만 한다면 분량이 문제가 아니라 **승격이 밀린 것**이다. 그때는 항목을 지우지 말고 밀린 것부터 처리한다.

- 승격한 항목은 **한 줄 + `→ <어디>로 승격 (날짜)`** 로 줄인다. 기계가 막기 시작했으면 더 읽을 필요가 없다
- 지우지는 않는다. 같은 문제가 재발했을 때 "전에 이랬다"가 남아 있어야 한다
- **갈래별로 가르지 않는다.** 태그는 "누가 겪었나"이지 "누가 읽어야 하나"가 아니다 —
  `[WS-A]` 항목이 본문에 "전부 같은 함정이다"라고 적고 있는 예가 있다. 가르면 그 경고가 다른 갈래에 안 간다
- 250줄을 넘으면 분량을 손대기 전에 **밀린 것부터 센다** — `grep -cE "^(제안|미완):" docs/LEARNINGS.md`.
  `제안:`은 승인을 기다리는 규칙, `미완:`은 하기로 했는데 안 한 것이다.
  **승격되면 그 줄을 지우고 항목을 한 줄로 줄인다** — 남겨 두면 이 수치가 거짓이 된다

---

## 기록

(여기부터 쌓는다. 가장 최근 것이 위로.)

## [WS-E] 2026-09-21 · 색도 비율도 맞는데 **높이가 0** 이었다 — 검사가 원리적으로 못 보는 자리
증상:  진행 막대가 화면에 **통째로 안 보였다.** 숫자 넷(5·1·0·4)과 라벨은 멀쩡히 떴다.
       단위 검사는 「막대 네 칸의 flex 비율이 counts 와 같다」를 단언하고 **초록이었다**
원인:  `.modal-body` 가 세로 flex 라 `height: 6px` 인 막대가 **`flex-shrink` 로 0 까지 줄었다.**
       내용이 모달 높이를 넘으면 브라우저가 줄일 수 있는 것부터 줄이는데 그게 이 막대였다.
       **jsdom 에는 레이아웃 엔진이 없어 `getBoundingClientRect()` 가 늘 0 을 낸다** —
       비율(flex 값)은 단언할 수 있어도 실제 높이는 원리적으로 못 잰다
해법:  `.modal-body .stripe { flex-shrink: 0 }`. 그리고 `styles.test.ts` 에 **그 규칙이 있는지** 보는
       단언을 더했다 — 높이는 못 재도 규칙이 사라지는 것은 막는다.
       규칙을 일부러 빼서 그 단언만 빨개지는 것을 확인했다
교훈:  **「검사가 초록이다」와 「화면에 보인다」는 다른 말이다.** 특히 크기·겹침·줄바꿈이 그렇다.
       그 축은 브라우저로만 보이고, 본 뒤에는 **기계가 볼 수 있는 모양으로 바꿔** 남긴다 —
       높이를 못 재면 그 높이를 만드는 **규칙의 존재**를 대신 단언한다


## [WS-E] 2026-09-21 · 없는 구분을 **있는 척하는 화면**을 그릴 뻔했다
증상:  진행 막대를 「통과 · 실패 · **실행중** · **대기**」 넷으로 그리기로 계획에 적었다.
       구현자가 서버 셈법을 열어 보니 **대기 칸이 구조적으로 언제나 0** 이었다.
       40건짜리 실행에서 「실행중 40% · 대기 0%」로 그려져 **마흔이 동시에 도는 것처럼** 보인다
원인:  `counts.running` 이 `count(*) FILTER (WHERE finished_at IS NULL)` 다 — 「도는 것」이 아니라
       **「안 끝난 것 전부」**다. `run_item` 행은 `createRun` 때 한꺼번에 박히고,
       항목이 실제로 시작될 때 **`started_at` 을 고치는 코드가 없다.**
       즉 「실행 중」과 「대기」를 가를 신호가 저장된 값에 **아예 없는데** 이름만 갈라 놨다
해법:  막대를 `counts` 가 실제로 나눈 넷(`통과·실패·미실행·남은것`)으로 바꿨다. 넷의 합이 정확히 `total` 이다.
       「진행 중인 항목」은 **남기되 근사치임을 코드 주석에 적었다** — 빼면 사람이 러너 로그를 계속 열어야 한다
교훈:  **화면에 칸을 만들기 전에 그 칸을 채울 값이 실제로 있는지 센다.**
       이름이 있으면 값이 있는 줄 알기 쉽다 — `running` 이라는 이름이 「지금 도는 것」으로 읽혔다.
       **모르는 것은 모른다고 적는다.** 근사치를 빼는 것과 확정처럼 적는 것 사이에 정직한 세 번째 길이 있다

## [공통] 2026-09-21 · 접두사가 **표에 있는데도** 남의 fixture 를 지울 뻔했다 (2회차)
증상:  계획이 WS-D fixture 접두사로 `XDG` 를 지정했다. CLAUDE.md §3 표에 이미 있는 값이다.
       그대로 `DELETE ... LIKE 'XDG%'` 를 썼으면 `generate.test.ts` 의 `XDG 증적 생성 실행` fixture 를
       **실행 도중에** 지웠다
원인:  표가 **갈래 단위**로만 적혀 있어 「그 접두사 아래를 이미 누가 쓰는지」가 안 보였다.
       `XFS1` 에서 똑같이 겪고 그 줄을 적어 뒀는데 **`XDG` 에는 같은 점검을 안 했다**
해법:  구현자가 `XDG 견주기` 로 좁혔다. CLAUDE.md §3 에 `XDG` 아래 둘을 적었다
교훈:  **접두사가 표에 있어도 그 아래를 누가 쓰는지 다시 본다.** 표에 있다는 것은 갈래가 정해졌다는 뜻이지
       비어 있다는 뜻이 아니다. → **CLAUDE.md §3 접두사 줄에 하위 구분을 적는 것으로 승격 (2026-09-21)**

## [WS-E] 2026-09-21 · 문 **둘로 막힌 것**을 하나만 부수고 「그물이 없다」고 할 뻔했다
증상:  「이미 끝난 실행을 열면 상자가 뜨면 안 된다」는 단언이 무는지 보려고
       초기값을 `useState(false)` → `useState(true)` 로 되돌렸는데 **검사가 그대로 초록이었다**
원인:  그 사고를 막는 문이 **둘**이었다 — 여는 조건(`도는중(status)` 일 때만)과
       실행을 갈아탈 때의 리셋. 하나만 열어서는 사고가 안 난다
해법:  둘을 각각 부숴 각각 빨개지는 것을 확인했다. 단언은 멀쩡했다
교훈:  **부숴 보는 확인(spec-review G8)이 초록이면 「그물이 없다」가 아니라 「부순 자리가 틀렸다」를 먼저 의심한다.**
       무엇이 그 사고를 막고 있는지 세어 보고, 막는 것이 여럿이면 각각 부순다

## [공통] 2026-09-21 · 검사 규칙을 **양방향으로** 적었더니 정상 동작이 위반으로 걸렸다
증상:  `tpx-cases` 의 관문 2(표 ↔ 코드 대조)를 「표에 없는데 코드에 있으면 지어낸 것」까지
       보게 적었다. 돌려 보니 **R7 이 시킨 준비 스텝**(화면을 연다·데이터를 만든다)이 전부 걸렸다
원인:  규칙을 **머리로 적고 한 번도 안 돌렸다.** 같은 문서 안의 R7 과 충돌하는 것을 산문만으로는 못 봤다
해법:  대조를 **표 기준 한 방향**으로 못박았다 — 표의 줄마다 전제·조작·결과 셋을 코드에서 찾고,
       코드에만 있는 준비 스텝은 보지 않는다
교훈:  **검사 규칙은 한 번 돌려보기 전엔 정상 동작을 위반으로 잡는지 모른다.** 새 검사를 문서로 적었으면
       그 자리에서 **정상 입력 하나를 통과시켜 본다** — 「빨개지는지」(G8)만 보면 이 부류는 안 보인다

## [WS-E] 2026-09-21 · 기준을 옮기면 **기준의 방향까지 뒤집힐 수 있다**
증상:  띠 바탕에서 서비스 색을 빼고 8px 네모로 옮기는 계획을 세우면서
       「덤으로 명암비 제약이 풀려 고를 수 있는 색이 넓어진다」고 적었다. **실측은 반대였다.**
       DB 에 있는 색이 새 자리에서 전부 미달이었다 — 기본서비스색 1.57 · DEMO 2.52 · 시안 녹색 1.44
원인:  옛 기준은 「흰 글자가 읽히나」라 **어두울수록 통과**였고, 새 자리는 「청색 띠 위에서 구분되나」라
       **밝을수록 통과**다. 같은 「명암비 몇 대 몇」이라는 말이 **정반대 집합**을 가리켰다.
       숫자(4.5 → 3)만 보고 「느슨해진다」고 읽었다
해법:  표시를 **흰 면으로** 내렸다. 그러면 방향이 지금과 같아져 기존 색이 전부 살아난다.
       색 16³ 격자를 전수로 훑어 「옛 기준 통과 · 새 기준 미달」인 색이 0건임을 확인하고 계획에 적었다
교훈:  **기준을 옮길 때는 숫자가 아니라 「무엇에 대비되는가」를 먼저 본다.**
       배경이 바뀌면 통과 집합이 뒤집힐 수 있다. 그리고 그것은 **재 봐야만 보인다** —
       두 기준이 같은 방향인지 아닌지를 전수로 확인하는 것이 가장 싼 증거였다

## [WS-E] 2026-09-21 · 문서의 기준을 고치면서 **그 기준을 재던 코드**를 안 고칠 뻔했다
증상:  DESIGN.md 의 서비스 색 기준을 바꾸는 계획을 다 쓰고도 `settingsView.ts` 가 계획에 없었다.
       그 파일의 `명암비()` 주석이 「이 색은 띠의 바탕이고 그 위에 흰 글자가 올라간다」이고
       `색사유()` 가 「띠의 흰 글자가 잘 안 보입니다」를 화면에 띄운다. **둘 다 거짓이 될 참이었다**
원인:  2026-09-19 에 「기준이 문서에만 있으면 지켜지지 않는다」를 배우고 재는 코드를 만들었다.
       **그 코드가 생겼다는 사실을 다음 계획이 몰랐다.** 문서만 훑고 코드를 안 훑었다
해법:  선행 읽기에서 `grep` 으로 잡아 계획 검토에 실었다. 기준·문구·검사를 한 단계에서 같이 고쳤다
교훈:  **검사를 만들면 그 검사도 유지 대상이 된다.** 기준을 바꾸는 계획은
       「이 기준을 재는 코드가 어디 있나」를 `grep` 으로 먼저 찾는다 — 문서 목록만 보면 놓친다.
       → `styles.test.ts` 가 토큰 표를 대조하고 `html.ts` 와 화면 CSS 를 견주도록 **승격 (2026-09-21)**

## [공통] 2026-09-21 · 검사 건수를 적으면서 **어느 환경에서 잰 것인지**를 안 적어 거짓말이 됐다
증상:  게이트 요약에 「62파일 702건, 건너뛴 것 0」을 적었다. **CI 기준으로 거짓이었다.**
       `ci.yml` 에 `DATABASE_URL` 도 `services:` 도 없어 DB 를 타는 검사가 **13파일이 통째로**,
       부분까지 합쳐 **212건**이 건너뛴다. 병합을 막는 초록불이 실제로 보는 것은 501건이다
원인:  **내 셸에는 `DATABASE_URL` 이 있었다.** 같은 `npm test` 가 두 환경에서 다른 숫자를 내는데
       한쪽만 적고 환경을 안 적었다. `describe.skipIf(연결 === undefined)` 라 **실패가 아니라 침묵**이고,
       「건너뛴 것 0」이라는 한 줄이 「그물이 전부 돈다」로 읽힌다
해법:  기록에 **두 줄을 나란히** 적는다 — DB 붙인 값과 DB 없는 값. CI 에 postgres 를 붙이는 것은
       `GUARD` 표면이라 이번 범위 밖이고 `docs/WORKSTREAMS.md` 넘긴 것 ⑮ 로 넘겼다
교훈:  **건수는 환경을 달고 다녀야 한다.** 이 저장소의 「손으로 적은 숫자는 전부 같은 병이다」와는
       다른 갈래다 — 저건 **손으로 적어서** 낡았고, 이건 **기계가 정확히 잰 숫자인데 잰 자리가 달라서** 거짓이 됐다

## [WS-E] 2026-09-21 · 셈의 **단위가 다른 상수**를 가져다 썼다 — 경계에서 주석의 근거까지 거짓이었다
증상:  「전체 실행」이 쪽을 되돌면서 `runPlan` 의 `상한`(1000)으로 끊었다. 뒤에 케이스가 더 있어도
       **조용히 앞 1000건만** 걸렸다. 사람은 버튼에 적힌 「전체」를 믿는다
원인:  `상한` 은 **실행 항목 수**(Σ디바이스 수 × 반복)이고 반복문이 센 것은 **케이스 수**다.
       이름이 둘 다 「상한」이라 단위가 안 보였다. 끊는 줄의 주석은 「넘으면 어차피 실행이 거절된다」였는데
       **그것도 거짓이다** — 케이스 1000건이 전부 디바이스 하나면 항목 수도 1000 이고
       `넘었나` 가 `> 상한` 이라 **경계에서 거절이 안 걸린다**
해법:  `쪽상한 = 40` 을 따로 뒀다(무한 반복 방어도 겸한다). 잘렸으면 모달이 사람에게 말한다
교훈:  **상수를 빌려 쓰기 전에 「무엇을 세는 수인가」를 묻는다.** 그리고 **「어차피 뒤에서 막힌다」는
       주석은 막는 조건을 열어서 세어 본 뒤에 적는다** — 등호 하나 차이로 근거가 통째로 뒤집힌다

## [WS-E] 2026-09-21 · `useRef` 로 든 진행 상태는 **화면에 안 뜬다** — 연타 방어와 진행 표시를 하나로 묶었다
증상:  `실행하기` 를 누르면 화면이 침묵했다. 최대 1000건을 만드는 요청이 도는 내내 버튼 글자도
       상태도 그대로였고 두 번 누르면 조용히 무시됐다. **이 PR 이 고치려던 고장이 모달 안에서 되살아났다**
원인:  `거는중` 을 `useRef` 로 들었다. 값은 정확히 바뀌지만 **다시 그리지 않는다.**
       연타 방어(값이 즉시 보여야 한다)와 진행 표시(화면이 따라와야 한다)는 **요구가 다른데**
       상태 하나로 겸하게 두고 렌더링이 필요 없는 쪽만 봤다
해법:  `useState` 로 바꾸고 모달에 `거는중` 을 내려보낸다. 도는 동안 버튼이 「실행을 거는 중」으로 바뀌고 안 눌린다
교훈:  **누른 뒤 기다림이 생기는 자리는 「막았나」가 아니라 「사람이 그것을 아나」까지가 요구다.**
       `useRef` 는 화면에 안 나오는 값에만 쓴다

## [공통] 2026-09-20 · 명세 문구를 **글자까지** 맞췄더니 한국어 조사가 틀렸다
증상:  §8.4 상태표에 `{라벨} 를 만들지 못했습니다` 를 적고 코드가 그대로 따랐다.
       실제로 찍히면 `PDF 를`(맞다) · **`엑셀 를`** · **`HTML 를`**(둘 다 틀렸다) — 셋 중 둘이
       틀린 조사로 사용자에게 보인다. 검사는 `'엑셀'` 포함만 봐서 못 잡았다
원인:  **명세가 코드더러 채우라고 남긴 자리(`{라벨}`) 뒤에 조사를 붙였다.**
       채우는 값이 받침을 가리는데 명세는 한 경우만 보고 썼다. 구현자는 「글자까지 맞추라」는
       지시를 정확히 따랐고 — **지시가 맞을수록 틀린 문구가 그대로 퍼진다**
해법:  조사를 안 타는 꼴로 바꿨다 — `{라벨} 증적을 만들지 못했습니다`.
       왜 그렇게 썼는지를 명세 옆에 적어 다음 사람이 `{라벨} 를` 로 되돌리지 않게 했다
교훈:  **명세가 빈칸을 남길 때는 그 뒤에 조사를 붙이지 않는다.** 채울 값을 모르면서
       받침을 아는 척한 것이다. 빈칸 뒤에는 조사가 아니라 **낱말**을 둔다(`{라벨} 증적을`)

## [공통] 2026-09-20 · 검사 기록이 **저장소와 반대로** 적혀 있었다
증상:  `docs/reviews/` 의 검사 기록이 「`package.json` 에 이름을 잇지 **않았다**」
       「승인이 필요한 곳은 거기다」라고 적었는데, **그 뒤에 실제로 이었다.**
       게이트 2 가 읽는 정본 기록이 안 한 일을 했다고, 한 일을 안 했다고 적고 있었다
원인:  **기록을 쓰는 손과 그 뒤에 일하는 손이 달랐다.** 하위 작업자가 자기 판단으로
       기록을 닫았고, 컨트롤러가 그 판단을 뒤집었는데 기록은 안 따라왔다.
       기록 문서는 **한 번 쓰고 끝나는 물건처럼** 다뤄졌다
해법:  네 자리를 고치고 **왜 뒤집었는지**를 같이 적었다(CI 가 `check:workflow` 를 안 돌아서
       그 검사기가 CI 에서 한 번도 안 돈다). 뒤집은 판단을 지우지 않고 남겼다 (§2.7 ②)
교훈:  **검사 기록은 마지막에 쓰거나, 뒤에 무언가 바뀌면 같이 고친다.**
       이 저장소가 계속 지우고 있는 「기록이 거짓말한다」의 한 갈래인데,
       **낡아서가 아니라 같은 PR 안에서 뒤집혀서** 그렇게 된 경우다.
       앞 항목(「명세가 아직 안 했다고 적은 할 일 넷」)은 시간이 지나 낡은 것이고 이건 **그 자리에서 어긋났다**

## [공통] 2026-09-20 · 표면 표가 **폴더 단위로만** 적혀 있어 새 파일이 조용히 등급을 낮췄다
증상:  이번 PR 이 만든 `apps/admin/src/routeParams.ts` 가 **미분류**로 나왔다. 서버 코드인데 2등급에 안 걸렸다.
       그 파일만 바뀐 PR 이었으면 **규칙 ② 대로 1등급**이 되어 `spec-review` 도 `/security-review` 도 안 돈다
원인:  `surfaces.mjs` 의 `ADMIN` 이 하위 폴더 넷(`catalog/**` 같은)과 **파일 하나(`app.ts`)를 이름으로** 적고 있었다.
       폴더 밑은 다 걸리는데 **`src/` 바로 밑에 새 파일이 생기면 아무 데도 안 걸린다**
해법:  이름으로 적던 `apps/admin/src/app.ts` 를 `apps/admin/src/*.ts` 로 넓혔다.
       `*` 는 `/` 를 안 넘어 하위 폴더를 안 삼키고, 같은 자리의 `*.test.ts` 는 표 맨 앞 `TESTS` 가 먼저 가져간다
교훈:  **표면 표에 파일 이름을 하나씩 적으면 그 옆에 생기는 파일이 샌다.** 폴더든 무늬든 「자리」로 적는다.
       **두 번째다** — 2026-09-18 에 `.claude/hooks/**` 가 같은 이유로 미분류였다
       → `.claude/scripts/surfaces.test.mjs` 의 단언으로 승격 (2026-09-20).
       **미분류를 게이트 2 요약에 싣는 규율이 이걸 잡았다** — 기계가 「모르겠다」고 말하게 해 둔 덕이다
3회차 (2026-09-20, 형식고르기 PR) — **`.github/workflows/ci.yml` 이 미분류였다.**
   그 PR 이 새 검사기를 만들고 CI 에 한 줄을 이었는데, **CI 파일 자체가 어느 표면에도 안 걸렸다.**
   CI 만 고치는 PR 이면 규칙 ② 로 1등급이 되어 3등급 렌즈가 안 돈다 —
   **CI 는 줄 하나를 빠뜨리면 그 검사가 영영 안 돌고 초록불만 남는 자리**라 가장 위험한 축에 든다.
   → `GUARD` 에 `.github/workflows/**` 를 더했다. **세 번 다 같은 원인이다** —
   표면 표가 「자리」가 아니라 「그때 있던 것」을 적었다. 새 자리가 생기면 또 샌다

## [공통] 2026-09-20 · 명세가 「아직 안 했다」고 적은 할 일 넷이 **넷 다 이미 끝나 있었다**
증상:  `docs/spec/` 의 `[계약 변경 필요]` 블록 넷이 열린 채였는데, 코드로 대조하니 넷 다 반영이 끝나 있었다.
       명세를 여는 사람은 **있지도 않은 할 일 넷**을 본다. 진행 기록에도 닫힌 항목 다섯이 열린 채였다
원인:  블록에 **「지금 어떻게 됐나」를 적는 칸이 없었다.** 반영한 세션은 코드만 고치고 블록은 그대로 뒀다.
       3등급 표면이라 그 뒤로 아무도 안 열었다 — **닫는 일이 아무의 일도 아니었다**
해법:  블록마다 `상태: 대기 | 반영 완료 (YYYY-MM-DD, 어디에) | 철회됨` 한 줄을 강제한다
교훈:  **「나중에 닫는다」에 기댄 기록은 안 닫힌다.** 상태 칸이 없는 할 일 기록은 반드시 낡는다
       → `.claude/scripts/check-spec-refs.mjs` (`npm run check:spec`, CI 가 돈다) 로 승격 (2026-09-20)

## [공통] 2026-09-20 · 우리가 쓴 검사 안에 「구현이 어떻게 망가져도 참인 단언」이 둘 있었다
증상:  화면에 그물을 씌우고 초록을 받았는데, 그중 두 단언은 아무것도 안 보고 있었다 —
       `Form.test.tsx` 의 「버튼이 0개다」와 「비활성화된 것이 0개다」. `Form.tsx` 는 버튼도
       `disabled` 도 애초에 안 그린다
원인:  단언을 **명세 문장에서** 뽑고 **구현을 안 봤다.** 둘째는 이름이 「아무것도 비활성화하지
       않는다 (SPEC §8.2)」라 **다음 세션이 그 규칙은 그물이 본다고 읽게 만든다.** 진짜 자리는
       `RunSetup.tsx` 고 그물 밖이다. 검사 렌즈 둘이 독립적으로 같은 것을 잡았다
해법:  둘 다 지우고 §8.2 주장을 이름에서 뺐다. 「초록인 것과 무는 것은 다르다」를 규칙으로 굳혔다
       → `spec-review` **G8** 로 승격 (2026-09-20) — 새로 만든 검사는 한 자리 부숴 빨개지는 것을
       보고 그 결과를 PR 에 적는다


## [공통] 2026-09-19 · 없는 깃발을 주면 조용히 무시되고 본문이 기본값으로 덮인다
증상:  `pr-update.mjs --pr 30 --comment-file <파일>` 이 코멘트를 안 달고 상황판을 1/7·1등급으로 되돌렸다
원인:  `--comment-file` 은 없는 깃발이다. `arg()` 가 못 찾으면 `null` 을 주고 그대로 본문 갱신으로 흘러가는데,
       그 경로의 `--step`·`--tier` 기본값이 1 이다. **두 번 똑같이 겪었다** (PR #28 · #30)
해법:  → `.claude/scripts/pr-update.mjs` 의 깃발 검사로 승격 (2026-09-19). 모르는 깃발이면 종료코드 2 로 멈춘다

## [공통] 2026-09-19 · 확인 도구가 정규화해서 그 부류를 원리적으로 못 봤다
증상:  문(`auth/gate.ts`)이 배정 안 받은 서비스의 자원을 막는다고 검사 스물넷으로 단언했고
       전부 초록이었다. 실제로는 **주소를 퍼센트로 감싸기만 하면 전부 지나갔다** —
       `/api/runs/%35%38%36%37` · `/%61pi/settings/users`(로그인조차 안 거친다)
원인:  두 겹이다.
       ① **제품** — 문은 `req.url`(디코딩 **전**)을 글자로 읽고 라우터는 디코딩한 **뒤**
          라우트를 찾는다. 같은 주소를 둘이 다르게 읽으니 그 틈으로 다 빠져나갔다
       ② **검사** — `app.inject` 가 주소를 **정규화해 버린다.** 그래서 이 부류는
          검사를 아무리 늘려도 **원리적으로 안 보인다.** 축을 더 넣는 것으로는 못 잡는다
해법:  제품은 `req.url` 을 판정에 안 쓴다 — 등록된 라우트 틀(`req.routeOptions.url`)과
       `req.params` 만 본다. 틀은 디코딩과 무관한 값이라 틈이 애초에 안 생긴다.
       검사는 그 자리만 **진짜 소켓**으로 건다 (`app.listen({port:0})` + `fetch`).
교훈:  **「검사를 더 썼다」가 「그 부류를 봤다」는 뜻이 아니다.** 도구가 입력을 다듬어
       주는 종류의 구멍은 **도구를 바꿔야만** 보인다. 확인 방법을 세기 전에
       *이 도구가 무엇을 못 보는가* 를 먼저 묻는다.
       이번엔 바깥 렌즈가 그것을 알고 생소켓으로 재현해 줘서 잡혔다 (CLAUDE.md §2.3).

## [WS-E] 2026-09-19 · SPEC 두 절이 서로를 막는다 — 색을 고르라면서 색을 못 보여준다
증상:  설정 화면(§8.8)이 「이름·색을 언제든 고친다」를 시키는데,
       §8 과 DESIGN.md 원칙 1 은 「서비스 색은 맨 위 띠에서만 쓴다」고 막는다
원인:  §8 의 금지는 **판정 색과 헷갈리지 않게** 하려는 것이고, §8.8 은 그 색을 고르는 자리다.
       두 절이 각각은 맞는데 한 화면에서 만나면 **색을 안 보여주고 색을 고르게 하라**가 된다
해법:  이번엔 그대로 뒀다 — 색 고르개와 띠 미리보기가 있어야 흰 글자가 읽히는지 볼 수 있고,
       그것이 명암비 경고와 짝이다. **두 렌즈가 독립적으로 같은 제안을 냈다** —
       §8 에 「설정 화면(§8.8)의 색 칸과 미리보기는 예외」 한 줄을 더한다
교훈:  **「어디서만 쓴다」는 금지는 그것을 고르는 화면을 예외로 적어 둬야 한다.**
       금지를 쓸 때 그 값을 만드는 자리가 어디인지 같이 본다.
       제안: SPEC 변경이라 게이트 0 이 필요하다 — 다음 작업이 받는다 (`docs/reviews/2026-09-19-WS-E-3.md`)

## [WS-E] 2026-09-19 · 안내가 보낸 곳을 안내 자신이 막고 있었다
증상:  배정받은 서비스가 없으면 「설정에서 자기 자신을 배정하세요」가 뜨는데,
       설정을 눌러도 같은 안내가 떴다. 서비스가 0개인 첫 운영자는 영영 못 빠져나온다
원인:  Shell 이 `빈띠사유()` 가 있으면 **모든 화면**을 그 안내로 덮었다.
       설정은 배정돼야 쓰는 화면이 아니라 **그 배정을 만드는 화면**이라 성질이 다른데
       한 줄로 뭉뚱그렸다
해법:  `빈띠사유(user, 지금자리)` — 설정 자리면 null. 단 설정을 못 여는 등급에게는 그대로 보여준다
교훈:  **「이렇게 하세요」라고 적을 때 그 길이 지금 열려 있는지 같이 확인한다.**
       검사 일곱과 타입 검사를 전부 통과한 채로 있었다 — 눌러 보지 않으면 안 나온다.
       빈 상태 안내를 쓰는 자리마다 「거기로 갈 수 있나」를 봐야 한다

## [WS-E] 2026-09-19 · 기준을 재는 장치를 만들자마자 기존 값이 걸렸다
증상:  서비스 색 명암비 함수를 TDD 로 쓰면서 「add-service.ts 의 기본색은 통과한다」를
       단언했는데 실패했다. `#5B7FDE` 가 3.79 로 DESIGN.md 기준(4.5) 미달이었다
원인:  ① 에서 「DEMO 서비스의 `#888888` 이 미달」로 넘겼는데, 그 하나가 아니라
       **새로 만드는 모든 서비스**가 미달 색을 받고 있었다. 재는 장치가 없어서 아무도 몰랐다
해법:  `#3A5FCD`(5.68)로 바꾸고, **옛 값이 미달이었다는 단언**을 검사에 남겼다
교훈:  **기준이 문서에만 있으면 지켜지지 않는다.** 재는 코드를 쓰는 순간 기존 값이 걸린다 —
       그래서 「사후에 사람이 본다」보다 「만들 때 기계가 잰다」가 싸다.
       그리고 **테스트의 가정이 틀린 것과 구현이 틀린 것을 가른다** — 여기서는 제품이 틀렸다

## [공통] 2026-09-19 · 착수 때 선언한 등급을 게이트 0 뒤에 다시 안 쟀다 (2회)
증상:  ① 선언 1 / 실측 2 ② 선언 2 / 실측 3 — 둘 다 [6] 검사가 잡을 때까지 몰랐다
원인:  착수 시점에는 SPEC 을 고칠지 모르는데, **그것을 확정하는 자리가 게이트 0** 이다.
       체인에 「게이트 0 뒤에 다시 잰다」가 없어서 선언이 그대로 굳었다
       → `.claude/skills/tpx/SKILL.md` **등급 판정 표와 게이트 절로 승격 (2026-09-19)**

## [WS-D] 2026-09-19 · 테스트가 초록인데 실제로는 안 됐다 — 한 갈래에서 두 번
증상:  ① 증적 문서의 스크린샷이 **세 형식 어디에서도 안 떴다.** 검사 14건은 전부 초록이었다
       ② `exceljs` 를 붙이고 검사 9건이 초록이었는데 실제로 뽑아 보니 서버에서 터졌다 —
          `SyntaxError: does not provide an export named 'Workbook'`
원인:  ① **픽스처가 실제 저장 형식이 아니었다.** `/shots/깨진화면.png` 로 적혀 있었고 실제는
          `artifacts/runs/<runId>/<historyId>/<seq>.png` 다. 단언도 「문자열이 HTML 안에 있나」만 봤다.
          실제로는 HTML 이 `/api/evidence/artifacts/…` 로 풀려 404 이고, PDF 는 `setContent` 라
          기준 주소가 `about:blank` 이어서 **요청조차 안 나갔다**
       ② `exceljs` 진입점이 `module.exports = require(…)` 라 노드의 CJS 이름 추출기가 못 읽는다.
          **Vite 가 번들링으로 덮어 줘서 Vitest 에서는 안 터진다.** 서버는 노드 ESM 이다
해법:  ① 그림을 `data:` URI 로 문서 안에 심는다 (`generate.ts` 의 `스크린샷을심는다()`).
          PDF 는 이 방법 말고는 없고, 증적은 파일만 떼어 보내는 물건이라 HTML 도 같아야 한다
       ② `import ExcelJS from 'exceljs'` + `new ExcelJS.Workbook()`
교훈:  **파일을 만들어 내는 코드는 실제로 한 번 뽑아서 열어 봐야 한다.**
       픽스처는 **실제 저장 형식**을 쓴다. 지어낸 형식은 그 형식만 검사한다
       → `generate.test.ts` 가 이제 실제 형식으로 **진짜 PNG** 를 깔고
         「그림이 문서 안에 들어있는가 · 없는 파일과 경로 벗어남은 조용히 빠지는가」를 단언한다 (2026-09-19)

## [WS-D] 2026-09-19 · 실패 방향을 열린 쪽으로 뒀다 — 비밀값이 「모르면 보여준다」였다
표시(`secret`)가 없으면 안 가렸는데 박제 이전 행에는 표시가 아예 없다. 이름으로도 판단하게 고쳤다.
**두 번째였다** — 증적(WS-D)에 이어 화면(`web/ItemDetail.tsx`)에서 같은 구멍이 났다.
→ `npm run check:secret-names` 로 승격 (2026-09-19). 세 곳의 목록이 갈라지면 빨간불이 난다

## [WS-D] 2026-09-19 · 갈래가 읽을 장 표 밖에 답이 있었다
증상:  증적 문서의 판정 글자를 영문으로 찍었다. SPEC §8.4 스케치가 `[FAIL]` 이라고 적고 있었다
원인:  **정본이 둘이었다.** `docs/DESIGN.md:187` 은 「글자로 쓴다. `통과`/`실패`/`미실행`」이고
       화면 코드도 한글이다. §8.4 의 영문은 ASCII 도해의 편의였다.
       그리고 「`NA` 와 `NOT_RUN` 이 둘 다 미실행이라 겹친다」고 판단했는데,
       **답은 `도메인/실행.md` §8.3 에 있었다** — 「돌지 못한 항목은 `NA` 로 닫고 미실행 배지 옆에 사유를 적는다」.
       배지는 같고 **사유가 가른다**는 것이 설계였다. 그 장은 WS-D 의 읽을 장 표에 없다
해법:  DESIGN.md 를 정본으로 골랐다. `NA` 항목에도 사유를 채웠다
교훈:  **한 장 안에서도 어긋난다** (§2.7 ①). 도해의 예시 문자열을 규칙으로 읽지 않는다.
       읽을 장 표는 시작점이지 울타리가 아니다 — 표기 규칙이 안 잡히면 다른 장을 확인한다
미해결: 같은 유형 셋이 남았다. 전부 §1.2 승인이 필요해 손대지 않고 `docs/progress/WS-D.md` 에 적었다 —
       엑셀 칸 목록에 「사유」 없음 · 실행 중 생성과 재현 불변식 · 「실패 TOP 10」의 회차 접기

## [WS-E] 2026-09-19 · 확인하는 방법이 버그를 가렸다 (4회)
증상:  ① 세션이 도중에 끊겨도 로그인 화면이 안 뜨는 것을 못 봤다 (① 덩이)
       ② 증적 문서가 「만드는 중입니다」에서 굳는 것을 못 봤다 (② 덩이)
원인:  **둘 다 내가 확인한 방법 때문에 안 보였다.**
       ① 은 로그인 **전** 경로만 눌렀다 — 그건 제외 목록이라 정상 동작한다
       ② 는 확인할 때 `location.reload()` 를 했다 — 폴링이 안 돌아도 새로고침이 메워 준다
해법:  ① 계정을 비활성으로 내려 실제로 끊었다 ② 새로고침 없이 9초를 기다렸다
교훈:  **화면이 뒤에서 갱신되는 기능은 「새로고침·재진입 없이」 되는지 봐야 한다.**
       새로고침은 폴링·상태 갱신 버그를 통째로 가린다.
       두 번 다 `/code-review` 가 코드만 읽고 잡았다 — 자기검사는
       「내가 어떻게 확인했는가」의 사각지대를 그대로 물려받는다 (CLAUDE.md §2.3)
       → `spec-review` **G6 으로 승격 (2026-09-19)**. 이제 검사가 「어떻게 확인했는가」를 묻는다
3회차 (2026-09-19, WS-E 셋째 덩이) — **확인한 방법이 아니라 확인할 수단이 아예 없었다.**
   임시 비밀번호 상자가 **화면 밖에** 그려졌다. 계정이 열 명만 돼도 안 보이는데 그 사이 옛 비밀번호는
   이미 죽어 있어 본인은 영문도 모르고 로그인이 막힌다. **판단은 맞았고 검사도 초록이었다** —
   「한 번만 보여준다」는 정확히 계산됐는데 그 계산을 그리는 쪽을 보는 그물이 없었다
   (`docs/progress/WS-E.md` · `docs/reviews/2026-09-19-WS-E-3.md`).
   → **`vitest.config.ts` 의 `include` 로 승격 (2026-09-19, PR #30)** — `apps/**/*.test.tsx` 를 더해
   화면 파일이 그물에 들어온다. **사고가 난 상자 자체(`SettingsPassword.tsx`)는 이번 범위에 들었다.**
   그 상자를 띄우는 `SettingsUser.tsx` 는 계획의 셋에 **없었고** 같은 PR 에서 뒤늦게 덮였다 (`bcddb58`).
   나머지 화면은 그대로 그물 밖이다 — 몇 대 몇인지는 세어서 본다.
   `ls apps/admin/src/web/*.tsx | grep -v '\.test\.tsx$' | wc -l` 와 `ls apps/admin/src/web/*.test.tsx | wc -l`
   주의: 이 그물이 무는 것은 `scrollIntoView` 가 **불렸다**까지다. jsdom 에는 레이아웃 엔진도 CSS 계산도
   없어서 배치·간격·색·애니메이션은 여전히 사람이 브라우저로 본다 (SPEC 공통/6-인프라 §9.1)
4회차 (2026-09-20, 마무리 PR) — **이번엔 조사하는 방법이 「안 만든 것」을 가렸다.**
   「스펙 기능이 다 만들어졌나」를 전수 조사하고 **「코드에 미구현 표시 0건」**을 근거로
   `docs/WORKFLOW.md` 에 「기능 구현은 됐고 확인이 남았다」를 적었다. 그런데 SPEC §8.4 가
   「증적은 **결과 화면에서 형식을 골라** 만든다」고 정본으로 적어 둔 그 UI 가 아예 없었다 —
   `RunResult.tsx` 가 `'PDF'` 를 박아 놨다. **아무도 안 쓴 코드에는 TODO 도 없어서**
   그 조사 방식으로는 원리적으로 안 걸린다. 완료 기준 상자 넷이 거기 매달려 있었다.
   **낡은 기록을 지우러 온 PR 이 같은 종류의 거짓을 새로 심을 뻔했다.**
   → `spec-review` **F10 으로 승격 (2026-09-20)** — 「다 됐다」를 적으려면
   명세가 요구하는 화면 자리를 열어서 봤는지 묻는다

## [WS-E] 2026-09-19 · 실패한 테스트를 러너 장애로 뒤바꿨다
증상:  Playwright 타임아웃으로 그냥 실패한 행에 「러너에 닿지 못했습니다」가 붙었다
원인:  사유를 고를 때 `status` 를 안 가렸다. **러너는 `FAIL` 이 예외로 끝나도 `error` 를 채운다**
       (`packages/kit` 의 리포터). 그 영문 메시지를 「러너에 닿지 못했습니다」로 바꾸는 함수에 그대로 넘겼다
해법:  `칸사유()` 가 `NA` 항목만 본다
교훈:  **SPEC §8.3 이 「둘을 구분할 수 없으면 원인을 못 찾는다」고 한 자리를 오히려 뒤섞었다.**
       구분하라는 규칙을 읽고 만들면서 그 구분의 **입력**을 안 가린 것이다.
       사람이 인프라를 뒤지다 테스트 자체가 깨진 것을 못 본다
주의:  `error` 칸 하나를 여러 상태가 나눠 쓰면 **읽는 쪽이 status 를 같이 봐야 한다**

## [WS-E] 2026-09-19 · 반쪽만 고치고 「고쳤다」고 적었다 — 자기검사가 못 잡았다 (2회)
2회차 (2026-09-19, 넘긴 것 셋) — 문의 서비스 경계를 고치면서 **`닿는서비스()` 축만 고치고
인증·등급 축은 같은 파일 스물세 줄 위에 그대로 뒀다.** 그래서 `/%61pi/**` 가 로그인조차
안 거쳤다. 두 번 다 **고친 쪽만 확인**한 것이 원인이다.
→ `spec-review` **G7** 로 승격 (2026-09-19) — 「경계·권한 검사가 **부르는 쪽이 적는 값**으로
우회되는가. 검사가 **그 축을 바꿔 가며** 돌리는가」. 축을 하나만 보는 확인을 기계가 묻는다.

증상:  세션이 도중에 끊기면 로그인 화면이 **끝내 안 떴다.** 고치려던 증상이 그대로 남아 있었다
원인:  `api.ts` 가 401 에 주소를 `#/login` 으로 바꿨는데 **화면이 들고 있는 로그인 상태는 그대로**였다.
       그래서 「로그인했는데 주소가 로그인 화면이면 집으로」 갈래가 곧장 되돌렸다.
       **통로 한쪽만 고치고 반대쪽이 되돌리는 것을 안 봤다**
해법:  `세션끊김을받는다()` 로 화면에 알려 상태를 바꾼다. 주소만 바꾸는 것으로는 부족하다
교훈:  **내 브라우저 확인이 이걸 못 본 이유가 핵심이다** — 로그인 **전** 경로만 눌렀다.
       그건 `/auth/me` 401 이라 제외 목록에 있어 정상 동작한다. 도중에 끊기는 경로는 안 봤다.
       **자기검사는 「내가 어떻게 확인했는가」의 사각지대를 그대로 물려받는다.**
       `/code-review` 가 코드만 읽고 잡았다 — CLAUDE.md §2.3 이 다른 세션을 요구하는 이유가 이것이다
주의:  고친 뒤에는 **그 경로를 실제로 만들어** 확인한다. 이번에는 계정을 비활성으로 내려 세션을 끊었다

## [공통] 2026-09-19 · `import.meta.url === \`file://${process.argv[1]}\`` 는 한글·공백 경로에서 안 맞는다
증상:  새로 만든 `check-secret-names.mjs` 가 **출력 없이 종료 코드 0** 을 냈다. CI 는 초록불이다
원인:  `import.meta.url` 은 퍼센트 인코딩되고 `process.argv[1]` 은 안 된다.
       경로에 공백이나 한글이 있으면 두 문자열이 달라 `main()` 이 아예 안 돈다.
       **이 저장소에는 이미 `docs/plans/2026-09-19-ws-e-뼈대.md` 가 있다** — 가정이 아니다
해법:  `pathToFileURL(process.argv[1]).href` 와 비교한다 (`node:url`)
교훈:  **아무것도 안 하고 0 으로 끝나는 것이 가장 나쁜 실패다.** 검사기를 만들면
       「일부러 틀리게 해서 1 이 나오는가」뿐 아니라 **「엉뚱한 자리에서 돌려도 도는가」**를 본다
확인:  고치기 전 버전을 한글 경로에 복사해 돌려 출력 없이 0 이 나오는 것을 눈으로 봤다

## [WS-E] 2026-09-19 · SPEC 이 안 정한 주소를 조용히 지어냈다
증상:  자리 넷의 `그래프 ↗` 를 `/grafana` 로 보냈다. admin 에 그런 경로가 없어 눌러도 404 다
원인:  SPEC §8 은 「그래프는 Grafana 라 바깥으로 나간다」까지만 적고 **화면이 그 주소를 어떻게 아는지 안 정했다.**
       없는 결정을 만들면서 멈추지 않았다 — CLAUDE.md §1.4 가 금지한 자리다
해법:  `docs/SETUP.md` 의 `GRAFANA_PORT` 기본값(3001)을 쓰되 **근거와 한계를 주석에 적었다.**
       배포가 포트를 바꾸면 틀린다는 것까지 적고 설정으로 뺄지는 다음 덩이로 넘겼다
후속:  **2026-09-20 에 빌드 시 설정값으로 정했다** — `docker-compose.yml` 의 `build.args`
       → `Dockerfile` 의 `ARG`/`ENV` → `vite build` 가 `VITE_GRAFANA_PORT` 를 번들에 굽는다.
       **한계는 남는다** — 값이 번들에 구워져서 포트를 바꾸면 admin 이미지를 다시 빌드해야 하고,
       안 하면 **오류 없이 링크만 틀린다** (`docs/SETUP.md` · SPEC §9.2 · `docs/progress/WS-E.md`)
교훈:  **타입 검사도 테스트도 「이 링크가 어디로 가는지」를 안 본다.**
       자리·버튼을 더했으면 눌러서 갈 곳이 있는지 본다. 같은 유형으로 `#/settings` 도 오류 화면으로 갔다
       → `spec-review` 체크리스트에 F8 로 더할 것을 제안했다 (`docs/reviews/2026-09-19-WS-E.md`)

## [공통] 2026-09-18 · 짧은 슬래시 이름은 내장 명령에 먹힌다 (`/tp` → `/tpx`)
증상:  `/tp` 를 치면 내장 `/teleport` 가 먼저 떴다. 체인을 부를 방법이 없었다
원인:  자동완성이 **부분열**로 찾는다 — 순서만 맞으면 매치다. `/tp` 는 **52개**와 겹쳤고 `tpx` 는 0개다
해법:  `(?<![a-zA-Z-])tp(?![a-zA-Z])` 한 줄. **이 항목은 옛 이름을 일부러 적고 있다** — 다시 돌리면 망가진다
교훈:  **새 이름은 설치된 명령 전체와 부분열로 대조한다.** 앞글자 비교로는 못 잡는다
주의:  **개명은 저장소 밖에도 있다** — ① `~/.claude/…/memory/` 가 다음 세션을 옛 이름으로 되돌린다
       ② PR 상황판 마커에 이름이 박혀 블록이 둘로 늘었다 (**닻 문자열에 이름을 넣지 않는다**)
       ③ 스킬 색인은 더하기만 따라온다 — 새 이름은 대개 재시작 없이 뜬다

## [공통] 2026-09-18 · 등급은 어디를 건드리나를 재지 얼마나 위험한가를 재지 않는다
증상:  `tpx` 개명은 치환 한 줄인데 3등급이 나와 계획·검토 2종·게이트 셋을 다 돌았다
원인:  색인 `docs/SPEC.md` 에 체인 설명 두 줄이 있어 `SPEC` 표면에 걸렸다. 내용이 산문 한 낱말이어도 표면은 같다
해법:  등급대로 돌되 하위 작업자 발행만 인라인으로 했다. 규율(RED→GREEN·종료 코드 판정)은 그대로 지켰다
교훈:  **대체로 맞는 근사지만 기계적 개명에서는 어긋난다.** 지금은 게이트에서 사람이 조정한다 —
       **두 번째가 오면** `surfaces.mjs` 에 「치환만 하는 변경」 예외를 검토한다 (§2.5)

## [공통] 2026-09-18 · 훅 파일을 판별식이 import 하면 거기서 멈춘다
증상:  `guard.test.mjs` 를 돌리니 **2분 동안 아무 응답이 없었다.** 그다음엔 5개 중 1개만 돌았다
원인:  `guard.mjs` 가 맨 위에서 `readFileSync(0)` 으로 stdin 을 읽고, 맨 끝에서 `ok()`
       (`process.exit(0)`)를 무조건 부른다. **훅으로 돌 때는 맞지만 import 하면 둘 다 재앙이다** —
       앞엣것은 영영 막고, 뒤엣것은 테스트 러너를 죽인다
해법:  둘 다 `if (mode)` 로 감쌌다. `mode` 는 훅이 넘기는 `argv[2]` 라 import 시엔 없다
교훈:  **훅 스크립트를 판별식이 보게 만들려면 「부수효과를 mode 뒤로 미룬다」가 먼저다.**
       판정 로직을 `export` 하는 것만으로는 부족하다

## [공통] 2026-09-18 · 뮤테이션이 내가 쓴 죽은 줄을 잡았다
증상:  가드 오탐을 고치며 「git 구간만 본다」 판정을 더했는데, **그 줄을 지워도 검사가 초록**이었다
원인:  따옴표 안을 걷어내는 것만으로 오탐이 이미 막혔다. 더한 줄이 아무것도 안 하고 있었다
해법:  지웠다. 훅 모드 7사례를 다시 돌려 그대로 통과하는 것을 확인했다
교훈:  **안전 장치에 줄을 더할 때는 「이 줄을 지우면 빨간불이 나는가」를 본다.**
       안 나면 그 줄은 방어가 아니라 착시다. 분기가 하나 늘면 틀릴 자리가 하나 는다

## [공통] 2026-09-18 · PR 체크리스트의 마지막 칸이 영영 안 채워졌다
증상:  병합까지 끝났는데 PR 본문의 `7. 병합` 이 `[ ]` 로 남았다
원인:  `pr-update.mjs --step 7` 은 7번을 「지금 여기」로 그린다. **그 뒤에 갱신하는 자리가 없다**
해법:  `--done` 을 더했다. 일곱 단계와 게이트 둘을 전부 `[x]` 로 닫고 「다음 멈춤」을 `없음 — 끝` 으로
교훈:  **진행 표시는 「마지막 칸을 누가 채우나」까지 설계해야 한다.**
       N단계를 그리는 코드는 N+1 상태(끝)를 따로 갖는다

## [공통] 2026-09-18 · 오케스트레이터를 접었다 — 킥오프 앵커 검사가 같이 사라졌다
증상:  `docs/orchestration.yaml` 을 지우니 `check:spec` 의 검사 5(킥오프 이름 대조)가
       **대조할 상대를 잃었다.** 파일이 없으면 조용히 건너뛰게 돼 있어 초록불이 거짓말을 한다
원인:  검사 5 는 매니페스트의 `kickoff:` 이름이 `WORKSTREAMS.md` 에 실재하는지를 봤다.
       가리키는 쪽이 사라졌다
해법:  검사를 명시적으로 걷어냈다. **조용히 꺼지게 두지 않는다**
주의:  **검사 능력이 하나 줄었다.** `WORKSTREAMS.md` 킥오프 절 제목은 여전히 중요하다 —
       `tpx-plan` 의 작업 원천이다. 없어진 것은 그 제목을 **기계가 대조할 근거**이지
       제목의 중요성이 아니다. 지금은 사람이 본다 (`tpx-spec` §2.7 ④ 에 「기계가 안 본다」로 적었다)
교훈:  **장치를 걷어낼 때는 「대상이 없어졌다」와 「검사가 줄었다」를 가려 적는다.**
       앞엣것으로 적으면 다음 사람이 손실을 모른다

## [공통] 2026-09-18 · 정리를 구현 단계에 넣을 뻔했다
증상:  레거시 제거 계획이 「작업방·브랜치 정리」를 할 일 6 으로 두고 `depends-on: [1..5]` 를 달았다
원인:  정리도 할 일처럼 보였다. **PR 이 병합되기 전에 작업방이 사라진다**는 것을 놓쳤다
해법:  계획 검토가 BLOCKER 로 잡았다. `[7] tpx-merge` 의 정리 항목으로 옮겼다
교훈:  **git 정리는 할 일이 아니라 단계다.** `tpx-merge` Step 1 의 「미커밋·미푸시 0 확인」이
       그 앞에 있는 이유가 이것이다. 구현 단계에서 지우면 그 확인을 건너뛴다

## [WS-F] 2026-09-18 · 검사 파일 둘이 같은 접두사를 두고 반대로 단언했다
한 줄 요약: `store.test.ts` 는 `XBQ` 가 **없는 서비스**라 단언하고 `queries.test.ts` 는 그것을 **만든다.**
같은 갈래 안이라 접두사 규칙(§3)은 지켜져 있었고 정리 구문도 멀쩡했다. 9회 중 2회 깨졌다.
→ `spec-review` **G5** 로 승격 (2026-09-18)

## [환경] 2026-09-18 · 선언한 부품이 설치 안 된 채로 병합됐다 (2회)
증상:  `contracts` 병합 뒤 **어느 브랜치에서든 푸시가 막혔다.** 훅이 `npm test` 로 막는데
       에러는 `Cannot find package '@fastify/secure-session'` 이라 **내 변경 때문인지
       환경 문제인지 그 자리에서 구분이 안 된다.** 확인에 도구 호출 다섯 번이 들었다
원인:  `package.json` 에 부품을 더한 커밋이 병합됐는데 아무도 `npm install` 을 안 돌렸다
해법:  `npm install`. 그리고 **기계가 먼저 말하게 한다** — `npm run check:deps`
주의:  **디렉터리 존재로 판정하면 안 된다.** 워크트리에는 자기 `node_modules` 가 없고
       Node 가 상위로 올라가 루트 것을 쓴다. `npm ls --depth=0` 도 같은 이유로
       워크트리에서 거짓 양성이다 (루트에 설치된 것까지 `UNMET DEPENDENCY` 로 찍는다).
       `createRequire().resolve()` 로 **Node 의 해석 경로를 그대로 따라가야** 답이 맞는다
       → `npm run check:deps` · `spec-review` 밖 판별식으로 승격 (2026-09-18)

1회차 — 워크트리의 `node_modules` 가 비어 있는데 `npx vitest` 는 돌았다. 상위 폴더의
것이 잡혀서, 거기 없는 새 부품만 조용히 빠졌다. **그때는 "워크트리에서 npm install 을
한 번 돌린다"로 끝냈다.** 사람이 기억해야 하는 해법이라 두 번째가 왔다.

## [WS-F] 2026-09-18 · SPEC 이 정하지 않은 자리 셋을 판단했다
1. **스캔(`POST /api/catalog/scan`)의 등급** — §7 등급 표에 이름으로 없다. 방식으로 갈라 `operator` 로 뒀다
2. **등급 부족의 오류 이름** — §7 은 403 만 정한다. `{ error: 'FORBIDDEN', need: '<등급>' }`
3. **아이디 중복의 오류 이름** — §7 은 접두사 중복만 이름을 준다. `USERNAME_TAKEN` 으로 맞췄다
셋 다 화면이 그 자리를 안 보여주므로(§3.5) 사람 눈에 닿지 않는다. 화면이 붙을 때 이름을 맞춘다

## [WS-F] 2026-09-18 · 운영 명령을 순수 JavaScript 로 두면 구현이 두 벌이 된다
한 줄 요약: `node scripts/*.js` 로 두면 비밀번호 해시 형식과 `createRun` 을 각각 다시 구현하게 된다.
컨테이너가 이미 `tsx` 로 돌고 있어 `.ts` 로 두고 진짜 함수를 부른다.
→ SPEC §9.2 로 승격 (2026-09-18)

## [WS-E] 2026-09-17 · 좁은 화면에서 제목이 한 글자씩 세로로 흘렀다 **(2026-09-19 재발)**
증상:  목록 행에 집계(통과/실패/미실행)를 더했더니 375px 에서 케이스명이 한 글자씩 줄바꿈됐다
원인:  `.row` 격자가 620px 아래에서 `4px 1fr` 두 칸이 된다. 제목과 오른쪽 칸이 **같은 줄을 나눠 쓰는데**
       오른쪽에 넓은 것이 들어오면 제목 칸이 0에 수렴한다
해법:  `.gutter{grid-row:1/-1}` 로 색 띠를 세로로 펴고 `.tcid·.title·.right` 를 전부 2열로 보내 **세로로 쌓는다**
주의:  실물로 렌더해 보기 전에는 안 보인다. 목록 행에 무언가를 더하면 **375px 에서 한 번 열어 본다**

**재발한 이유 (2026-09-19)** — 해법을 여기 적어 두고 **`.right` 만 2열로 옮겼다.**
`.gutter` 의 `grid-row` 도 `.tcid`·`.title` 의 `grid-column` 도 코드에 없었다.
**「해법을 적었다」와 「코드에 넣었다」는 다르다.** 기록장이 통과를 보장하지 않는다.
이번에는 셋을 다 넣고 그 이유를 CSS 주석에 적었다 — 다음 사람이 한 줄만 옮기지 않게.

**창 리사이즈로는 확인할 수 없었다** (2026-09-16 에도 같은 벽에 막혔다).
**375px `iframe` 을 페이지에 띄우면 미디어 쿼리가 그 폭에 반응한다.** 그 방법으로 처음 실제로 봤다.

## [공통] 2026-09-17 · 검사기가 자기 사각지대를 초록불로 보고했다
증상:  `check:spec` 이 통과라는데 §8.8 참조 다섯 곳이 실제로 없는 절이었다
원인:  검사기가 `SPEC §N` 만 셌다. **장 안에서는 `→ §8.8` 로 쓰는 것이 보통**인데 그 형태를 안 봤다.
       12장으로 가르면서 맨절 참조가 늘었는데 검사기는 한 파일이던 때 모양 그대로였다
해법:  SPEC 문서 안에서는 `§N` 만으로, 밖(코드·훅)에서는 `SPEC §N` 으로 센다
교훈:  **검사기를 만들 때 "무엇을 안 보는가"를 같이 적어 둔다.** 초록불은 안 본 것도 초록이다
       → `.claude/scripts/check-spec-refs.mjs` 로 승격 (2026-09-17)

## [공통] 2026-09-17 · 색인의 분량 숫자가 12곳 전부 틀려 있었다
증상:  갈래별 "537줄" 같은 숫자가 실제 장 길이와 어긋났다. 아무도 못 봤다
원인:  장을 고칠 때마다 사람이 손으로 고쳐야 했다. 앞 세션이 "이 표의 최신성은 사람이 본다"고
       적어 둔 그 자리다. 사람이 보기로 한 것은 안 본다
해법:  `check:spec` 이 색인 숫자와 실제 줄 수를 대조한다. 일부러 틀린 숫자를 넣어 잡히는 것까지 확인했다
교훈:  세션이 "4장 537줄"을 믿고 계획을 세운다. 숫자가 틀리면 계획이 틀린다
3회차: 「221군데」가 242군데였고 「검색 조건 다섯」이 세 장에 남아 있었다 (2026-09-18).
       **손으로 적은 숫자는 전부 같은 병이다.** 참조 수도 기계가 세게 하고, 산문 속 개수는 체크리스트로 올렸다
       → `.claude/scripts/check-spec-refs.mjs` (분량·참조 수) · `spec-review` **H5** (산문 속 개수) 로 승격
4회차: **그 검사기 자신에게 안 보이는 행이 하나 있었다** (2026-09-19). 색인의
       「**12장 전부** — 2440줄」 행은 판별식이 `` `spec/ `` 가 든 행만 요구해서 **한 번도 검사받은 적이 없고**,
       적힌 2388 이 실제 2427 과 **39줄 어긋난 채로** 있었다. 일부러 9999 를 넣어도 통과했다.
       위 항목의 교훈(「검사기를 만들 때 **무엇을 안 보는가**를 같이 적어 둔다」)이 그대로 재발한 자리다 —
       **초록불은 안 본 것도 초록이다.** 경로가 안 적힌 행은 12장 전체 합과 대조하게 고쳤다
       → `.claude/scripts/check-spec-refs.mjs` 로 승격 (2026-09-19)
5회차: **완료 기준 상자 개수를 계획이 손으로 적었다** (2026-09-20). 「59상자」라고 썼는데
       같은 PR 이 상자를 하나 더해 기계는 60 을 셌다. **낡은 기록을 지우러 온 PR 이 제 손 숫자로 시작했다.**
       같은 PR 안에서 **두 번** 났다 — 계획의 PDF 크기도 손 숫자였다
       → `npm run count:done` (`package.json`) 으로 승격 (2026-09-20).
       **개수를 적을 자리에는 숫자 대신 그 명령을 가리킨다** — `docs/WORKFLOW.md` 현재 위치가 그 본이다

## [공통] 2026-09-18 · 색인을 고치고도 "색인이 안내하는 동선"은 안 봤다
증상:  SPEC 개정을 12장에 다 반영하고 색인의 숫자·표까지 맞췄는데, 사용자가 "색인에도 반영해야 하지 않나"라고 짚자 3건이 나왔다
원인:  색인을 **데이터**(줄 수·절 목록·표 주인)로만 보고 **안내문**(「무엇을 하려는가 → 무엇을 읽는가」 라우터 표)으로 안 봤다.
       개정이 만든 새 개념(서비스·등급)에 라우터 줄이 없으면 세션이 어느 장을 열지 모른다
해법:  라우터 표에 줄을 더하고, 절 번호 표의 옛 이름(§8 인스턴스 띠)을 고쳤다
교훈:  **색인은 표가 아니라 길잡이다.** 새 개념을 만들면 "그걸 하려는 사람이 여기서 길을 찾는가"를 묻는다
       → `spec-review` **H3** 확장으로 승격 (2026-09-18)
5회차: 같은 날 K2(케이스 파일 규칙)가 옛 접두사 형식으로 남아 있었고, WS-A 킥오프도 개정 전이었다.
       **낱낱이 잡는 방식으로는 계속 샌다** — 절차가 없는 것이 원인이었다
       → `CLAUDE.md` **§2.7 「SPEC을 고치면 같이 움직이는 것」** 신설로 승격 (2026-09-18).
       찾는 순서·뒤집은 결정 처리 기준·SPEC 밖 여섯 곳·코드에 박힌 상수까지 적었다

## [공통] 2026-09-17 · 도메인으로 문서를 가를 수 있는지 잘못 셌다
증상:  "도메인 고유 내용은 전체의 13%뿐이라 도메인 단위 분리는 실익이 없다"고 사용자에게 보고했다
원인:  컨텍스트 절(§3.x)만 셌다. 표·API·화면은 도메인을 가로지를 거라고 지레짐작하고 세지 않았다
해법:  실물을 열어 보니 표는 주인이 하나씩이고(`test_case`→카탈로그 · `evidence_document`→리포팅) API도 `# Auth`·`# Catalog`로 이미 갈려 있었다. 배분하니 45%였다
주의:  "이건 공유라 못 나눈다"는 판단은 **실물을 열어 배분해 본 뒤에** 한다. 목차만 보고 세면 틀린 숫자로 사용자를 설득하게 된다

## [공통] 2026-09-17 · 문서가 규정한 것이 코드에 안 걸려 있다 (3회)
→ **CLAUDE.md §1.2 · spec-review H2 로 승격 (2026-09-17). 3회차에 spec-review C4 를 더했다 (2026-09-19).**
   1회차 — §2가 "tcId 접두사는 기능 영역을 뜻한다"고 규정했는데 접두사를 잘라 쓰는 코드가 **0줄**이었다.
   2회차 — WORKSTREAMS 산문이 `package.json` 을 "잠긴 파일"로 적어 놔 SPEC 검사가 **없는 위반을 중대로 보고**했다
   (`docs/reviews/2026-09-17-SPEC분할.md` 에서 철회). 규칙 — 잠금·금지·계약이라 적혀 있어도
   **강제 장치(guard.mjs `LOCKED`·검사기·코드)를 먼저 센다. 0줄이면 계약이 아니라 관례다**
   3회차 (2026-09-19, WS-C) — **방향이 반대다.** 앞의 둘은 「막는다고 적혀 있는데 안 막아서 없는 위반을 보고」했고,
   이번엔 **「막아 준다고 적고 그걸 근거로 위험을 안 막았다」.** `abort()` 주석에 "admin이 닫는 UPDATE마다
   `finished_at IS NULL`을 붙여 이미 막고 있다"고 썼는데, 그 가드는 `store.ts` 의 `CLOSE_UNFINISHED` 에만 있고
   러너 응답을 기록하는 `finishItem()` 에는 **없었다.** 근거로 든 주석 바로 위 문장이 「언제나 붙인다」라고
   규정하고 있어서 그 말을 확인 없이 믿었다. **규칙이 §1.2 로 승격돼 있었는데도 읽고 안 지켰다** —
   승격만으로는 안 막혔다. 그래서 `spec-review` **C4** 로 체크리스트에 올렸다.
   규칙 — **주석이 「다른 파일이 막아 준다」를 근거로 들면 그 파일을 열어 해당 SQL·조건을 눈으로 센다.**
   같은 파일의 다른 주석이 그렇게 말한다는 것은 근거가 아니다

## [공통] 2026-09-17 · 병합 안 된 브랜치를 안 보고 "그 기능 없다"고 세 번 보고했다
증상:  대조 분석에서 케이스 검색창·실행 진행 표시·재실행 버튼을 "SPEC에도 코드에도 없다"고 적었다. 셋 다 WS-E가 이미 만들어 뒀다
원인:  SPEC과 **로컬 main 체크아웃**만 봤다. WS-E 작업은 그때 `origin/worktree-ws-e-web`에만 있었고 로컬 main은 뒤처져 있었다
해법:  코드가 있나 없나를 판정하기 전에 `git branch -r`로 병합 안 된 브랜치를 먼저 본다. 진행 기록(`docs/progress/`)도 같이 읽는다
주의:  워크트리는 **origin/main 기준**으로 생긴다. 로컬 main이 낡아도 워크트리는 최신일 수 있어 둘이 다르게 보인다

## [공통] 2026-09-17 · 문서를 여러 갈래가 나눠 고치면 참조가 허공을 가리킨다
→ **spec-review H1 로 승격 (2026-09-17).** `npm run check:spec` 이 §번호와 장 사이 링크를 기계로 본다.
   원인은 변경안이 길이 제한에 잘려 §1 관련 세 건이 통째로 빠진 것을 못 알아챈 것이다

## [WS-E] 2026-09-16 · Vite 프록시 접두사 '/api'가 소스 파일 /api.ts까지 가로챈다
증상:  화면이 통째로 비었다. 콘솔에 에러가 없고 index.html·main.tsx는 200인데 화면만 안 그려진다
원인:  `proxy: { '/api': ... }`는 **접두사 일치**다. 브라우저가 `/api.ts`(내 소스 파일)를 달라고 하면 그것도 admin으로 넘어가 404가 된다
해법:  `'^/api/'`로 적는다. `^`로 시작하면 Vite가 정규식으로 본다. 파일 이름을 바꿔 피할 수도 있다
주의:  모듈 로드 실패는 콘솔이 아니라 네트워크에 남는다. 화면이 비면 각 소스 파일의 HTTP 코드부터 본다

## [WS-E] 2026-09-16 · POST /api/runs는 입력값을 명세로 검증하지 않는다
증상:  SPEC §8.2는 "검증 실패 시 칸 아래에 이유를 표시"인데, 실행 요청은 title·platforms·케이스 존재만 본다
판단:  화면이 실행 전에 검증한다. 검증기를 새로 쓰지 않고 서버의 `execution/validate.ts`를 그대로 import 했다
왜:    두 벌을 두면 '실행하기'와 '입력값 세트로 저장'(POST param-sets, 서버가 검증)이 같은 값에 다른 사유를 낸다
주의:  그 파일은 순수 함수여야 화면 번들에 들어간다. DB나 fastify를 import 하면 화면 빌드가 깨진다

## [WS-B] 2026-09-16 · reply.type()을 먼저 박으면 404 본문이 500으로 나간다
증상:  없는 스크린샷을 요청하면 404가 아니라 500이 떨어졌다
원인:  `reply.type('image/png').send(await readFile(...))`는 readFile보다 type()이 먼저 평가된다. 실패해서 catch로 가면 응답은 이미 image/png라 JSON 본문을 직렬화하지 못한다
해법:  읽을 것을 손에 쥔 뒤에 형식을 정한다. 먼저 buffer를 받고, 성공했을 때만 type()을 건다
주의:  reply를 체이닝하는 모든 자리에 같은 함정이 있다. 에러 경로가 다른 content-type을 쓴다면 형식은 마지막에 정한다

## [공통] 2026-09-16 · 테스트 정리 구문의 LIKE 패턴이 넓으면 다른 갈래의 테스트를 지운다 (4회)
4회차 (2026-09-19, WS-C 작업 중 발견): **범위를 줄 수 없는 제품 코드도 있다.**
   `recoverRunning()` 이 `SELECT run_id FROM test_run WHERE status = 'RUNNING'` 으로 **DB 전체**를 훑어
   미완 항목을 닫는다. 3회차의 해법(함수에 범위를 받게 한다)이 여기서는 **안 통한다** —
   부팅 직후 남은 것을 전부 닫는 것이 그 함수의 존재 이유이고, 프로덕션에서는 그게 옳다.
   `store.test.ts` 가 그 함수를 부르면 병렬로 돌던 `queries.test.ts` 의 미완 항목까지 닫혀
   `lastByCase()` 가 2건을 봤다. 3회 중 1회꼴. 증상이 **다른 파일에서** 터져 범인 찾기가 어렵다.
   해법은 **`vitest.config.ts` 에 `fileParallelism: false`** — 접두사도 범위 조건도 못 막는 간섭을
   구조로 끊는다. 313건 2.6초 → 314건 15.3초. **흔들리는 테스트보다 12초가 싸다**
   → `vitest.config.ts` 로 **구조적 차단** (2026-09-19). G3·G4 는 그대로 두되 이제 기계가 먼저 막는다

## (아래는 1~3회차 기록)
→ **CLAUDE.md §3 · spec-review G3 로 승격 (2026-09-17).**
   Vitest가 파일을 병렬로 돌려 `DELETE FROM test_case WHERE tc_id LIKE 'ZZ%'` 가 남의 fixture를 쓰는 도중에 지웠다.
   접두사는 갈래마다 고유하게, 정리 패턴은 자기 것에만. 확인은 **연속 3회** — 1회차 171건 통과, 2회차 4건 실패였다
3회차 (2026-09-18): **범인이 테스트가 아니라 제품 코드였다.** `save()` 의 비활성 처리가
   `WHERE is_active AND tc_id <> ALL($1)` 로 **DB 전체**를 범위로 잡아, 실제 스캔이 돌 때마다
   남의 fixture 를 비활성으로 내렸다. 접두사를 갈래마다 나눠 써도 이 함수 앞에서는 소용이 없었다.
   증상이 **양방향**이라 어느 쪽이 범인인지 가리기 어려웠다 — 8회 중 1회꼴로 store 쪽이 3건 깨지거나
   routes 쪽이 3건 깨졌다. 해법은 `save(specs, deactivateMissing, prefix)` 로 범위를 받는 것이고,
   이것은 §9.2 「서비스마다 자기 폴더만」이 요구하는 동작과 같다
   교훈: **fixture 격리가 안 되면 정리 구문만 보지 말고 그 표를 일괄 갱신하는 제품 코드를 본다**
   → `spec-review` **G4** 로 승격 (2026-09-18)

## [WS-B] 2026-09-18 · 서비스의 테스트 폴더가 없으면 스캔이 통째로 죽었다
증상:  execution 테스트와 같이 돌리면 카탈로그 스캔이 0건을 돌려줬다. 단독으로는 10건이었다
원인:  `service` 에 등록됐는데 `tests_dir` 폴더가 실제로 없으면 `readdir` 이 던지고,
       그것이 서비스 루프 **밖**에서 잡혀 다른 서비스의 케이스까지 통째로 사라졌다
해법:  서비스마다 try/catch 로 접고 사유만 남긴 뒤 나머지를 계속 훑는다
주의:  **「스캔이 파일 하나 때문에 통째로 죽으면 안 된다」(2026-09-16)와 같은 함정이 한 단위 위에서 다시 났다.**
       루프를 하나 더 감쌀 때는 바깥 try 가 무엇까지 삼키는지 다시 본다

## [WS-B] 2026-09-18 · §8.9가 「미실행만 남은 실행」을 어느 머리글로 보낼지 안 적었다
증상:  Slack 머리글이 `[실패]`·`[통과]`·`[중단]` 셋인데, 실패 0 · 미실행 10 인 실행이 어디에도 안 맞았다
판단:  **`[실패]` 로 보낸다.** 러너가 전부 죽어 한 건도 못 돈 실행에 `[통과]` 가 나가면 아무도 그 알림을 안 믿는다
주의:  화면의 완료 모달(§8.9)도 같은 갈림을 만난다. 합칠 때 두 곳이 같은 기준인지 본다

## [WS-A] 2026-09-18 · §7의 `?active=`가 화면 칩 두 개와 어떻게 대응하는지 안 적혀 있었다
증상:  §8.1 표는 칩이 「활성만 / 전체」 둘인데 §7은 `?active= true | false. 생략하면 활성만` 이다.
       `false` 가 「비활성만」인지 「전체」인지 읽히지 않는다
판단:  **「전체」로 읽었다.** 칩이 둘이고 값이 둘이라 1:1 로 맞는 해석이 이것뿐이다.
       「비활성만」으로 읽으면 화면이 「전체」를 보낼 방법이 아예 없어진다
주의:  화면(WS-E)이 반대로 구현하면 조용히 어긋난다. 합칠 때 이 한 줄을 맞춰 본다

## [환경] 2026-09-16 · 컨테이너가 병합 전 이미지를 물고 있으면 케이스가 전부 FAIL로 보인다
증상:  러너에 DEMO-001을 보내면 `TypeError: (0 , _kit.defineCase) is not a function`. 호스트에서 같은 코드는 멀쩡하다
원인:  이미지가 `packages/kit`을 빌드 시점에 굽는다. WS-C 병합 전에 뜬 컨테이너는 옛 kit을 그대로 들고 있다
해법:  `docker compose build runner && docker compose up -d runner`. admin도 같다
주의:  코드가 아니라 이미지가 낡은 것이다. 러너 응답이 통째로 이상하면 먼저 `docker compose ps`의 CREATED와 마지막 커밋 시각을 대 본다

## [환경] 2026-09-16 · 워크트리 세션은 메인 체크아웃 쪽 파일로 돈다 (3회)
→ **HOOKS.md 로 승격 (2026-09-17).** `.claude/settings.local.json` 은 gitignore라 새 워크트리에 안 딸려온다.
   메인 체크아웃 쪽을 고치면 즉시 반영된다 (세션 재시작 불필요)
   **3회차 (2026-09-17)** — 설정 파일만이 아니라 **훅 스크립트도 같다.** 워크트리에서 `guard.mjs` 의
   잠금 목록을 비웠는데 훅은 `$CLAUDE_PROJECT_DIR`(= 메인 체크아웃)의 파일을 읽어 그 세션에서 계속 막았다.
   워크트리의 변경은 **main 에 병합된 뒤부터** 적용된다.
   규칙 — `.claude/` 아래 무엇을 고치든 지금 세션에 먹일 거면 메인 체크아웃 쪽을 고친다

## [WS-A] 2026-09-16 · PLATFORM_SCAN이 자식 playwright로 새어 K8이 전부 거짓 위반이 됐다
증상:  `check:tests`가 멀쩡한 10건을 전부 "테스트를 하나도 등록하지 않았다"로 찍었다
원인:  스캐너가 `process.env.PLATFORM_SCAN = '1'`을 켠 채로 두고, 그 뒤 `--list`를 자식으로 띄웠다. 자식이 그대로 물려받아 kit이 등록을 건너뛴다
해법:  자식에게 넘길 env에서 `PLATFORM_SCAN`을 지운다. 순서에 기대지 않으려면 명시적으로 지우는 쪽이 안전하다
주의:  스캐너를 한 프로세스에서 다른 도구와 같이 돌리는 곳은 전부 같은 함정이다

## [WS-A] 2026-09-16 · 스캔이 파일 하나 때문에 통째로 죽으면 다른 파일의 위반이 묻힌다
증상:  `params` 키가 빠진 파일 하나 때문에 스캔이 예외로 끝나, 다른 파일의 tcId 형식·빈 name 위반이 아예 출력되지 않았다
원인:  `scan()`이 첫 실패에서 던지게 만들었다. 검사기는 "한 번에 전부 보여주는" 것이 목적인데 설계가 반대였다
해법:  `{ specs, failures, duplicates }`를 돌려주고 판단은 부르는 쪽이 한다. 기동 시 자동 스캔도 같은 성질이 필요하다(실패해도 admin은 떠야 한다 — SPEC §3.1)
주의:  읽지 못한 파일이 있으면 무엇이 정말 사라졌는지 알 수 없다. 그때는 `is_active=false` 처리를 건너뛴다

## [WS-A] 2026-09-16 · 일부러 규칙을 어긴 테스트 fixture는 저장소에 둘 수 없다
증상:  K4(키 누락) 검사용 fixture를 `apps/**` 안에 두자 `npm run typecheck`가 먼저 터졌다
원인:  `DefineCaseInput`이 `params`·`expected`를 필수 키로 요구한다. tsconfig의 `include`가 `apps/**/*.ts`라 fixture까지 컴파일 대상이다
해법:  소스 규칙 검사기를 `(파일명, 소스문자열) → 위반목록` 순수 함수로 만들었다. fixture가 문자열이면 파일이 아예 필요 없다
주의:  `PLATFORM_TESTS_DIR`로 임시 폴더를 가리켜 손으로 확인할 수는 있다. 그 폴더는 `tests/`가 아니라 자기 소유 경로 안에 둬야 훅이 막지 않는다

## [WS-A] 2026-09-16 · SPEC이 tcId 중복을 한 쌍으로도 여러 쌍으로도 읽힌다
증상:  §3.1은 "중복 발견 시 양쪽 파일 경로를 보고"(한 쌍), §7은 `duplicates[]`(여러 쌍)
판단:  배열로 맞췄다. 한 쌍만 돌려주면 고치고 다시 돌렸을 때 다음 쌍이 또 나와 왕복이 늘어난다
주의:  §7의 POST 응답 줄에는 `scannedAt`·`error?`가 없는데 GET에는 있다. 구현은 둘을 같은 모양으로 돌려준다 (docs/reviews/2026-09-16-WS-A.md 기타 1)

## [WS-C] 2026-09-16 · 러너 이미지의 Node 24에서는 리포터의 상대 import가 안 풀린다
증상:  컨테이너에서만 `Failed to load the ES module: .../protocol.ts` 경고 하나만 남고 실행이 통째로 실패한다. 호스트에서는 멀쩡하다
원인:  호스트는 Node 22라 Playwright 변환기가 `./x.js`를 `x.ts`로 풀어준다. 이미지의 Node 24는 타입 스트리핑이 기본이라 그 변환을 안 타고, Node는 그 변환을 하지 않는다
해법:  Playwright가 직접 로드하는 파일(리포터)에는 **값을 가져오는 상대 import를 두지 않는다.** 타입 import는 지워지므로 괜찮다
주의:  테스트 파일과 kit 본체는 `/tests`에 package.json이 없어 CJS 경로를 타므로 영향이 없다. 러너·admin은 tsx가 풀어준다

## [WS-C] 2026-09-16 · child.kill()은 npx만 죽이고 브라우저가 남는다
증상:  `timeoutMs: 5000`을 준 DEMO-007이 33초 만에 응답했다. 판정은 NA로 맞지만 시간이 6배다
원인:  `npx → node → 브라우저`인데 자식만 죽였다. 손자가 stdout 파이프를 쥔 채 남아 close 이벤트가 30초 뒤에야 왔다
해법:  `spawn(..., { detached: true })`로 띄우고 `process.kill(-pid, 'SIGKILL')`로 그룹째 끊는다
주의:  admin의 HTTP 타임아웃은 `timeoutMs + 30초`다. 이걸 안 고치면 긴 케이스에서 admin이 먼저 끊긴다

## [WS-C] 2026-09-16 · zod-to-json-schema가 zod 4 스키마에서 빈 결과를 뱉는다
→ **코드 · package.json 으로 승격 (2026-09-17).** zod 4 내장 `z.toJSONSchema(schema, { io: 'input' })` 를 쓴다
   (`io:'input'` 이라야 `.default()` 가 있는 필드가 required 에서 빠진다).
   의존성·킥오프 목록·SPEC 스택 표·`types.ts` 주석까지 전부 지웠다. 테스트 141건 그대로 통과한다

## [WS-C] 2026-09-16 · Playwright가 테스트 위치를 kit의 래퍼 파일로 잡는다
증상:  `npx playwright test --list`가 모든 케이스를 `packages/kit/src/runtime/test.ts:95`로 표시하고 "1 file"로 센다
원인:  Playwright는 `test()`를 부른 스택의 첫 프레임을 위치로 쓴다. kit 래퍼가 부르므로 래퍼 파일이 찍힌다
해법:  기계 검사는 `--list --reporter=json`을 쓴다. `suites[].file`은 케이스 파일이 정확히 들어오고 파일마다 specs가 1건이다
주의:  WS-A의 K8 검사가 사람이 읽는 `--list` 출력을 파싱하면 어긋난다. JSON으로 봐야 한다

## [WS-C] 2026-09-16 · 스크린샷은 리포터가 아니라 kit이 찍는다
증상:  킥오프는 "리포터가 실패한 스텝의 스크린샷을 저장한다"인데 리포터에는 `page`가 없다
원인:  Playwright 리포터는 결과만 받는 관찰자다. 절차 단위로 화면을 찍으려면 실행 중인 픽스처를 쥔 쪽이어야 한다
해법:  kit의 절차 래퍼가 찍어 경로를 StepResult에 담고, 리포터는 조립·출력만 한다. 계약(`StepResult`)은 그대로다
주의:  경로에 필요한 runId·historyId는 러너가 `PLATFORM_RUN_ID`·`PLATFORM_HISTORY_ID`로 넘긴다. 화면을 연 적 없는 API 케이스는 찍지 않고 `httpTrace`를 남긴다

## [환경] 2026-09-16 · 호스트 5432는 이미 로컬 PostgreSQL이 잡고 있다
→ **docker-compose.yml 로 승격 (2026-09-16).** 호스트 쪽은 `5433:5432`. 컨테이너끼리는 5432 그대로.
   호스트에서 DB를 열 때만 5433을 쓴다 (`docker compose exec postgres psql`은 포트와 무관)

## [Phase0] 2026-09-16 · /tests 안에서 @playwright/test를 못 찾는다
→ **admin·runner Dockerfile 로 승격 (2026-09-16).** 두 이미지 다 `ln -s /app/node_modules /node_modules`.
   `/tests/demo` → `/tests` → `/` 경로가 `/app`을 지나지 않아 루트에 링크가 필요하다

## [환경] 2026-09-16 · `ALLOW_PROTECTED=1 claude`로 띄워도 훅이 계속 막는다
→ **SETUP.md §5 · HOOKS.md · setup/install.sh 로 승격 (2026-09-16).** 환경변수가 아니라
   `.claude/settings.local.json`에 넣는다 — 세션은 터미널이 아니라 상주 데몬에서 태어난다.
   Phase 0·병합 때만 두고 끝나면 지운다

## [Phase0] 2026-09-16 · 도커 빌드 컨텍스트를 앱 폴더가 아니라 루트로 잡았다
→ **docker-compose.yml 로 승격 (2026-09-16).** `context: .` + `dockerfile: apps/<앱>/Dockerfile`.
   모노레포는 워크스페이스 전체가 한 덩어리로 설치돼야 모듈이 풀린다

## [Phase0] 2026-09-16 · PLATFORM_PARAMS에 expected도 같이 싣는다
→ **코드로 승격 (2026-09-16).** `{"params":{…},"expected":{…}}` 한 객체
   (`runner/src/execute.ts` · `kit/src/runtime/inputs.ts`)

## [Phase0] 2026-09-16 · Vitest가 tests/** 의 Playwright 스펙까지 집어간다
→ **vitest.config.ts 로 승격 (2026-09-16).** include를 `apps/**/*.test.ts`·`packages/**/*.test.ts`로 좁혔다.
   단위 테스트는 `*.test.ts`, Playwright는 `*.spec.ts`로 확장자를 가른다
   **2026-09-19 에 화면용 `apps/**/*.test.tsx` 를 더했다** (PR #30). 가르는 축은 그대로다 —
   단위 테스트는 `*.test.*`, Playwright는 `*.spec.ts`

## [환경] 2026-09-16 · Bash로 파일을 고치면 protected/ownership 훅이 안 걸렸다
→ **guard.mjs bash 모드로 승격 (2026-09-16).** `python -c` 같은 우회는 여전히 못 잡는다 —
   spec-review A1~A3이 최종 방어선이다

## [환경] 2026-09-16 · bash 훅이 echo 안에 인용된 문구까지 막는다
증상:  훅 검증용으로 강제 push 문구를 echo로 흘려보내자 그 명령 자체가 차단됐다
원인:  bash 검사는 명령 문자열 전체를 정규식으로 본다
해법:  그대로 둔다 (안전 쪽 오탐). 검증할 때는 문구를 쪼개서 쓴다
