# 작성

> **이 장이 담는 절** — §3.6 · §7 Authoring API
> 기획서를 넣으면 맥이 테스트 코드를 만들어 초안 PR 을 열고, 사람이 화면에서 머지를 누르면 맥이 CI 를 기다렸다 병합한다.
> **코드는 저장소에만 생긴다.** 맥이 무엇을 어떤 순서로 하는지는 §3.6 「맥 경로는 전용 체인이다」가 정본이다.
> **쓰는 표** `authoring_request` · `authoring_asset` (2026-09-23) — 정의는 [공통/4-데이터모델.md](../공통/4-데이터모델.md) §6 에 있다.
> 피그마 토큰은 서비스 설정(`service.figma_token`)에서 읽기만 한다.
> 전체 목차와 작업별 읽기 동선은 [SPEC.md](../../SPEC.md).

---

### 3.6 작성 — 기획서를 테스트 코드로 (2026-09-22 신설)

- **책임**: 「이 기획서로 테스트를 만들어 달라」는 요청을 대기줄에 세우고, 맥에서 도는 작성 에이전트가
  집어 가 일하는 동안의 상태·작업 단계·결과를 화면에 낸다
- **애그리거트 루트**: `AuthoringRequest` (식별자는 대기줄 행 번호)
- **용어는 [공통/1-제품과-구조.md](../공통/1-제품과-구조.md) §2 가 정본이다** — `작성 요청`·`작성 에이전트`·`작업 단계`.
  여기서 새 이름을 짓지 않는다
- **불변식**
  - **코드는 저장소에만 생긴다.** 이 컨텍스트가 드는 것은 작성 요청과 작업 단계뿐이다.
    테스트 코드는 작성 에이전트가 **초안 PR**(draft pull request, 「아직 병합하지 말라」고 표시해
    올리는 변경 요청)로 저장소에 올리고, 사람이 화면에서 머지를 눌러야 코드가 된다.
    PR 을 여는 것은 **맥 스크립트**다 — 흐름은 아래 「맥 경로는 전용 체인이다」
    **근거의 정본은 §1 「영구히 하지 않는다」 표의 「화면이 테스트 코드를 직접 들고 편집하는 기능」 행이다** —
    2026-09-22 에 그 행이 **화면이 코드 본문을 들고 있을 때**로 좁혀졌다. 여기 옮겨 적지 않는다
  - **`test_source` 는 그 금지의 예외가 아니다.** 편집 대상이 아니라 화면이 실패한 줄을 발췌하려고
    받아 두는 스냅샷이고, 머지 전 파일이 admin 의 `/tests` 에 없어서 필요하다 (§6)
  - **한 요청은 한 서비스다** — `authoring_request.service_id`. 실행이 `test_run.service_id` 로
    같은 것을 거는 것과 같은 규칙이다 (§1 · §6)
  - **집어 가는 쪽은 맥 하나다. 그래서 줄도 하나다.** 작성·재실행·머지가 `kind` 로 갈릴 뿐 한 줄에 선다.
    집는 일은 `PENDING`→`RUNNING` 한 줄 UPDATE 이고 그것이 곧 잠금이다 — **정본은 §6**
  - **진행은 두 층이고 이 컨텍스트는 위층만 든다. 이 구분이 이 장의 핵심이다**
    - **위층 — 작업 단계** (`stage`). 「요구사항 표 만드는 중」·「케이스 2건째」·「관문 3 도는 중」.
      케이스를 **만드는 일**의 진척이고, 이 표가 새로 드는 것은 이것뿐이다
    - **아래층 — 테스트 절차** (`Step`). **이미 kit 과 러너가 든다** (§5.1 · §5.2). 새로 만들지 않는다
    - 아래층을 `stage` 에 또 담으면 같은 것이 두 곳에서 다른 말을 한다. 아래층은 `result` 에 통째로 실려 온다 (§6)
  - **입력은 자료 목록이다** (2026-09-23). 기획서 파일 여럿과 피그마 주소 여럿이 한 세트이고,
    **다 올리기 전에는 줄에 안 선다** (`DRAFT`). 정본은 아래 §7 「자료」
  - **맥이 꺼져 있으면 줄이 안 준다.** 요청은 `PENDING` 으로 쌓여 있다가 켤 때 처리된다.
    고장이 아니라 구조다 — 없애려면 러너가 브랜치를 보게 해야 하고 그것은 배포 구성과 러너 계약(§5.2)을 건드린다

#### ★ 인증을 뚫지 않는다

작성 에이전트는 **컨테이너 밖**에서 돌지만 **계정으로 로그인해 사람과 같은 문**을 쓴다
(비밀번호 대신 그 계정의 에이전트 토큰으로 — 2026-09-23, [인증.md](인증.md) §7).
전용 경로도, 인증을 건너뛰는 예외도 만들지 않는다. 등급도 사람과 똑같이 걸린다 —
등급이 모자라면 작성 요청을 집어가지 못한다.

**★ 그 계정은 `operator` 다. `admin` 을 주지 않는다** (2026-09-22 검토가 잡았다).

**★ 그리고 서버가 그 계정을 이름으로 안다** (2026-09-22 보안 검토가 잡았다).
**「집어 가는 쪽은 맥 하나다」는 전제이지 장치가 아니었다** — 서버가 그 지목을 안 보면
그 서비스에 배정된 **아무 `operator`** 가 집기를 두드려 맥보다 먼저 집고, 「끝났다」에
**자기가 고른 PR 주소**를 실을 수 있다. 그 행은 정상 완료로 보이고 `admin` 이 머지를 누르면
**그 사람이 고른 PR 이 병합된다** — 이 장이 막으려는 바로 그것이다.
그래서 **집기·단계 올리기·사진 올리기·끝내기 넷은 정해진 계정 이름에만 연다.**
이름이 안 정해져 있으면 **아무도 못 집는다.**
맥이 **행을 바꾸는** 일은 **집기·단계 올리기·사진 올리기·끝났다고 알리기** 넷뿐이고 넷 다 `operator` 다.
맥은 **기획서 파일도 내려받는다** (2026-09-23) — 그것은 읽기라 `viewer` 로 열려 있고 사람도 상세에서 받는다.
위 넷처럼 계정 이름을 볼 까닭이 없다. 남의 행을 바꾸는 일이 아니다.
**머지 행을 만드는 것은 사람**이고 맥은 그것을 집어 실행할 뿐이다 —
그래서 맥에 `admin` 이 필요 없다. 실행이 무엇인지(초안 풀기 · CI 기다리기 · 병합)는 아래 「맥 경로는 전용 체인이다」.

**왜 못박나** — 안 적으면 다음 세션이 「편하게 `admin` 으로 두자」를 고른다.
그 순간 **맥에 저장된 비밀값 하나가 `/api/settings/**` 전부를 연다** —
이 장이 세운 「실행할 수 있는 사람 ≠ 저장소를 바꿀 수 있는 사람」이 계정 설정 하나로 사라진다.

- **근거의 정본은 [인증.md](인증.md) §7 「인증 적용 범위」다.** `POST /api/auth/login` 을 뺀
  모든 `/api/**` 는 로그인이 필요하고 **예외는 없다**
- 그 절에 **2026-09-16 에 예외를 만들었다가 2026-09-17 에 지운 이력**이 있다. 그때의 판정이 이것이다 —
  그 예외가 내세운 방어는 **귀속**을 말할 뿐 **접근**을 막지 못한다. 이 길에도 그대로 적용된다
- **「맥에서 도는 거니까 토큰 하나 파자」로 되돌리지 마라.** 로그인 없이 지나가는 자리를 하나 내는 순간,
  §3.5 가 러너 포트를 닫으며 막으려 한 바로 그 뒷길이 admin 에 생긴다.
  **2026-09-23 의 에이전트 토큰은 이것이 아니다** — 계정에 묶여 확인 함수가 같은 사람을 돌려주므로 등급·배정·
  정해진 계정 이름이 그대로 걸린다. 계정 없는 토큰·공용 토큰은 여전히 금지다 ([인증.md](인증.md) §7 「인증 적용 범위」)

**§9.2 정기 실행 선례와 다른 이유** — 그쪽은 HTTP 를 안 쓰고 **컨테이너 안 명령**으로 풀었다.
작성 에이전트는 컨테이너 밖에 있어 그 길이 없다.
**설명의 정본은 [공통/1-제품과-구조.md](../공통/1-제품과-구조.md) §3 컨텍스트 지도다** — 여기 옮겨 적지 않는다.

#### ★ 머지는 운영 등급이다

| 할 일 | 최소 등급 | 왜 |
|---|---|---|
| 작성 · 재실행 | `operator` | 결과가 **초안 PR** 이라 마음에 안 들면 버리면 된다. 지금 「실행」과 같은 무게다 |
| **머지** | `admin` | **저장소를 영구히 바꾼다.** 되돌리려면 사람이 손으로 해야 한다 |

**둘에 같은 등급을 주면 「실행할 수 있는 사람 = 저장소를 고칠 수 있는 사람」이 된다.**

**★ 그런데 지금 서버는 이것을 표현할 수 없다.** `apps/admin/src/auth/gate.ts` 가
「설정 자리면 `admin`, 아니면 읽기는 `viewer`, **나머지 전부 `operator`**」로 경로와 방식만 보고 가른다.
그대로 두면 머지 통로가 무조건 `operator` 다. **화면이 버튼을 안 그리는 것은 편의이지 방어가 아니다** —
이대로 만들면 실행 등급이면 누구나 머지 통로를 직접 찌를 수 있다.

**이 장은 「무엇이 어느 등급인가」만 적는다. 「서버를 어떻게 고치나」는 [인증.md](인증.md) §7
「등급으로 갈리는 자리」가 맡는다.** 여기 중복해 적지 않는다.

**에이전트가 머지를 판단하지 않는다. 손일 뿐이다.** 사람이 화면에서 눌러야만 머지 요청이 생긴다.
사람 게이트가 사라지는 것이 아니라 자리를 옮기는 것이다.
맥이 보는 것은 「CI 가 초록인가」 하나이고, 초록이 아니면 병합하지 않고 멈춘다 (아래 절).

#### ★ 맥 경로는 전용 체인이다 (2026-09-23) — 정본은 여기다

맥에서 작성 요청 하나를 처리하는 일은 **`tpx-author` 전용 체인**을 탄다. 일을 둘로 나눈다.
**git 과 GitHub 을 만지는 일은 전부 맥 스크립트**가 하고, **자식 Claude**(맥 스크립트가 띄우는 `claude -p`)는
케이스를 만드는 일만 한다.

| 누가 | 무엇을 | 하지 않는 것 |
|---|---|---|
| 맥 스크립트 — 준비 | GitHub 이 말하는 main SHA(`git ls-remote`)를 받아 그 판으로 **작업방**(요청마다 따로 쓰는 폴더)을 연다. 로컬 브랜치는 만들지 않는다 | — |
| 자식 Claude | 자료 → 요구사항 표 → `tpx-cases` 로 케이스 → 관문 넷. 끝나면 결과 요약만 남긴다 | **git · gh · push · PR · `/tpx` · 백그라운드(background) 작업** |
| 맥 스크립트 — 마무리 | 바뀐 파일이 케이스뿐인지 확인 → commit → push → **초안 PR** 을 연다 → 서버에 끝났다고 보고(`prUrl` 을 싣는다) → 작업방을 치운다 | — |

- **PR 본문에 맥의 관문 3(3회 실행) 기록을 싣는다.** 아래 「테스트만 바뀐 PR」 이 그것을 병합 근거로 쓴다
- **머지** — 사람이 누르면 맥이 초안을 풀고(Ready), **PR 의 맨 끝 커밋(head)에 대한 최신 `check` 실행이 `success` 일 때만** 병합한다.
  그 밖(`failure` · `cancelled` · `skipped` · `timed_out` · 결과 없음)이면 병합하지 않고 `FAILED` 로 끝낸다.
  **다시 누르면 CI 를 다시 돌린다** — 이미 Ready 인 PR 은 풀어도 새 실행이 안 생기기 때문이다
- **★ 빨간 PR 의 병합을 막는 것은 GitHub 이 아니다.** main 보호가 관리자에게 강제되지 않고(`enforce_admins: false`)
  맥은 저장소 주인 계정으로 돈다. 그래서 **막는 것은 맥의 판정과 「관리자 우회를 쓰지 않는다」 둘뿐이다.**
  「GitHub 이 막아 준다」고 믿고 이 판정을 느슨하게 만들지 마라
- **테스트만 바뀐 PR 은 CI 가 가벼운 길로 돈다** — 타입 · 케이스 파일 규칙(K1~K10) · 비밀값 이름만 본다.
  **CI 는 새 케이스를 실행하지 않는다.** 사내 대상·비밀값이 필요한 케이스를 CI 러너가 못 돌리기 때문이다.
  그래서 **병합 근거는 PR 본문에 실린 맥의 관문 3 기록**이다. 무엇이 「테스트만」이고 무엇이 빠지는지의 정본은
  [`docs/HOOKS.md`](../../HOOKS.md) 「CI 와 병합 차단」이다 — 여기 옮겨 적지 않는다
- **맥이 켜질 때 자기 이름으로 잡힌 채 멈춘 `RUNNING` 을 `FAILED` 로 닫는다.** 사유는 「맥이 꺼져 중단됐다 — 다시 넣어라」.
  머지는 CI 를 기다리느라 오래 잡혀 있어서, 그사이 맥이 꺼지면 그 행이 영원히 `RUNNING` 으로 남는다.
  새 통로는 없다 — 목록(`?status=RUNNING`)과 끝내기만 쓴다

**신뢰 경계 (2026-09-23).** 기획서를 올릴 수 있는 사람(그 서비스의 `operator`)의 파일은 맥에서 자식 Claude 가 **셸 권한으로** 읽는다.
그래서 기획서에 숨긴 지시로 자식이 움직인다고 보고 막는다. 자식은 **GitHub 자격증명을 뺀 환경**으로 떠서 push·PR·병합을 못 하고
(`Bash(git:*)` 같은 이름 막기는 실수 방지일 뿐이다), 맥은 올리기 전과 병합 직전에 **GitHub 이 말하는 main SHA** 기준으로,
**켤 때 메모리에 고정한** 판정 스크립트로 「테스트만」을 판정한다. **그래도 자식은 맥 계정의 파일을 읽고 쓸 수 있다**(keychain 포함).
맥이 commit·push 할 때 도는 공유 훅 폴더(`core.hooksPath`)와 `.git/config` 도 자식이 쓸 수 있는 자리다 — 키체인과 함께 전용 macOS 계정으로 푼다 (후속).

**뒤집은 판단 — 「자식이 `/tpx` 를 타고 초안 PR 을 연다」.** 앞 판은 자식 Claude 에게 `/tpx` 일곱 단계를 통째로 맡겨
작업방·초안 PR 까지 자식이 열게 했다. **첫 한 바퀴 실측에서 무너졌다** (PR #62, 닫음).
관문 넷까지는 약 10분에 끝났는데 [6] 독립 검사가 30분을 넘기다 자식이 백그라운드 검사를 남긴 채 먼저 끝났고,
PR 은 [6] 에서 멈췄다. [6] 은 관문 넷과 겹치고 [7] 은 사람이 화면에서 누르니 자식에게 할 일이 없었다.
PR 주소도 자식 출력에서 찾아야 해서 고리가 약했다 — 이제 PR 을 스크립트가 여니 주소를 직접 안다.
**판단 기준이 아직 쓸모 있어 남긴다** — 다음에 「자식에게 `/tpx` 를 통째로 맡기자」가 나오면 이 실측부터 본다 (CLAUDE.md §2.7 ②).

---

## 7. Admin API

**새 통로를 라우트표에 등록한다.** `apps/admin/src/auth/scope.ts` 가 **「모르면 막는다」**로 돌아
등록 안 된 틀은 문이 403 으로 떨어뜨린다. 안 적으면 **모든 요청이 막히고 원인이 권한처럼 안 보인다.**
번호에서 서비스를 찾는 갈래가 하나 필요하다 — `authoring_request.service_id` 다.

**등급의 정본은 `도메인/인증` §7 「등급으로 갈리는 자리」다.** 아래 묶음 머리의 등급 표시는
그 규칙을 이 경로들에 **적용한 결과**이지 새로 정하는 자리가 아니다 — 둘이 어긋나면 인증 장이 이긴다.

```
# Authoring — 화면이 부른다. 읽기는 보기만(viewer), 넣기는 실행까지(operator)
POST   /api/authoring/requests           작성·재실행 요청을 만든다
         ?service=    **필수.** 보고 있는 서비스의 접두사. 배정받지 않은 것이면 403
         { kind, sourceId?, figma?, params? }
         ★ kind 가 AUTHOR 나 RERUN 이 아니면 400. 이 통로로는 MERGE 가 절대 안 들어온다
         AUTHOR — figma 는 피그마 주소 목록이다(없어도 된다). 행이 DRAFT 로 서고 주소마다 자료 한 행이 붙는다.
                  주소가 아래 「자료」의 모양이 아니면 400 { error: 'BAD_FIGMA_URL' }
         RERUN  — 자료를 새로 안 받는다. 곧장 PENDING 이다.
                  원본이 AUTHOR 이고 DRAFT 가 아니어야 한다. 아니면 409 { error: 'BAD_SOURCE' }
         (2026-09-23. 앞 판은 본문에 specText 를 실었다 — 아래 「자료」)
       → 201 { id }
POST   /api/authoring/requests/:id/assets        기획서 파일 하나를 올린다 (2026-09-23)
         ?name=       파일 이름. 한글이 된다
         본문은 파일 바이트 그대로 (application/octet-stream)
         ★ 요청한 사람만. 남이면 403 { error: 'NOT_REQUESTER' }. DRAFT 가 아니면 409
       → 200 { id }
POST   /api/authoring/requests/:id/submit        줄에 세운다. DRAFT → PENDING (2026-09-23)
         ★ 요청한 사람만. 자료가 0 이면 409 { error: 'NO_ASSETS' }. DRAFT 가 아니면 409
       → 200
GET    /api/authoring/requests           대기줄 한 쪽
         ?service=    **필수**
         ?status=     DRAFT | PENDING | RUNNING | DONE | FAILED. 생략하면 전부
         ?page=
       → { items, total, page, pageSize }
         items 에 기획서 본문(specText)을 싣지 않는다 (2026-09-23). 목록은 본문을 안 그린다
GET    /api/authoring/requests/:id       한 건 상세
       → { stage, stageAt, result, prUrl, testSource, assets, ... }
GET    /api/authoring/requests/:id/assets/:assetId  기획서 파일 하나를 받는다 (2026-09-23). 맥도 이것을 부른다
       → 파일 바이트. 피그마 자료이거나 다른 요청의 자료 번호면 404

# Authoring 머지 — 운영(admin) 등급만. 저장소를 영구히 바꾼다
POST   /api/authoring/merges             머지를 줄에 세운다
         ?service=    **필수**
         { sourceId }
       → 201 { id }    kind = MERGE 인 대기줄 한 행

# Authoring — 맥이 부른다. 사람과 같은 문을 지난다 (실행까지 등급)
POST   /api/authoring/requests/claim     줄에서 한 건 집어 간다
         ?service=    **필수**
       → 집은 한 건 + assets. 줄이 비었으면 204
         피그마 자료가 있을 때만 figmaToken(그 서비스의 피그마 토큰)을 싣는다. 없으면 키 자체가 없다
PATCH  /api/authoring/requests/:id/stage 작업 단계를 올린다
         { stage }
       → 200
POST   /api/authoring/requests/:id/screenshots   사진 파일을 올린다
       → 200 { dir }
POST   /api/authoring/requests/:id/finish        끝났다고 알린다
         { status, result?, testSource?, prUrl?, error? }
       → 200
```

- **화면이 부르는 것과 맥이 부르는 것이 갈린다.** 넣기·자료 올리기·줄에 세우기·읽기·머지 요청은 화면이 부르고,
  집기·단계 올리기·사진 올리기·끝났다고 알리기는 맥이 부른다. 자료 내려받기는 둘 다 부른다.
  **같은 문을 지난다** (위 ★ 절).
  **단 사진 올리기는 지금 맥 코드가 안 부르고, 그래서 에이전트 토큰으로는 안 열린다** (2026-09-23) —
  맥이 부르게 되면 `auth/gate.ts` 의 `토큰통로` 에 한 줄을 더한다. 목록의 정본은 [인증.md](인증.md) §7 「인증 적용 범위」
- **머지만 경로가 갈린 이유** — 같은 경로에 `kind: 'MERGE'` 로 얹으면 등급이 **본문 값**에 따라
  갈려야 하고 그러려면 문이 본문을 읽어야 한다. 경로가 다르면 경로만 보고 가른다
- **★ 경로를 가른 것만으로는 안 닫힌다 — 본문도 가둔다** (2026-09-22 검토가 잡았다).
  문은 **경로**를 보고 `operator` 를 통과시키는데, 그 뒤 라우트가 본문의 `kind` 를 그대로 받으면
  `{ kind: 'MERGE', sourceId: <초안 PR 번호> }` 한 줄로 **머지 행이 대기줄에 서고 맥이 집어 간다.**
  그러면 **실행 등급인 사람이 저장소를 영구히 바꾼다** — 이 장이 막으려는 바로 그것이다.
  **막는 자리를 둘로 겹친다.**
  ⒜ 이 통로가 `AUTHOR`·`RERUN` 밖의 `kind` 를 **`400` 으로 거부한다** (위 §7)
  ⒝ 표가 `kind` 값 자체를 가둔다 (`공통/4-데이터모델` §6 의 `CHECK`)
  **하나만으로는 부족하다** — ⒜ 는 새 라우트가 생기면 빠질 수 있고, ⒝ 만 있으면 `MERGE` 가
  대기줄에 서는 것을 막지 못한다 (`CHECK` 는 값의 모양만 본다)
- **`requestedBy` 는 요청에 싣지 않는다.** 로그인한 세션에서 채운다 —
  실행이 `triggeredBy` 를 그렇게 하는 것과 같은 이유다 (§7.1 · §3.5)
- **사진만 따로 올린다.** 판정과 소스 본문은 `finish` 가 JSON 으로 한 번에 올리지만 사진은 파일이다.
  러너는 admin 과 같은 볼륨을 봐서 경로만 주고받으면 되는데 **맥은 다른 기계라 파일 자체를 올려야 한다** (§6)
- `result` 의 모양은 **§5.1 이 정본이다.** 맥이 러너와 같은 리포터를 타므로 같은 모양으로 온다
- **★ `?service=` 하나로 판정을 끝내지 않는다** (2026-09-22 검토가 잡았다).
  그 값은 **부르는 쪽이 적는 것**이고 문은 그것을 배정과 대조할 뿐이다 —
  `auth/gate.ts` 가 자기 주석에 그 함정을 적어 뒀다
  (「`/api/runs/<남의번호>?service=<내것>` 이 통과한다」, 2026-09-19 실측).
  **줄을 거르는 것은 라우트다.** 둘을 못박는다.
  ⒜ **`claim` 은 그 서비스의 `PENDING` 행만 집는다.** 자원을 **서버가 고르는** 통로라
  안 거르면 **남의 서비스 행**을 집어 주고, 그 행에는 기획서 자료가 딸려 있다(옛 행은 `spec_text` 본문 통째).
  피그마 자료가 있으면 **그 서비스의 피그마 토큰까지** 응답에 실린다
  ⒝ **`sourceId` 가 가리키는 행의 `service_id` 가 `?service=` 와 다르면 `403`.**
  본문에 실린 번호라 경로 검사가 안 닿는다 — 안 막으면 남의 서비스 요청을 가리키는
  `RERUN`·`MERGE` 가 선다
- 경로에 실린 번호(`:id` · `:assetId`)는 **열 자리 숫자 글자만** 받는다. 그 밖이면 `400` —
  문(`auth/scope.ts`)과 라우트가 같은 값을 읽어야 한다

#### 자료 — 파일과 피그마 (2026-09-23 신설)

**작성 요청의 입력은 자료 목록이다.** 기획서 파일 여럿과 피그마 주소 여럿이 한 세트다.
표 정의와 파일 저장 자리는 [공통/4-데이터모델.md](../공통/4-데이터모델.md) §6 「작성 자료」가 정본이다.
**아래 상한과 종류는 이 절에만 적는다.** 다른 곳은 이 절을 가리킨다.

**본문을 붙여 넣던 방식을 접었다.** 앞 판은 화면이 기획서 본문(`specText`)을 실어 보냈다.
경로를 보내면 **맥은 다른 기계라 그 파일을 못 읽기** 때문이었다. **그 판단 기준은 지금도 맞다** —
그래서 이번에도 경로가 아니라 **파일 자체를 서버에 올린다.** 바뀐 것은 사실 하나다.
기획서는 **PDF·워드 파일로 오고 피그마가 같이 온다.** 글만 붙여 넣으면 표와 그림이 빠진다.
다음에 「그냥 붙여 넣게 하자」가 나오면 이 사실이 아직 맞는지부터 본다 (CLAUDE.md §2.7 ②).

| 무엇 | 정한 것 (2026-09-23 승인) |
|------|------|
| 받는 파일 종류 | `.pdf` · `.docx` · `.doc` · `.md` · `.txt`. 그 밖(`.hwp` · `.pptx` 등)은 400 `{ error: 'BAD_FILE_TYPE' }` 이고 화면은 **이유를 문구로** 보인다 |
| 한 파일 상한 | **20MB.** 넘으면 413 |
| 한 요청의 자료 개수 상한 | 파일과 피그마를 합쳐 **20.** 넘으면 만들 때는 400, 올릴 때는 409 |
| 파일 이름 | 한글이 된다. 제어 문자 · `"` · `/` · `\` · `..` 가 있으면 400 `{ error: 'BAD_NAME' }` |
| 피그마 주소 | `https://` 이고 호스트가 `figma.com`·`www.figma.com`, 경로가 `design`·`file`·`proto` 인 것만 받는다. **통과·거절이 아니라 정규화한다** — 파일 키와 `node-id` 만 뽑아 `https://www.figma.com/design/<키>/?node-id=<숫자-숫자>` 로 **다시 조립해 저장한다.** 파일 전체 링크처럼 `node-id` 가 없으면 `https://www.figma.com/design/<키>/` 로 저장한다. 「Copy link」가 붙이는 `&t=…` 같은 꼬리는 버린다. 저장값에 셸 특수 글자가 원천적으로 없어서 맥이 명령줄에 끼워도 안전하다. **브랜치 링크**(`/design/<키>/branch/<브랜치키>/…`)는 브랜치 키로 조립한다 — 본 파일 키로 줄이면 main 을 읽는다. 브랜치 키도 같은 `[A-Za-z0-9]{1,64}` 로 좁힌다 (2026-09-23) |
| FigJam (`/board`) | **안 받는다.** 화면 디자인이 아니다. 400 `BAD_FIGMA_URL` 과 이유 문구 |

- **순서는 만들기 → 올리기 × 파일 수 → 줄에 세우기다.** 올리는 동안 행은 `DRAFT` 라 맥이 안 집는다.
  줄에 세우기는 `WHERE status='DRAFT'` 한 문장 UPDATE 다
- **중간에 실패한 `DRAFT` 는 안내만 한다.** 「이 요청은 줄에 서지 않았다. 새 요청으로 다시 넣어라」를
  새 요청 폼과 그 요청의 상세에 보인다. 이어 올리기와 지우기는 만들지 않는다
- **올리는 서버는 행 → 파일 순서로 쓴다.** 파일 쓰기가 실패하면 그 행을 지운다 —
  행만 남으면 내려받기가 없는 파일을 찾는다
- **내려받기는 브라우저가 파일을 열지 못하게 준다.** `content-type: application/octet-stream` 고정 ·
  `content-disposition: attachment; filename*=UTF-8''<퍼센트 인코딩한 이름>` · `X-Content-Type-Options: nosniff`.
  확장자로 `text/html` 을 짐작하게 두면 올린 파일이 admin 과 같은 출처에서 열린다
- **피그마 토큰은 서비스 설정에 있다** ([인증.md](인증.md) §8.8). 맥에는 안 둔다.
  집기 응답이 피그마 자료가 있을 때만 싣고, 맥은 그 값을 **claude 자식 프로세스의 환경에만** 넘긴다.
  토큰이 없는데 피그마 자료가 있으면 맥은 claude 를 안 부르고 `FAILED` 로 끝낸다 — 사유는 「설정 화면에 피그마 토큰을 넣어라」
- **피그마를 읽는 도구는 npm 패키지 `figma-reader` 다** (GitHub 저장소 이름은 `figma-reader-cli` — 둘을 헷갈리지 마라).
  부르는 명령과 판 번호의 정본은 `.claude/skills/tpx-cases/SKILL.md` 다
- **자료도 본문도 없는 행은 돌리지 않는다.** 맥은 claude 를 안 부르고 `FAILED` 로 끝낸다. 옛 행은 `spec_text` 로 돈다

```
[계약 변경 필요]
대상:    Admin API — POST /api/authoring/requests 요청 형태 · 새 통로 셋 · 목록·상세·집기 응답
현재:    요청 만들기가 { kind, sourceId?, specText, params? } 를 받고 곧장 PENDING 으로 줄에 선다.
         목록 응답이 기획서 본문(specText)을 통째로 싣는다
제안:    AUTHOR 는 { kind, figma? } 로 DRAFT 행을 만든다. 자료 올리기 · 줄에 세우기 · 자료 내려받기 통로가 는다 (위 §7).
         목록에서 specText 를 뺀다. 상세와 집기 응답에 assets 를, 집기에는 피그마 자료가 있을 때만 figmaToken 을 싣는다
이유:    입력이 파일 여럿과 피그마 여럿이다. 올리는 동안 줄에 서면 맥이 빈 것을 집는다
영향:    WS-작성(화면 · 서버 · 맥 에이전트)
상태: 반영 완료 (2026-09-23, apps/admin/src/authoring/routes.ts · assets.ts)
```

---

### 화면에서 어디에 서나 — **정해졌다**

**정본은 [공통/5-화면공통.md](../공통/5-화면공통.md) §8 「자리 목록」이다.**
`테스트 작성` 은 `테스트 케이스` **다음**이다. **여기 옮겨 적지 않는다** — 자리가 늘거나 순서가 바뀌면
그 절만 고친다.

> **2026-09-22 에 정해졌다.** 앞 판이 「위치는 화면 리뉴얼 뒤에 정한다」로 미뤄 뒀는데
> **그 리뉴얼이 끝났고**(PR #51) 같은 날 자리 절이 다섯째를 받았다.
> **미뤄 둔 문장을 그대로 두면 다음 세션이 자리를 다시 고른다** — 그러면 사이드바 순서가
> 정본과 어긋나고, 그 어긋남은 **화면을 눈으로 보기 전까지 아무 검사에도 안 걸린다**
> (2026-09-22 검토가 잡았다).

**왜 자기 자리를 갖나** — 작성은 대기줄·진행·결과·머지를 다 담아 버튼 하나가 아니고,
「케이스」 화면이 **이미 있는 것을 보는 곳**인 데 반해 여기는 **아직 없는 것을 만드는 곳**이다.

### 아직 알 수 없는 것

**지어내지 않는다.** 화면 설계가 아직 안 정한 자리다
(`docs/plans/2026-09-21-테스트작성-화면-설계.md` 의 「아직 안 정한 것」).

| 무엇 | 지금 |
|------|------|
| `status` 를 몇으로 두나 · `screenshot_dir` 를 무엇으로 가르나 · `params` 로 무엇을 바꿀 수 있나 · 한 번에 몇 건까지 줄에 세우나 | **[공통/4-데이터모델.md](../공통/4-데이터모델.md) §6 「아직 알 수 없는 것」 표가 정본이다.** 여기 옮겨 적지 않는다 — 정해지면 그 표를 고친다 |
| 서버가 머지 등급을 **어떻게 표현하나** | [인증.md](인증.md) §7 이 맡는다 (위 ★ 절) |
| 실패한 작성 요청을 어떻게 치우나 | 다시 시도인지 · 버리는 것인지 · 둘 다인지 **안 정했다.** 정하면 위 §7 에 통로가 는다. 중간에 실패한 `DRAFT` 도 같다 — 지금은 안내만 한다 (§7 「자료」) |
| ~~**지금 있는 셸 진입점을 남기나 없애나**~~ **→ 2026-09-22 에 없앴다** | **들어오는 길은 화면뿐이다.** `npm run authoring-agent -- <기획서 경로>` 는 **admin 서버를 아예 안 불러 로그인을 지나지 않았다** — §3.5 가 러너 포트를 닫으며 막은 뒷길과 같은 성질이라 대기줄이 선 뒤에도 두면 「로그인을 안 지나는 작성 경로」가 남는다. 이제 그 명령은 **인자 없이** 대기줄을 도는 일만 한다 ([`docs/SETUP.md`](../../SETUP.md) §8). **판단 근거를 남긴다** — 다음에 「편하게 셸에서 바로 돌리자」가 나올 때 같은 질문을 다시 해야 한다 (CLAUDE.md §2.7 ②) |
| ~~**맥 계정의 비밀값을 어디에 두나**~~ **→ 2026-09-23 에 닫혔다** (PR #58) **→ 같은 날 뒤집었다** (PR #67, 게이트 0) | **★ 지금은 에이전트 토큰이다.** 설정 화면에서 작성 에이전트 계정에만 발급하고, 맥은 첫 실행에 한 번 받아 홈 폴더의 파일(`~/.test-platform/agent-token`, 본인만 읽기)에 둔다. **왜 뒤집었나** — 켤 때마다 치는 비밀번호가 실제로 허들이 됐다(사용자, 2026-09-23. 임시 비밀번호 재발급에서 한 번 헤맸다). 운영체제 키체인은 윈도우에서 같은 방식이 안 돼 버렸다. **아래 「그 파일이 곧 열쇠」 판단은 지금도 맞다** — 그래서 열쇠를 좁혔다. 토큰으로는 맥이 부르는 통로만 열리고(요청 만들기·머지·설정은 안 열린다) 화면에서 언제든 취소한다 ([인증.md](인증.md) §7 「인증 적용 범위」). **자식 세션이 그 파일을 읽을 수 있다** — 같은 OS 계정이라서다. 자식은 GitHub 자격증명이 없지만 이 토큰으로 줄을 집을 수는 있다. 전용 OS 계정도 이것은 못 막는다(사람 계정과 떼어 놓을 뿐이다). 그래서 믿을 수 있는 기획서만 넣는다는 전제(§3.6)가 그대로다. **옛 판단** — **어디에도 안 둔다. 켤 때 묻는다.** 맥에 남는 것은 아이디(`AUTHORING_AGENT_USER`)뿐이고, 비밀번호는 에이전트를 켤 때 사람이 터미널에 직접 친다. 파일이나 파이프로 먹이면 거부한다 — 그 파일이 곧 열쇠가 되기 때문이다 (`scripts/authoring-agent.ts` 의 `비밀번호묻기`). **피그마 토큰도 맥에 안 둔다** (2026-09-23) — 서버의 서비스 설정(`service.figma_token`)에 있고 맥은 집기 응답으로 받는다 (§7 「자료」). **옛 물음을 남긴다** — 「편하게 `.env` 에 두자」가 나오면 같은 질문을 다시 해야 한다 |
| ~~**`finish` 를 부른 쪽이 집은 쪽인지 어떻게 보나**~~ **→ 2026-09-22 에 닫혔다** | **막는 자리를 셋으로 겹쳤다.** ⒜ 표에 `claimed_by` 칸을 두고 단계·사진·끝내기가 **집은 쪽과 대조**한다 ⒝ ★ **집기 자체를 맥 전용 계정 이름에만 연다** — ⒜ 만으로는 못 막는다. 그것은 「A 가 집은 행에 B 가 쓰는 것」을 막을 뿐이고 **B 는 그냥 먼저 집으면 된다. 경계가 아니라 경주다** (2026-09-22 보안 검토가 잡았다). 이름이 안 정해져 있으면 **아무도 못 집는다** ⒞ `pr_url` 이 **그 서비스의 저장소 주소 + `/pull/<숫자>`** 모양인지 본다 — 안 보면 **다른 저장소의 PR** 이 병합되고, 맥이 그 값을 명령줄에 끼워 넣으면 **맥에서 임의 명령이 돈다**. **옛 판정은 지우지 않는다** — 집는 일이 한 줄 UPDATE 라 「누가 집었는지」가 잠금에는 필요 없다는 판단은 지금도 맞다. 필요해진 것은 **귀속**이다 |
