# 검사 기록 — PR #74 역방향 계약 반영 (2026-09-25)

선언 3등급 / 실측 3등급 (KIT · MIGRATION · SPEC · TESTS · HARNESS · DOC) · 차선 full · 미분류 없음
kit `CaseSpec.unconfirmed` · `defineCase` 입력 · `db/migrations/20260925000001_reverse_mode.sql` · 계약 블록 둘 반영 완료

## 렌즈

| 렌즈 | 판정 |
|---|---|
| spec-review (A·C·H 절) | 치명 0 · 중대 1 · 경미 2 |
| 코드 + 보안 검토 | BLOCKER 0 · 주의 3 (code-review 스킬은 뒤에서 돌아 결과가 늦어 검토자가 직접 봤다) |

## 지적 (심각도 순)

1. **2-명세선언:112 괄호의 「상태: 대기」** (spec 중대) — §5.1 블록은 반영 완료가 됐는데 이 줄이 「계약 대기」로 읽힌다. K11 검사기가 없다는 사실만 남기고 괄호를 고친다
2. **공백만 있는 사유도 미확정** (코드 주의) — `defineCase.ts:67` 의 `if (input.unconfirmed)` 는 `' '` 도 참. `trim()` 으로 거르고 검사 하나
3. **설정 저장마다 테스트 계정이 지워진다** (코드 주의) — `settings/store.ts:70-78` 이 줄을 지우고 `(env, base_url)` 만 다시 넣는다. 지금 쓰는 곳이 없어 드러나지 않는다. WS-F 킥오프에 이미 항목이 있다(`docs/WORKSTREAMS.md` 📐 WS-F)
4. **CLAUDE.md 「이 넷」** (spec 경미) — XRC 가 끼어 어느 넷인지 흐려졌다
5. **부숴서 확인한 기록 없음** (spec 경미 G8)

## 사실 확인 — 렌즈가 틀린 것

- 코드 렌즈의 「DB 검사가 한 번도 안 돌았다」는 pre-push(DB 없음) 기록만 본 것이다. [5] 최종 점검에서 검사용 DB `platform_xrc` 를 붙여 vitest 전체를 돌렸고 107 파일 · 1529 검사 전부 통과했다(skip 0). 마이그레이션 적용 전 RED(제약 이름까지 맞춘 거절 검사 전부 실패)도 [5] 작업자가 확인했다

## 비밀번호 유출 — 없음

- `service_env` 조회 셋(`settings/store.ts:94` · `auth/store.ts:54` · `execution/store.ts:158`)은 전부 칸 이름을 적어 읽는다. `SELECT *` 없음
- `grafana_ro` 는 `service_env` 에 권한이 없다(20260917000001 에서 전부 회수 뒤 `test_run`·`run_item` 만)

## 검사 명령 (같은 HEAD)

check:deps · typecheck · check:workflow · check:spec · check:tests · vitest 전체(DATABASE_URL=platform_xrc) — 전부 EXIT=0

## 게이트 2 1차 → 「고치고 재검사」

| 지적 | 처리 |
|---|---|
| 1 명세선언:112 괄호 | 「계약 대기」를 빼고 「타입은 반영, 검사기만 남음」으로 |
| 2 공백 사유 | 검사 `공백뿐이면 키를 싣지 않는다` 추가 → RED(1 failed) 확인 → `trim()` → GREEN |
| 3 설정 저장이 계정을 지움 | 범위 밖 — WS-F 항목 그대로 |
| 4 CLAUDE.md 「이 넷」 | 개수 대신 이름 다섯 |
| 5 G8 부숴서 확인 | 지적 2 의 RED 로 kit 쪽 확인. DB 검사는 적용 전 RED(9건 실패)가 같은 몫 |
| 재검사 경미 — K11 은 빈 문자열만 적음 | WORKSTREAMS WS-A K11 항목에 「공백뿐도 위반」 한 줄 |
