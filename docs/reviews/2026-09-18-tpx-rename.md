# SPEC 검사 결과 — tpx-rename (하네스) (2026-09-18)

`[자기검사]` — **개명을 만든 세션이 그대로 검사했다** (철칙 1 위반).
체인이 자기 이름을 바꾸는 작업이라 다른 세션이 들어올 자리가 없었다.
게이트 2 요약에 이 사실을 실었다.

## 요약

치명 0건 · 중대 0건 · 경미 0건 — **병합 가능**

범위: PR #20 · `origin/main...HEAD` 17파일 +404/-148
선언 3등급 / 실측 3등급 · 미분류 없음

## 범위 판정

**제품 코드 0건.** `apps/` `packages/` `tests/` `db/` 어느 것도 diff 에 없다.
바뀐 것은 스킬 여덟 · 스크립트 셋 · 문서 다섯뿐이다.

따라서 A(계약) · B(도메인 불변식) · C2~C3(경계) · D(용어) · F(화면) 은
**대조할 대상이 없다.** 「통과」가 아니라 **해당 없음**으로 적는다 —
공허한 초록불을 통과로 세지 않는다.

| 절 | 판정 | 근거 |
|---|---|---|
| A1~A6 계약 | 해당 없음 | `packages/kit` · `db/migrations` · `docker-compose.yml` · 러너 · Admin API 전부 diff 에 없음 |
| B1~B8 불변식 | 해당 없음 | 도메인 코드 0건 |
| C1 소유 경로 | **통과** | 하네스 작업이라 `WORKSTREAM` 을 안 켰다. 갈래 소유 경로를 하나도 안 건드렸다 |
| C2~C3 경계 | 해당 없음 | 제품 모듈 0건 |
| D1~D5 용어 | 해당 없음 | `tcId` 계열 표기를 안 건드림 |
| E1 `check:tests` | **통과** | EXIT=0 · 케이스 10건 · K1~K10 |
| E2~E4 데모 | 해당 없음 | `tests/` 변경 0건 |
| F1~F7 화면 | 해당 없음 | `apps/admin/src/web` 변경 0건 |
| G1~G2 TDD | **통과** | 아래 참조 |
| G3~G5 fixture | 해당 없음 | DB 테스트 변경 0건 |
| H1~H5 문서 정합 | **통과** | 아래 참조 |

## G — TDD

**G1 대응 테스트가 있는가 → 있다.** 개명을 지키는 판별식이 `chain-contract.test.mjs` 다.
이번 변경으로 실제 RED → GREEN 을 돌렸다.

```
디렉터리 옮기기 전:  19 통과 / 0 실패
디렉터리 옮긴 직후:   1 통과 / 18 실패
본문·상수 맞춘 뒤:   19 통과 / 0 실패
```

**중간의 18 실패가 비-공허의 증거다.** 판별식을 손대기 전에 먼저 깨뜨려 빨간불을 확인했다.

**G2 테스트를 느슨하게 고친 흔적 → 없다.** `chain-contract.test.mjs` 의 변경은
**이름 문자열뿐**이다. 단언 자체는 한 줄도 안 바뀌었다. 통과 수가 19 로 같다.

## H — 문서 정합

| # | 검사 | 결과 |
|---|---|---|
| H1 | `npm run check:spec` | EXIT=0 |
| H2 | 같은 규칙이 두 곳에서 다른 말을 하나 | 정본 선언 일관. `CLAUDE.md` 16·22행이 `.claude/skills/tpx/SKILL.md` 를 가리키고, SPEC 색인 37·54행이 `tpx-spec` 을 부른다 |
| H3 | 색인 네 곳 | 절을 새로 안 만들었다. 「기능을 더하거나 고칠 때」 라우터 표 2곳 살아 있음 |
| H4 | 절 번호를 바꿨나 | 안 바꿈. `docs/SPEC.md` diff 에 `§` 가 붙은 줄 변경 0건 |
| H5 | 손으로 적은 숫자 | 셋 다 재현 확인 — 판별식 **49**(`# tests 49`) · 스킬 **8**(`ls | grep -c '^tpx'`) · 충돌 **52**(설치된 명령 전체 대조) |

## 기타 — 체크리스트 밖에서 하나 나왔다

**개명이 열려 있던 PR 의 상황판을 깨뜨렸다.**
`pr-update.mjs` 가 블록을 찾는 마커가 `<!-- tp:status -->` 였다. 이름을 바꾸니 옛 블록을
못 찾고 앞에 새로 붙여 PR #20 본문에 「진행 상태」가 둘이 됐다. 손으로 걷어냈다.

기계가 쓰는 닻에 사람이 읽는 이름이 박혀 있던 것이 원인이다. `CLAUDE.md §2.5` 기준
첫 번째라 `LEARNINGS` 에만 남겼다 — 두 번째 개명이 오면 마커를 이름과 무관하게 바꾼다.

**체크리스트 항목 제안: 없음.** H 절이 「문서가 서로를 가리키는 것」을 이미 다루고,
이건 문서가 아니라 GitHub 본문이라 이 스킬의 범위 밖이다. 한 번 더 나면 그때 항목을 만든다.

## 검사 명령 종료 코드

```
check:deps      EXIT=0
typecheck       EXIT=0
check:workflow  EXIT=0   (# tests 49 · # pass 49 · # fail 0)
check:spec      EXIT=0
check:tests     EXIT=0
npm test        EXIT=0   (테스트 파일 30개)
```

**건수가 아니라 종료 코드로 판정했다.**
DB 를 안 건드렸으므로 G3 의 연속 3회는 돌리지 않았다.
