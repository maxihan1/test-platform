# 2026-09-23 · PR #67 검사 — 에이전트 토큰 · 머지 뒤 pull · 파일 나누기

등급: 선언 3 / 실측 3 (HARNESS·TESTS·AUTH·WEB·MIGRATION·DOC·SPEC·GUARD) · 미분류 없음 · 35파일 +1570/−440

## 렌즈

| 렌즈 | 판정 |
|---|---|
| code-review (superpowers:code-reviewer) | BLOCKER 0 · 주의 3 |
| spec-review | 치명 0 · 주의 4 (중대 2 · 경미 2) |
| security-review | BLOCKER 0 · 주의 3 |

## 지적 (합쳐서 겹친 것은 하나로)

1. **작성 계정 이름을 바꾸면 옛 계정의 토큰이 살고 화면에서 못 지운다** (code·security) — `토큰주인` 이 `작성계정인가` 를 안 본다 · 화면은 `isAuthoringAgent` 만 보고 칸을 그린다
2. **SPEC 두 곳이 옛 말을 한다** (spec 중대) — `공통/6-인프라.md` §9.2 「비밀번호는 켤 때 묻고 맥에 남는 비밀값 0」 · `도메인/작성.md` 「사진 올리기는 맥이 부른다」 ↔ `토큰통로` 에 사진 없음
3. **`.env.example` 안내가 옛 비밀번호 문구** (spec 경미)
4. **허용 목록 통과 검사가 두 통로뿐 · HEAD→GET 검사 없음** (code)
5. **병합 뒤 `git pull` 이 post-merge 훅·config 를 부모 권한으로 돈다** (security) — 새로 연 문은 post-merge 하나. `-c core.hooksPath=/dev/null -c core.fsmonitor=false` 권장
6. **작성 계정 판정이 두 곳** (code) — `작성계정인가` · `authoring/routes.ts` `맥계정인가`
7. **자식이 토큰 파일을 읽으면 남의 요청 집기·피그마 토큰·아무 PR 주소로 끝내기가 가능** (security) — 작성 §3.6·결정표가 받아들인 위험. `finish` 의 prUrl 을 `올릴브랜치(id)` 로 좁히는 것은 후속
8. **계획 files 와 diff 차이** (spec C5) — 서버 검사를 `auth/agentToken.test.ts` 하나로 모았다. `scope.test.ts` 는 라우트표를 자동으로 훑어 새 틀을 덮는다. 실질 빈틈 없음

## 통과한 것 (요약)

틀(routeOptions.url)로 판정 · 틀린 Bearer 는 세션으로 안 넘어감 · 토큰 요청에도 등급·서비스 배정 그대로 · 설정·머지·새 요청·사진 403 · 256비트 무작위 + SHA-256 + 부분 유일 인덱스 · grafana_ro 는 app_user 못 읽음 · 원문은 발급 응답 한 번 · 파일 0600 · http 비-localhost 거부 · 마이그레이션 = §6 DDL · 6e69367 순수 이동 · 새 파일 머리 주석·300줄 · fixture 접두사 등록

## 게이트 2 — 「고치고 재검사」 (2026-09-23, 사용자)

- 1 · 6 · 4 → `토큰주인` 이 지금의 작성 계정만 받는다 · `맥계정인가` 가 `작성계정인가` 를 쓴다 · 토큰만 남은 계정에 [토큰 취소] · `토큰통로` ⊂ `등급표` 단언 · HEAD 검사 (0390972)
- 5 → 당길 때 `-c core.hooksPath=/dev/null -c core.fsmonitor=false`
- 2 · 3 → 6-인프라 §9.2 · 작성 §7 사진 문장 · `.env.example`
- 7 → 후속 (`docs/progress/WS-작성.md` 8회차)
- 8 → 위 사유로 닫는다

## 검사 명령

check:deps · typecheck · check:workflow · check:spec · check:tests **EXIT 0** · DB 붙인 `npm test` 연속 3회 **EXIT 0 (1436)**
