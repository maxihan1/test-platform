# 검사 기록 — 역방향 WS-B (PR #76, 2026-09-26)

범위 `git diff origin/main...HEAD` 15파일 · 선언 3등급 / 실측 3등급(HARNESS·TESTS·ADMIN·SPEC·DOC) · 미분류 없음 · 차선 full

## 렌즈

| 렌즈 | 판정 |
|---|---|
| spec-review | 치명 0 · 주의 2 · 경미 3 |
| code-review | BLOCKER 0 · 주의 0 · 경미 5 |
| ponytail-review | BLOCKER 0 · 주의 1 · 경미 4 |

## 검사 명령 (검사용 DB `platform_wsb`)

check:deps · typecheck · check:workflow · check:spec · check:tests(K1~K11 통과) · test:changed(137) · test:always(198) — 전부 EXIT=0

## 지적

### spec-review
- 주의 C6 — `docs/WORKSTREAMS.md:40` 「나머지(통로·집계·설정)는 아직 대기」 — 집계는 이번에 닫혔다
- 주의 H2 — 숫자 줄의 미확정 미실행 칸 · 0 인 칸 빼기(게이트 1 결정)가 명세에 없다. `notify.ts` 주석이 없는 문장(§8.9)을 근거로 든다
- 경미 C5 — `runTypes.ts` 는 계획 밖(300줄 규칙으로 구현 중 추가)
- 경미 G8 — RED 확인 기록 없음. 실제: 새 DB 검사 5건 전부 빨강(박제 NULL · counts pass 2 · failed 거르개 1줄 · items undefined · 머리 [실패]) → 초록, Slack 순수 검사 7건 중 5건 빨강 → 초록
- 경미 — `docs/WORKFLOW.md:33` 「구현은 한 줄도 없다」가 낡았다(이번 diff 이전부터)

### code-review
- `runSummary.ts:62` `allPass` 필드 주석이 옛 뜻 · `queries.ts` 빈 줄 두 줄 두 곳 · `runSummary.ts` 의 `db()` 복사(저장소 관례) · `FailedCase.unconfirmed`(boolean)가 다른 자리의 같은 이름(사유 문자열)과 헷갈린다 · `store.ts` 369줄(main 에서 이미 366줄)
- 확인: 항등식 성립 · Slack TARGET 은 예전과 같은 방식 · 머리 분기 경계(빈 실행 [통과]) · DISTINCT 두 줄 안 됨 · 분리 동작 불변

### ponytail-review
- 주의 — 확정/미확정 조건이 네 곳(`RUN_COLUMNS`·`runSummary` 하위 질의·HAVING·notify TARGET)에 복사
- 경미 — `queries.ts` 의 타입 re-export 를 쓰는 곳이 없다 · 빈 줄 · 실패 목록 정렬을 SQL 로 · `unconfirmed?` 선택 칸일 필요 없음

## 사고 한 건

spec-review 렌즈가 DB 검사를 기본 DB(`platform`)에 대고 돌렸다 — 칸이 없어 61건이 처음에 실패했다. 운영 DB 를 읽기 전용으로 확인했고 `X`·`ZZ` 접두사 서비스·케이스·실행 0건으로 찌꺼기가 없다.

## 재검사 (게이트 2 「고치고 재검사」)

- 고친 것: 명세 §3.2 에 숫자 줄 꼴 `미확정 N(통과 a · 실패 b · 미실행 c)` · 0 칸 빼기 한 줄(§8.x 막대 줄은 §3.2 를 가리킴) · `notify.ts` 근거 주석 · WORKSTREAMS:40 · WORKFLOW:33 ·
  `allPass`·`hasFail` 필드 주석 · 빈 줄 · 쓰이지 않는 타입 re-export 삭제 · `FailedCase.unconfirmed` → `is_unconfirmed`
- 그대로 둔 것: 확정 조건 네 곳 복사(목록·알림 질의를 한 몸으로 묶는 편이 더 복잡하다) · `store.ts` 369줄(main 에서 이미 366줄) · 증적 숫자 줄의 0 칸 규칙은 WS-D 가 §3.2 를 따른다
- 검사 명령 7종 EXIT=0 · 실행 갈래 DB 검사 138 통과(`platform_wsb`)
