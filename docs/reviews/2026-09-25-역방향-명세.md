# 검사 기록 — PR #73 역방향(모드 B) 명세 (2026-09-25)

선언 3등급 / 실측 3등급 (SPEC · HARNESS · DOC) · 차선 full · 미분류 없음
명세·문서·스킬만 — 코드 0줄. 계약 블록은 전부 `상태: 대기`

## 렌즈

| 렌즈 | 1차 판정 | 재검사 |
|---|---|---|
| spec-review (H 절 포함) | 치명 0 · 중대 3 · 경미 9 | — |
| 보안 검토(명세 설계 — 명세대로 만들면 구멍이 생기나) | 치명 0 · 중대 8 · 경미 7 | — |
| 재검사(지적 닫힘 · 새 문장끼리 정합) | — | 치명 0 · 중대 1 · 경미 3 → 전부 고침 |
| code-review | 건너뜀 — 코드 없음 | — |

게이트 2 1차에서 사용자가 「고치고 재검사」를 골랐다.

## 1차 지적 (심각도 순, 겹친 것은 합침)

1. **`outputs` 통로가 인증 등급표에 없다** (spec 중대 · 보안 경미) — 글자대로 만들면 에이전트가 403. 등급표·토큰 통로·라우트표 셋에 넣고 「넷」 숫자를 뺐다
2. **비밀번호가 에이전트 산출물을 타고 viewer 에게 샌다** (보안 중대) — 에이전트 스크립트가 올리기·끝내기·push 전에 원문을 찾아 FAILED
3. **operator 는 사실상 비밀번호를 얻는다** (보안 중대) — 「operator 전원이 알 수 있는 값」 · 전용 낮은 권한 계정 · 운영 줄 금지
4. **리다이렉트로 다른 출처에 계정을 친다 · SSO 미정** (보안 중대) — 입력 직전 출처 확인 · 리다이렉트도 안 따라감 · SSO 이번 판 미지원
5. **startUrl 정규화 없음** (보안 중대) — http/https · `new URL()` 재조립 · userinfo 거절 · 셸 문자열 금지 · 집기 때 재대조
6. **훑기가 금지 목록이라 빈틈 · 운영 서버 막는 규칙 없음** (보안 중대) — 허용 목록 · 계정 넣은 줄(아이디·비밀번호 둘 다)만
7. **outputs 파일 종류가 사람 입력 규칙 그대로** (보안 중대) — `.doc`→`.docx` · 역기획서 `.docx` 만 · 이름·순서·헤더 · 개수 상한 제외 · `BAD_SOURCE`
8. **피그마 쓰기 권한의 위험 미기재** (보안 중대) — 계정이 닿는 모든 파일 · 전용 피그마 계정
9. **미확정 꼬리표로 실패를 숨길 수 있다** (보안 중대, 품질) — 기존 케이스에 새로 달면 경고 · Slack 머리 `[통과 · 미확정 실패 N]`
10. **K11 이 「누가 달았나」를 본다고 읽힌다** (spec 중대) — 「K11 은 모양만 · 늘 사람이 본다」 네 자리 통일
11. **insights 의 미확정 처리 없음** (spec 중대) — 판정·덩어리에서 뺀다, 응답 모양은 그대로(계약을 늘리지 않게 컨트롤러가 좁힘)

경미: SETUP 피그마 3번 · 2-명세선언 107·111 · compare 아닐 때 오류 코드 · 「미확정 N」 정의 · 미확정만 돌린 실행 색 · WORKSTREAMS K 목록·WS-E 항목 · 계획 files 경로 · 상세에 계정 없음 · 로그 가림 · 화면만 예외 · storageState 대안

## 재검사 지적

- **중대** SETUP 「계정 없는 줄은 화면에서 못 고른다」가 구현 불가 — 화면은 `/auth/me` 로 계정 유무를 모른다. 계약을 늘리지 않고 「서버가 `BAD_ENV` 로 거절」로 고쳤다. 목록에서 빼려면 `/auth/me` envs 에 `hasLogin` 이 필요 — 구현 PR 에서 사용자 결정
- 경미 화면만 예외 문장 순서 · 줄이 지워진 compare 행의 target 모양(null 로 싣는다) · RERUN 에 역방향 칸이 오면 `BAD_ENV` · WORKSTREAMS 줄 번호

## 컨트롤러가 정한 것

startUrl 뜻(대조에서는 선택) · outputs 응답 `200 { id }` · 미확정 나이는 사유가 바뀌어도 유지 · K11 검사기 반영 전 줄 · insights 응답 모양 유지 · 「계정을 넣은 줄」 = 아이디·비밀번호 둘 다 · 재대조 FAILED 는 에이전트가 알린다 · 운영 서버 = 계정 넣은 줄만 · SSO 미지원

## 검사 명령

check:deps · typecheck · check:workflow · check:spec · check:tests · check:docs-contract · test:always — 전부 EXIT=0
