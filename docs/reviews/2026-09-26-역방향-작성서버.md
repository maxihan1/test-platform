# 검사 기록 — 역방향 WS-작성 ① 서버 통로 (PR #79)

2026-09-26 · 선언 3등급 / 실측 3등급(HARNESS · ADMIN · TESTS · AUTH · SPEC · DOC, 차선 full) · 미분류 없음 · 22파일 +1218/−233

## 검사 명령 (DATABASE_URL = 검사용 DB `platform_wsw`)

| 명령 | 종료 코드 |
|---|---|
| check:deps · typecheck · check:workflow · check:spec · check:tests | 0 · 0 · 0 · 0 · 0 |
| test:changed -- origin/main (226) · test:always (198) | 0 · 0 |

`check:tests` 에 「이미 있던 케이스에 미확정 꼬리표」 경고 없음 (케이스 변경 없음).

## 렌즈

| 렌즈 | 판정 |
|---|---|
| code-review | 주의 2 · BLOCKER 0 |
| spec-review | 주의 2 · 치명 0 · 경미 1 |
| security-review | 통과 (취약 0) |

### 주의
1. code · `assets.ts:76` 주석이 4MB 사진 파서를 `routes.ts` 에 있다고 적음 — 이제 `agentRoutes.ts`
2. code · `agentRoutes.ts:124` 주석 「넷(집기·단계·사진·끝내기)」 — `outputs` 도 `집은쪽인가` 를 써서 다섯
3. spec A5·H2 · `compare` 가 참·거짓이 아니면 400 `BAD_ENV` 가 명세 §7 에 없음 — 한 줄 더한다
4. spec C5 · 계획 할 일 9 `files` 의 `docs/WORKFLOW.md` 가 diff 에 없음 — WORKFLOW 는 WORKSTREAMS 를 가리켜 바꿀 것이 없다

### 경미
- spec H5 · 계획 「Plan 메타」 검사 DB 가 `platform_wsa` — 게이트 1 표는 `platform_wsw`
- `store.ts` 363줄 (원래 353) — 킥오프·진행 기록에 「다음 PR 이 뗀다」로 적음

### 보안이 확인한 것
비밀번호는 집기 `target`(대조 행 · 작성 계정)에만 · 목록·상세·400 본문·로그 없음 · outputs 권한(등급·토큰·라우트표·작성 계정·집은 쪽) ·
경로 거슬러 오르기 없음 · 내려받기 헤더 고정 · startUrl 은 서버가 fetch 안 함 · 잠금으로 TOCTOU 없음 · 운영 줄 선택 불가.
남는 한계(명세 §7) — 만든 뒤 admin 이 `base_url` 을 바꾸면 집기에 새 서버 · 옛 시작 주소가 나간다. 가려 실패시키는 것은 에이전트(③)다.

## 재작업 (게이트 2 「고치고 재검사」, 2026-09-26)
주의 1·2 주석 고침 · 주의 3 작성 §7 에 「compare 가 참·거짓이 아니면 400 BAD_ENV」 · 주의 4 계획 files 에 WORKFLOW 무변경 사유 · 경미 계획 메타 DB 이름 `platform_wsw` · 색인 분량(1815 · 4182 · 537).
재검사 — 검사 일곱 EXIT=0 (test:changed 226 · test:always 198). 코드 동작 변경 없음(주석 둘뿐)이라 렌즈는 다시 안 돌렸다.
