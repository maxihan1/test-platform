# SPEC 검사 결과 — PR #63 작성 전용 체인 · `git diff origin/main...HEAD` (2026-09-23)

## 요약
치명 0건 · 중대 0건 · 주의 4건 — **병합 가능** (주의는 사용자가 판단)

검사는 만든 세션이 아닌 하위 세션이 돌렸다. 코드는 고치지 않았다.
G3(연속 3회)은 부르는 쪽이 이미 돌린 결과(DATABASE_URL 붙여 `npm test` 3회 EXIT 0, 1349건)를 그대로 쓴다.
이 세션에서 직접 돌린 것은 `npm run check:spec`(통과) · `npm run check:workflow`(111/111 통과)다.

## 주의

### 주의 1 — H2 코드 주석이 옛 흐름을 말한다
어디:   scripts/authoring-assets.ts:94
무엇:   `셸허용됐나` 주석이 「자식은 git·gh·npm·npx 를 다 쓴다」라고 적는다.
        같은 PR 이 SETUP §8 에서는 그 구절을 지웠고, 작성 §3.6 · tpx-author 는 「자식은 git·gh 를 안 쓴다」로 바꿨다.
왜 문제: 다음 세션이 이 주석을 근거로 부분 허용(`Bash(npx:*)`)을 거부하는 이유를 잘못 읽는다. 결론(`Bash`·`Bash(*)`만 인정)은 그대로 맞다.

### 주의 2 — PR #63 본문이 게이트 1 결정 이전의 흐름을 적고 있다
어디:   PR #63 본문 「무엇을 만드나」
무엇:   「② 가벼운 길 (타입 · 케이스 형식 · **바뀐 케이스만 실행**)」·「준비: 작업방·**브랜치**」·「CI 초록이면 **자동 병합**」.
        게이트 1 결정과 저장소 문서는 「케이스를 실행하지 않는다」·「로컬 브랜치를 안 만든다(`--detach`)」·「`--auto` 가 아니라 맥이 기다렸다 병합」이다.
왜 문제: 게이트 2 에서 사람이 읽는 곳이 PR 본문이다. 「CI 가 새 케이스를 돌려 준다」로 읽고 승인하면 병합 근거(관문 3 기록)를 안 보게 된다.

### 주의 3 — G8 부숴서 빨개지는 것을 본 기록이 PR 에 없다
어디:   PR #63 코멘트 · 커밋 메시지
무엇:   새 검사(`cases-only.test.mjs` · `ci-covers-tests` 가벼운 길 단언 넷 · `hook-contract` 임시 저장소 검사 · `chain-contract` tpx-author · `authoring-chain.test.ts`)를
        구현을 한 자리 부숴 빨개지는지 확인했다는 기록을 찾지 못했다.
왜 문제: 초록인 것과 무는 것은 다르다. 게이트 2 요약에 싣거나 그 자리에서 한 번 부숴 본다.

### 주의 4 — G9 가벼운 길에서 유일한 규칙 검사가 「없으면 넘어감」 깃발을 단다
어디:   .claude/hooks/pre-push:65 · .github/workflows/ci.yml:109
무엇:   가벼운 길에서 도는 규칙 검사는 `check:tests` 하나뿐인데, 그 줄이 `--if-present` 를 단다(무거운 길의 :81 과 같은 옛 모양을 옮겨 왔다).
        `package.json` 에서 그 스크립트가 빠지면 가벼운 길은 **타입 검사만** 보고 초록이 된다.
        `ci-covers-tests` 의 판정 단계 단언도 `if … then light=1 else light=0` 모양만 보므로, 그 앞에 무조건 `light=1` 을 쓰는 줄이 끼면 걸리지 않는다.
왜 문제: 가벼운 길의 초록불은 「형식이 맞다」를 말해야 하는데, 형식 검사가 사라져도 초록이 된다.
        `chain-contract` 의 「스킬이 부르는 npm 스크립트가 package.json 에 있다」가 일부를 덮지만, 훅·CI 줄 자체는 덮지 않는다.

## H 절 결과
- H1 `check:spec` 통과 — 없는 절 0 · 깨진 링크 0 · 틀린 분량 0 · 상태 줄 없는 블록 0
- H2 같은 규칙 대조 (작성 §3.6 ↔ HOOKS ↔ SETUP §8 ↔ CLAUDE.md §0·§2.3 ↔ 코드)
  - 정본 배치: 흐름 = 작성 §3.6 · 가벼운 길 = HOOKS 「테스트만 바뀐 PR·push 는 가벼운 길」. 명세선언 §4 · CLAUDE.md · WORKSTREAMS · SETUP · spec-review B9 가 둘을 가리키고 옮겨 적지 않는다 — 맞음
  - 「CI 는 새 케이스를 실행하지 않는다 · 근거는 관문 3 기록」: 작성 §3.6 · HOOKS · SETUP · 명세선언 · ci.yml 머리 · tpx-author · `PR본문()` 모두 같음
  - 「GitHub 이 막는 게 아니다 · 관리자 우회 없음」: 작성 §3.6 · HOOKS · `authoring-chain.ts` 머지인자 주석 · `authoring-merge.ts` 머리 같음. 「GitHub 이 막는다」는 ci.yml:7(초안은 원래 병합 불가 — 옛 줄, 사실)에만 있고 새 문장에는 없다. 머지인자 검사가 `--admin`·`--auto`·`--force` 를 막는다
  - 17분: SETUP 「최대 약 17분」 = `authoring-merge.ts` `전체제한 17 * 60_000` · 실패 사유 문구. SPEC 에는 숫자가 없다(맞다 — 숫자를 한 곳에만)
  - 15초 폴링: 코드에만 있고 문서가 안 적는다 — 어긋남 없음
  - 다시 돌리기: 작성 §3.6 「다시 누르면 CI 를 다시 돌린다」 · SETUP 「다시 누르면 다시 돌리고 다시 기다린다」 ↔ 코드는 누름 한 번에 rerun 최대 한 번, 이미 Ready 이고 최신 실행이 빨강일 때만. 처음 누름의 빨강은 바로 FAILED — SETUP 「빨갛거나 시간 안에 안 끝나면 실패로 끝낸다」와 같음
  - CLAUDE.md §2.3 예외 문장은 「기존 폴더」 조건을 생략하지만 HOOKS 를 정본으로 가리킨다 — 어긋남 아님
  - 어긋남 1건: 주의 1 (코드 주석)
- H3 새 절 없음(§3.6 안의 소제목). 라우터 표에 「맥 작성 경로(전용 체인·머지·CI 가벼운 길)를 바꿀 때」 줄 있음 — 통과
- H4 기존 절 번호 안 바뀜 — 통과
- H5 SPEC.md 분량(작성 307 · 명세선언 187 · WS-작성 1464 · 전부 3655 등) check:spec 대조 통과. SETUP 「거부당하면 — 셋 중 하나」·「셋 다」 = 표 3행 — 맞음

## SPEC 동반 수정 대조 — 닫힘
- 할 일 1 — 작성 :4·:20·:58·:93 흐름 문장 · §3.6 「맥 경로는 전용 체인이다」(표 · 머지 판정 · `enforce_admins: false` · 가벼운 길 근거 · 켤 때 RUNNING 닫기 · 뒤집은 판단 남김) · 명세선언 §4 CI 줄 · 인증 :191 · 데이터모델 :178 `pr_url` — 전부 diff 에 있음
- 할 일 2 — SPEC.md 분량 · 라우터 한 줄 — 있음
- 할 일 10 — CLAUDE.md §0 규칙 · §2.3 예외 · HOOKS 가벼운 길 정본 · SETUP §8(맥이 PR 을 연다 · CI 기다림 · pull 뒤 다시 켜기 · `core.hooksPath`) · WORKSTREAMS `scripts/authoring-*.ts` · WORKFLOW 단위 상태 · spec-review B9 — 전부 있음
- C5 계획 `files` 전부 diff 에 있음. 계획 밖 `scripts/authoring-assets.ts` 는 게이트 1 반영 절 할 일 6 ⒢(셸허용은 사용자 설정만)가 이유 — C1 통과

## 통과한 항목
A1~A6(계약 파일 변경 없음), B1~B9, C1~C6, D1~D5, E1~E4(tests 변경 없음), F1~F10(화면 변경 없음), G1~G7, H1~H5(H2 는 주의 1 외 통과)

## 기타
- `scripts/authoring-agent.ts` 472줄 — CLAUDE.md §3 300줄 상한을 넘는다(715 → 472 로 줄었지만 아직 넘음). 알려진 `authoring-run.ts` 308줄과 별개. 다음 분리 후보
- (코드 리뷰 영역) `authoring-merge.ts` — `gh run rerun` 직후 15초 뒤 첫 폴링이 아직 옛 `completed/failure` 를 읽으면 `다시돌림=true` 라 바로 FAILED 로 닫힌다. `/code-review` 렌즈에 넘긴다
