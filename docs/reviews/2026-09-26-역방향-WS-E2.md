# 검사 기록 — 역방향 WS-E ② 작성 화면 (PR #80)

2026-09-26 · 선언 1등급 / 실측 1등급(WEB · TESTS · DOC, 차선 full) · 미분류 없음 · 13파일 +634/−33

## 검사 명령 (DB 없음 — 화면만)

| 명령 | 종료 코드 |
|---|---|
| check:deps · typecheck · check:workflow · check:spec · check:tests | 0 · 0 · 0 · 0 · 0 |
| test:changed -- origin/main (548) · test:always (163, DB 검사 36 건너뜀) | 0 · 0 |

## 렌즈

| 렌즈 | 판정 |
|---|---|
| code-review | 주의 1 · BLOCKER 0 |
| 화면 규칙 대조(DESIGN.md · CSS · 목업) — `/qa-only` 대신. 역방향 요청이 끝난 기록이 없어 앱을 눌러 볼 수 없다 (LEARNINGS 2026-09-26 교훈) | 주의 3 · BLOCKER 0 |

### 주의
1. code · `AuthoringNew.tsx` `서버 = 고른서버 ?? envs[0]` — 서비스를 바꾸면 옛 서비스에서 고른 서버가 남아 화면에 보이는 것과 다른 `env` 를 보낸다(BAD_ENV), 서버 0 인 서비스에서도 보내기가 켜진다
2. 화면 · `AuthoringDetail.tsx` 상세의 `화면만 — …` `small` 이 앞 글자에 붙어 나온다(`.detail dd small` 규칙 없음)
3. 화면 · `.mono` 규칙이 styles.css 에 없다 — 번호·케이스·시작 주소가 고정폭으로 안 나온다(원래 있던 빈 클래스지만 이번에 세 곳 더 기댄다)
4. 화면 · 목업 차이 표가 5칸 — DESIGN.md·코드는 7칸(`자리`·`케이스` 빠짐)

### 통과한 것
판정 색 없음(차이·표시 실패·목록 표시) · `.error-text` 는 같은 폼의 기존 오류 관례 · 빌려 쓴 `.dhist`·`.authoring-assets` 규칙에 판정 색 없음 ·
좁은 화면에서 표만 구름 · 새 클래스 전부 CSS 에 있음 · 접근 이름(체크박스·select·시작 주소·표·목록) · `--ink-faint` 12px 명암비 5.14 이상 ·
요청 본문이 서버 계약과 맞음(대조 끄면 칸 없음 · 시작 주소 trim · 기본 서버 첫 줄) · `차이목록` 방어 · i18n 키 전부.
