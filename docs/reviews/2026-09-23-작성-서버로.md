# 2026-09-23 · PR #68 검사 — 작성 에이전트를 서버 컨테이너로

등급: 선언 3 / 실측 3 (COMPOSE·TESTS·DOC·SPEC·GUARD) · **미분류 `apps/authoring/start.sh`** · 26파일 +599/−87

| 렌즈 | 판정 |
|---|---|
| code-review | BLOCKER 1 · 주의 4 |
| spec-review | 치명 1 · 주의 7 |
| security-review | BLOCKER 1 · 주의 3 |

## BLOCKER·치명

1. **(security) 자식이 서버 저장소 전체(/repo, 쓰기 가능)를 PR 없이 고칠 수 있다** — `tests/*.spec.ts`(admin 목록에 바로, runner 가 실행 — runner 는 postgres 망), `scripts/authoring-*.ts`·`.claude/hooks`·`docker-compose.yml`(다음 재시작 때 진짜 토큰으로). 맥 경로에도 같은 구멍이 있었다(맥 체크아웃 = admin 이 보는 tests). 근본 해결은 별도 clone + 자식 uid 분리 → **사용자 결정**
2. **(code) ssh 원격이면 fetch·push·pull 이 전부 실패** — `url.https.insteadOf` 를 start.sh 에
3. **(spec 치명) 6-인프라 §9 compose 스케치에 admin networks · 최상위 networks · `author_node_modules` · author restart·env·mem·shm 가 없다**

## 주의 (합침)

- 한계 문구에 **에이전트 토큰**도 `/proc/<부모>/environ` 으로 읽힌다 · 서버 `.git/config`·전역 설정(HOME 공유) 변조 한계 누락
- postgres 가 호스트 `0.0.0.0:5433` — authoring 망에서 게이트웨이로 닿을 수 있다 → `127.0.0.1` 로 좁힌다
- 옛 「맥이 기본」 문장: 공통/1 용어 표 · 작성 39·44 · `admin주소` 주석 · tpx-cases SKILL·`assets.ts` 의 textutil 안내 · WORKFLOW.md · SETUP 「병합되면 다시 켠다」에 서버 재시작 없음 · 토큰 저장 문구
- `deploy.test.ts` 의 `not.toMatch(/default/)` 가 주석에도 깨진다 → networks 줄로 좁힘
- `authoring-agent.test.ts` 320줄(원래 307) · host 에 없는 `.env`/`node_modules` 는 root 소유 mountpoint

## 통과 (요약)

set -e 안전 · 볼륨 위 `npm ci` 정상 · 권한 0777 상속 · compose 구성이 게이트 1 과 일치 · 자식 credential.helper 비우기가 전역 helper 까지 덮는다 · 과금 방어 충분 · 명령 주입 없음 · docker socket 없음 · 맥 경로 회귀 없음 · 테스트 197건·tsc 0

## 게이트 2 — 「고치고 재검사」 · 격리는 후속 (2026-09-23, 사용자)

- 1 → **후속 PR**(고객사 설치 전 필수). 작성 §3.6 · SETUP §8 「남는 한계」에 적었다
- 2 → start.sh 에 `url.https.insteadOf` 두 줄 · 3 → §9 스케치에 admin networks · 최상위 networks · `author_node_modules` · author restart·depends·env·mem·shm
- 주의 → 한계 문구(에이전트 토큰·HOME·저장소 쓰기) · postgres `127.0.0.1` (+ deploy 검사) · 옛 문장(공통/1 용어 · 작성 39·44 · `admin주소` · tpx-cases · `assets.ts` · WORKFLOW · SETUP 재시작·저장 문구) · `not.toMatch` 를 networks 줄로 · `authoring-agent.test.ts` 에서 주소 검사를 `authoring-address.test.ts` 로(320→277)

## 검사 명령

check 5종 EXIT 0 · DB 붙인 `npm test` 연속 3회 EXIT 0 (1455)
