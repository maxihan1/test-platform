# 검사 기록 — 역방향 WS-작성 ③-1 에이전트 뼈대 (PR #81)

2026-09-26 · 선언 2등급 / 실측 2등급(HARNESS · DOC · TESTS · GUARD, 차선 full) · 미분류 없음 · 13파일 +952/−22

## 검사 명령

| 명령 | 종료 코드 |
|---|---|
| check:deps · typecheck · check:workflow · check:spec · check:tests | 0 · 0 · 0 · 0 · 0 |
| test:changed -- origin/main (151) · test:always (163) | 0 · 0 |

## 렌즈

| 렌즈 | 판정 |
|---|---|
| code-review | BLOCKER 1 · 주의 1 |
| spec-review | 주의 3 · 치명 0 · 경미 1 |
| security-review (2등급 필수 아님 — 비밀번호를 다루는 PR 이라 더했다) | 취약 0 · 주의 2(낮음) |

### BLOCKER
1. code · `authoring-upload.ts` PR 뒤 구간 — `역기획서올리기` 의 fetch 가 시간 초과·연결 실패 두 번으로 **던지면** `닫으며` 가 FAILED 로 닫는다. 게이트 1 「PR 뒤 실패는 DONE+이유」 위반 · `prUrl`·`diffs` 가 안 실려 PR 주소를 잃는다

### 주의 (합침)
1. spec·security · `diffs.json` 은 날 글자만 검사 — 비밀번호에 `"`·`\` 가 있으면 JSON 이스케이프로 비껴가고, 푼 값이 `result.diffs` 로 저장된다. **finish 직전에 푼 값의 글 칸**을 검사해야 한다(명세 「finish 전」)
2. security · 되읽기가 `-t plain` 이라 링크 주소·문서 메타데이터(YAML `title`)를 버린다 — HTML 엔티티로 쪼갠 원문이 docx 링크 대상·`core.xml` 에 들어가 올라갈 수 있다
3. spec · 바꾼 docx 에서 비밀번호가 나오면 DONE+error 로 뒀는데 명세 문구는 FAILED
4. code·spec · `scripts/authoring-agent.test.ts` 282 → 311줄
5. spec 경미 G8 · 새 검사를 일부러 부숴 빨개지는 것을 본 기록이 없다

### 통과한 것
재대조 다섯 조건 · `TARGET_*` 환경만(인자·프롬프트에 값 없음, 자리 uid 가 달라 `/proc/*/environ` 못 읽음) · 사유 거르기 · push 전 케이스·PR 본문·차이·원고 검사 ·
`out/` 읽기(자식을 다 거둔 뒤 · lstat · nlink · realpath — 맥 `/private/var` 도 양쪽 realpath 라 안전) · pandoc 자식 uid · 맥 변환 환경에 토큰 없음 ·
그림 거절로 참조형 그림까지 막힘(`&#33;[`·`!\[` 는 그림이 아님 · raw HTML img·`\includegraphics` 는 docx 가 버림) · 화면만 판정 두 곳 일치 · 입력만 양쪽 · 파일 300줄(제품 코드) · 스킬 200줄.

### 남는 한계
로그인 직전 출처 확인은 스킬 지시이고 코드가 강제하지 않는다 · 자식이 비밀번호를 찍으면 컨테이너 로그(`흘림`)에 남는다(서버·PR 로는 안 간다) — 명세 「남는 한계」의 「자식이 직접 보내는 것은 못 막는다」와 같은 층이다.
