# 검사 기록 — PR #71 작성 경로 스킬 정리 (2026-09-25)

선언 3등급 / 실측 3등급 (GUARD · HARNESS · TESTS · AUTH · WEB · DOC · SPEC) · 차선 full · 미분류 없음
45 파일 +1656/−1244

## 렌즈

| 렌즈 | 판정 |
|---|---|
| code-review | 주의 2 · BLOCKER 0 |
| spec-review (A~H 전부) | 치명 0 · 중대 3 · 경미 2 |
| security-review | 주의 2 · BLOCKER 0 |
| qa-only | 건너뜀 — 화면 코드 변경 없음 (`web/api.ts` 선택 칸 하나) |

## 지적 (심각도 순, 겹친 것은 합침)

1. **가벼운 길이 받는 새 폴더 모양이 CI 면제 모양보다 넓다** (code 주의 1 · security 주의 1)
   `cases-only.mjs:23` 은 `tests/<아무 폴더>/<아무 이름>.spec.ts` 를 받는데 `ci-covers-tests` 면제는 `<접두사>-NNN.spec.ts` 만 든 폴더다.
   ⒜ `tests/newsvc/login.spec.ts` 같은 PR 이 cases 차선으로 병합되면 다음 full PR 이 남의 폴더 때문에 빨개진다 ⒝ 자식이 기획서 지시로 `tests/<아무 이름>/` 을 심어도 「테스트만」 통과 — 이번 PR 로 새 폴더까지 넓어졌다.
   고칠 것: 새 폴더 spec 이름을 `<접두사>-\d{3}\.spec\.ts` 로 좁혀 두 규칙을 맞춘다. 에이전트 쪽 「내 서비스 폴더·접두사만」 검사는 후속 후보
2. **「맥」 정리가 반쯤** (spec 중대) — `spec-review/references/checklist-a-c.md:39`(B9) · 작성.md:60-64·246·261-265 · 4-데이터모델 `pr_url` 주석
3. **「집지 않고 FAILED」 가 코드·상태 전이와 다르다** (spec 중대) — 작성.md:173. 코드는 집은 뒤(RUNNING) 작업방 전에 FAILED
4. **옮긴 절을 옛 자리로 가리킴** (code 주의 2 · spec 중대) — SETUP.md:358 · SPEC.md:90 · 작성.md:330 (피그마 명령은 `references/1-input.md`). `docs/cases/TODO.md:151` 은 검사(`cases-probe-contract`)가 SKILL.md §4 모양을 요구해 그대로 둠
5. **testsDir 검증 정본이 에이전트 한쪽** (security 주의 2 · spec 경미) — 설정 API 는 `min(1)`. `.git`·`.claude` 같은 점으로 시작하는 이름 통과. PR 전부터 있던 admin 전용 문제

경미: HOOKS.md:246 「알려진 한계」가 기존 폴더만 말함 · 계획 표의 「인증 §4」 는 §7

## 계획 밖 파일
- `apps/admin/src/execution/runs.test.ts:120` — 필수 칸 `testsDir` 때문에 fixture 한 칸
- `docs/SETUP.md` · `docs/WORKSTREAMS.md` · `docs/SPEC.md` · `CLAUDE.md` — §2.7 동반 수정

## 안 한 것
- 고객사 별도 `tests_repo` 를 집기 전에 거르기 — `/auth/me` 에 저장소 주소를 더 실어야 해서(계약). 지금은 서버 끝내기 대조가 막는다 (작성 §3.6 「아직 막지 않는 것」)

## 검사 명령
check:deps 0 · typecheck 0 · check:spec 0 · check:tests 0 · check:workflow 0 · test:changed 0 (556) · test:always 0 (198) — `DATABASE_URL` 붙임

## 게이트 2 → 고치고 재검사 (사용자, 2026-09-25)

| 지적 | 조치 |
|---|---|
| 1 새 폴더 판정이 넓다 | `cases-only.mjs` spec 이름을 `<접두사>-\d{3}\.spec\.ts` 로 좁혔다 — RED(`login`·`pay-001`·`PAY-01` 등 거짓) → GREEN. 에이전트 쪽 「내 서비스 폴더만」 검사는 후속 |
| 2 맥 잔여 | B9 · 작성.md:60-64·246·261-265 · 4-데이터모델 `pr_url` 주석 고침. 나머지 옛 문장은 작성.md 머리말 「용어」 한 줄로 — 「맥」 = 작성 에이전트 |
| 3 「집지 않고」 | 「집은 뒤 작업방을 만들기 전에 `FAILED`」 로 |
| 4 옛 자리 | SETUP.md:358 · SPEC.md:90 · 작성.md:330 → `references/1-input.md` |
| 5 testsDir 정본 | 안 고침 — 설정 API 검사는 WS-F 범위이고 PR 전부터 있던 admin 전용 문제. 후속 |
| 경미 | HOOKS 「알려진 한계」 새 서비스 폴더 포함 · 계획 표 §4 → §7 |

재검사: 렌즈는 다시 돌리지 않았다(고친 것이 지적 자리 그대로라).
재검사 명령: check:deps 0 · typecheck 0 · check:spec 0 · check:tests 0 · check:workflow 0 · test:changed 0 (556) · test:always 0 (198) — `DATABASE_URL` 붙임

## 통과한 것 (요약)
A1~A6 · B1~B9 · C2~C4 · C6 · D · H1·H3~H5 · `..`/rev-parse fail-closed · MERGE 흐름 무영향 · 재실행 폴더 동일 · 스킬 분리 규칙 손실 없음(줄 대조) · 200줄 상한 · 고아 references 없음 · `/auth/me` viewer 노출 무해
