# 검사 기록 — 역방향 WS-F (PR #77, 2026-09-26)

범위 `git diff origin/main...HEAD` · 선언 3등급 / 실측 3등급(HARNESS·TESTS·AUTH·SPEC·DOC) · 미분류 없음 · 차선 full

## 렌즈

| 렌즈 | 판정 |
|---|---|
| security-review | BLOCKER 0 · 주의 0 · 경미 2 |
| spec-review | 치명 0 · 주의 2 · 경미 3 |
| code-review | BLOCKER 0 · 주의 2 · 경미 4 |

렌즈에는 「검사 명령을 돌리지 마라」를 넣었다(LEARNINGS 2026-09-26 렌즈 DB 항목).

## 검사 명령 (검사용 DB `platform_wsf`)

check:deps · typecheck · check:workflow · check:spec · check:tests(K1~K11) · test:changed(40) · test:always(198) — 전부 EXIT=0 · 설정·인증 DB 검사 107 통과

## 지적

### security-review
- 확인: 원문은 목록·`/auth/me`·실행 경로·400·500·로그 어디에도 없다 · 설정 API 는 admin 만 · SQL 은 전부 자리표시자 · 이어받기는 `service_id` 로 묶여 다른 서비스 계정을 못 가져온다 · 트랜잭션·행 잠금
- 경미: 평문 저장(명세 결정 — `grafana_ro` 에 `service_env` 권한을 안 준 동안 받아들일 만) · `loginId`·`loginPassword` 길이 상한 없음(admin 만, 본문 1MB 상한, `baseUrl` 과 같은 방식)

### spec-review
- 주의 H2 — `공통/4-데이터모델.md:530` 이 저장 규칙을 옛 모양(null 만 지움)으로 옮겨 적는다 → 가리키기만
- 주의 C6 — `WORKSTREAMS.md:40` 「나머지(통로·설정)는 대기」 — 설정은 닫혔다
- 경미 — `작성.md:497` 영향 줄이 outputs 세 줄을 WS-F 로 · PR 본문 두 문장이 게이트 1 결정과 반대 · G8 부숴 본 기록

### code-review
- 주의 — `settings/store.ts` 354줄(main 에서 이미 327줄) · 누출 검사 앞에 「원문이 실제로 저장됐다」 전제 줄이 없어 옛 코드에서도 초록
- 경미 — `<> ''` 조건은 쓰는 길상 불필요(옛 행 방어로 둠) · 아이디 앞뒤 공백 · `계정칸` 위 주석이 「무엇」 · 같은 규칙 설명이 코드 세 곳

## RED 확인

새 검사 5건 중 4건이 옛 코드에서 빨강(아이디 응답 없음 · 주소만 보내면 계정 사라짐 · null/빈 글자 · 새 값)이었고 구현 뒤 초록. 누출 검사는 지키는 검사라 처음부터 초록이었다

## 재검사 (게이트 2 「고치고 재검사」)

- 고친 것: `4-데이터모델.md:530` 은 인증 §7 을 가리키기만 · `WORKSTREAMS.md:40` 설정 반영 완료 · `작성.md:499` 영향 줄을 WS-작성으로 · `store.ts` 의 「무엇」 주석과 중복 규칙 설명 · 누출 검사 앞에 원문 저장 전제(이제 옛 코드에서 빨강) · PR 본문 두 문장 · 진행 기록에 store.ts 분리 과제
- 그대로 둔 것: `<> ''` 판정(손으로 넣은 옛 행 방어 · 웹훅·피그마와 같은 꼴) · 아이디 앞뒤 공백(웹훅·피그마와 같은 동작) · 길이 상한 · 평문 저장(명세)
