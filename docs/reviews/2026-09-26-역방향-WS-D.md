# 검사 기록 — 역방향 WS-D (PR #83)

2026-09-26 · 선언 2등급 / 실측 2등급(HARNESS · TESTS · ADMIN · DOC, 차선 full) · **미분류 `infra/grafana/provisioning/dashboards/test-platform.json`** · 17파일 +581/−26

## 검사 명령 (DATABASE_URL = 검사용 DB `platform_wsd`)

| 명령 | 종료 코드 |
|---|---|
| check:deps · typecheck · check:workflow · check:spec · check:tests | 0 · 0 · 0 · 0 · 0 |
| test:changed (116) · test:always (201) | 0 · 0 |

## 렌즈

| 렌즈 | 판정 |
|---|---|
| code-review | 통과 (BLOCKER 0 · 주의 0) |
| spec-review | 주의 3 · 치명 0 |

### 주의와 처리
1. spec H2 · 리포팅 §8.4 머리말 절에 판정 줄이 없다(미확정 절은 요약을 전제) · insights 「직전만 미확정이면 새 케이스처럼」도 명세에 없다 → **명세 보강은 「Grafana 작성 현황」 PR 과 같이**(명세 변경 · 3등급) — WORKSTREAMS 에 적었다
2. spec C5 · 계획 files 가 `collect.test.ts`·`insights.test.ts`·`html.test.ts` 로 적혀 있었는데 실제는 새 파일(기존이 300줄 초과) · 접두사 표기 → 계획을 실제와 맞췄다
3. spec · `html.test.ts` 351 → 352(항목 함수에 `unconfirmed: null` 한 줄) · `collect.test`·`insights.test` 와 함께 분리 대상 → 진행 기록에 적었다

### 참고 (code, 결함 아님)
- 직전에만 미확정이던 케이스가 이번 실행에 아예 없으면 `빠진건수` +1 — 「앞에 있고 이번에 없음」 정의에 맞다. 앞 미확정까지 빼려면 명세가 먼저
- `finished_at` 이 빈 행은 증적에서 미실행, 화면은 running — 이번 PR 전부터의 설계(증적은 끝난 실행에서만)

### 통과한 것
박제값만 읽음 · `bool_or` 로 접어도 안전(케이스 단위 박제) · 이번 미확정은 빠진건수·덩어리에서 제외 · 확정/미확정 블록 순서 유지 · 이스케이프 · 판정 색 없음 · 판정 줄 세는 단위와 0 칸 규칙이 실행 §3.2·화면과 같음(검사가 맞대 봄) ·
PDF·HTML 이 `renderHtml` 하나 · 엑셀 칸 번호 전부 +1 · Grafana 두 CTE 가 GROUP BY 앞에서 거름(미확정만 있는 날은 0 나누기 대신 점 없음) · `grafana_ro` 로 실제 돎 · fixture XDU·XDV 등록·정리 · 제품 파일 300줄 안
