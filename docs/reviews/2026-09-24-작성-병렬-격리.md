# 2026-09-24 · PR #69 검사 — 작성 에이전트 서비스별 병렬 · 자식 격리 · 모델/CLI 최신화

등급: 선언 3 / 실측 3 (COMPOSE·SPEC·DOC·TESTS·GUARD · 미분류 `apps/authoring/start.sh`)
계획: `docs/plans/2026-09-24-작성-병렬-격리.md` (할 일 1~9 · 할 일 10 은 다음 PR)

## 1차 검사 (게이트 2 첫 판)

| 렌즈 | 판정 |
|---|---|
| code-review | BLOCKER 1 · 주의 7 |
| security-review | BLOCKER 2 · 주의 6 |
| spec-review | 치명 0 · 주의 9 |

BLOCKER 셋 — 게이트 2 에서 사용자가 「고치고 재검사」
1. (code) 맥에서 자식 HOME 을 바꿔 Playwright 가 브라우저를 못 찾는다 → 자리 uid 로 돌 때만 HOME·TMPDIR 을 바꾼다
2. (security) 자리 uid 로 띄운 kill 이 부모 환경(토큰 셋)을 물려받는다 → `칠환경`: uid 가 있으면 준 env 만 (`authoring-io.ts`)
3. (security) 같은 자리 uid 가 건을 넘어 /tmp 를 공유 → 작업마다 TMPDIR · umask 077 · 거둔 뒤 공용 임시의 그 uid 파일 삭제

주의 반영 — `init: true` · 거두기 확인 · 켤 때 최신화 두 번 방지 · 머지 원본번호 `sourceId` · 거절 뒤 자리 받은 줄·머지 CI 루프가 손 뗌(`멈춤`) ·
사본 만들다 실패하면 치움 · `@platform` 은 사본 packages(`부품링크`) · pandoc 자리 uid·빈 환경 · 호스트 git 은 PATH·HOME·GH_TOKEN 만(`호스트환경`) ·
자식 gid 겹침 거부 · start.sh(HOST_GID 검사 · 호스트 uid 에서 토큰 뺌 · root TMPDIR · 틀린 훅 알림 삭제) · 문서 9건(에이전트 push 훅 없음 · 신뢰 경계 · compose 주석 ·
설정값 표 모순 · 손 숫자 · 라우터 · 인용 · 계획 밖 파일 사유 · 실측 기록)

계획 밖 파일(C1) — 300줄 규칙으로 뗐다: `authoring-model.ts`(+test) · `authoring-child.ts` · `authoring-upload.ts` · `authoring-copy.test.ts` · `authoring-proc.test.ts`.
`authoring-assets.test.ts` 는 `클로드인자` 인자가 늘어 6줄

## 재검사

| 렌즈 | 판정 |
|---|---|
| 재검사(코드·보안·문서) | BLOCKER 1 · 주의 4 — 1차 BLOCKER 셋은 닫힘 |

- BLOCKER — 바탕 이미지 `/ms-playwright` 가 0777 → Dockerfile `chmod -R go-w` + deploy 검사
- 주의 — 못 거둬도 자리를 놓음 → 에이전트를 멈춘다 · 확인 fail-open → `pgrep` 종료 코드 1 일 때만, 흔적 지우기 실패도 실패, `/run/lock` 추가 ·
  바탕이 /tmp 아래면 흔적 지우기가 사본을 지움 → root 면 켤 때 거부 · 맥 TMPDIR → 자리 uid 일 때만

## 실측 (가짜 토큰 · 이미지 빌드)

- 두 번 켜도 에이전트까지 · CLI 2.1.280 → stable(2.1.273) 점검 통과 · 자식 uid 칸 비우면 거부
- 리눅스 fs: 자식이 서버 저장소·tests 에 못 씀 · 호스트 uid 는 씀 (맥 Docker Desktop 바인드는 uid 를 안 지켜 뚫림 → SPEC 한계)
- 실제 코드 사본: 소유 자리 uid · `@platform/kit`→사본 packages · status 깨끗 · 자식 uid·작업 임시·umask 077 로 Chromium 관문 통과
- 거두기: 남은 0 · /tmp 흔적 지움 · 자리 uid 프로세스의 TOKEN 환경 0 · 자식이 브라우저 폴더에 못 씀 · 다른 자리 environ·트리·에이전트 집 막힘
- 발견 — `kill -0 -1` 은 남은 게 없어도 성공한다(권한 없는 남의 프로세스를 센다) → `pgrep` 으로 (LEARNINGS)

## 검사 명령

check:deps · typecheck · check:workflow · check:spec · check:tests EXIT 0 · DB 테스트 3회 EXIT 0 (1513)

## 게이트 2 — (사용자 결정 대기)
