# SPEC 검사 결과 — PR #61 작성 자료목록 (2026-09-23)

범위: `git diff origin/main...HEAD` (49 파일) · 다른 세션이 검사 · G3 연속 3회는 호출자가 이미 돌림 (96 파일 · 1299 검사 · 3회 EXIT 0)

## 요약
치명 0건 · 중대 1건 · 주의 4건 — **병합 가능 (중대는 사용자가 판단)**

## 중대

### H2 — `spec_text` 「옛 행만 찬다」가 머지 행과 어긋남
어디:   docs/spec/공통/4-데이터모델.md:167 ↔ apps/admin/src/authoring/routes.ts:304 (store.ts:126 주석)
무엇:   명세 칸 주석은 「옛 행만 찬다」. 코드는 새로 만드는 MERGE 행에도 `머지 요청 — 원본 #N` 자리표시를 채운다 (store.ts 주석도 「머지 행 자리표시만 채운다」라고 스스로 적음)
왜 문제: 다음 세션이 「spec_text 가 차 있으면 옛 AUTHOR 행」으로 읽고 분기를 짜면 머지 행을 옛 작성 행으로 오판한다
고칠 곳: 명세 칸 주석에 「머지 행은 자리표시를 채운다」 한 구절 (또는 머지 행도 NULL 로)

## 주의
1. 피그마 주소에 `node-id` 가 없으면 코드는 `https://www.figma.com/design/<키>/` 로 받는다 (routes.ts:83). 작성.md §7 「자료」 표는 `?node-id=<숫자-숫자>` 붙은 모양만 적는다. 검사(routes.test.ts:298·337·454)는 없는 모양을 쓴다 — 명세 표에 한 줄 더할 것 (CLAUDE.md §1.4)
2. C5 — 계획 할 일 18 `files` 의 `.claude/scripts/check-secret-names.mjs` 가 diff 에 없다. 이미 `token` 이 목록에 있어 필요 없었던 것으로 보이나 이유가 아무 데도 없다 → 게이트 2 요약에 한 줄
3. C1 — `apps/admin/src/web/styles.test.ts` 는 계획 `files` 밖 (commit fecc9e7 `.screen` 줄어듦 수정). 게이트 2 요약에 싣는다
4. H5 — 계획 「Plan 메타」의 「예상 묶음 7개」 뒤 괄호에 묶음이 6개다 (docs/plans/2026-09-23-작성-자료목록.md:309). 숫자를 빼고 괄호를 가리키게

## H 절
- H1 `check:spec` 통과 (없는 절 0 · 깨진 링크 0 · 틀린 분량 0 · 상태 줄 없는 블록 0, 계약 블록 6건)
- H2 위 중대 1건 · 주의 1. 그 밖 — 상태 다섯·전이 셋(데이터모델 §6 ↔ store.ts ↔ 마이그레이션 ↔ 화면 타입) · 에러 코드(BAD_FIGMA_URL·BAD_SOURCE·NOT_REQUESTER·NO_ASSETS·BAD_FILE_TYPE·BAD_NAME, 413, 개수 초과 만들기 400/올리기 409) · 상한 숫자(20MB·20개는 작성 §7 한 곳, 코드 상수는 그 절을 가리킴) · 등급(인증 §7 표 ↔ gate.ts ↔ scope.ts) 일치
- H3 라우터 표·절 번호 표·표 주인 표·장 목록 분량 모두 반영
- H4 절 번호 변경 없음 (새 내용은 `####` 소절)
- H5 SETUP 「넷 중 하나」(표 4줄)·CLAUDE.md 「이 넷」(접두사 4) 맞음. 계획 1곳 어긋남(주의 4)

## 통과한 항목
A1(kit 변경 없음) · A2~A6 · B1~B9 · C2~C4 · C6 · D1~D5 · E1(26건 K1~K10) · E2~E4 · F1~F10 · G1~G5 · G7 · G9(해당 없음)
G6·G8 은 게이트 2 요약에서 확인 결과를 싣는다
