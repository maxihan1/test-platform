# SPEC 검사 결과 — PR #49 화면 리뉴얼 ② (2026-09-21) `[자기검사]`

> **철칙 1 위반 표시.** 이 코드를 만든 세션이 그대로 검사했다. 자기 가정을 통과시킬 수 있다.
> 병합 직전 전체 검사는 다른 세션이 돌려야 한다 (CLAUDE.md §2.3).

## 요약

치명 0건 · **중대 2건 (둘 다 닫음)** · 경미 1건 — **병합 가능**

> **검사 뒤에 고쳤다.** 철칙 2(검사자는 고치지 않는다)는 **검사자와 구현자가 다른 세션일 때**의 규칙이고,
> 이 검사는 애초에 `[자기검사]` 다. 고친 내용과 **고친 것이 정말 무는지**를 아래에 적었다.

범위는 `git diff origin/main...HEAD` 36파일 (+2100/-194). 실측 등급 3 (SPEC·ADMIN·WEB·DOC·TESTS).

## 중대

### H2 — 실행 §8.10 이 모달을 아직 「늘 코드의 기본값으로 연다」고 말한다

어디:   `docs/spec/도메인/실행.md:471`

무엇:   그 줄이 「**기본은 전부 접혀 있고 케이스 코드에 적힌 기본값으로 돈다**」라고 적는다.
        이번 PR 이 **목록에서 고친 값을 모달의 시작값으로 실어 보낸다.**
        그리고 카탈로그 §8.1 에는 「목록과 모달이 **같은 표**를 쓴다」고 새로 적었다.
        **두 절이 서로 다른 말을 한다.**

        474줄의 「`값 고치기` 접개」도 이미 사실이 아니다 — 앞 PR 이 그 접개를 없앴고
        `RunPickModal.test.tsx:71` 이 「그 버튼이 없다」를 단언하고 있다.
        **이건 이번 PR 이 만든 어긋남이 아니라 물려받은 것이다.**

왜 문제: 다음 세션이 §8.10 을 읽고 「모달은 늘 기본값으로 열려야 한다」로 구현을 되돌린다.
        그러면 목록에서 고친 값이 모달을 여는 순간 사라지고, **사람은 고친 줄 알고
        기본값으로 돌린 결과를 증적으로 제출한다.**

고칠 곳: `도메인/실행.md §8.10`
**닫음** — 세 줄로 고쳤다. ① 「아무것도 안 고쳐도 돈다」 ② 「목록에서 이미 고친 값이 있으면 그 값으로 열린다.
        **한 벌의 표를 쓴다**」 ③ 「칸은 접지 않는다 (2026-09-21 수정)」 — 접개를 왜 뒤집었는지를 남겼다 (§2.7 ②).

### C5 — 계획의 `files` 에 있는 `api.test.ts` 가 diff 에 없다

어디:   계획 「할 일 6」의 `files` · `apps/admin/src/web/api.test.ts`

무엇:   `api.ts` 에 `caseHistory(tcId)` 를 더했는데 **그 통로의 검사를 안 썼다.**
        `api.test.ts` 는 다른 통로들의 **주소를 단언하는 파일**이다
        (`expect(부름[0]?.url).toBe('/api/auth/login')` 같은 줄이 여럿).
        `caseHistory` 만 그 줄이 없다.

왜 문제: `CaseDetail.test.tsx` 는 `api.caseHistory` 를 **가짜로 갈아 끼워** 시험한다.
        그래서 **주소가 틀려도 전부 초록이다.** `/cases/…/history` 를 `/catalog/cases/…/history`
        라고 적어도 아무 검사가 안 문다. 화면을 띄워야 404 로 드러난다.

고칠 곳: `api.test.ts`
**닫음** — 그 한 줄을 넣고 **부숴서 확인했다.** `/cases/…` 를 `/catalog/cases/…` 로 바꾸니
        `api.test.ts` **하나만** 빨갛고 `CaseDetail.test.tsx` 다섯은 **전부 초록**이었다.
        지적한 구멍이 실재했다는 증거다.

> **`RunPickModal.test.tsx` 도 계획에 있고 diff 에 없다.** 이쪽은 이유가 있다 —
> 새로 생긴 `초기글자` 경로를 `CaseList.test.tsx` 의 「고친 값이 모달에 그대로 떠 있다」가
> **진짜 모달을 그려서** 덮는다. 가짜로 갈아 끼우지 않으므로 C5 의 그물에 걸리지 않는다.

## 경미

### C1 — 계획 밖 파일 넷이 diff 에 들어왔다. 게이트 2 요약에 실어야 한다

어디:   `layout.ts` · `layout.test.ts` · `Shell.test.tsx` · `docs/spec/공통/5-화면공통.md`
        (그리고 `catalogView.test.ts` · `pickRun.test.ts`)

무엇:   **사이드바 첫 자리 이름을 `케이스` → `테스트케이스 목록` 으로 바꿨다.**
        구현 도중에 사용자가 직접 요청한 것이라 계획에 없다.
        `catalogView.test.ts` · `pickRun.test.ts` 는 `LastResult` 에 `recent` 가 생겨
        고정값(fixture)이 타입을 못 맞춘 것이다.

왜 문제: 문제가 아니다 (CLAUDE.md §1.1 — 범위를 벗어나는 것 자체는 금지가 아니다).
        다만 **이유가 어딘가에 적혀 있어야** 한다. 구현 중에 늘어난 것은 게이트 2 요약이 그 자리다.

고칠 곳: 게이트 2 요약. 이 보고서가 그 근거다.

## 통과한 항목

**A1~A6** — `packages/kit/src/types.ts` · `db/migrations/` · `docker-compose.yml` · `Dockerfile` **변경 없음**.
Admin API 는 경로를 안 더했다 — `recent` 는 이미 있는 응답에 칸 하나를 더한 것이고
`공통/3-공유계약.md:121` 과 `도메인/실행.md:92` 양쪽에 같은 말로 적혀 있다.

**B1~B8** — 이번 PR 은 `run_item` 을 **읽기만** 한다. 스냅샷 규칙을 건드리지 않았다.
목록에서 고친 값은 `POST /api/runs` 의 `params` 가 되고 거기서부터는 기존 경로 그대로다.

**C2** — 경계 침범 없음. 판정 흐름 데이터는 **실행 쪽**(`execution/history.ts`)이 낸다 —
카탈로그가 `run_item` 을 읽으면 경계가 깨진다 (카탈로그 §3.1). `grep` 으로 교차 import 0건 확인.

**C3 · C4 · C6** — 러너를 안 건드렸다. 「다른 파일이 막아 준다」 식 주석 없음.
`grep -rn "상태: 대기" docs/` 결과에 이번 PR 이 닫은 블록 없음.

**D1~D5** — `testId` · `caseId` · `test_case_id` **0건**. 화면 표기는 `PC`/`모바일` (`PLATFORM_LABEL`).

**E1~E4** — `tests/**` 를 안 건드렸다. `npm run check:tests` EXIT=0 (케이스 26건 · K1~K10 통과).

**F1 — JSON 원문 노출 없음.** 오히려 **새로 막았다.** `Form` 이 객체 기본값을 `JSON.stringify` 로
펴는데 목록에 그것이 나가면 금지 위반이라, `CaseRowParams` 의 `줄에낼수있나` 가 그 칸을 줄에서 뺀다.
검사 둘이 문다 (`객체 기본값은 줄에 펴지 않고 상세로 넘긴다` · `배열 기본값도 줄에 펴지 않는다`).

**F2 · F3** — 디바이스별 판정은 한 줄에 묶여 있고 흐름 막대도 그 안이다.
라벨은 전부 `schemaToFields` 가 `.describe()` 에서 뽑는다. 하드코딩 없음.

**F6 — 색만으로 전달하지 않는다.** 흐름 막대는 `aria-hidden` 이고 **판정마다 높이가 다르며**
옆에 `통과 4 · 실패 1` 을 글자로 적는다. 검사 셋이 문다 (`Summary.test.tsx`).

**F8** — 판정 색은 판정에만. 흐름 막대의 `--pass`/`--fail`/`--na` 가 그 자리다.
`--chrome` 은 `.dlabel`(구획 제목 라벨)과 `.pmore`·`.dhist a`(링크) 에 쓴다 — DESIGN.md 원칙 1 의 허용 자리다.

**F9 — 고친 화면마다 그물이 있다.** 고친 화면 일곱(`CaseList` · `CaseListParts` · `CaseRowParams` ·
`CaseDetail` · `Summary` · `Shell` · `RunPickModal`) **전부** 같은 이름의 `*.test.tsx` 가 있다.
저장소 전체 비율은 25 대 18 로 전과 같다 — 이번에 늘리지도 줄이지도 않았다.

**F10** — 「다 됐다」를 적지 않았다.

**G1 · G2** — 기능마다 RED 를 먼저 썼다. 테스트를 느슨하게 고친 자리는 하나다 —
`CaseListParts.test.tsx` 의 값 칩 검사를 입력칸 검사로 **다시 썼다.** 읽기 칩이 입력칸이 된
**동작 변경**이고, 보호 항목(비밀값 · JSON 원문 금지)은 **하나도 안 줄였다**. 오히려 둘을 더했다.

**G3 — 연속 3회 통과.** `DATABASE_URL` 붙여 968건 × 3회, 종료 코드 전부 0, 건수도 세 번 다 같다.
`runs.test.ts` 는 이미 쓰던 `XBX` 접두사를 그대로 썼다 — 접두사 표가 안 길어졌다.

**G4 · G5 · G7** — 일괄 갱신하는 제품 코드 없음. 「없어야 한다」 단언과 겹치는 이름 없음.
권한 축을 안 건드렸다 (`scope.ts` 의 `/api/cases/:tcId/history` 는 이미 등록돼 있다).

**G8 — 부숴서 확인했다. 넷.**

| 무엇을 부쉈나 | 결과 |
|---|---|
| `최근몇건 = 5` → `1` | 빨강 (1 failed) |
| `줄에보일칸수 = 4` → `10` | 빨강 (1 failed) |
| `Form` 의 `autoComplete={secret ? 'new-password' : 'off'}` → `"off"` | 빨강 (1 failed) |
| `<nav>` → `<div>` · 옛 이름 `band` 되살리기 | 각각 빨강 (1 failed) |

**H1** — `npm run check:spec` EXIT=0 (없는 절 0 · 깨진 링크 0 · 틀린 분량 0 · 상태 줄 없는 블록 0).

**H3 — 라우터 표에 두 줄을 올렸다.** §2.7 이 「가장 잘 빠뜨린다」고 적은 자리다 —
「목록 줄에서 값을 고치거나 상세를 펼치는 자리를 고친다」와 「최근 판정 흐름을 고친다」.

**H4** — 절 번호를 안 바꿨다. §8.1 안에 소제목만 더했다.

**H5** — 손으로 적은 숫자를 안 남겼다. 상한 넷과 흐름 다섯은 **코드 상수가 정본**이고
문서는 그 파일을 가리킨다 (`CaseRowParams.tsx` 의 `줄에보일칸수` · `history.ts` 의 `최근몇건`).

## 기타

**G6(브라우저 확인)을 안 했다.** 이 화면은 로그인이 필요해 브라우저로 직접 못 띄운다
(LEARNINGS 의 `admin-screen-qa-via-css-harness`). 대신 제품 CSS 를 그대로 불러 실측하는 하네스가
있는데 **이번에는 그것도 안 돌렸다.** 화면 검사 475건과 CSS 규칙 검사 다섯이 대신 보고 있지만,
**jsdom 은 레이아웃이 없어 「줄 높이가 두 배가 됐다」 같은 것을 원리적으로 못 본다.**
사용자가 `docker compose up -d --build admin` 으로 직접 볼 자리다 — `WORKFLOW.md` 에 길을 적어 뒀다.

체크리스트 제안: 없음. G6 이 이미 이 자리를 묻고 있고, 「해당 없음」이 아니라
「안 했다」로 적는 것이 맞다.
