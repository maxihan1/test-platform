# SPEC 검사 결과 — 전체 (병합 전 검사) (2026-09-18)

**범위** — `worktree-manifest-contracts` 가 `origin/main` 대비 바꾼 전부 (25 파일 · +2408 / −391).
**검사자** — 이 변경을 만들지 않은 별도 세션. CLAUDE.md §2.3 이 요구하는 「다른 세션의 검사」다.
작성자 자기검사(`docs/reviews/2026-09-18-SPEC개정반영.md`)는 참고만 하고 전부 직접 확인했다.

## 요약

치명 **2건** · 중대 **13건** · 경미 **6건** — **병합 불가**

작성자 보고는 「치명 0 · 중대 11(전부 고침) · 병합 가능」이었다. **그 뒤로 21건이 더 나왔다.**
전부 같은 유형이다 — **개정 SPEC 본문은 맞는데, 그것을 읽고 일할 사람이 먼저 읽는 글(킥오프·
매니페스트·SETUP)과 계약을 적는 자리(§7 API 블록)가 옛 상태로 남았다.**

작성자가 자기검사에서 「7건 → 고침 → 재검사 0건」이라고 적은 H2 가, 같은 파일 안에서
**고친 줄 바로 옆에** 또 남아 있다 (WS-D 킥오프 244줄 · 259줄).

## 돌린 명령

| 명령 | 결과 |
|------|------|
| `npm run check:spec` | **통과** — 없는 절 0 · 깨진 링크 0 · 틀린 분량 0 (SPEC 밖 참조 259군데) |
| `npm run typecheck` | **통과** |
| `npm test` | **통과** — 24 파일 · 205건 |
| `npm run check:tests` | **통과** — 케이스 10건 · K1~K8 |

---

## 치명

### 치1 — WS-F 킥오프가 「인증 없이 부를 수 있는 API」를 만들라고 시킨다 `[재발]`

```
어디:   docs/WORKSTREAMS.md:396~398 (WS-F 인증 킥오프 3번) — 이 브랜치가 새로 쓴 절이다
무엇:   "인증 미들웨어 — POST /api/auth/login과 POST /api/runs를 뺀 모든 /api/**에 로그인을 요구한다.
        POST /api/runs를 여는 이유는 정기 실행이 로그인 화면을 쓸 수 없어서다."
왜 문제: 개정 SPEC 이 **정확히 이것을 지웠다.** 도메인/인증.md:122 — 「POST /api/auth/login 을 뺀
        모든 /api/** 는 로그인이 필요하다. 예외는 없다」. 공통/7-데모와-완료.md:110 은 완료 기준으로
        「로그인 없이 부를 수 있는 API 가 하나도 없다」를 건다.
        킥오프는 세션이 SPEC 보다 **먼저** 읽는 글이다. WS-F 가 이대로 만들면 로그인을 지나지 않고
        대상 서버를 아무 때나 두드릴 수 있는 문이 열린다. SPEC 이 러너 포트를 닫은 이유와 같은 구멍이고,
        §7 이 「접근을 막지 못한다」고 이름 붙여 폐기한 방어 논리("스케줄러로 박제하니 사칭 못 한다")가
        킥오프에 그대로 살아 있다.
        정기 실행은 이제 HTTP 를 안 쓴다 — 컨테이너 안 명령이다 (공통/6-인프라.md:176~185).
고칠 곳: 이 브랜치(SPEC 개정 반영 담당). WS-F 킥오프 3번을 「login 을 뺀 전부에 로그인 요구. 예외 없음」
        으로 고치고, 4번 옆에 `scripts/run-scheduled.js` 를 더한다
[재발]  eng-review D1 이 같은 자리를 지적해 H2 가 체크리스트에 들어왔고, 작성자 자기검사 H2-1 도
        같은 유형이었다. **네 번째다.** 낱낱이 잡는 방식이 계속 새는 자리가 「킥오프」다 —
        CLAUDE.md §2.7 ④ 가 이미 그렇게 적었는데도 §7 을 고칠 때 킥오프를 같이 안 봤다
```

### 치2 — `orchestration.yaml` 이 WS-B·WS-C 를 `done` 으로 두는데, 개정이 시킨 일이 코드에 0줄이다

```
어디:   docs/orchestration.yaml:44~45 · :115
무엇:   - { id: WS-B, name: 실행, status: done }
        - { id: WS-C, name: 러너, status: done }
        integration.depends_on: [WS-A, WS-D, WS-E, WS-F]   ← B·C 가 없다
확인:   apps/admin/src/execution/** 와 apps/runner/src/** 를 grep 한 결과
        abort 0 · attempt 0 · repeat 0 · baseUrl 0 · notify_slack 0 · TOO_MANY_ITEMS 0 ·
        timeout_ms 0 · service_id 0 · ABORTED 0 — **하나도 없다**
왜 문제: 개정 SPEC 이 실행·러너에 얹은 일이 통째로 스케줄되지 않는다.
        `POST /api/runs/:runId/abort`(§7) · 러너 `POST /abort`(§5.2) · 회차(`attempt`)와 `repeat` ·
        실행 항목 1000건 상한(§8.2) · `env`→`base_url` 박제(§7.1) · 서비스 스냅샷(§6) ·
        `param_schema`·`expected_schema`·`timeout_ms`·`file_path`·`param_set_id` 박제(§6) ·
        `AND finished_at IS NULL` 가드 · admin 재기동 복구(§3.2) ·
        **Slack 을 보내는 주체(§8.9 "보내는 주체는 Execution 이다")** 가 전부 여기다.
        이대로 두면 오케스트레이터가 WS-A·contracts·WS-D·WS-E·WS-F 만 돌리고 `integration` 으로 넘어가,
        `final_check: spec#Phase 1`(§11) 에서 못 채우는 항목이 20개 넘게 남는다.
        **WS-A 는 같은 이유로 `done`→`pending` 으로 되살렸는데 B·C 만 안 봤다.**
고칠 곳: 이 브랜치. WS-B·WS-C 를 `pending` 으로 되살리고 `depends_on: [contracts]` 를 걸고,
        WORKSTREAMS.md 의 WS-B·WS-C 킥오프에 「2026-09-17~18 개정으로 더 할 것」 절을 더한다
        (WS-A·WS-D·WS-E 킥오프가 이미 그 모양이다). `integration.depends_on` 에도 넣는다
```

---

## 중대

### 중1 — WS-D 킥오프가 한 킥오프 안에서 「PDF 하나」와 「§8.4 셋」을 같이 말한다

```
어디:   docs/WORKSTREAMS.md:244  ↔  :259~261 (같은 킥오프, 15줄 차이)
무엇:   244 — "내는 형식은 PDF 하나다. HTML은 제출물이 아니라 PDF를 만드는 중간 산물이다"
        259 — "내는 형식과 표 모양은 SPEC §8.4 표가 정본이다. 거기 적힌 것을 전부 만든다. 엑셀은…"
왜 문제: §8.4(2026-09-18)가 정본이고 형식은 **PDF · 엑셀(XLSX) · HTML 셋**이다.
        WS-D 세션이 244 를 읽고 PDF 만 만들면 §11 Phase 2 의 엑셀·HTML 항목 4개가 비고,
        `evidence_document.format` 의 `XLSX`·`HTML` 을 쓰는 코드가 안 생긴다.
        작성자가 259~261 을 **새로 더하면서 바로 위 244 를 안 지웠다**
고칠 곳: 이 브랜치
```

### 중2 — `docs/SETUP.md` §6 이 「PDF 하나 · 엑셀 양식은 나중에」로 남아 있다

```
어디:   docs/SETUP.md:159~160 · :166~167
무엇:   159 "증적 문서 양식 → §8.4가 정본이 됐다. **PDF 하나**로 내고 HTML은 중간 산물이다"
        160 "**엑셀 양식만 남았다** — 검수처가 주는 양식을 받아 본 뒤 표 모양을 정한다 (§1.1 L4)"
        167 "나중에 정할 것 — 증적 문서 **엑셀** 양식 — 검수처 양식을 받을 때 (§1.1 L4)"
왜 문제: §8.4 「엑셀 표 모양」(2026-09-18)이 표 모양을 이미 정했고, §1.1 L4 도
        「2026-09-18에 「가공용 평평한 엑셀」은 범위 안으로 들어왔다(§8.4)」로 고쳐져 있다.
        SETUP.md 는 사람이 따라 하는 문서라 여기가 「나중에」면 아무도 안 만든다.
        같은 파일 §7 은 이 브랜치가 고쳤는데 §6 은 안 봤다
고칠 곳: 이 브랜치 (CLAUDE.md §2.5 표에서 SETUP.md 는 직접 고쳐도 되는 자리다)
```

### 중3 — `POST /api/runs` 계약에 Slack 알림 칸이 없다

```
어디:   docs/spec/도메인/실행.md:68 (§7 Execution)
무엇:   { title, env, repeat?, items: [{ tcId, platforms[], params, expected, timeoutMs?, paramSetId? }] }
왜 문제: §8.2 는 실행 설정에 `끝나면 Slack 알리기` 체크박스를 두고(기본 꺼짐),
        §6 은 `test_run.notify_slack` 칸을 만들고, §8.9 는 「사람은 고른다」고 한다.
        그런데 **화면이 그 값을 서버에 실어 보낼 칸이 계약에 없다.**
        WS-E 는 체크박스를 만들고 WS-B 는 받을 자리가 없어, 붙이는 날 각자 다른 이름을 지어낸다
고칠 곳: 이 브랜치 (§7 은 계약이라 CLAUDE.md §1.2 절차)
```

### 중4 — `POST /api/runs` 계약이 「상한 없음」이라고 적고 400 을 안 적는다

```
어디:   docs/spec/도메인/실행.md:72~73
무엇:   "repeat는 요청 최상위에 1개. 회차 수다 (기본 1, 상한 없음)"
        "run_item 수 = 항목 수 × platforms 길이 × repeat"
왜 문제: §8.2 「한 번에 만들 수 있는 실행 항목은 1000건까지」가 「서버도 같은 것을 본다. 넘으면
        400 { error: 'TOO_MANY_ITEMS', limit, requested }」라고 못 박았다.
        다른 400(`MIXED_SERVICE`·`PREFIX_TAKEN`·`LAST_ADMIN`)은 전부 §7 API 블록에 적혀 있는데
        이것만 없다. API 블록만 읽는 세션은 상한 없이 만든다 —
        작성자가 자기검사 H2-1 에서 §3.2 불변식을 고친 것과 **똑같은 자리를 하나 더 놓쳤다**
고칠 곳: 이 브랜치
```

### 중5 — `GET /api/runs` 계약에 `?service=` 가 없다

```
어디:   docs/spec/도메인/실행.md:79
무엇:   "GET    /api/runs?page=    실행 목록"
왜 문제: §8.7 「더할 것 1 — 서비스로 거른다. 띠에서 고른 서비스의 실행만 보인다 (`?service=`)」.
        §8 은 「한 번에 한 서비스만 본다」가 불변식이다.
        카탈로그 쪽(§7 Catalog)은 `?service=` 를 **필수**로 적어 뒀는데 실행 쪽만 안 적었다
고칠 곳: 이 브랜치
```

### 중6 — 증적 문서 API 응답에 `status` 가 없다

```
어디:   docs/spec/도메인/리포팅.md:50 · docs/spec/도메인/실행.md:83
무엇:   POST /api/runs/:runId/evidence  → { id, format, filePath, generatedAt }
        GET  /api/runs/:runId          → evidence: [{ id, format, filePath, generatedAt }]
왜 문제: §6 이 `evidence_document.status`(PENDING | READY | FAILED)와 `error` 를 새로 만들었고,
        §8.4 는 화면이 **네 상태**(도는 중 · 없음 · 만드는 중 · 실패 · 만들어짐)로 버튼 글자를
        바꾸라고 한다. `status` 가 응답에 없으면 화면이 그걸 알 길이 없다.
        게다가 `PENDING` 행은 `file_path` 가 NULL 인데(§6 `DROP NOT NULL`) 응답 모양은
        `filePath` 를 항상 주는 것처럼 적혀 있다
고칠 곳: 이 브랜치
```

### 중7 — §7 Auth 응답의 사람 이름 칸이 `name` 인데 §3.5 는 `displayName` 이다

```
어디:   docs/spec/도메인/인증.md:85   (이 브랜치가 새로 추가한 줄)
무엇:   user = { username, name, role, services: [{ id, prefix, name, color }] }
대조:   :19 · :55 — 확인 함수는 `{ username, displayName, role, services }` 를 돌려준다
        :98~102 — 설정 API 는 전부 `displayName`
        §6 `app_user.display_name`
왜 문제: 같은 장 안에서 같은 칸을 두 이름으로 부른다. 더 나쁜 것은 같은 줄의
        `services[].name`(서비스 이름)과 글자가 겹쳐, WS-E 가 `user.name` 을 서비스 이름으로
        읽을 여지가 있다. 이 칸은 증적 문서 실행자 칸의 원천이다
고칠 곳: 이 브랜치
```

### 중8 — WS-F 킥오프의 확인 함수가 등급·서비스를 안 돌려준다

```
어디:   docs/WORKSTREAMS.md:390
무엇:   "확인 함수 하나 — 요청을 받아 { username, displayName }을 돌려준다"
왜 문제: §3.5 가 2026-09-17 에 `{ username, displayName, role, services }` 로 넓혔다
        (인증.md:19·:55 — 「확인 함수는 등급까지 돌려준다」).
        옛 모양으로 만들면 §8 의 등급별 화면(자리 넷 · 설정 감추기)과 §8 의 서비스 띠 목록이
        원천을 못 찾는다. §3.5 는 「이 함수 하나만 갈아 끼운다」가 불변식이라 나중에 넓히기도 어렵다
고칠 곳: 이 브랜치
```

### 중9 — 계약 반영 킥오프 4번이 §9 에서 빠진 것을 시키고 들어간 것을 빠뜨린다

```
어디:   docs/WORKSTREAMS.md:368~369
무엇:   "docker-compose.yml · apps/admin/Dockerfile — 포트 설정화, 러너 포트 닫기,
         상시 4개 재시작 정책, **인스턴스 설정 전달**, 화면 빌드 단계 (§9)"
왜 문제: ① 「인스턴스 설정 전달」은 §9 이 지운 것이다 — `PLATFORM_INSTANCE_*` 넷은 DB(`service`)로
        내려갔다 (공통/6-인프라.md:64·:96~106).
        ② §9 이 요구하는 **`SESSION_SECRET` 전달**(없으면 admin 기동 거부)과
        **글꼴을 이미지에 담기**(2026-09-18 결정, 6-인프라.md:88~91)가 빠졌다.
        같은 단위를 적은 `docs/orchestration.yaml:72` 는 셋 다 맞게 적혀 있다 —
        **같은 step 을 두 문서가 다르게 말한다**
고칠 곳: 이 브랜치
```

### 중10 — 계약 반영 킥오프 5번이 새 부품을 하나만 시킨다

```
어디:   docs/WORKSTREAMS.md:370~371
무엇:   "package.json — Fastify 쿠키·세션 플러그인 **하나만** 더한다"
왜 문제: §9.1 「승인받은 새 부품」이 **둘**이다 — Fastify 쿠키·세션 플러그인과 **`exceljs`**
        (2026-09-18 승인, 6-인프라.md:126·:135).
        `orchestration.yaml:74` 는 「둘을 더한다」로 맞게 적혀 있다.
        exceljs 가 안 깔리면 WS-D 가 §8.4 엑셀을 만들 수 없는데, 갈래 세션은 의존성 칸을 못 고친다
고칠 곳: 이 브랜치
```

### 중11 — `apps/admin/src/catalog/rules.ts` 의 tcId 정규식이 옛 형식이다 (D2)

```
어디:   apps/admin/src/catalog/rules.ts:152  const TCID = /^[A-Z]{2,6}-\d{3}$/;
        apps/admin/src/catalog/rules.ts:222  "<대문자 2~6자>-<3자리>여야 한다"
왜 문제: §2 와 §4 K2 가 접두사를 자유 형식 `^[A-Z][A-Z0-9]{0,11}$` 로 바꿨다.
        코드는 `PAY2-001`·`ORDERING-003` 을 아직 위반으로 찍는다.
        문서가 허용한 것을 검사기가 막으면, 그 접두사로 서비스를 만든 조직의 케이스가
        스캔에서 통째로 걸린다
고칠 곳: **WS-A** — `docs/WORKSTREAMS.md:141~143` (킥오프 8번)에 **이미 넘겨져 있다.**
        이 브랜치가 할 일은 없다. 판정만 「불일치」로 남긴다
```

### 중12 — 「Phase 1 병렬 5갈래」와 「여섯 갈래」가 SPEC 안에서 충돌한다 (H5)

```
어디:   docs/spec/공통/7-데모와-완료.md:53   "### Phase 1 — 병렬 5갈래 (WORKSTREAMS.md 참조)"
        docs/spec/공통/3-공유계약.md:4      "여섯 갈래가 전부 공유한다"
왜 문제: WS-F(인증)가 2026-09-17 에 생겨 갈래는 A~F **여섯**이고, 담당 없는 `contracts` 단위까지
        합치면 일곱 단위다. 손으로 적은 숫자가 또 뒤처졌다 — **H5 가 걸린 네 번째 유형이다**
고칠 곳: 이 브랜치. §11 의 숫자를 빼고 「WORKSTREAMS.md 참조」만 남긴다
```

### 중13 — 색인의 「뒤집은 것 넷」이 뒤집힌 것을 절반만 담는다 (H3 · H5)

```
어디:   docs/SPEC.md:10~20
무엇:   "### 2026-09-17 판이 뒤집은 것 넷" — 서비스 통합 · 접두사 자유 형식 · 등급 셋 · 인증 예외 삭제
왜 문제: 이 표의 목적이 「앞 판을 기억하는 세션이 옛 전제로 일하지 않게」다.
        그런데 **같은 개정에서 뒤집힌 셋이 빠져 있다.**
          · 반복 실행 「상한 없음」 → 실행 항목 **1000건 상한** (2026-09-17, §8.2)
          · 검색 조건 **다섯 → 넷** (2026-09-17, §8.1)
          · 증적 **PDF 하나 → PDF·엑셀·HTML 셋** (2026-09-18, §8.4)
        앞 판을 기억하는 세션이 이 표만 믿으면 저 셋을 옛 전제로 일한다.
        「넷」도 손으로 적은 숫자다 (H5)
고칠 곳: 이 브랜치. 제목에서 숫자를 빼고 2026-09-18 분까지 줄을 더한다
```

---

## 경미

### 경1 — 장 머리말 「이 장이 담는 절」 셋이 색인과 다르다

| 파일 | 머리말 | 색인이 적은 것 |
|------|-------|--------------|
| `docs/spec/도메인/실행.md:3` | §3.2 · §7 · §8.2 · §8.3 | **§8.7 · §8.9 가 빠졌다** (이 브랜치가 두 절을 새로 넣었다) |
| `docs/spec/도메인/인증.md:3` | §3.5 · §7 Auth·인증 적용 범위 · §8.6 | **설정 API · §8.8 이 빠졌다** |
| `docs/spec/공통/1-제품과-구조.md:3` | §1 · §2 · §3 | **§1.1 이 빠졌다** |

세션이 장을 열었을 때 맨 위 두 줄로 「여기 뭐가 있나」를 판단한다. 색인만 고치고 장 머리말을 안 고쳤다.

### 경2 — WS-E 킥오프의 화면 절 목록에 §8.7·§8.8·§8.9 가 없다

`docs/WORKSTREAMS.md:283~284` — "카탈로그 §8.1 · 실행 §8.2·8.3 · 리포팅 §8.4·8.5 · 인증 §8.6".
정작 같은 킥오프 14·15·21번이 §8.7·§8.8·§8.9 를 시킨다. 읽을 절 목록만 개정 전이다.

### 경3 — `orchestration.yaml:102` 「킥오프에 18건」이 실제 23건이다 (H5)

WS-E 킥오프는 1~23번이다. 세션이 18 을 믿고 계획을 세우면 다섯 건이 빠진다.

### 경4 — `docs/WORKFLOW.md` 가 개정 전 상태다

```
:16  " [2] Phase 1 병렬     WS-A ~ WS-E"          → WS-F 가 빠졌다
:27  "현재 위치: … WS-A·B·C·E 완료."              → orchestration.yaml 은 WS-A·WS-E 를 pending 으로
                                                     되살렸다. 두 문서가 정반대를 말한다
:46 :72  "5갈래"                                  → 여섯이다
```

CLAUDE.md §0 이 세션마다 읽으라고 시키는 문서인데, CLAUDE.md §2.7 ④ 의 「SPEC 밖으로 나간다」
표에는 `WORKFLOW.md` 가 **없다.** 표에 한 줄을 더하는 것까지가 이 항목의 조치다.

### 경5 — `check:spec` 이 CI 에도 파이프라인 QA 에도 없다

`package.json:14` 에 스크립트는 있지만 `.github/workflows/ci.yml` 과
`docs/orchestration.yaml:33~36`(qa.commands: typecheck · test×3 · check:tests)에는 없다.
LEARNINGS 는 이 항목을 「기계로 승격 (2026-09-17)」으로 적었는데 **기계가 자동으로 돌리지 않는다** —
사람이 spec-review 를 돌릴 때만 돈다. 승격이 반만 됐다.
(CLAUDE.md §2.5 표상 검사기·CI 는 직접 고쳐도 되는 자리다.)

### 경6 — §4 머리말은 K 표 전체를 CI 가 본다고 적는데 실제는 K1~K8 이다

`docs/spec/공통/2-명세선언.md:103` — "아래 규칙은 … 저장소에 올릴 때 CI가 검사한다" (표는 K1~K10).
실제 `npm run check:tests` 출력은 「K1~K8 통과」이고 `.github/workflows/ci.yml` 의 step 이름도
「SPEC §4 K1~K8」이다. §11 Phase 1 은 K1~K10 을 요구한다.
K9·K10 구현은 WS-A 킥오프 7번에 넘겨져 있으므로 **일감은 잡혀 있다.**
다만 §4 머리말이 「이미 된 것」처럼 읽히므로 한 줄(「K9·K10 은 아직 사람이 본다」)을 더한다.

---

## 통과한 항목

| 항목 | 결과 |
|------|------|
| **A1·A2·A3** `types.ts` · `db/migrations/` · `docker-compose.yml` | **변경 없음** — `git diff --name-status origin/main...HEAD` 에 `apps/` · `packages/` · `tests/` · `db/` · `docker-compose.yml` 이 한 줄도 없다 |
| **A4·A5·A6** 러너 계약 · Admin API · 컬럼명 | **해당 없음** — 코드 변경 0건 |
| **B1~B8** 도메인 불변식 | **해당 없음** — 코드 변경 0건 |
| **C1** 소유 경로 | **통과** — 바뀐 것이 전부 `docs/` · `.claude/` · 루트 `CLAUDE.md`. `guard.mjs` 의 `SHARED` 안이다 |
| **C2·C3** 컨텍스트 경계 · 러너 쓰기 경로 | **해당 없음** |
| **D1** `testId`·`caseId`·`test_case_id` | **없음** |
| **D2** tcId 형식 | **불일치 → 중11** (문서는 전부 일치. 코드 한 줄만 옛 형식이고 WS-A 로 이관됨) |
| **D5** 접두사의 뜻을 읽는 코드 | **없음** — `rules.ts` 는 모양만 본다 |
| **D3·D4** projects 이름 · 화면 표기 | **해당 없음** |
| **E1~E4** 테스트 코드 규칙 | **해당 없음** — `tests/**` 변경 0건. `check:tests` 는 통과(K1~K8) |
| **F1~F7** 화면 규칙 | **해당 없음** — 화면 코드 변경 0건. `design-mockup.html` 은 개정본과 일치(형식 셋 · 1000건 상한 · Slack 칸 · 완료 모달 · 설정 화면 · 새 색 토큰 · `tabular-nums`) |
| **G1~G3** TDD | **해당 없음** — 코드 변경 0건 |
| **H1** `npm run check:spec` | **통과** |
| **H4** 기존 절 번호 유지 | **안 바꿈** — 실재하는 절 33개, 새로 더한 것은 §8.7·§8.9 로 전부 뒤에 붙였다 |

## H2 · H3 · H5 판정

- **H2** — **아니오.** 치1 · 중1~중10 · 중12 (11건)
- **H3** — **아니오.** 중13 (색인 「뒤집은 것」 표) · 경1 (장 머리말) · 경2 (WS-E 킥오프)
  ※ 「기능을 더하거나 고칠 때」 라우터 표 자체는 서비스·등급·알림·증적 형식 줄이 전부 있어 **통과**
- **H5** — **아니오.** 중12 (5갈래) · 중13 (넷) · 경3 (18건)
  ※ 기계가 세는 것(장별 줄 수 · 절 참조 수)은 전부 맞다

---

## 기타 — 체크리스트에 더할 만한 것

**H6 (제안) — 「SPEC 밖 여섯 곳」을 기계가 세게 한다.**
이번 21건 중 **13건이 SPEC 본문이 아니라 SPEC 밖**(킥오프 7 · orchestration 2 · SETUP 1 ·
WORKFLOW 1 · CI 1 · 코드 1)이었다. CLAUDE.md §2.7 ④ 가 갈 곳을 이미 표로 적어 뒀는데
**표만 있고 검사가 없다.** `check:spec` 에 한 가지를 더할 수 있다 —
`docs/orchestration.yaml` 의 `units[].kickoff` 가 가리키는 `workstreams#<절>` 이 실재하는지,
그리고 `units` 의 id 집합이 `WORKSTREAMS.md` 소유 경로 표의 ID 집합과 같은지.
치2(WS-B·WS-C 누락)는 그 검사 하나로 잡혔다.

**H7 (제안) — 「§7 API 블록에 적히지 않은 에러·칸」을 H 절에 명시한다.**
중3~중6 이 전부 같은 자리다 — 화면 절(§8.x)이 새 칸·새 에러를 정했는데 §7 계약 블록에 안 내려왔다.
H2 로도 잡히지만 「두 장」이 아니라 **한 장 안의 §7 ↔ §8.x** 라 사람이 잘 못 본다.

**CLAUDE.md §2.7 ④ 표에 `docs/WORKFLOW.md` 한 줄을 더할 것** (경4).

---

## 병합 판정

**아니오 — 병합 불가.**

치명 2건을 고치기 전에는 합치지 않는다. 특히 치1 은 **이 브랜치가 새로 쓴 글이 보안 구멍을 지시**하는
것이고, 치2 는 **개정 SPEC 이 시킨 일의 3분의 1이 영영 스케줄되지 않는** 상태다.
중대 13건 중 12건은 이 브랜치가 고쳐야 하고(중11 만 WS-A 이관 완료), 전부 문서 수정이라
코드 위험은 없다. 고친 뒤 `npm run check:spec` 만 다시 돌리면 된다.
