# 화면·UX 검토 — 개정 SPEC 에서 예측한 화면 (2026-09-17)

`/plan-design-review`. 대상은 개정 `docs/SPEC.md` §8 화면 절과 거기서 나올 사용자 흐름이다.
**서비스 통합 결정(`2026-09-17-eng-review.md` D5)을 전제로 본다.** 코드는 한 줄도 고치지 않았다.

## 요약

전체 **6/10 → 8/10**. 결정 **8건** 확정, 남은 것 6건.

**가장 큰 두 건은 설계서를 뒤집는다** — D7 이 §9.2(인스턴스 복제 절차)를 무효로 만들고, D8 이 §6 의 「권한 칸을 만들지 않는다」를 뒤집는다.

| 관점 | 전 | 후 | 무엇이 바뀌었나 |
|------|----|----|---------------|
| 정보 구조 | 3 | **8** | 서비스 전환 모델 · 자리 셋 · 도는 중 알림 줄 |
| 상태 표현 | 4 | **7** | 빈 화면을 세 갈래로 가름 |
| 사용자 여정 | 4 | **8** | 증적 문서를 새 창에서 보고 저장 |
| 흔한 AI 디자인 위험 | 9 | **9** | 손댈 것 없음 |
| 디자인 기준 정합 | 7 | **9** | 서비스 색의 예외 범위를 못박음 |
| 좁은 화면·접근성 | 6 | **8** | 휴대폰으로 하는 일을 하나로 좁힘 |

---

## 확정된 결정 6건

### D1. 서비스는 「골라서 그 안에서 일한다」

한 번에 한 서비스만 본다. 맨 위에서 바꾼다. 여러 서비스가 한 목록에 섞이지 않는다.

```
┌─────────────────────────────────────┐
│ ▼ 결제 서비스      김철수 · 로그아웃 │  ← 여기서 서비스를 바꾼다
├─────────────────────────────────────┤
│ 케이스 목록                          │
│ PAY-001  결제하면 주문이 생긴다  통과 │
│ PAY-002  카드가 거절되면 안내   실패 │
└─────────────────────────────────────┘
```

**왜** — 지금 §8 의 화면 명세가 그대로 살아남는다. 「엉뚱한 서비스에서 실행을 누르는 사고」를 막던 상단 띠의 역할도 그대로다.
**비용** — 세 서비스를 맡은 사람이 「오늘 다 괜찮나」를 보려면 세 번 전환해야 한다. 그 갈증은 Grafana 쪽에서 푼다.

### D2. 케이스 목록이 집. 위에 자리 셋. 도는 중이면 한 줄 알린다

```
│  케이스   실행 기록   그래프 ↗  │  ← 밑줄 = 지금 여기
├─────────────────────────────────┤
│ ▶ RUN 2113 이 도는 중입니다 12/40│  ← 도는 게 있을 때만
```

**왜** — 이 도구의 일은 「무엇을 돌릴까」에서 시작한다. 다만 한 번 돌리면 최악 50분이라 자리를 떴다 돌아오는 진입이 흔하다. 알림 줄 하나로 두 상황을 다 받는다.

**부수 발견** — **`실행 기록 목록` 화면이 설계서 §8 에 없다.** 코드에는 있다(`apps/admin/src/web/RunList.tsx`). 명세 없이 만들어진 화면이므로 §8 에 절을 새로 쓴다.

### D3. 빈 목록을 세 갈래로 가른다

지금은 0건이면 언제나 `찾는 케이스가 없습니다.` 하나다 (`CaseList.tsx:108`). 검색한 적 없는 사람에게도 「찾는」이라고 말한다.

| 왜 비었나 | 무엇을 보여주나 | 다음 행동 |
|----------|---------------|----------|
| 아직 한 번도 안 불러옴 | `아직 케이스를 불러오지 않았습니다` + 설명 두 줄 | 가운데 큰 `케이스 불러오기` 버튼 |
| 불러왔는데 0건 | `불러왔지만 케이스가 하나도 없습니다` | `규칙에 걸린 파일 N건 · 사유 보기` |
| 검색·필터에 안 걸림 | `'장바구니' 에 맞는 케이스가 없습니다` | `검색 지우기` |

**왜** — 첫째 줄이 이 도구를 처음 켠 사람이 만나는 화면이다. 여기서 막히면 도구가 고장난 것처럼 보인다.

### D4. 증적 문서는 새 창으로 열어서 보고, 거기서 저장한다

```
│  증적 문서                       │
│  · 9월 15일 17:22 만듦  [열기 ↗] │
│  · 9월 15일 17:20 만듦  [열기 ↗] │
│                  [다시 만들기]     │
```

**왜** — 화면의 `항목 상세`는 항목 **한 건**이고 증적 문서는 실행 **전체 한 부**다. 지금 명세대로면 사람은 검수처에 내는 문서 전체를 한 번도 못 보고 낸다. 브라우저가 이미 문서를 보여주는 기능을 갖고 있으므로 새로 만들 것이 없다.
**같이 정해진 것** — 만든 것이 여러 개면 만든 시각과 함께 줄로 쌓인다. 파일 이름에 `(1)`, `(2)` 가 붙어 어느 것이 최신인지 모르게 되는 일이 없다.

### D5. 서비스 색은 맨 위 띠 한 줄에서만

디자인 기준 문서의 금지 목록에 **「판정 외의 색 사용」** 이 있다. 서비스마다 색을 주면 이 약속이 깨진다.

**결정** — 띠에만 서비스 색을 허용하고 본문은 판정 색만 쓴다. 기준 문서에 **「맨 위 띠는 예외」** 를 한 줄 적는다.
**왜** — D1 으로 한 화면에 두 서비스가 같이 나올 일이 없어졌다. 그러면 본문에 서비스 색이 있을 이유도 없다. 본문에서 색을 보면 언제나 판정이다.

### D6. 휴대폰으로 하는 일은 「끝났나 보기」 하나

| 좁은 화면에서 제대로 | 책상에서만 |
|---|---|
| 실행 기록 목록 · 실행 결과 보기 | 입력 폼 채우기 · 증적 문서 보기 · 실패 지점 코드 보기 |

**왜** — 규칙은 기준 문서에 있는데 설계서에는 없고, 만든 세션도 「눈으로 확인하지 못했다」고 남겼다. 확인해야 할 곳을 좁혀야 실제로 확인이 끝난다. 증적 문서는 A4 고정폭이라 애초에 좁은 화면 대상이 아니다.
**따라오는 것** — 손가락으로 누르는 영역 최소 크기(44px)는 **실행 기록·실행 결과 두 화면에만** 건다.

### D7. 서비스·계정·권한은 관리자 화면에서 만든다

서비스 통합은 **§9.2 를 통째로 무효로 만든다.** 지금 「인스턴스를 하나 더 만들기」는 화면이 아니라 설정 파일 여섯 줄을 고치고 컨테이너를 새로 띄우는 **서버 작업**이다. 합치면 서비스는 배포 설정이 아니라 **시스템 안의 데이터**가 된다.

```
│ 케이스  실행 기록  그래프↗  설정  │  ← 자리 하나 늘어난다
├─────────────────────────────────┤
│ 서비스                  [더하기] │
│ ▓ 결제 서비스  PAY-  테스트 200건 │
│ ▓ 회원 서비스  MEM-  테스트 150건 │
│                                  │
│ 계정                    [더하기] │
│ 김철수  결제 · 회원        운영   │
│ 박영희  회원          실행까지   │
```

**통합이 새로 만들어 내는 설정거리**

| 설정 | 지금 설계서 | 합친 뒤 |
|---|---|---|
| 서비스 만들기 | 설정 파일 6줄 + 컨테이너 새로 띄움 | 화면에서 만든다 |
| 케이스 번호 접두사 | 없던 개념 | 서비스마다 배정 + **중복 검사** |
| 테스트 코드 폴더 | 한 벌에 하나 | 서비스마다 하나 |
| 대상 서버 주소 | 환경변수 **한 줄** | 서비스마다 다름 → **표** |
| 권한 | 없던 개념 | 계정마다 |

**대상 서버 주소가 특히 못 버틴다.** 지금은 `dev=주소,qa=주소` 문자열 한 줄인데 서비스가 셋이면 아홉 개가 한 줄에 들어간다.

**비밀번호를 화면에서 다뤄도 안전한 이유** — §9.2 가 계정을 서버 명령으로 만들게 한 이유는 「비밀번호를 명령에 적으면 기록에 평문으로 남는다」였다. 막으려던 것은 **사람이 비밀번호를 타이핑해서 넘기는 것**이지 명령이냐 화면이냐가 아니다. 시스템이 무작위로 만들어 한 번만 보여주면 화면도 같은 수준이다.

**지우기 규칙** — 서비스도 계정도 **지우지 않고 비활성으로 둔다.** 지우면 그 서비스로 돌린 과거 실행의 증적이 흔들린다. §6 이 계정에 대해 이미 같은 이유로 외래키를 걸지 않기로 했다.

### D8. 사용자 등급을 셋으로 나눈다 — **설계서의 경고를 뒤집는 결정**

| 등급 | 할 수 있는 것 |
|------|-------------|
| 보기만 | 케이스·실행 기록·결과·증적 문서를 본다 |
| 실행까지 | + 실행·멈춤 |
| 운영 | + 설정 자리 |

**이 결정은 §6 의 다음 문장을 뒤집는다.**

> **권한 칸을 만들지 않는다.** 쓰지도 않을 칸을 미리 만들면 전부 같은 값이 들어가고, 그 칸을 보는 코드가 생기면서 「구분이 있는 것처럼」 보이기 시작한다. 구분이 실제로 필요해지는 날 칸을 더한다.

설정 화면(D7)이 「구분이 실제로 필요해지는 날」을 앞당겼다. 다만 §6 이 경고한 것은 **등급 수**였고, 이 결정은 그 경고를 알고 내렸다.

**이 결정이 옳으려면 전제가 하나 필요하다** — **「보기만」 등급인 사람이 실제로 존재해야 한다.** 결과만 확인하는 기획자·PM 같은 사람이다. 전부 「실행까지」거나 「운영」이면 쓰이지 않는 등급을 먼저 만들고 그걸 보는 코드를 이어서 만들게 되는데, 그게 §6 이 경고한 상황이다. 이 전제가 사실인지는 사용자가 판단했다.

**화면에서 지켜야 할 것** — §8.6 의 「누를 수 없는 메뉴가 있으면 사람이 그것이 올 때까지 기다린다」를 그대로 적용한다. **등급이 낮아서 못 하는 것은 회색으로 두지 말고 아예 안 보이게 한다.** 「보기만」 등급에게 실행 버튼은 흐리게가 아니라 없다.

---

## 남은 것 6건

| # | 무엇 | 왜 지금 못 정했나 |
|---|------|-----------------|
| 1 | 증적 `만드는 중` 상태를 어디에 저장하나 | 화면 모양은 D4 로 정했지만 저장할 칸이 없다. 기술 검토 F5 와 같은 자리다 |
| 2 | 러너가 고장났을 때 사람이 보는 것 | 설계서에 실행 실패 시 화면 문구가 없다. 지금은 빈 화면이나 원문 오류가 나올 가능성이 높다 |
| 3 | 실행이 끝났을 때 알릴 것인가 | §1.1 L8 이 「자리를 뜨게 될 때」로 미뤄 뒀다. D6 으로 휴대폰 확인 경로가 생겼으니 그 조건이 이미 온 것일 수도 있다 |
| 4 | 글자 대비율 수치 | 기준 문서에 「색만으로 판정 금지」는 있는데 숫자 기준이 없다 |
| 5 | 글꼴이 외부 주소에서 온다 | 사내망에서 그 주소가 막히면 글꼴이 시스템 기본으로 떨어지고, 그러면 **「숫자는 세로로 맞춘다」 규칙이 같이 깨진다.** 글꼴 파일을 안에 넣어 두는 것이 대안이다 |
| 6 | 증적 문서를 「보기만」 등급이 **만들** 수 있나 | **권고 — 받기는 「보기만」부터, 만들기는 「실행까지」부터.** 받는 것은 읽기이고 만드는 것은 서버에 파일을 남기는 쓰기다. 검수처에 낼 문서를 뽑는 일이 결과만 보는 사람 몫일 수 있어 사용자 확인이 필요하다 |

---

## 손댈 것이 없는 축 — 흔한 AI 디자인 위험 9/10

기준 문서의 「금지」 목록이 요즘 AI 가 만드는 화면의 특징을 하나씩 반대로 막아 놓았다.

| 흔한 AI 디자인 | 이 프로젝트 |
|---|---|
| 보라색 그라데이션 배경 | 그라데이션 금지 |
| 아이콘을 원에 넣은 3단 카드 | 카드 대신 괘선. 아이콘으로 판정 표시 금지 |
| 모든 요소에 같은 둥근 모서리 | 컨트롤 2px만 |
| 알록달록한 색 | 판정 외의 색 사용 금지 |
| 시스템 기본 글꼴 | Pretendard 지정 |

이 축은 이미 10점에 가깝다. 위 5번(글꼴 외부 주소)만 확인하면 된다.

## 검토 범위 밖

- **실제 화면을 열어서 보는 검사** — `/design-review` 가 따로 담당한다. 지금은 화면 빌드 단계가 없어 컨테이너로 화면이 안 뜬다(기술 검토에서 찾은 문제). 그것부터 고쳐야 한다
- **AI 목업 생성** — 기준 문서가 이미 확고하고, 생성기는 그 반대 방향(둥근 카드·그라데이션)을 만들어 낸다. 대신 이 문서의 글자 그림으로 결정했다
- **권한별로 화면이 어떻게 달라지나** — 서비스 통합이 설계서에 반영된 뒤에 본다
- **Grafana 대시보드 구성** — 별도 도구라 이 플랫폼 화면 규칙 밖이다

## 다음 세션이 할 일

1. D1~D6 을 `docs/SPEC.md` §8 에 반영한다. **`실행 기록 목록` 절을 새로 쓴다**(D2 부수 발견)
2. **D7 로 §9.2 를 다시 쓴다.** 「인스턴스 복제할 때 바꿀 것 여섯」 절이 통째로 사라지고 「설정 화면」 절이 들어온다. 대상 서버 주소가 환경변수 한 줄에서 표로 바뀌므로 §6 과 §9 도 같이 손댄다
3. **D8 로 §6 의 「권한 칸을 만들지 않는다」 문단을 고친다.** 지우지 말고 **왜 뒤집었는지를 덧붙인다** — 그 문단이 왜 있었는지가 남아야 다음에 또 늘릴 때 같은 판단을 다시 한다. §3.5 의 「자리만 남긴다」도 실제 등급 셋으로 바꾼다
4. `docs/DESIGN.md` 에 두 줄 — 「맨 위 띠는 판정 외 색의 예외」(D5), 「손가락 영역 44px 은 실행 기록·실행 결과에만」(D6)
5. 남은 것 6건 중 1·2·6 은 위 반영 때 같이 정한다

**반영할 절 목록** — §2(번호 규칙) · §3.5(인증·등급) · §6(권한 칸·대상 서버 표) · §7(설정 API) · §8(화면 전부 + 실행 기록 목록 + 설정 화면) · §9.2(운영 절차) · §1.1(L12·L13 정리)
