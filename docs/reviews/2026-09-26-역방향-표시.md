# 검사 기록 — 역방향 WS-작성 ③-2 표시 (PR #82)

2026-09-26 · 선언 2등급 / 실측 2등급(HARNESS · DOC · GUARD · TESTS, 차선 full) · **미분류 `package-lock.json`** · 17파일 +1031/−47

## 검사 명령

| 명령 | 종료 코드 |
|---|---|
| check:deps · typecheck · check:workflow · check:spec · check:tests | 0 · 0 · 0 · 0 · 0 |
| test:changed (183) · test:always (163) | 0 · 0 |
| vitest 전체 (`package.json` 을 바꿔서) — 1329 통과 · DB 검사 421 건너뜀 | 0 |

## 렌즈

| 렌즈 | 판정 |
|---|---|
| code-review | 주의 5 · BLOCKER 0 |
| spec-review | 주의 3(중대 1) · 치명 0 |
| security-review (비밀번호·외부 API·믿을 수 없는 zip 을 다루는 PR 이라 더했다) | 취약 0 · 주의 1(낮음) |

### 주의 (합침)
1. spec 중대 · 표시 사본에서 비밀번호가 나오면 명세 §3.6 「남는 한계」는 **FAILED** 인데 구현은 그 차이에 `markError` 만 남기고 DONE — 역기획서(push 전 · FAILED)와 같은 절이 다른 말을 한다
2. code · 역기획서 올리기와 `표시하기` 가 한 `try` — 앞이 던지면 표시를 통째로 건너뛰고 차이에 옛 문구 「표시는 아직 안 한다」가 남는다 · 표시가 던져도 사유가 「역기획서를 못 올렸다」
3. code · `시작자리` 가 `<w:p>` 바로 뒤 `<w:pPr` 만 본다 — 줄바꿈·들여쓰기가 있으면 메모 시작이 pPr 앞에 끼어 워드가 「읽을 수 없는 내용」
4. code·security · 메모 글에서 XML 1.0 이 못 쓰는 제어 문자를 안 거른다 — 워드가 사본을 못 연다
5. code · 문장을 하나도 못 찾아도(메모 0) 표시 사본을 올린다 — 원본과 같은 파일이 「표시 사본」으로 선다
6. spec 기타 · 비밀번호 검사에서 조각을 잇는 것이 본문 문단뿐 — 머리글·바닥글·각주에서 조각이 갈리면 놓친다
7. code·spec · `scripts/authoring-reverse.test.ts` 286 → 318줄
8. spec 경미 G8 · 새 검사를 일부러 부숴 본 기록이 없다

### 통과한 것
압축 폭탄(실제 푼 바이트를 남은 예산까지 셈 · 다시 묶기도 그 안) · zip 조각을 디스크에 안 씀 · 정규식은 입력 길이에 비례·파트 10MB 로 묶임 ·
메모 글은 요소 내용 자리에만 · anchor 는 문서에 안 넣음 · 피그마 주소는 저장 모양만·요청은 api.figma.com 고정·노드는 숫자:숫자 ·
asset 은 이 요청의 입력 자료 안에서만 · 토큰은 헤더에만 · `보낼차이` 가 표시 자리를 뺌 ·
문단 정규식(`<w:p/>`·`w:pPr`·`w:proofErr`·`w:ptab` 안 잡음) · 한 문단 메모 둘의 순서 · `commentReference` 위치 · 번호·rId 최대값 뒤 · Override 속성 순서 무관 · 빈 메모 파트 펴기 ·
run.ts `자료받기` 리팩터링 동작 같음 · 피그마 거절 한 번이면 멈춤 · jszip 3.10.2 가 package.json·lock 에서 맞음 · 새 코드 파일 300줄 안 · reverse.md 칸 이름이 코드와 같음

## 재작업 (게이트 2 「고치고 재검사」, 2026-09-26)
- 주의 1 — 표시를 **PR 전 준비**(원본 다시 받기 · 메모 사본 · 올릴 사본·메모 글에서 비밀번호·피그마 토큰 검사 → 새면 **FAILED**)와 **PR 뒤 올리기**(사본 · 댓글)로 갈랐다. 명세 §3.6 그대로 — 명세는 안 고쳤다
- 주의 2 — 역기획서 올리기와 표시를 따로 잡는다. 표시가 던지면 차이마다 「원본에 표시하다 멈췄다: …」
- 주의 3 — 문단 여는 태그 뒤 공백을 건너뛰고 문단 속성을 찾는다 · RED
- 주의 4 — 메모 글의 XML 금지 제어 문자를 뺀다 · RED
- 주의 5 — 문장을 하나도 못 찾으면 사본을 안 올리고 「못 찾았다」
- 주의 6 — 파트마다 `<w:t>` 를 이어 붙인 글도 검사(머리글·각주) · RED
- 주의 7 — 표시 자리 검사를 `authoring-mark.test.ts` 로(reverse.test 318 → 286줄)
- 주의 8 G8 — 푼 크기 상한 · 문단 속성 뒤 끼우기 · 번호 이어 붙이기를 일부러 부숴 `authoring-docx.test.ts` 4건이 빨개지는 것을 봤다(15 중). 되돌린 뒤 초록
재검사 — 아래 검사 일곱 · 전체 vitest.
