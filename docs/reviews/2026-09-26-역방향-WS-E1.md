# 검사 기록 — 역방향 WS-E ① (PR #78, 2026-09-26)

범위 `git diff origin/main...HEAD` 27파일 · 선언 1등급 / 실측 1등급(TESTS·WEB·DOC) · 미분류 없음 · 차선 full

## 렌즈

| 렌즈 | 판정 |
|---|---|
| code-review | BLOCKER 0 · 주의 3 · 경미 6 |
| 화면 규칙 대조(`/qa-only` 대신 — 미확정 데이터가 있는 실행이 없어 앱을 띄워 볼 수 없다) | BLOCKER 1 · 주의 3 · 경미 6 |

## 검사 명령

check:deps · typecheck · check:workflow · check:spec · check:tests(K1~K11) · test:changed(522) · test:always(162, DB 36 건너뜀 — 화면만 바뀌어 DB 주소를 안 줬다) — 전부 EXIT=0 · 화면 검사 686 통과

## 지적

### BLOCKER
- `RunResultRow.tsx:52` 미확정 사유 줄이 `small.why`(= `--na`, 미실행 판정 색)를 입는다 — DESIGN.md 미확정 표 「판정 색을 쓰지 않는다」 위반

### 주의
- `SettingsEnvs.tsx` 비밀번호·아이디 칸에 `autoComplete` 없음 — 브라우저가 플랫폼 로그인 비밀번호를 채워 넣을 수 있다(`Form.tsx:81-84` 선례)
- 화면에서 비밀번호를 지울 길이 없다(명세 §7 은 null = 지움)
- 줄을 빼고 같은 이름으로 새 줄을 더하면 서버가 옛 계정을 되살린다 — 새 줄은 빈 칸을 null 로 명시해야 한다
- 결과·진행 막대에서 미확정(`--ink-faint`)과 미실행(`--na`) 칸이 명도 1.01:1 로 붙는다 — 칸 사이 틈 필요
- `.set-env-login` 안 입력칸이 `.field input { width:100% }` 를 받아 둘째 줄이 셋으로 접힌다 · `.hint` 가 flex 한 칸에 낀다
- 아이디 칸에 보이는 라벨이 없다 — 「설정됨」이 무엇인지 안 읽힌다

### 경미
- 이름 바꾸기 안내 조건(아이디만 있을 때·새 비밀번호를 넣었을 때도 뜸) · 계정 칸 버튼 aria-label 에 줄 번호 없음 · 머리에서 `oldestSince` 없으면 건수까지 숨김 · 0일째/1 days · 미확정만 돌린 실행의 알림 줄 「0 통과」 · 모달에서 같은 수가 칸과 글자로 두 번 · `.case-tag` 규칙 두 곳·nowrap 없음 · 실행 기록 줄 미확정 칸 높이 · 목업 막대 칸·설정 예시 · 빈 줄
