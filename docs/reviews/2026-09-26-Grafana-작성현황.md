# 검사 기록 — Grafana 작성 현황 (PR #84)

2026-09-26 · 선언 3등급 / 실측 3등급(HARNESS · TESTS · MIGRATION · SPEC · DOC · GUARD, 차선 full) · **미분류 `infra/grafana/provisioning/dashboards/test-platform.json`** · 14파일

## 검사 명령 (DATABASE_URL = 검사용 DB `platform_wsg`)

| 명령 | 종료 코드 |
|---|---|
| check:deps · typecheck · check:workflow · check:spec · check:tests | 0 · 0 · 0 · 0 · 0 |
| vitest 전체 (마이그레이션·package.json 변경) 122파일 1789건 | 0 |
| 마이그레이션 up → rollback → up (dbmate) | 성공 |

## 렌즈

| 렌즈 | 판정 |
|---|---|
| code-review | 통과 · 주의 1 |
| spec-review | 치명 0 · 주의 5 |
| security-review | 통과 (BLOCKER 0 · 주의 0) |

### 주의와 처리
1. code · 「끝난 요청」 칸이 성공만 센다 — 실패가 든 합계로 읽힌다 → 칸 이름 「성공」 · 명세에 「끝난 = 성공 + 실패」
2. spec C5 · 미확정 칸 이름이 게이트 1 표(「카탈로그에 들어온 뒤 (일)」)와 다름 → 표대로 바꿈
3. spec C1 · 계획 files 밖 — `package.json`(새 검사가 대시보드 JSON 을 직접 읽어 `check:workflow` 가 `test:always` 등록을 요구) · `docs/SPEC.md`(분량 줄·라우터 표) → 이 기록과 게이트 2 요약에 싣는다
4. spec G8 · 부숴서 빨개지는지 → 아래 표
5. spec H3 · 라우터 표에 「대시보드 칸 권한」 없음 → 「대시보드에 패널을 더한다」 줄에 더함
6. spec H5 · 손으로 적은 개수(「앞의 넷」·「여섯」) → 패널 이름 범위·숫자 없는 문장으로

### 부숴서 확인 (G8)

| 부순 것 | 결과 |
|---|---|
| 검사용 DB 에서 `authoring_request.started_at` 권한 REVOKE | 3건 빨강 → 되돌림 |
| 미확정 패널의 `AND s.is_active` 삭제 | 「내린 서비스는 나오지 않는다」 빨강 → 되돌림 |
| 걸린 시간 패널의 `a.kind = 'AUTHOR'` 삭제 | 「보통 요청 …」 빨강(RERUN 이 섞임) → 되돌림 |

### 참고 (결함 아님 · 범위 밖)
- security · `run_item` 은 2026-09-17 부터 표 단위라 `params`·`error`·`expected` 도 대시보드가 읽는다. 칸 단위 원칙을 맞추려면 따로
- code · 대기줄로 돌아간 요청은 `started_at` 이 비워져 대기에 앞선 시도가 든다 — 명세의 대기 정의(행이 생긴 때 → 집은 때)에 맞다
- spec · 옛 마이그레이션 주석(20260922·20260923)의 「grafana_ro 에 아무것도 주지 않는다」는 지난 기록이라 그대로

### 통과한 것
칸 목록이 마이그레이션·명세 표·`column_privileges` 에서 같음 · 비밀값이 뷰·함수·기본 권한·PUBLIC·pg_stats 로도 안 샘 · down 이 표 단위 권한을 안 건드림 ·
미확정 짝 규칙(`store.ts` upsert) · 나이 버림이 화면과 같음 · §8.4 판정 줄·§7 insights 문장이 코드와 같음 · fixture XDH·XDHOFF 등록·부모 행까지 정리 · 새 검사 176줄
