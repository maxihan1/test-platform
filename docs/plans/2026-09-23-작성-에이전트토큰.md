# 맥 에이전트 — 비밀번호 대신 에이전트 토큰 · 머지 뒤 pull · 파일 나누기

등급: 3 · 갈래: WS-작성 · 2026-09-23 · PR #67

## 도메인 정리

- BC: 인증(§3.5 · §7 · §8.8) · 작성(§3.6 · §7)
- 계약 변경(게이트 0 승인, 2026-09-23): `app_user.agent_token_hash` 칸 · 발급·취소 통로 둘 · 계정 목록 `hasAgentToken` ·
  `Authorization: Bearer` 를 계정 확인 수단으로 받는다 (작성 통로와 `GET /api/auth/me` 만)
- 토큰은 **뒷문이 아니라 같은 계정의 두 번째 열쇠**다 — 확인 함수가 돌려주는 사람이 같으므로 등급·서비스 배정·
  「맥 계정만 집는다」가 그대로 걸린다

## 왜

맥 에이전트를 켤 때마다 비밀번호를 쳐야 해서 허들이 됐다 (사용자, 2026-09-23). 임시 비밀번호 재발급도
두 번 눌러야 떠서 한 번 헤맸다. 운영체제와 무관한 방식이어야 윈도우에서도 쓴다 — 그래서 키체인이 아니라 서버 발급 토큰.

같은 PR 에 셋을 더 담는다 (사용자 요청 「1개 PR 로」).
- 머지가 끝나도 맥의 main 체크아웃이 낡아서 서버가 새 테스트를 못 봤다 (#3811 뒤 실측, 손으로 `git pull`)
- 자식 Claude 가 사람 계정의 파일·키체인에 닿는다 — 전용 OS 계정 안내 (코드 아님, 절차)
- `scripts/authoring-agent.ts` 478줄 — 300줄 규칙 위반

## Plan

### 할 일 1. SPEC 을 먼저 고친다

- **RED** — 해당 없음 (문서). 대신 `check:spec` 이 기준
- **GREEN** — 인증 §7 API 블록에 발급·취소 통로와 `hasAgentToken`, 「인증 적용 범위」에 토큰 수단과 범위(작성 통로 + `me`),
  「등급으로 갈리는 자리」 표에 새 두 줄(admin), §8.8 계정 표에 「에이전트 토큰」 칸 ·
  작성 §3.6 「인증을 뚫지 않는다」에 토큰이 왜 뒷문이 아닌지 · 결정표 312행 뒤집기(옛 물음 남김) ·
  공통/4 `app_user` DDL · 공통/1 §3 컨텍스트 지도 문장 · `docs/SPEC.md` 라우터 표 한 줄
- **REFACTOR** — 없음

**files**: docs/spec/도메인/인증.md, docs/spec/도메인/작성.md, docs/spec/공통/4-데이터모델.md, docs/spec/공통/1-제품과-구조.md, docs/SPEC.md
**depends-on**: []
**검증**: `npm run check:spec`

### 할 일 2. 마이그레이션 — `agent_token_hash`

- **RED** — 할 일 3 의 저장소 검사가 칸이 없어 실패한다
- **GREEN** — `ALTER TABLE app_user ADD COLUMN agent_token_hash TEXT` + 부분 유일 인덱스(NULL 제외). down 은 역순
- **REFACTOR** — 없음

**files**: db/migrations/20260923000002_agent_token.sql
**depends-on**: [1]
**검증**: 로컬 DB 에 dbmate up → `\d app_user` 에 칸이 보인다

### 할 일 3. 설정 — 발급 · 취소 · 있는지

- **RED** — `settings/routes.test.ts`: 발급하면 `agentToken` 이 한 번 오고 DB 에는 해시만 있다 · 다시 발급하면 옛 토큰이 안 먹는다 ·
  취소하면 204 와 `hasAgentToken=false` · 운영 아닌 등급은 403
- **GREEN** — `settings/store.ts` 에 `에이전트토큰만들기`·`에이전트토큰지우기`, 목록에 `hasAgentToken`. 라우트 둘.
  `gate.ts` 등급표 · `scope.ts` 라우트표에 새 틀
- **REFACTOR** — 없음

**files**: apps/admin/src/settings/store.ts, apps/admin/src/settings/routes.ts, apps/admin/src/settings/routes.test.ts, apps/admin/src/auth/gate.ts, apps/admin/src/auth/scope.ts, apps/admin/src/auth/password.ts
**depends-on**: [2]
**검증**: `DATABASE_URL=… npx vitest run apps/admin/src/settings apps/admin/src/auth/gate.test.ts`

### 할 일 4. 문 — `Bearer` 로 사람을 알아낸다, 작성 통로만

- **RED** — `auth/gate.test.ts`: 토큰으로 `GET /api/auth/me` 200 · 작성 집기 통과 · `/api/runs` 는 403 `AGENT_TOKEN_SCOPE` ·
  취소한 토큰·비활성 계정은 401 · 틀린 모양은 401
- **GREEN** — `identify.ts` 의 `확인` 이 `authorization` 헤더를 먼저 보고 해시로 찾는다. 돌려주는 값에 수단(세션·토큰)을 싣고
  문(`gate.ts` onRequest)이 토큰이면 틀이 `/api/authoring/` 또는 `/api/auth/me` 인지 본다
- **REFACTOR** — 없음

**files**: apps/admin/src/auth/identify.ts, apps/admin/src/auth/identify.test.ts, apps/admin/src/auth/store.ts, apps/admin/src/auth/gate.ts, apps/admin/src/auth/gate.test.ts
**depends-on**: [3]
**검증**: `DATABASE_URL=… npx vitest run apps/admin/src/auth`

### 할 일 5. 설정 화면 — 토큰 발급·취소

- **RED** — `AgentToken.test.tsx`: 발급을 누르면 토큰이 한 번 보이고 복사 안내가 뜬다 · 있으면 취소 버튼이 보인다
- **GREEN** — `SettingsUser.tsx` 편집 칸에 새 부품 `AgentToken.tsx` 를 붙인다(파일이 298줄이라 따로 둔다). `api.ts` 에 두 부름
- **REFACTOR** — 없음

**files**: apps/admin/src/web/AgentToken.tsx, apps/admin/src/web/AgentToken.test.tsx, apps/admin/src/web/SettingsUser.tsx, apps/admin/src/web/api.ts
**depends-on**: [3]
**검증**: `npx vitest run apps/admin/src/web/AgentToken.test.tsx` + 브라우저 밝게·어둡게

### 할 일 6. `authoring-agent.ts` 나누기 (동작 그대로)

- **RED** — 없음 (기존 `authoring-agent.test.ts` 가 옮긴 뒤에도 통과해야 한다)
- **GREEN** — 순수 판정(과금위험 · 클로드인자 · 선행검사 · 대기줄전제 · 줄프롬프트 · 머지할수있나 · 주소 · 응답 판정 · 보고간격)을
  `scripts/authoring-rules.ts` 로 옮긴다. `authoring-agent.ts` 는 켜기와 줄 돌기만
- **REFACTOR** — 두 파일 다 300줄 이하

**files**: scripts/authoring-agent.ts, scripts/authoring-rules.ts, scripts/authoring-agent.test.ts, scripts/authoring-run.ts
**depends-on**: []
**검증**: `npx vitest run scripts/authoring-agent.test.ts && npm run typecheck`

### 할 일 7. 맥 — 비밀번호 대신 토큰

- **RED** — `authoring-token.test.ts`: 토큰 파일 자리가 홈 아래 한 곳 · 모양이 아닌 값은 거부 · `부른다` 가 `authorization: Bearer` 를 싣는다(쿠키 아님)
- **GREEN** — `scripts/authoring-token.ts`: 파일이 있으면 읽고, 없으면 터미널에서 한 번 묻고 본인만 읽게(0600) 저장.
  켤 때 `GET /api/auth/me` 로 이름·서비스·서버표를 받는다. 401 이면 파일을 지우고 「다시 발급해 넣어라」로 끝낸다.
  `부른다`·자료 받기가 쿠키 대신 토큰. 맥의 `AUTHORING_AGENT_USER` 는 필요 없어진다(서버는 그대로 씀)
- **REFACTOR** — 비밀번호묻기 · 로그인 삭제

**files**: scripts/authoring-token.ts, scripts/authoring-token.test.ts, scripts/authoring-agent.ts, scripts/authoring-rules.ts, scripts/authoring-io.ts, scripts/authoring-io.test.ts, scripts/authoring-run.ts, scripts/authoring-agent.test.ts
**depends-on**: [4, 6]
**검증**: `npx vitest run scripts && npm run typecheck`

### 할 일 8. 머지 뒤 main 을 당긴다

- **RED** — `authoring-merge.test.ts`: 병합 뒤 당김 인자는 `pull --ff-only origin main` 이고, 뿌리가 main 이 아니거나 더러우면 건너뛴다(사유 글)
- **GREEN** — `머지처리` 가 MERGED 로 끝낸 뒤 뿌리에서 당긴다. 실패는 로그만 — 요청은 이미 DONE 이다
- **REFACTOR** — 없음

**files**: scripts/authoring-merge.ts, scripts/authoring-merge.test.ts
**depends-on**: []
**검증**: `npx vitest run scripts/authoring-merge.test.ts`

### 할 일 9. 안내 문서 — 토큰 · 전용 OS 계정

- **RED** — 해당 없음
- **GREEN** — `docs/SETUP.md` §8: 토큰 발급→첫 실행에 한 번 붙여넣기 · 취소하는 법 · 전용 OS 계정(macOS 절차, 윈도우 한 줄) ·
  「비밀번호는 어디에도 안 적는다」 절을 토큰 절로 바꾼다
- **REFACTOR** — 없음

**files**: docs/SETUP.md
**depends-on**: [7]
**검증**: 눈으로 — 적힌 명령을 처음부터 따라 해 본다

## SPEC 동반 수정 (§2.7)

- ① 같은 규칙 찾기 — `grep -rn "비밀번호\|토큰 하나\|계정으로 로그인" docs/spec docs/SPEC.md` 결과 중 맥 경로를 말하는 줄: 작성.md 44·68·71·312, 공통/1 245~249. 할 일 1 이 전부 본다
- ② 뒤집는 결정 — 312행은 **판단 기준이 아직 쓸모 있다**(파일이 곧 열쇠). 남기고 왜 뒤집었는지 덧붙인다. 71행 「토큰 하나 파자로 되돌리지 마라」는 **로그인 없는 토큰**을 겨눈 것이라 문장을 좁힌다
- ③ 색인 — 라우터 표 「작성한 것을 머지하는 자리」 옆에 「맥 에이전트 인증을 바꿀 때」 한 줄. 장 목록·절 번호·표 주인은 해당 없음(새 절 없음)
- ④ SPEC 밖 — SETUP.md(할 일 9) · WORKSTREAMS 킥오프(작성 갈래에 「비밀번호」 언급 있으면 고침) · spec-review·check:* 해당 없음 · DESIGN·mockup 해당 없음 · **코드 상수**: `gate.ts` 등급표·`scope.ts` 라우트표(할 일 3)
- ⑤ 숫자 — 새로 적는 개수 없음
- ⑥ `npm run check:spec` → [6]

## Plan 메타

할 일 9개 · 예상 묶음 4개 ([1,6,8] → [2] → [3] → [4,5] → [7] → [9]) · 구현 규율: TDD · 추가 검증: DB 붙인 `npm test` 연속 3회

## 리뷰 결과

(계획 검토가 채운다)
