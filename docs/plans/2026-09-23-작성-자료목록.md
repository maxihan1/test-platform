# 작성 입력을 자료 목록으로 — 파일 첨부와 피그마

등급: 3 · 갈래: WS-작성 · 2026-09-23 · PR #61

## 도메인 정리

- **BC** — 작성(`도메인/작성` §3.6). 다른 컨텍스트는 안 건드린다
- **계약 변경** — 게이트 0 승인 (2026-09-23)
  ① 새 표 `authoring_asset` · `spec_text` NOT NULL 풀기 · `status` 에 `DRAFT`
  ② 통로 — 요청 만들기 모양이 바뀌고, 자료 올리기 · 자료 내려받기 · 줄에 세우기 셋이 는다
  ③ 목록 응답에서 `specText` 를 뺀다. 상세 응답에 `assets` 를 더한다
  ④ `packages/kit` 안 건드림

| 무엇 | 영향 |
|---|---|
| 표 | `db/migrations/` 새 파일 하나 |
| 서버 | `apps/admin/src/authoring/*` · `auth/scope.ts` · `auth/gate.ts` · `app.ts` |
| 화면 | `AuthoringNew` · `AuthoringDetail` · `authoringView` · `api.ts` · 문구 |
| 맥 | `scripts/authoring-agent.ts` + 새 순수 함수 파일 |
| 스킬 | `tpx-cases` — 자료 여럿을 요구사항 표 하나로 |

## 왜

기획서는 붙여넣는 본문이 아니라 **PDF·워드 파일**로 온다. 피그마도 같이 온다. 문서도 피그마도 **여럿이 한 세트**다
(사용자가 세 번에 걸쳐 못박았다 — PR #61 인계 코멘트 §1).

**`DRAFT` 가 핵심이다.** 요청을 만들고 파일을 하나씩 올리는 동안 줄에 서 있으면 맥이 집어 가 빈 것을 보고 실패한다.
다 올린 뒤 「줄에 세우기」가 `DRAFT → PENDING` 으로 바꾼다.

### 흐름

```
화면  POST /requests {kind:AUTHOR, figma:[주소…]}  → DRAFT 행 + FIGMA 자료 행   → 201 {id}
화면  POST /requests/:id/assets?name=기획서.pdf  (원시 바이트)  × 파일 수     → FILE 자료 행
화면  POST /requests/:id/submit                    → DRAFT → PENDING (자료 0 이면 409)
맥    POST /requests/claim                          → 집은 한 건 (+ assets)
맥    GET  /requests/:id/assets/:assetId            → 파일 받아 임시 폴더에
맥    docx 는 textutil 로 글자만 · pdf 는 그대로 · 피그마 주소는 프롬프트에
맥    claude -p --add-dir <임시 폴더>               → tpx-cases 가 npx figma-reader-cli 로 피그마를 읽는다
```

- **`RERUN` 은 `DRAFT` 를 안 거친다.** 자료를 새로 안 받고 원본 행의 자료를 맥이 읽는다 — 곧장 `PENDING`
- **`MERGE` 는 지금 그대로다** (자료 없음 · 바로 `PENDING`)
- **파일 저장 자리는 `authoring-assets/<요청번호>/`** — 사진 폴더 `authoring/<번호>/` 안에 두면
  사진 장수 상한(`readdir` 200)이 자료까지 센다. 뿌리는 사진과 같은 `PLATFORM_ARTIFACTS_DIR` (볼륨으로 묶인 유일한 자리)

### 실측 — 파일이 이미 크다 (CLAUDE.md §3 300줄)

| 파일 | 줄 | 이번에 |
|---|---|---|
| `authoring/routes.ts` | 379 | 자료 통로 셋을 **새 파일 `authoring/assets.ts`** 로 뗀다 — 파서 상한도 따로 가져야 해서 어차피 갈라야 한다 |
| `authoring/store.ts` | 306 | 자료 읽기·쓰기를 **새 파일 `authoring/assetStore.ts`** 로 |
| `scripts/authoring-agent.ts` | 600 | 자료 판단 순수 함수를 **새 파일 `scripts/authoring-assets.ts`** 로 |
| `web/api.ts` | 553 | 함수 셋만 더한다. 쪼개기는 범위 밖 — 게이트 2 요약에 싣는다 |

**Fastify 파서 상한은 파서 쪽이 이긴다.** `routes.ts:165` 의 `octet-stream` 파서가 `bodyLimit: 4MB`(사진)로 걸려 있어
같은 플러그인 안에서는 라우트 `bodyLimit` 으로 못 넓힌다. 자료 통로는 **다른 플러그인**에서 자기 파서를 등록한다.

### 게이트 1 에서 정할 것 (명세에 없는 값 — CLAUDE.md §1.4)

| 무엇 | 제안 |
|---|---|
| 받는 파일 종류 | `.pdf` · `.docx` · `.md` · `.txt` |
| 한 파일 상한 | 20MB |
| 한 요청의 자료 개수 상한 | 파일 + 피그마 합쳐 20 |
| 피그마 토큰 자리 | **맥 비밀번호와 같은 방식** — 켤 때 묻고 메모리에만. 빈 입력이면 피그마 없이 돌고, 피그마 자료가 든 요청만 `FAILED`(「피그마 토큰 없이 켰다」) |
| `spec_text` 를 맥이 채우나 | **칸만 풀고 채우는 일은 미룬다.** 지금 그 값을 만드는 쪽도 읽는 쪽도 없다 |
| `figma-reader-cli` 스킬 설치 | **안 한다.** `tpx-cases` 가 `npx figma-reader-cli@<고정 판> inspect` 를 직접 부른다. 벤더 폴더가 안 생기므로 CLAUDE.md 벤더 경계 절을 안 고친다 |

## LEARNINGS 근거

- 넓은 `DELETE ... LIKE` 가 남의 fixture 를 지운 일 (CLAUDE.md §3) — 자료 표 검사도 **`XWA`·`XWAR` 의 자기 `service_id` 로만** 지운다. `authoring_asset` 은 `ON DELETE CASCADE` 로 부모와 함께 간다
- 경로 번호를 느슨하게 읽어 문과 라우트가 다른 값을 본 일 — `:assetId` 도 `번호()` 로 읽는다
- 폴더 거슬러 오르기 — 파일 이름은 모양을 좁히고 저장 이름은 **자료 행 번호**로 짓는다
- 맥이 보낸 값을 명령줄에 끼운 일(`pr_url`) — 피그마 주소도 **모양을 좁혀 받는다**. 자식이 그 값을 `npx` 인자로 쓴다

## Plan

### 할 일 1. 마이그레이션 — 자료 표 · DRAFT · spec_text 풀기

- **RED** — `store.test.ts` 에 셋. ⒜ `status='DRAFT'` 인 행 INSERT 가 성공 ⒝ `spec_text` 없이 INSERT 성공 ⒞ `authoring_asset` 에 `kind='X'` INSERT 가 CHECK 로 실패. 지금은 표가 없고 CHECK 가 `DRAFT` 를 막아 실패
- **GREEN** — `20260923000001_authoring_asset.sql`. `ALTER COLUMN spec_text DROP NOT NULL` · status CHECK 를 지우고 `DRAFT` 를 더해 다시 건다 · `authoring_asset(id, request_id FK ON DELETE CASCADE, position, kind CHECK IN ('FILE','FIGMA'), name, figma_url, size, created_at)` + `CHECK ((kind='FILE') = (figma_url IS NULL))` + `UNIQUE (request_id, position)`. `migrate:down` 도 쓴다
- **REFACTOR** — 없음

**files**: `db/migrations/20260923000001_authoring_asset.sql` · `apps/admin/src/authoring/store.test.ts`
**depends-on**: []
**검증**: `docker compose run --rm migrate && npx vitest run apps/admin/src/authoring/store.test.ts`

### 할 일 2. 자료 저장소 — 준비 행 세우기 · 자료 더하기 · 목록 · 줄에 세우기

- **RED** — `assetStore.test.ts`. ⒜ `준비세우기` 가 `DRAFT` 행과 피그마 자료 행을 한 트랜잭션으로 만든다 ⒝ `자료더하기` 는 `DRAFT` 행에만 붙고 다음 `position` 을 준다 ⒞ `자료목록` 이 순서대로 준다 ⒟ `제출` 이 자료 0 이면 false, 있으면 `PENDING` 으로 바꾸고 `DRAFT` 가 아니면 false. fixture 접두사 `XWS`
- **GREEN** — 새 파일 `authoring/assetStore.ts`. 전이는 `WHERE status='DRAFT'` 한 문장 UPDATE (store.ts 의 집기와 같은 모양)
- **REFACTOR** — 없음

**files**: `apps/admin/src/authoring/assetStore.ts` · `apps/admin/src/authoring/assetStore.test.ts` · `CLAUDE.md`(fixture 접두사 줄에 `XWS` 추가)
**depends-on**: [1]
**검증**: `npx vitest run apps/admin/src/authoring/assetStore.test.ts`

### 할 일 3. 목록에서 본문 빼기 · 상세에 자료 싣기

- **RED** — `store.test.ts` 에 ⒜ `한쪽` 의 항목에 `specText` 키가 없다 ⒝ `routes.test.ts` 에 상세 응답이 `assets` 배열을 든다
- **GREEN** — `한쪽` 이 `spec_text` 를 안 고른다(요약 타입). `요청.specText` 를 `string | null` 로. 상세 라우트가 `자료목록` 을 붙인다
- **REFACTOR** — 없음

**files**: `apps/admin/src/authoring/store.ts` · `apps/admin/src/authoring/store.test.ts` · `apps/admin/src/authoring/routes.ts` · `apps/admin/src/authoring/routes.test.ts`
**depends-on**: [2]
**검증**: `npx vitest run apps/admin/src/authoring/`

### 할 일 4. 요청 만들기 — AUTHOR 는 DRAFT, 피그마 주소는 모양을 좁혀 받는다

- **RED** — `routes.test.ts`. ⒜ `{kind:'AUTHOR', figma:['https://www.figma.com/design/abc/x']}` → 201, 행이 `DRAFT` ⒝ 피그마 주소가 모양 밖(`http:` · 다른 호스트 · 따옴표·세미콜론)이면 400 `BAD_FIGMA_URL` ⒞ `RERUN` 은 `specText` 없이 `PENDING` ⒟ 옛 `specText` 만 보낸 AUTHOR 는 더 이상 본문을 받지 않는다(무시하고 DRAFT). 지금은 `SPEC_TEXT_REQUIRED` 로 실패
- **GREEN** — `피그마주소인가` 순수 함수(`^https://www\.figma\.com/(design|file|proto)/[A-Za-z0-9]{1,64}(/[^\s"'`;|&$<>\\]*)?(\?[^\s"'`;|&$<>\\]*)?$`) + 개수 상한. AUTHOR 는 `준비세우기`, RERUN 은 `줄세우기`(기획서 null). 목록 `?status=` 허용 값에 `DRAFT`
- **REFACTOR** — 없음

**files**: `apps/admin/src/authoring/routes.ts` · `apps/admin/src/authoring/routes.test.ts` · `apps/admin/src/authoring/store.ts`
**depends-on**: [3]
**검증**: `npx vitest run apps/admin/src/authoring/routes.test.ts`

### 할 일 5. 자료 올리기 통로 — 새 플러그인 · 문 등록

- **RED** — `assets.test.ts`. ⒜ 요청한 사람이 `DRAFT` 행에 `?name=기획서.pdf` 로 올리면 200 ⒝ 남이 올리면 403 `NOT_REQUESTER` ⒞ `PENDING` 행이면 409 ⒟ 이름 모양 밖(`../x.pdf` · `.exe`)이면 400 ⒠ 20MB 초과 413 ⒡ 개수 상한 409. `scope.test.ts` · `gate.test.ts` 에 새 틀이 `작성요청` 갈래 · `operator` 로 잡힌다. 지금은 라우트가 없어 404/403
- **GREEN** — `authoring/assets.ts` 플러그인(자기 `octet-stream` 파서 · 20MB). 저장은 `authoring-assets/<요청>/<자료번호>.<확장자>` — **사람이 준 이름은 표에만** 적고 디스크 이름으로 안 쓴다. `app.ts` 등록 · `scope.ts`·`gate.ts` 에 한 줄씩
- **REFACTOR** — `사진뿌리()` 와 `번호()` 를 `routes.ts` 에서 export 해 같이 쓴다 (복사 금지)

**files**: `apps/admin/src/authoring/assets.ts` · `apps/admin/src/authoring/assets.test.ts` · `apps/admin/src/authoring/routes.ts` · `apps/admin/src/app.ts` · `apps/admin/src/auth/scope.ts` · `apps/admin/src/auth/scope.test.ts` · `apps/admin/src/auth/gate.ts` · `apps/admin/src/auth/gate.test.ts`
**depends-on**: [4]
**검증**: `npx vitest run apps/admin/src/authoring/assets.test.ts apps/admin/src/auth/`

### 할 일 6. 줄에 세우기 통로

- **RED** — `assets.test.ts`. ⒜ 자료가 있는 `DRAFT` → `POST /:id/submit` 200, 행이 `PENDING` ⒝ 자료 0 → 409 `NO_ASSETS` ⒞ 두 번 부르면 409 ⒟ 남이 부르면 403. 문 등록 검사 한 줄씩
- **GREEN** — `제출` 을 부르는 라우트 하나 + 문 등록
- **REFACTOR** — 없음

**files**: `apps/admin/src/authoring/assets.ts` · `apps/admin/src/authoring/assets.test.ts` · `apps/admin/src/auth/scope.ts` · `apps/admin/src/auth/scope.test.ts` · `apps/admin/src/auth/gate.ts` · `apps/admin/src/auth/gate.test.ts`
**depends-on**: [5]
**검증**: `npx vitest run apps/admin/src/authoring/assets.test.ts apps/admin/src/auth/`

### 할 일 7. 자료 내려받기 통로

- **RED** — `assets.test.ts`. ⒜ `GET /:id/assets/:assetId` 가 올린 바이트 그대로 + `content-disposition` 에 원래 이름 ⒝ 다른 요청의 자료 번호면 404 (요청 번호와 자료 번호 짝을 본다) ⒞ 피그마 자료면 404 ⒟ `:assetId` 가 `1e3` 이면 400. `viewer` 로 열린다
- **GREEN** — 라우트 하나 + 문 등록
- **REFACTOR** — 없음

**files**: `apps/admin/src/authoring/assets.ts` · `apps/admin/src/authoring/assets.test.ts` · `apps/admin/src/authoring/assetStore.ts` · `apps/admin/src/auth/scope.ts` · `apps/admin/src/auth/scope.test.ts` · `apps/admin/src/auth/gate.ts` · `apps/admin/src/auth/gate.test.ts`
**depends-on**: [6]
**검증**: `npx vitest run apps/admin/src/authoring/assets.test.ts apps/admin/src/auth/`

### 할 일 8. 화면 API — 만들기 · 올리기 · 세우기

- **RED** — `api.test.ts`. ⒜ `createAuthoringRequest(service, {kind:'AUTHOR', figma})` 가 `specText` 없이 간다 ⒝ `uploadAuthoringAsset` 이 `application/octet-stream` 과 `?name=` 으로 파일 바이트를 보낸다 ⒞ `submitAuthoringRequest` 경로. `AuthoringRow.status` 에 `DRAFT`, `assets?` 칸
- **GREEN** — 함수 셋과 타입
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/api.ts` · `apps/admin/src/web/api.test.ts`
**depends-on**: [7]
**검증**: `npx vitest run apps/admin/src/web/api.test.ts`

### 할 일 9. 새 요청 화면 — 파일 여럿 + 피그마 주소 여럿

- **RED** — `AuthoringNew.test.tsx`. ⒜ 파일 둘 + 피그마 주소 한 줄을 넣고 보내면 만들기 1회 → 올리기 2회 → 세우기 1회 순서 ⒝ 파일도 주소도 없으면 버튼이 꺼진다 ⒞ 올리기 중간에 실패하면 세우기를 안 부르고 오류를 보인다(행은 `DRAFT` 로 남는다). 지금은 `textarea` 하나라 실패
- **GREEN** — `<input type="file" multiple accept=".pdf,.docx,.md,.txt">` + 피그마 주소 `textarea`(줄마다 하나). 머리 주석의 「경로가 아니라 본문을 보낸다」를 **뒤집은 이유와 함께** 고친다 (§2.7 ②). 문구는 `messages/authoring.ts`
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/AuthoringNew.tsx` · `apps/admin/src/web/AuthoringNew.test.tsx` · `apps/admin/src/web/messages/authoring.ts` · `apps/admin/src/web/styles.css`
**depends-on**: [8]
**검증**: `npx vitest run apps/admin/src/web/AuthoringNew.test.tsx`

### 할 일 10. 목록·상세 — 준비 중 라벨과 자료 목록

- **RED** — `authoringView.test.ts` 에 `DRAFT` 줄의 라벨이 「준비 중」. `AuthoringDetail.test.tsx` 에 자료 이름이 순서대로 보이고 파일은 내려받기 링크, 피그마는 원래 주소 링크(`rel="noopener noreferrer"`)
- **GREEN** — 보임 판정에 한 갈래 · 상세에 목록 한 덩어리
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/authoringView.ts` · `apps/admin/src/web/authoringView.test.ts` · `apps/admin/src/web/AuthoringDetail.tsx` · `apps/admin/src/web/AuthoringDetail.test.tsx` · `apps/admin/src/web/messages/authoring.ts`
**depends-on**: [8]
**검증**: `npx vitest run apps/admin/src/web/authoringView.test.ts apps/admin/src/web/AuthoringDetail.test.tsx`

### 할 일 11. 맥 — 자료를 무엇으로 바꿀지 정하는 순수 함수

- **RED** — `scripts/authoring-assets.test.ts`. ⒜ `자료계획(assets)` 이 `.docx` 는 `textutil -convert txt`, `.pdf`·`.md`·`.txt` 는 그대로, 피그마는 주소만 ⒝ `피그마토큰필요(assets)` ⒞ 줄프롬프트에 **파일 경로 목록과 피그마 주소 목록**이 순서대로 실리고 기획서 본문 칸은 없다 ⒟ `클로드인자(폴더)` 에 `--add-dir <폴더>` 가 들고 `--bare` 는 여전히 없다. 지금은 파일이 없다
- **GREEN** — 새 파일 `scripts/authoring-assets.ts`. `authoring-agent.ts` 의 `줄프롬프트` · `클로드인자` · `집은것` 이 자료를 받게 고친다
- **REFACTOR** — 없음

**files**: `scripts/authoring-assets.ts` · `scripts/authoring-assets.test.ts` · `scripts/authoring-agent.ts` · `scripts/authoring-agent.test.ts`
**depends-on**: [7]
**검증**: `npx vitest run scripts/`

### 할 일 12. 맥 껍데기 — 내려받기 · 변환 · 피그마 토큰

- **RED** — 껍데기는 검사를 안 붙인다(CLAUDE.md §3 스크립트 규칙). 대신 할 일 11 의 순수 함수가 판단을 다 든다
- **GREEN** — 집은 뒤 `GET .../assets/:assetId` 로 `os.tmpdir()/authoring-<번호>/` 에 받는다 · `docx` 는 `spawnSync('textutil', …)` · `RERUN` 은 원본 행의 자료를 받는다 · 켤 때 피그마 토큰을 비밀번호처럼 묻고 **자식 환경에만** `FIGMA_TOKEN` 으로 넘긴다 · 토큰 없이 켰는데 피그마 자료면 `FAILED` · 끝나면 임시 폴더를 지운다
- **REFACTOR** — 없음

**files**: `scripts/authoring-agent.ts`
**depends-on**: [11]
**검증**: `npx tsc -p . --noEmit && npx vitest run scripts/`

### 할 일 13. tpx-cases — 자료 여럿을 요구사항 표 하나로 · 피그마 읽기

- **RED** — 하네스라 단위 검사 없음 (스킬 검사기는 없다). 아래 grep 과 게이트 2 의 눈으로
- **GREEN** — 입력 절에 「자료 목록」: 파일은 Read(PDF 그대로 · docx 는 맥이 바꾼 txt), 피그마는 `npx figma-reader-cli@<고정 판> inspect <주소>`. **여럿을 요구사항 표 하나로 합치고 표의 각 줄에 출처 자료를 적는다.** 자료끼리 어긋나면 표에 그대로 적고 지어내지 않는다. 기대값 규칙(사람이 판정한 것만)은 그대로
- **REFACTOR** — 없음

**files**: `.claude/skills/tpx-cases/SKILL.md`
**depends-on**: []
**검증**: `grep -n "figma-reader-cli\|자료 목록" .claude/skills/tpx-cases/SKILL.md`

### 할 일 14. 실제로 한 바퀴 돌린다

- **RED** — 인계 §8 「아직 한 바퀴를 통째로 돌려 본 적이 없다」. 돌리기 전까지 모른다
- **GREEN** — `docker compose up` → 화면에서 PDF 하나로 요청 → `npm run authoring-agent` → 초안 PR 이 열리고 화면에 머지 버튼이 뜨는지. **구독 한도를 쓴다 — 돌리기 전에 사용자에게 묻는다.** 피그마는 토큰이 생긴 뒤에
- **REFACTOR** — 막힌 자리는 LEARNINGS

**files**: `docs/LEARNINGS.md`
**depends-on**: [9, 12, 13]
**검증**: 화면 `테스트 작성` 줄이 `끝남` + 머지 버튼

## SPEC 동반 수정 (§2.7)

### 할 일 15. SPEC 본문 — 데이터모델 §6 · 작성 §3.6 · §7 · 「아직 알 수 없는 것」

- **RED** — `npm run check:spec` 은 지금 통과한다. 고친 뒤에도 통과해야 한다
- **GREEN** —
  - `공통/4-데이터모델` §6 — `spec_text` 줄(:165 「기획서 본문 그대로」)을 자료 표로 · `status` 에 `DRAFT` 와 전이 `DRAFT→PENDING`(제출) · 새 표 `authoring_asset` SQL · :266 「기획서 본문도 … 이 표를 지난다」를 「자료가 이 표와 자료 폴더를 지난다」로 · 계약 변경 블록을 `상태: 반영 완료 (2026-09-23, db/migrations/20260923000001_authoring_asset.sql)` 로
  - `도메인/작성` §7 — :106 `specText` 를 `figma?` 로 · 통로 셋 추가 · :111 `?status=` 에 `DRAFT` · :161 「`spec_text`(기획서 본문 통째)」 근거를 「자료」로 · 게이트 1 에서 정한 상한 셋
  - `도메인/작성` 「아직 알 수 없는 것」 — :196 「맥 계정의 비밀값을 어디에 두나 — 안 정했다」를 **닫는다**(PR #58 이 정했다: 어디에도 안 두고 켤 때 묻는다). 피그마 토큰도 같은 줄에
  - 판단이 뒤집힌 자리(「본문을 싣는다」)는 **이유를 덧붙여 남긴다** (§2.7 ②)
- **REFACTOR** — 같은 규칙을 두 곳에 옮겨 적지 않는다. 상한 숫자는 §7 에만 두고 §6 은 가리킨다

**grep 근거** (고친 문장의 핵심 낱말로 전량):
```
docs/spec/도메인/작성.md:106 specText        docs/spec/공통/4-데이터모델.md:165 spec_text · 기획서 본문
docs/spec/도메인/작성.md:111 PENDING|RUNNING docs/spec/공통/4-데이터모델.md:170 PENDING|RUNNING
docs/spec/도메인/작성.md:161 spec_text      docs/spec/공통/4-데이터모델.md:215 spec_text
docs/spec/도메인/작성.md:196 비밀값을 어디에 docs/spec/공통/4-데이터모델.md:266 기획서 본문
```
구현 전에 한 번 더 돌린다 — 「본문」·「붙여」·「기획서를 넣」 으로도.

**files**: `docs/spec/공통/4-데이터모델.md` · `docs/spec/도메인/작성.md` · `docs/spec/도메인/인증.md`(§7 등급 표에 새 통로 셋)
**depends-on**: []
**검증**: `npm run check:spec`

### 할 일 16. 색인 네 곳

- **GREEN** — `docs/SPEC.md` 장 목록 분량(**`check:spec` 이 찍는 값을 복사**) · 절 번호 표 · 표 주인(`authoring_asset` → §6) · **라우터 표에 「작성 입력(파일·피그마)을 바꿀 때」 한 줄**
**files**: `docs/SPEC.md`
**depends-on**: [15]
**검증**: `npm run check:spec`

### 할 일 17. SPEC 밖 — 문서·검사·목업·코드 상수

| 어디 | 무엇 |
|---|---|
| `docs/WORKSTREAMS.md` WS-작성 킥오프 | 입력이 자료 목록 · `DRAFT` |
| `docs/SETUP.md` §8 (:242 「붙여 넣」) | 파일·피그마로 요청 · 맥 켤 때 피그마 토큰을 묻는다 · 토큰 만드는 곳 |
| `docs/WORKFLOW.md` | WS-작성 단위 상태 |
| `docs/design-mockup.html` | 새 요청 폼(파일 고르기 + 피그마 주소) · 상세 자료 목록 |
| `spec-review` 체크리스트 · `check:*` | 작성 절에 「자료 표 CHECK · DRAFT 전이 · 자료 통로 문 등록」 한 줄 |
| **코드 상수** | `routes.ts` 의 `상태들` 배열 · `store.ts` 의 `상태` 타입 · 화면 `AuthoringRow['status']` — 할 일 3·4·8 이 고친다. 여기서는 빠진 것이 없는지 grep 으로 확인 |
| `docs/DESIGN.md` · `docs/HOOKS.md` | 해당 없음 — 화면 기준과 훅이 안 바뀐다 |
| 숫자 빼기 | 상한 셋은 §7 에만. 다른 문서는 절을 가리킨다 |

**files**: `docs/WORKSTREAMS.md` · `docs/SETUP.md` · `docs/WORKFLOW.md` · `docs/design-mockup.html` · `.claude/skills/spec-review/SKILL.md`
**depends-on**: [15]
**검증**: `grep -rn "DRAFT\|PENDING" apps/admin/src scripts --include=*.ts --include=*.tsx | grep -v test` 에 `DRAFT` 가 빠진 상태 목록이 없다 · `npm run check:spec`

## Plan 메타

할 일 17개 · 예상 묶음 6개(서버 1→7 직렬 · 화면 8→9·10 · 맥 11→12 · 스킬 13 · SPEC 15→16·17 · 마무리 14) · 구현 규율: TDD · 추가 검증: `npm test` 연속 3회 · `npm run check:spec` · `npm run typecheck`

**10개를 넘는 이유.** 계약 셋(표·통로·응답)이 서버·화면·맥·스킬 넷에 동시에 닿고, 3등급이라 SPEC 동반 수정이 셋 붙는다.
**쪼갤 수 있는 자리는 하나다** — 할 일 11·12·13·14(맥이 자료를 읽는 쪽)를 다음 PR 로. 그러면 이번 PR 은 「화면에서 파일·피그마를
넣으면 표에 쌓인다」까지이고, **맥이 그 사이 옛 모양(`specText`)을 기대하므로 작성이 깨진 채로 병합된다.** 그래서 한 PR 을 권한다.

## 리뷰 결과

**렌즈**: plan-eng-review · plan-ceo-review (3등급 = 2종, 서로 독립한 하위 작업자) · 2026-09-23
**판정**: BLOCKER 1건 (두 렌즈 공통) · 주의 12건 (겹친 것은 합침)

### BLOCKER 1 — 피그마 주소 검사가 평범하게 복사한 링크를 거절한다
할 일 4 정규식이 쿼리에서 `&` 를 막는다. 피그마 「Copy link」는 거의 항상 `?node-id=12-34&t=AbCd-0` 이다.
**나중에** — 첫 피그마 입력이 400 으로 튕긴다. 검사 예시에 쿼리가 없어 테스트는 초록이다. `&` 를 풀면 자식의 `npx … inspect <주소>` 에서 명령 구분자로 산다.
**고칠 것** — 통과/거절이 아니라 **정규화**. `new URL()` 로 읽고 호스트·경로 종류·파일 키를 본 뒤 `https://www.figma.com/design/<키>/?node-id=<숫자-숫자>` 로 **다시 조립해 저장**한다. RED 에 `&t=` 붙은 실제 링크가 201 이고 저장값에 `t=` 가 없음을 넣는다. FigJam(`/board`) 을 받을지 게이트 1 에서 정한다.

### 주의 1 — 「SPEC 먼저」인데 SPEC 할 일이 15~17 (eng·ceo 공통)
게이트 0 답이 「SPEC 먼저 고친다」이고 CLAUDE.md §1.3 도 그렇다. 할 일 1 과 15 가 둘 다 `depends-on: []` 이라 번호대로 가면 마이그레이션이 먼저다.
**고칠 것** — 할 일 1 의 `depends-on` 에 [15].

### 주의 2 — 한글 파일 이름이 올리기·내려받기 양쪽에서 깨진다 (eng·ceo 공통)
사진 규칙(`routes.ts:322`, ASCII 만)을 따라 하면 「기획서.pdf」가 400. 비껴가도 `content-disposition` 에 한글을 넣으면 Node 가 `ERR_INVALID_CHAR` 로 500.
**고칠 것** — 이름은 한글 허용, 제어문자·`"`·`/`·`\`·`..` 막기. 내려받기는 `attachment; filename*=UTF-8''<퍼센트 인코딩>` · `content-type: application/octet-stream` 고정 · `X-Content-Type-Options: nosniff` (확장자로 `text/html` 을 추정하면 올린 파일이 admin 브라우저에서 같은 출처로 열린다). 검사 예시는 한글 이름으로.

### 주의 3 — 중간에 실패한 DRAFT 가 되살릴 수도 치울 수도 없다 (eng·ceo 공통)
이어 올리기·제출·지우기 길이 없다. 목록에 「준비 중」이 영원히 남고(「기다리면 된다」로 읽힌다) 파일도 쌓인다. 두 번 누르면 DRAFT 둘.
**고칠 것** — 최소 ① 실패 문구 「이 요청은 줄에 서지 않았다. 새 요청으로 다시 넣어라」 ② 보내는 동안 버튼 끄기 ③ DRAFT 상세에 같은 안내. 이어 올리기·삭제를 넣을지는 명세에 없는 결정이라 게이트 1 에서 정한다 (CLAUDE.md §1.4).

### 주의 4 — 자료 없이 줄에 선 행을 맥이 빈 입력으로 돌린다 (eng·ceo 공통)
⒜ 마이그레이션 전 AUTHOR 행은 `spec_text` 만 있고 자료 0 ⒝ RERUN 원본이 RERUN·MERGE·DRAFT 일 수 있다 — `원본확인`(routes.ts:83)이 종류·상태를 안 본다 ⒞ 병합 뒤 맥을 다시 켜기 전 옛 에이전트가 새 행을 집으면 `authoring-agent.ts:218` 의 `specText ?? ''` 가 빈 기획서로 claude 를 부른다.
**나중에** — 구독 한도를 빈 입력에 쓴다.
**고칠 것** — 서버가 RERUN 원본을 「AUTHOR 이고 DRAFT 아님」으로 좁힌다(할 일 4 RED). 할 일 11 에 「자료 0 이고 기획서도 없으면 claude 를 안 부르고 FAILED」 · 옛 행은 `spec_text` 를 그대로 쓴다. SETUP §8 에 「병합 뒤 맥을 다시 켜라」.

### 주의 5 — claim 응답에 자료가 따라오는 할 일이 없다 (eng)
흐름도는 `claim → (+ assets)` 인데 할 일 3 은 상세 응답에만 붙인다. 할 일 11 은 `집은것` 이 자료를 든다고 가정한다.
**고칠 것** — claim 응답에 assets 를 싣는 RED 를 할 일 3 에 넣거나, 맥이 집은 뒤 상세를 한 번 더 부르게 하고 할 일 11 에서 검사.

### 주의 6 — 동시 올리기에서 순서 번호·개수 상한·파일 쓰기 순서가 어긋난다 (eng)
MAX+1 은 겹치면 `UNIQUE` 에 걸려 500. 읽고 넣는 상한은 21개째를 통과시킨다. 행 먼저 넣고 파일 쓰기가 실패하면 내려받기가 ENOENT.
**고칠 것** — 개수 검사와 position 을 한 문장 `INSERT … SELECT` 로, 충돌은 409. 순서는 행 → 파일 → 실패 시 행 지우기.

### 주의 7 — `migrate:down` 이 안 되돌아간다 (eng)
DRAFT 행이나 `spec_text IS NULL` 행이 하나라도 있으면 down 의 `SET NOT NULL` 과 옛 CHECK 가 실패한다. 기존 CHECK 는 이름이 없어 자동 이름(`authoring_request_status_check`)을 맞춰야 한다.
**고칠 것** — down 에서 그 행들을 어떻게 치울지 적고, 새 CHECK 에 이름을 붙이고, 검증에 up → down → up.

### 주의 8 — 「Fastify 는 파서 상한이 이긴다」는 전제가 반대다 (eng, 실측)
Fastify 5.12.4 `content-type-parser.js:241` — 라우트 `bodyLimit` 이 이긴다. 파서 10바이트·라우트 100 에서 50바이트가 200. 형제 플러그인이 각자 파서를 거는 것도 된다.
**고칠 것** — 결론(새 파일)은 300줄 때문에 그대로. 근거 문장만 바꾼다. 틀린 근거를 코드 주석에 옮기지 않는다.

### 주의 9 — 자식 세션이 `npx figma-reader-cli` 를 실제로 돌릴 수 있나 (ceo · eng 가 엇갈림)
ceo — `acceptEdits` 는 편집만 자동 허용이라 Bash 가 조용히 거부될 수 있고, 거부돼도 종료 코드 0 이라 피그마를 빼고 만든 케이스가 「끝남」으로 온다. eng — `SETUP.md:303` 의 `Bash(*)` 허용 전제 안에 든다.
**고칠 것** — 할 일 13 에 `npx --yes figma-reader-cli@<고정 판>` · 못 읽으면 표에 「못 읽음」을 적고 멈춘다. 할 일 12 에 허용 여부 확인 단계. 엇갈림은 할 일 14 에서 실측으로 가린다.

### 주의 10 — 할 일 12 의 판단이 검사 없이 껍데기에 남는다 (eng)
「RERUN 이면 어느 행의 자료를 받나」·「토큰 없이 피그마면 FAILED」·「docx 만 변환」은 판단이다.
**고칠 것** — 할 일 11 순수 함수로 옮겨 RED 를 붙인다. 껍데기엔 I/O 만.

### 주의 11 — fixture 접두사와 같은 파일 병렬 (eng)
`assets.test.ts` 의 접두사가 없다 — `XWA`·`XWAR`·`XWS` 와 안 겹치는 것(예: `XWU`)을 정해 CLAUDE.md §3 에 적고, `service_id` 로만 지우고, 서비스 부모 행과 `PLATFORM_ARTIFACTS_DIR` 임시 폴더까지 치운다. 할 일 9 와 10 이 둘 다 `depends-on: [8]` 인데 `messages/authoring.ts` 를 같이 고친다 — 10 을 [9] 에 건다.

### 주의 12 — 실제 한 바퀴가 PDF 경로만 증명한다 (ceo)
docx 변환과 「여럿을 표 하나로」가 한 번도 안 돈다. `depends-on` 에 상세 화면(할 일 10)이 빠졌는데 검증 기준이 상세의 머지 버튼이다.
**고칠 것** — 입력을 PDF 1 + docx 1 로 (비용 거의 같음). `depends-on` 에 10. 게이트 2 요약에 「피그마 경로는 토큰이 없어 못 돌렸다」를 남은 위험으로.

### 통과한 것
- 계약 범위가 게이트 0 승인과 맞는다. `packages/kit` 안 건드림
- `DRAFT → PENDING` 을 `WHERE status='DRAFT'` 한 문장 UPDATE 로 — 올리는 도중에 맥이 집는 문제를 정확히 막는다
- 디스크 이름을 자료 행 번호로 — 폴더 거슬러 오르기가 구조적으로 막힌다
- 사진 폴더와 자료 폴더를 갈라 사진 200장 상한을 안 건드린다. 뿌리는 볼륨으로 묶인 자리
- 올리기·제출은 요청한 사람만, 내려받기는 viewer 부터 — 기존 상세 공개 범위와 같다
- `ON DELETE CASCADE` 로 기존 `service_id` 정리가 그대로 통한다
- 한 PR 판단 — 11~14 를 떼면 작성이 깨진 채 병합된다는 근거가 맞다
- 스킬 설치 없이 `npx` 로 부르는 판단 — 벤더 경계 절을 안 고쳐도 된다
- 게이트 1 제안값(20MB · 20개 · 토큰은 켤 때 묻기 · `spec_text` 채우기 미룸)이 합리적. 덧붙여 `.doc` 는 `textutil` 이 공짜로 처리한다 · `.hwp`·`.pptx` 거부 시 이유를 보일지
- SPEC 동반 수정 grep 근거가 전부 있고 숫자는 §7 한 곳에만
