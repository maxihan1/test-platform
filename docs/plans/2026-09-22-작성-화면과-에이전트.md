# 테스트 작성 화면 ＋ 에이전트를 대기줄에 붙인다

등급: 3 · 갈래: WS-작성 · 2026-09-22

## 도메인 정리

**컨텍스트** — 작성(Authoring). 정본은 `docs/spec/도메인/작성.md` §3.6 · §7.

**이 PR 은 새 도메인 규칙을 만들지 않는다.** 이미 선 것을 잇는 일이다.

| 이미 선 것 | 어디 | 언제 |
|---|---|---|
| 대기줄 표 `authoring_request` | `db/migrations/` · 공통/4-데이터모델 §6 | PR #56 |
| 서버 통로 여덟 | `apps/admin/src/authoring/` · 도메인/작성 §7 | PR #56 |
| 경로→등급 표 | `apps/admin/src/auth/gate.ts` · 도메인/인증 §7 | PR #56 |
| 기획서 → 케이스 엔진 | `.claude/skills/tpx-cases/` | PR #44·#50·#59 |

**영향** — WS-E(화면)의 `apps/admin/src/web/**` 에 자리가 하나 는다. 자리 목록의 정본은
`공통/5-화면공통.md` 이고 **명세에는 이미 들어가 있다** — 코드가 따라가는 일이다.

## 왜

**주문서만 쌓이고 아무도 안 집어 간다.**

실측 — `apps/admin/src/web/` 에 작성 화면 파일 **0개**, `scripts/authoring-agent.ts` 에 서버 호출
**0건**. 통로 여덟은 서 있는데 **부르는 쪽이 양쪽 다 없다.**

그리고 지금 유일하게 도는 길(`npm run authoring-agent -- <기획서 경로>`)은 **admin 을 아예 안 불러
로그인을 지나지 않는다.** 대기줄을 만든 뒤에도 두면 **「로그인을 안 지나는 작성 경로」가 남는다** —
§3.5 가 러너 포트를 닫으며 막은 뒷길과 같은 성질이다. **이 PR 이 그것을 치운다.**

### 정해진 것 다섯 — 다시 묻지 않는다

1. **맥 비밀번호를 저장하지 않는다. 켤 때 물어본다** — 설계 문서가 「숨은 데몬으로 두지 않는다」로
   이미 정했다. 사람이 켜므로 한 번 치는 것이 공짜다. **맥에 남는 비밀값 0**
2. **사진은 안 만든다** — 실측으로 「지금 아무도 안 올린다」를 확인했다. 안 올리니 볼 것도 없다
3. **재실행 값 바꾸기 · 줄 상한 · 실패한 것 치우기 — 셋 다 안 한다.** 명세의 「아직 안 정했다」 줄을 그대로 둔다
4. **에이전트는 `AUTHOR` 와 `MERGE` 만 처리한다** — `RERUN` 은 바꿀 값을 안 정해 내용이 없다
5. **`테스트 작성` 은 `테스트 케이스` 다음 자리** · 영어 이름 **`Authoring`**

## Plan

### 할 일 1. 자리 목록에 `테스트 작성` 을 더한다

- **RED** — `layout.test.ts` 에 「자리 목록의 둘째가 `테스트 작성` 이다」를 단언한다. 지금
  `자리목록()` 이 케이스·실행 기록·그래프 셋만 내므로 빨갛다
- **GREEN** — `자리목록()` 의 기본 배열에 `{ 이름: t('테스트 작성', 언어), 해시: '#/authoring' }` 을
  `테스트 케이스` 다음에 넣고, `messages/shell.ts` 에 `'테스트 작성': 'Authoring'` 을 더한다
- **REFACTOR** — 같은 파일의 **「자리 넷」 주석과 검사 제목**을 숫자 없는 말로 고친다

**files**: `apps/admin/src/web/layout.ts` · `apps/admin/src/web/layout.test.ts` · `apps/admin/src/web/messages/shell.ts` · `apps/admin/src/web/messages.test.ts`
**depends-on**: []
**검증**: `npx vitest run apps/admin/src/web/layout.test.ts apps/admin/src/web/messages.test.ts`

### 할 일 2. 해시 주소 둘을 푼다

- **RED** — `route.test.ts` 에 `#/authoring` → `{ name: 'authoring' }`, `#/authoring/12` →
  `{ name: 'authoringItem', id: 12 }` 를 단언한다. 지금 둘 다 `unknown` 이라 빨갛다
- **GREEN** — `Route` 유니언에 둘을 더하고 `route()` 에 갈래를 판다. **번호는 숫자 글자만 받는다** —
  아니면 `unknown` 으로 떨어뜨린다 (서버 `:id` 규칙과 같은 모양)
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/route.ts` · `apps/admin/src/web/route.test.ts`
**depends-on**: []
**검증**: `npx vitest run apps/admin/src/web/route.test.ts`

### 할 일 3. 등급 표에 작성 두 줄을 더한다

- **RED** — `role.test.ts` 에 「`operator` 는 작성 요청을 할 수 있고 머지는 못 한다」와
  「`admin` 은 둘 다 된다」를 단언한다. `할일` 유니언에 그 이름이 없어 타입부터 빨갛다
- **GREEN** — `할일` 에 `'작성요청'`·`'작성머지'` 를 더하고 `최소등급` 에 `operator`·`admin` 을 적는다
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/role.ts` · `apps/admin/src/web/role.test.ts`
**depends-on**: []
**검증**: `npx vitest run apps/admin/src/web/role.test.ts`

> **이것은 화면이 자리를 안 그리기 위한 것이고 방어가 아니다.** 그 파일 머리 주석이 이미
> 그렇게 적어 뒀고, 서버 `auth/gate.ts` 가 같은 것을 다시 막는다 (PR #56 에서 섰다).

### 할 일 4. 통로 함수 넷을 붙인다

- **RED** — `api.test.ts` 에 「작성 요청을 넣을 때 `?service=` 가 주소에 실린다」와
  「머지는 `/authoring/merges` 로 간다」를 단언한다. 함수가 없어 빨갛다
- **GREEN** — `api.ts` 에 넷을 더한다 — 줄 목록 · 한 건 상세 · 요청 넣기 · 머지 세우기.
  **기존 `call`·`json` 을 그대로 쓴다.** 응답 모양은 `authoring/routes.ts` 가 내는 것에 맞춘다
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/api.ts` · `apps/admin/src/web/api.test.ts`
**depends-on**: []
**검증**: `npx vitest run apps/admin/src/web/api.test.ts`

### 할 일 5. 목록과 새 요청 화면

- **RED** — `Authoring.test.tsx`(첫 줄에 `// @vitest-environment jsdom`)에 「줄마다 상태·종류·
  작업 단계가 보인다」와 「기획서를 안 적고 보내기를 누르면 서버를 안 부른다」를 단언한다
- **GREEN** — `Authoring.tsx` — 위에 기획서를 붙여넣는 상자와 보내기, 아래에 줄 목록.
  **`RunList.tsx` 의 모양을 그대로 쓴다.** 비었을 때는 `empty` 문구
- **REFACTOR** — 300줄을 넘으면 줄 하나를 그리는 부품을 파일로 뗀다

**files**: `apps/admin/src/web/Authoring.tsx` · `apps/admin/src/web/Authoring.test.tsx` · `apps/admin/src/web/messages/authoring.ts` · `apps/admin/src/web/messages.ts`
**depends-on**: [4]
**검증**: `npx vitest run apps/admin/src/web/Authoring.test.tsx`

### 할 일 6. 상세 화면과 머지 버튼

- **RED** — `AuthoringDetail.test.tsx` 에 「`admin` 에게만 머지 버튼이 보인다」와
  「끝나지 않은 요청에는 머지 버튼이 안 보인다」를 단언한다
- **GREEN** — `AuthoringDetail.tsx` — 작업 단계 한 줄 · 판정(`result`) · 초안 PR 링크 ·
  머지 버튼. **도는 동안 주기적으로 다시 읽는다** — `RunResult.tsx` 의 그 패턴을 그대로 쓴다
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/AuthoringDetail.tsx` · `apps/admin/src/web/AuthoringDetail.test.tsx` · `apps/admin/src/web/messages/authoring.ts`
**depends-on**: [3, 4]
**검증**: `npx vitest run apps/admin/src/web/AuthoringDetail.test.tsx`

### 할 일 7. 두 화면을 껍데기에 배선한다

- **RED** — `jsxEnv.test.ts` 나 새 검사로 「`#/authoring` 주소에서 작성 화면이 나온다」를 단언한다.
  `main.tsx` 의 `switch` 에 갈래가 없어 「없는 주소입니다」가 나오므로 빨갛다
- **GREEN** — `main.tsx` 의 `switch` 에 둘을 더하고, `지금자리()` 가 상세에서도 `#/authoring` 에
  밑줄을 두게 한다 (실행 기록이 `run`·`item` 에서 그러는 것과 같은 이유)
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/main.tsx` · `apps/admin/src/web/layout.ts` · `apps/admin/src/web/layout.test.ts`
**depends-on**: [1, 2, 5, 6]
**검증**: `npx vitest run apps/admin/src/web/layout.test.ts && npm run typecheck`

### 할 일 8. 에이전트가 로그인한다 — 비밀번호를 안 저장하고

- **RED** — `authoring-agent.test.ts` 에 「`AUTHORING_AGENT_USER` 가 비면 켜자마자 멈추고 왜인지
  알린다」와 「비밀번호를 파일이나 환경변수에서 읽지 않는다」를 단언한다
- **GREEN** — 켤 때 한 번 묻는 순수 함수와 로그인·배정 읽기. **입력은 Node 기본 기능만 쓴다.**
  **메모리에만 들고 어디에도 안 적는다**
- **REFACTOR** — 없음

**files**: `scripts/authoring-agent.ts` · `scripts/authoring-agent.test.ts`
**depends-on**: []
**검증**: `npx vitest run scripts/authoring-agent.test.ts`

> **판단은 순수 함수에, I/O 는 얇은 껍데기에 둔다** (CLAUDE.md §3 — `scripts/run-scheduled.ts` 가 본보기다).

### 할 일 9. 줄에서 한 건 집어 일한다 (`AUTHOR`)

- **RED** — 「줄이 비면 쉬고 다시 묻는다」와 「집은 뒤 작업 단계를 올린다」를 단언한다
- **GREEN** — 배정 서비스마다 집기를 두드리는 루프. 집으면 **`spec_text` 본문**으로 엔진을 돌리고
  단계를 올리고 끝나면 알린다. **입력이 파일 경로가 아니라 본문**인 것이 지금과 다른 점이다
- **REFACTOR** — 없음

**files**: `scripts/authoring-agent.ts` · `scripts/authoring-agent.test.ts`
**depends-on**: [8]
**검증**: `npx vitest run scripts/authoring-agent.test.ts`

### 할 일 10. 머지 요청을 집어 실행한다 (`MERGE`)

- **RED** — 「`pr_url` 이 없으면 실패로 끝낸다」와 「강제 깃발을 절대 안 붙인다」를 단언한다
- **GREEN** — `kind` 로 갈라 `gh pr merge` 를 친다. **판단하지 않는다** — 사람이 화면에서 이미 정했다
- **REFACTOR** — 없음

**files**: `scripts/authoring-agent.ts` · `scripts/authoring-agent.test.ts`
**depends-on**: [9]
**검증**: `npx vitest run scripts/authoring-agent.test.ts`

> **`--force` · `-D` · 마이그레이션 되돌리기는 금지다** (CLAUDE.md §5 · `guard.mjs` 의 `isBanned()`).

### 할 일 11. 셸 진입점을 없앤다

- **RED** — 「기획서 경로를 인자로 받는 진입 방식이 `package.json` 에 없다」를 단언하는 줄을
  `authoring-agent.test.ts` 에 둔다. 지금 있으므로 빨갛다
- **GREEN** — `package.json` 의 그 자리를 **폴링으로 도는 새 진입점**으로 바꾼다.
  `docs/SETUP.md` §8 의 문단과 명령을 새 안내로 갈고, 스크립트 머리 주석의
  「다음 PR 이 그 일을 한다」를 **이 PR 이 했다**로 고친다
- **REFACTOR** — 없음

**files**: `package.json` · `scripts/authoring-agent.ts` · `scripts/authoring-agent.test.ts` · `docs/SETUP.md`
**depends-on**: [10]
**검증**: `npx vitest run scripts/authoring-agent.test.ts && npm run check:workflow`

### 할 일 12. 사람이 낱말로 훑는다 — 「자리 넷」과 목업

- **RED** — 없다. **사람이 본다**
- **GREEN** — `자리 넷` · `자리가 넷` 을 `apps/` 와 `docs/` 에서 훑어 **살아 있는 문장만** 고친다.
  **그날의 사실은 안 고친다** (진행 기록 · 학습 기록 · 검사 기록 · 지난 계획).
  `docs/design-mockup.html` 의 `<nav>` 는 **이 PR 이 코드에 자리를 만들었으므로** 다섯째를 그리고
  그 위 주석을 고친다. `docs/WORKFLOW.md` 의 「눌러볼 길」도 **사람이 따라 하는 절차**라 고친다
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/**`(주석·검사 제목) · `docs/design-mockup.html` · `docs/WORKFLOW.md`
**depends-on**: [7]
**검증**: `grep -rn "자리 넷\|자리가 넷" apps docs` 결과를 계획의 `## 훑은 결과` 절에 붙인다

### 할 일 13. 명세의 「아직 알 수 없는 것」에서 정해진 줄을 닫는다

- **RED** — 없다. `npm run check:spec` 이 모양만 본다
- **GREEN** — `도메인/작성` 의 그 표에서 **셸 진입점 줄을 닫는다**(`반영 완료 (2026-09-22, ...)`).
  **나머지 셋은 그대로 둔다** — 이번에 안 하기로 했다. `공통/5-화면공통` 의
  「화면 제목과 같은 말을 두 번 하는지 아직 알 수 없다」는 **화면을 만들었으므로 답을 적는다**.
  `docs/WORKSTREAMS.md` 와 `docs/WORKFLOW.md` 의 단위 상태도 같이 움직인다
- **REFACTOR** — 없음

**files**: `docs/spec/도메인/작성.md` · `docs/spec/공통/5-화면공통.md` · `docs/WORKSTREAMS.md` · `docs/WORKFLOW.md`
**depends-on**: [12]
**검증**: `npm run check:spec`

### 할 일 14. 색인 분량을 맞춘다 — **맨 마지막**

- **RED** — 없다. `npm run check:spec` 이 실제 줄 수와 대조한다
- **GREEN** — `docs/SPEC.md` 의 분량 칸을 고친다. ★ **손으로 세지 않는다** —
  `check:spec` 이 찍는 「실제 N줄」을 복사한다. 라우터 표도 같이 본다
- **REFACTOR** — 없음

**files**: `docs/SPEC.md`
**depends-on**: [13]
**검증**: `npm run check:spec`

> **앞 PR 들에서 이 숫자가 네 번 틀어졌다.** 앞 할 일이 장 파일을 고치면 값이 움직이므로 마지막이다.

## SPEC 동반 수정 (§2.7)

**할 일 12·13·14 가 그것이다.** 절별로 적는다.

| §2.7 | 어디서 하나 |
|---|---|
| ① 같은 규칙이 적힌 다른 절 찾기 | 할 일 12 — 낱말로 훑는다. 결과를 `## 훑은 결과` 에 붙인다 |
| ② 뒤집은 결정 | 할 일 13 — 셸 진입점 줄은 **닫고**, 안 하기로 한 셋은 **남긴다** |
| ③ 색인 네 곳 | 할 일 14 — 장 목록 · 절 번호 · 표 주인 · **라우터 표** |
| ④ SPEC 밖 | 할 일 11(SETUP) · 12(목업·WORKFLOW) · 13(WORKSTREAMS) |
| ⑤ 숫자 빼기 | 할 일 1(코드 주석의 「자리 넷」) · 12(문서의 같은 말) |
| ⑥ 검사 | `npm run check:spec` · `spec-review` H 절 — [6] 이 돌린다 |

## 안 건드리는 것

- **역방향(모드 B)** — 별건. 사용자가 「PR58 먼저」로 정했다
- **`packages/kit/**`** — 한 줄도. `web/api.ts` 가 이미 쓰는 타입만 쓴다
- **`db/migrations/`** — 표가 이미 섰다
- **`apps/admin/src/authoring/**` 서버 통로** — 여덟이 이미 선다. 화면과 맥이 부를 뿐이다
- **사진 통로** · **`RERUN`** · **줄에 세울 수 있는 건수 상한** · **실패한 요청 치우기**

## 훑은 결과

(할 일 12 가 채운다)

## Plan 메타

할 일 14개 · 예상 묶음 6개 · 구현 규율: TDD · 추가 검증: `npm run check:spec` · `npm run check:workflow` ·
`DATABASE_URL` 붙여 `npm test` 연속 3회

**★ 할 일이 10개를 넘는다. 그런데 쪼갤 수 없다.**

| 쪼개는 안 | 왜 안 되나 |
|---|---|
| 화면만 먼저 | 주문이 줄에 서는데 **아무도 안 집어 간다.** 앞 PR(#56)이 이미 그 상태였다 |
| 에이전트만 먼저 | **주문을 넣을 길이 없다.** 셸 진입점을 없애므로 더 그렇다 |
| 셸 진입점 제거를 미룸 | **로그인을 안 지나는 경로가 그대로 산다.** 명세가 이 PR 에서 없애라고 못박았다 |

**셋이 한 덩이여야 기능이 한 번이라도 돈다.** 다만 **묶음 여섯으로 나뉘어** 직렬 구간이 짧다 —
1·2·3·4·8 이 서로 독립이라 첫 묶음이 넓다.

## 리뷰 결과

(계획 검토가 채운다)
