# 케이스를 여러 건 골라 한 번에 실행하는 자리

등급: 3 · 갈래: 없음(명세 + 화면) · 2026-09-21

## 도메인 정리

건드리는 BC 는 **카탈로그**(§8.1 케이스 목록)와 **실행**(§8.2 실행 설정) 둘이다.

| 영향 | 무엇 |
|------|------|
| 계약 | **없음.** 실행 만들기 API 의 `items` 가 이미 배열이고 상한이 없다 |
| DB | 없음 |
| 러너 | 없음 |
| 화면 | 케이스 목록에 고르는 칸과 실행 버튼, 고른 것을 확인하는 모달 |
| 명세 | 카탈로그 §8.1 줄 모양 표 · 실행 §8.2 · DESIGN 모달 규칙 |

## 왜

**여러 건 실행은 이미 세 곳에 있고 화면에만 없다.**

| 어디 | 무엇이 이미 되나 |
|------|----------------|
| 실행 만들기 API | `items` 가 `z.array(...).min(1)` 이고 상한 없음 |
| 정기 실행 (`scripts/run-scheduled.ts`) | 그 서비스 활성 케이스 **전부**를 `params: {}` 로 한 요청에 건다 |
| 화면 계산기 (`web/runPlan.ts`) | `항목수(케이스수, 디바이스수, 반복)` 이 **케이스 수를 인자로 받는다** |

그런데 `RunSetup.tsx:77` 이 `항목수(1, platforms.length, ...)` 로 **`1` 을 박아 놨다.**
케이스 목록에는 고르는 칸이 아예 없다.

**명세가 자기 안에서 어긋나 있다.** 실행 §8.2 는 항목 수를 「케이스 수 × 디바이스 수 × 횟수」로 세고
1000건 상한의 근거를 「케이스 10 × 디바이스 2 × 반복」으로 적었는데, 카탈로그 §8.1 의 줄 모양
`TC ID | 케이스명 | 지원 디바이스 | 마지막 결과 | [실행]` 에 **고르는 칸이 없어 그 「케이스 수」는 영원히 1이다.**

실측 확인 — API 로 11케이스(실행 단위 13개)를 한 요청으로 돌렸다. RUN 8802, 통과 10 · 실패 2 · 미실행 1.
**서버는 아무 문제 없이 받았다.**

## 화면 설계 — 이 계획이 정한다

게이트 0 의 답 넷을 그대로 따른다. 「구현자가 정한다」로 넘기지 않는다.

### ① 고르는 칸과 버튼

케이스 목록 각 줄의 **거터 바로 오른쪽, `tcId` 왼쪽**에 고르는 칸을 둔다.
620px 미만에서 줄이 2단(거터 + 내용)으로 접히므로 **내용 쪽 첫 요소**로 둔다 (DESIGN.md 반응형).

목록 위 `다시 스캔하기` 옆에 버튼 하나를 둔다. **글자가 상태에 따라 바뀐다.**

| 고른 것 | 버튼 글자 | 무엇을 담나 |
|---------|----------|------------|
| 없음 | `전체 실행하기` | 지금 걸러진 목록의 **활성 케이스 전부** |
| 1건 이상 | `고른 N건 실행하기` | 고른 것만 |

**버튼을 둘로 나누지 않는다.** 같은 자리에서 같은 일을 하고 담는 대상만 다르다.
두 개를 나란히 두면 「전체 실행」과 「고른 것 실행」이 동시에 눌릴 수 있는 것처럼 보인다.

버튼은 동사로 끝나고 화살표를 붙이지 않는다 (DESIGN.md).

### ② 고른 것을 확인하는 모달

버튼을 누르면 모달이 뜬다. **모달이 실행 설정을 겸한다** — 대상 서버를 안 고르면 실행이 시작되지 않아야
하므로(§8.2) 그 칸이 반드시 여기 있어야 한다.

```
┌─ 실행할 케이스 N건 ──────────── 최대 480px ─┐
│ 대상 서버  [드롭다운]  주소를 흐리게 옆에     │  ← 머리 (고정)
├──────────────────────────────────────────┤
│ DEMO-001  메인 화면이 열린다        PC       │  ← 본문 (여기만 스크롤)
│ DEMO-002  할 일을 하나 …    PC   [값 고치기] │
│   ↳ 펼치면 그 케이스의 입력칸이 나온다        │
│ …                                          │
├──────────────────────────────────────────┤
│ 반복 [1]   □ 끝나면 Slack 알리기             │  ← 바닥 (고정)
│ 실행 항목이 N건 생깁니다                      │
│                        [취소]  [실행하기]    │
└──────────────────────────────────────────┘
```

- **기본은 전부 접혀 있다.** 접힌 케이스는 **코드에 적힌 기본값**으로 돈다. K10 이 「사람이 값을
  채워야만 도는 케이스」를 막고 있어 **모든 케이스가 값 없이 돈다** — 정기 실행이 이미 그렇게 돈다
- **`[값 고치기]` 는 입력칸이 있는 케이스에만 붙는다.** 없는 케이스는 그 자리가 비어 있다
- 고친 값은 **그 실행에만** 쓰인다. 케이스 코드의 기본값은 안 바뀐다 — **이건 이미 그렇게 돼 있다.**
  실행할 때 준 값은 `run_item` 에 박히고 케이스 파일은 아무도 안 건드린다
- **디바이스는 케이스마다 자기가 선언한 것을 전부 쓴다.** 모달에서 따로 안 고른다 — 정기 실행과 같다.
  케이스마다 선언이 달라 한 자리에서 고르게 하면 「이 케이스엔 없는 디바이스」를 고를 수 있게 된다
- **본문만 스크롤한다.** 머리와 바닥은 고정이다. 11건이든 100건이든 대상 서버와 실행 버튼이 늘 보인다
- 480px 을 지킨다. 모달 규칙의 나머지(닫는 길 셋 · 포커스 가두기 · 애니메이션 없음 · 뒤 화면 잉크 40%)도 그대로다

### ③ 상한을 넘겼을 때

`실행 항목이 N건 생깁니다` 를 바닥에 적고, 1000건을 넘으면 `실행하기` 를 못 누르게 하고
**그 이유를 화면 줄로 적는다** (DESIGN.md `:205` — 말풍선에 적지 않는다).

### ④ 모달 규칙을 세 자리로 늘린다

`DESIGN.md` 가 「모달은 두 자리에만 쓴다」고 못 박고 있다. 게이트 0 이 **늘리는 쪽으로 승인**했다.
**왜 늘렸는지를 규칙 옆에 같이 적는다** (CLAUDE.md §2.7 ②) — 여러 건을 거는 것은 시간과 기록을
쓰는 일이라 누르기 전에 무엇이 도는지 확인할 자리가 필요하다.

### ⑤ 300줄 규칙 — 이번에 쪼갠다

`CaseList.tsx` 가 이미 **308줄**이고 여기에 고르는 상태와 버튼이 더해진다. **이번에 쪼갠다.**
직전 PR 이 이 판단을 게이트 2 까지 들고 갔다가 그 자리에서 쪼갰다 — 같은 일을 반복하지 않는다.

- **`pickRun.ts` (새 파일)** — 고른 것에서 실행 항목을 만드는 **순수 로직**. 화면이 없어 jsdom 없이 검사한다
- **`RunPickModal.tsx` (새 파일)** — 모달
- `CaseList.tsx` 는 목록과 고르는 상태만 남는다

**`RunSetup.tsx` 는 안 건드린다.** 한 건짜리 화면은 그대로 두고 여러 건은 모달이 맡는다.
`항목수(1, ...)` 도 한 건 화면에서는 맞는 값이다.

## Plan

### 할 일 1. 고른 것을 실행 항목으로 바꾸는 순수 로직

- **RED** — `pickRun.test.ts` 를 쓴다. ① 고른 것이 없으면 걸러진 목록 **전부**를 담는다 ② 고른 것이 있으면 그것만 담는다 ③ 비활성 케이스는 안 담는다 ④ 케이스마다 자기가 선언한 디바이스를 그대로 쓴다 ⑤ 값을 안 고친 케이스는 `params: {}` 다 ⑥ 고친 값이 있으면 그 케이스에만 실린다 ⑦ 항목 수가 `케이스 수 × 디바이스 수 × 반복` 이다 ⑧ 1000건을 넘으면 넘었다고 판정한다. **`pickRun.ts` 가 없어 import 가 깨진다**
- **GREEN** — `pickRun.ts` 를 만든다. `담을것(목록, 고른것, 고친값)` 과 `실행항목(담을것)` 과 `몇건(담을것, 반복)`. 상한 판정은 `runPlan.ts` 의 `넘었나` 를 **그대로 쓴다** — 1000 을 또 적지 않는다
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/pickRun.ts` · `apps/admin/src/web/pickRun.test.ts`
**depends-on**: []
**검증**: `npx vitest run apps/admin/src/web/pickRun.test.ts`

### 할 일 2. 케이스 목록에 고르는 칸과 버튼

- **RED** — `CaseList.test.tsx` 를 쓴다(첫 줄 `// @vitest-environment jsdom`). ① 줄마다 고르는 칸이 있다 ② 아무것도 안 고르면 버튼 글자가 `전체 실행하기` 다 ③ 하나 고르면 `고른 1건 실행하기` 로 바뀐다 ④ 두 개 고르면 숫자가 2가 된다 ⑤ 조건을 바꿔 목록이 줄어도 안 보이는 줄의 고름이 유지된다. **고르는 칸이 없어 전부 깨진다**
- **GREEN** — `CaseList.tsx` 에 고른 `tcId` 집합 상태와 칸, 버튼 하나를 더한다. 버튼은 아직 모달을 안 연다
- **REFACTOR** — 목록 줄 그리기를 파일 안 함수로 뺀다. **300줄을 넘기지 않는다**

**files**: `apps/admin/src/web/CaseList.tsx` · `apps/admin/src/web/CaseList.test.tsx` · `apps/admin/src/web/styles.css`
**depends-on**: [1]
**검증**: `npx vitest run apps/admin/src/web/CaseList.test.tsx`

### 할 일 3. 고른 것을 확인하는 모달

- **RED** — `RunPickModal.test.tsx` 를 쓴다(첫 줄 `// @vitest-environment jsdom`). ① 고른 케이스가 줄로 다 보인다 ② 입력칸이 있는 케이스에만 `값 고치기` 가 붙는다 ③ 펼치면 그 케이스의 입력칸이 보인다 ④ 대상 서버를 안 고르고 `실행하기` 를 누르면 **실행이 안 걸리고 사유가 화면 줄로 뜬다** ⑤ `실행 항목이 N건 생깁니다` 가 맞는 숫자다 ⑥ 1000건을 넘으면 `실행하기` 가 막히고 이유가 **말풍선이 아니라 화면 줄**로 뜬다. **`RunPickModal.tsx` 가 없어 깨진다**
- **GREEN** — `RunPickModal.tsx` 를 만든다. 있는 `Modal.tsx` 를 쓴다 — 새로 만들지 않는다. 입력칸 그리기는 있는 `Form.tsx` 를 쓴다
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/RunPickModal.tsx` · `apps/admin/src/web/RunPickModal.test.tsx` · `apps/admin/src/web/styles.css`
**depends-on**: [1]
**검증**: `npx vitest run apps/admin/src/web/RunPickModal.test.tsx`

### 할 일 4. 목록과 모달을 잇고 실제로 실행을 건다

- **RED** — `CaseList.test.tsx` 에 더한다. ① 버튼을 누르면 모달이 뜬다 ② 모달에서 `실행하기` 를 누르면 `createRun` 이 **고른 건수만큼의 items** 로 불린다 ③ 성공하면 그 실행 결과 화면으로 간다. **아직 안 이어져 있어 깨진다**
- **GREEN** — `CaseList.tsx` 가 모달을 열고 `api.createRun` 을 부른다
- **REFACTOR** — `CaseList.tsx` 가 300줄을 넘으면 목록 줄을 `CaseRow.tsx` 로 뺀다. **줄 수를 세어 판단한다**

**files**: `apps/admin/src/web/CaseList.tsx` · `apps/admin/src/web/CaseList.test.tsx`
**depends-on**: [2, 3]
**검증**: `npx vitest run apps/admin/src/web/CaseList.test.tsx apps/admin/src/web/RunPickModal.test.tsx`

### 할 일 5. 명세를 고친다 — 카탈로그 §8.1 과 실행 §8.2

- **RED** — 없다(문서). 대신 **고치기 전에** `grep -rn "체크박스\|케이스 수\|선택한 케이스" docs/spec/` 결과를 이 계획에 붙여 **같은 규칙이 적힌 다른 절이 없는지 확인한 근거**를 남긴다
- **GREEN** —
  - 카탈로그 §8.1 줄 모양 표에 **고르는 칸**을 더하고, 목록 위 버튼 문단에 **전체/고른 것 실행 버튼**을 적는다. 글자가 상태에 따라 바뀌는 규칙도 여기 적는다
  - 실행 §8.2 `:111` 「**선택한 케이스**의 paramSchema」 단수 표현을 여러 건까지 덮게 고친다
  - 실행 §8.2 에 **여러 건 실행 모달** 절을 새로 더한다(번호는 뒤에 붙인다). 접힘 기본 · 값은 그 실행에만 · 디바이스는 케이스 선언대로 · 머리와 바닥 고정
  - 「케이스 수」 두 줄은 **그대로 둔다** — 이제 사실이 된다
- **REFACTOR** — 없음

**files**: `docs/spec/도메인/카탈로그.md` · `docs/spec/도메인/실행.md`
**depends-on**: []
**검증**: `grep -n "고르는 칸\|실행하기" docs/spec/도메인/카탈로그.md docs/spec/도메인/실행.md`

### 할 일 6. 화면 기준과 목업

- **RED** — 없다(문서)
- **GREEN** —
  - `DESIGN.md` §모달 「두 자리」를 **「세 자리」**로 고치고 **왜 늘렸는지**를 같이 적는다 (§2.7 ②). 나머지 규칙은 그대로다
  - `design-mockup.html` 화면 1 에 고르는 칸과 버튼을 그리고, **모달 화면을 하나 더한다**
- **REFACTOR** — 없음

**files**: `docs/DESIGN.md` · `docs/design-mockup.html`
**depends-on**: []
**검증**: `grep -n "세 자리" docs/DESIGN.md` · 목업을 브라우저로 연다

### 할 일 7. 색인 네 곳과 완료 기준

- **RED** — `npm run check:spec` 이 **빨간 것을 먼저 본다.** 할 일 5·6 이 장 분량을 바꿨으므로 색인의 줄 수가 어긋나 있다
- **GREEN** —
  - `docs/SPEC.md` **라우터 표에 줄을 더한다** — 「케이스를 여러 건 골라 실행하는 자리를 고친다 → 카탈로그 §8.1 이 고르는 칸의 정본, 모달은 실행 §8.2」. **가장 잘 빠뜨리는 자리다**
  - 장 목록 표의 **줄 수**를 맞춘다. **손으로 세지 않는다** — `check:spec` 이 내는 숫자를 쓴다
  - `공통/7-데모와-완료.md` 완료 기준에 상자를 더한다. **개수는 적지 않는다** (§2.7 ⑤)
- **REFACTOR** — 없음

**files**: `docs/SPEC.md` · `docs/spec/공통/7-데모와-완료.md`
**depends-on**: [5, 6]
**검증**: `npm run check:spec`

### 할 일 8. 기록

- **RED** — 없다(문서)
- **GREEN** — `docs/reviews/2026-09-21-여러건실행.md`(G8 부순 자리 포함) · `docs/progress/WS-E.md` **추가** · `docs/WORKSTREAMS.md` · `docs/WORKFLOW.md` 현재 위치
- **REFACTOR** — 없음

**files**: `docs/reviews/2026-09-21-여러건실행.md` · `docs/progress/WS-E.md` · `docs/WORKSTREAMS.md` · `docs/WORKFLOW.md`
**depends-on**: [4, 7]
**검증**: `npm run check:workflow`

## SPEC 동반 수정 (§2.7)

| # | 어디 | 어느 할 일 |
|---|------|-----------|
| ① 같은 규칙 찾기 | `체크박스` 는 실행 §8.2 세 줄뿐(디바이스·Slack). **고르는 칸과 겹치지 않는다.** `케이스 수` 는 §8.2 두 줄뿐 | 할 일 5 가 근거를 붙인다 |
| ② 뒤집은 결정 | **DESIGN.md 모달 「두 자리」** — 근거가 아직 쓸모 있으므로 **남기고 왜 늘렸는지 덧붙인다** | 할 일 6 |
| ③ 색인 네 곳 | 장 목록 · 절 번호 표 · 표 주인 · **라우터 표** | 할 일 7 |
| ④ SPEC 밖 | WORKSTREAMS · DESIGN · design-mockup · WORKFLOW · **코드 상수** | 할 일 6 · 8 |
| ⑤ 숫자 빼기 | 완료 기준 개수를 안 적는다. 장 분량은 `check:spec` 이 낸다 | 할 일 7 |
| ⑥ 검사 | `check:spec` → `spec-review` H → [6] 다른 세션 | 할 일 7 · [6] |

**코드에 박힌 상수** — 1000 이 `runPlan.ts:11` 과 `execution/store.ts:35` 둘에 있다.
**이번에 안 건드린다.** 할 일 1 이 `runPlan.ts` 의 `넘었나` 를 그대로 써서 사본을 셋으로 늘리지 않는 것이 이 항목의 몫이다.

## Plan 메타

할 일 8개 · 예상 묶음 4개 · 구현 규율: TDD
묶음 ①{1, 5, 6} ②{2, 3} ③{4, 7} ④{8}

추가 검증 — `npm run check:deps` · `typecheck` · `check:workflow` · `check:spec` · `check:tests` · `check:secret-names` · `check:evidence-formats` · `npx vite build` · `DATABASE_URL=… npm test` **연속 3회**

**G8** — 부술 자리 셋. ① `pickRun` 에서 「고른 것 없으면 전부」를 「고른 것만」으로 바꿔 검사가 빨개지는지 ② 버튼 글자 바뀜을 고정 글자로 바꿔 빨개지는지 ③ 대상 서버 검사를 빼고 실행이 걸리는지.

## 리뷰 결과

(계획 검토가 채운다)

**렌즈**: eng · design · ceo 세 관점 (3등급) · 2026-09-21
**판정**: **BLOCKER 3건 · 주의 5건**

> **부실하게 돈 자리를 밝힌다** — 세 렌즈를 **독립 세션 셋으로 나누지 않고 한 번에** 돌았다.
> 독립성이 그만큼 약하다. 게이트 1 요약에 그대로 싣는다.

### BLOCKER 1 — §8.2 의 항목 수 식이 여러 건에서 틀리다. 계획이 「그대로 둔다」고 못 박았다

「케이스 수 × 디바이스 수 × 횟수」는 **모든 케이스의 디바이스 수가 같을 때만** 맞다.
실측 — 케이스 11건을 돌렸더니 실행 단위가 **13개**였다(`DEMO-008`·`DEMO-009` 는 둘, `DEMO-010` 은 하나).
올바른 식은 **Σ(케이스별 디바이스 수) × 횟수** 다.

**왜 문제인가** — 화면이 「실행 항목이 N건 생깁니다」를 **틀린 수로** 적는다.
1000건 상한도 틀린 수로 막아 넘지 않았는데 막거나 넘었는데 통과시킨다.
계획은 할 일 5 에서 「케이스 수 두 줄은 **그대로 둔다 — 이제 사실이 된다**」고 적었는데 **사실이 되지 않는다.**

**고칠 것** — §8.2 의 식을 고치고, `runPlan.ts` 의 `항목수()` 서명도 같이 바꾼다(지금은 `디바이스수` 하나를 받는다).

### BLOCKER 2 — 「전체 실행하기」가 보이는 한 쪽만 돌린다

케이스 목록은 서버에서 **한 쪽(50건)만** 받는다 (`catalog/routes.ts` `PAGE_SIZE = 50`).
계획은 「고른 것 없으면 걸러진 목록 **전부**」라고만 적고 그 「전부」가 무엇인지 안 정했다.
손에 있는 것을 쓰면 **51건째부터 조용히 빠진다.** 버튼 글자는 「전체」라고 말한다.

**왜 문제인가** — **지금 데모가 11건이라 어떤 검사에서도 안 드러난다.**
실제 서비스 케이스가 51건이 되는 날 처음 터지고, 그때 사람은 전부 돌린 줄 안다.

**고칠 것** — 정기 실행이 하는 대로 `pageSize: MAX_ITEMS` 로 다시 받아 오거나,
버튼 글자를 「이 쪽 전부」로 낮춘다. **둘 중 하나를 계획이 고르고 그 경계를 검사로 박는다.**

### BLOCKER 3 — 「마지막 결과」 조건이 걸리면 전체 실행의 뜻이 깨진다

검색 조건 다섯 중 **「마지막 결과」만 화면에서 거른다** (§8.1 표 「거르는 곳: 화면」).
그래서 「실패한 것만 전체 실행」은 **한 쪽 안에서만** 참이다.
명세도 이미 이 한계를 안다 — 「이 쪽에는 …인 케이스가 없습니다 / **다음 쪽에 있을 수 있습니다**」.

**왜 문제인가** — 「실패한 것 전부 다시 돌리기」가 이 기능의 가장 쓸모 있는 쓰임새인데 **그게 정확히 안 되는 조합**이다.

**고칠 것** — 그 조건이 걸렸을 때 전체 실행이 무엇을 담는지 명세와 **화면 줄**에 적는다.

### 주의 1 — 묶음 ②는 실제로 직렬이다

할 일 2와 3이 `styles.css` 를 같이 건드린다. `tpx-plan` §2 가 「`files` 가 겹치는 두 할 일은
`depends-on` 이 없어도 **자동으로 직렬화된다**」고 정했다. 묶음 수는 맞지만 그 안이 순차다.
계획이 이 사실을 안 적어 [5] 가 병렬로 던졌다가 충돌한다.

### 주의 2 — `몇건()` 이 `항목수()` 의 사본이 된다

상한 판정은 재사용하기로 해 놓고 계산은 새로 만든다. BLOCKER 1 을 고치면서 `항목수()` 자체를
여러 건용으로 바꾸는 편이 사본을 안 만든다.

### 주의 3 — 목업을 할 일 3보다 먼저 그려야 한다

모달 480px 안에 대상 서버 + 케이스 N줄 + 반복 + Slack + 항목 수 + 버튼 둘이 들어간다.
**계획이 실측 없이 「들어간다」고 단정했다.** 목업(할 일 6)이 묶음 ① 이라 순서는 맞지만
**계획이 이 의존을 안 적었다.** 안 들어가는 것이 할 일 3 뒤에 드러나면 구현을 다시 한다.

### 주의 4 — 620px 에서 고르는 칸이 혼자 뜬다

목록 행이 2단(거터 + 내용)으로 접히는데 고르는 칸을 「내용 쪽 첫 요소」로 두면
좁은 화면에서 **케이스명 위에 체크박스만 홀로 한 줄**이 된다. 계획이 이 모양을 안 그렸다.

### 주의 5 — 안 보이는 고름

조건 변경·쪽 넘김 너머로 고름을 유지하면 **화면에 없는 케이스를 실행하게 된다.**
모달이 전부 나열하므로 마지막에 볼 수는 있지만, 11건을 고른 줄 알았는데 모달에 23건이 뜨는 일이 생긴다.

### 제품 관점 — 이 버튼의 값은 「지금 당장」뿐이다

**정기 실행이 이미 같은 일을 한다.** 사람이 정기 실행을 켜 뒀다면 이 버튼이 주는 것은 「기다리지 않고 지금」이다.
완료 기준 상자 문구를 그 값에 맞춰 적어야 한다 — 「전부 돌릴 수 있다」가 아니라
「**기다리지 않고 지금** 골라 돌릴 수 있다」다.

### 통과한 것

- **계약 무변경 판정** — `items` 가 이미 `z.array(...).min(1)` 이고 상한이 없다. 확인했다
- **`Form.tsx`·`schema.ts` 재사용** — 한 건 화면과 모달이 입력 서식 그리기를 **중복하지 않는다.**
  `RunSetup.tsx`(364줄)를 안 건드리는 경계가 맞다
- **상한 1000 사본을 셋으로 안 늘리는 판단**
- **「전부 접힘 + 기본값」의 근거** — K10 이 강제하고 정기 실행이 같은 길을 이미 돈다. 충분하다
- **디바이스를 모달에서 못 고르게 한 판단** — 케이스마다 선언이 달라 한 자리에서 고르면
  「그 케이스엔 없는 디바이스」를 고를 수 있게 된다
- **300줄 처리를 이번에 하는 것** — 게이트 2 까지 들고 가지 않는다
