# 작성 경로 스킬 정리 — 맥 표현 · 딥링크 규칙 · 첫 케이스 폴더 · 스킬 200줄 분리

등급: 3 · 갈래: 작성 · 하네스 · 인증 · 2026-09-25

## 도메인 정리

| 무엇 | 어디 | 영향 |
|---|---|---|
| `GET /api/auth/me` 의 `services[]` 에 `testsDir` 추가 (**계약 변경 — 게이트 0 승인, 2026-09-25**) | `apps/admin/src/auth/store.ts` · 명세 `도메인/인증.md` §7 | 화면 타입 · 작성 에이전트 |
| 작성 에이전트가 테스트 폴더를 서비스 설정에서 받는다 | `scripts/authoring-*.ts` | 새 서비스 첫 요청이 실패하지 않는다 |
| 새 서비스 폴더도 가벼운 길 · CI 면제 | `.claude/scripts/cases-only.mjs` · `ci-covers-tests.test.mjs` · `docs/HOOKS.md` | 검사 그물 하나가 느슨해진다 (아래 「게이트 1 에서 볼 것」) |
| 스킬 문서 | `.claude/skills/{tpx,tpx-cases,tpx-author,tpx-merge,spec-review}` · `CLAUDE.md` §0 | 규칙 내용은 안 바뀐다. 자리만 옮긴다 (② 제외) |

## 왜

2026-09-25 대화 네 가지.

1. 사용자가 「서버에서 만들기로 했는데 맥이라고?」로 헷갈렸다. 기본 경로는 서버 `author` 컨테이너(작성 §3.6 「★ 서버 컨테이너가 기본이다」, 2026-09-23)인데 `tpx-author` 설명·CLAUDE.md §0·§3.6 제목이 「맥」이다
2. 메뉴로 닿지 않는 화면(딥링크·메일 링크)에 가는 규칙이 `tpx-cases` §4 에 없다
3. 케이스가 0건인 서비스는 `scripts/authoring-run.ts:176` 에서 「첫 케이스는 사람이 /tpx 로」로 실패한다. 폴더를 기존 케이스로 추정하는 이유는 「`testsDir` 가 서버 응답에 없고 더하면 계약 변경」(`authoring-chain.ts:257`) 이었다 — 이번에 승인됐다.
   남은 막힘 둘(실측): 새 폴더는 `cases-only.mjs` 가 무거운 길로 보내 에이전트 push 가 막히고, `ci-covers-tests` 가 「tests/ 아래 폴더는 CI 에서 돌거나 면제」를 요구한다. 서비스 폴더는 CI 가 못 돈다 — `PLATFORM_BASE_URL` 이 없고 대상이 사내다(계획서 2026-09-23 BLOCKER 4)
4. 자식 Claude 가 매번 `tpx-cases` 644줄을 읽는다. 사용자 지시: **스킬 200줄 이하, 모듈로 분리.** 200 초과는 `tpx`(203) · `tpx-merge`(254) · `spec-review`(339) · `tpx-cases`(644). `playwright-cli` 는 벤더 원본이라 제외(CLAUDE.md 「그 폴더를 손으로 고치지 않는다」)

## Plan

### 할 일 1. `/auth/me` 가 서비스마다 `testsDir` 를 낸다

- **RED** — `apps/admin/src/auth/routes.test.ts` 에 「`/auth/me` 의 `services[0].testsDir` 가 서비스 설정값과 같다」. 지금 칸이 없어 실패
- **GREEN** — `auth/store.ts` 의 `json_build_object` 에 `'testsDir', s.tests_dir` · `배정서비스` 타입 · `web/api.ts` 타입
- **REFACTOR** — 없음

**files**: apps/admin/src/auth/store.ts, apps/admin/src/auth/routes.test.ts, apps/admin/src/web/api.ts
**depends-on**: []
**검증**: `npx vitest run apps/admin/src/auth`

### 할 일 2. 작성 에이전트가 폴더를 `testsDir` 로 정한다

- **RED** — `authoring-token.test.ts`: `나풀기` 가 `폴더표[prefix] = testsDir` 를 낸다 · 빈 `testsDir` 면 그 서비스는 「테스트 폴더 설정이 비었다」 사유를 낸다
- **GREEN** — `나풀기` 확장 → `authoring-agent.ts` 가 넘기고 → `authoring-run.ts` 가 `tests/<testsDir>` 를 쓴다. 폴더가 없어도 실패하지 않는다
- **REFACTOR** — 안 쓰이게 된 `케이스폴더`(`authoring-chain.ts`)와 그 검사 제거

**files**: scripts/authoring-token.ts, scripts/authoring-token.test.ts, scripts/authoring-agent.ts, scripts/authoring-run.ts, scripts/authoring-chain.ts, scripts/authoring-chain.test.ts
**depends-on**: [1]
**검증**: `npx vitest run scripts/authoring-`

### 할 일 3. 가벼운 길이 새 서비스 폴더를 받는다

- **RED** — `cases-only.test.mjs`: `tests/<새폴더>/X-001.spec.ts` 만 바뀐 목록이 가벼운 길(true). 지금 false
- **GREEN** — `테스트만인가` 에서 「기존 폴더」 조건을 뺀다. `..`·spec 아닌 파일·빈 목록은 그대로 무겁다
- **REFACTOR** — 두 번째 인자(기존폴더)와 `git ls-tree` 호출 제거

**files**: .claude/scripts/cases-only.mjs, .claude/scripts/cases-only.test.mjs
**depends-on**: []
**검증**: `node --test .claude/scripts/cases-only.test.mjs`

### 할 일 4. CI 그물 — 플랫폼 표본 폴더만 필수, 서비스 폴더는 규칙으로 면제

- **RED** — `ci-covers-tests.test.mjs`: 「`tests/` 아래 폴더가 전부 돌거나 면제」를 「**플랫폼 표본 폴더(`todo`)는 반드시 돈다 · `demo` 는 사유 면제 · 그 밖은 서비스 폴더라 면제**」로 바꾼 단언. 임시 폴더를 만든 상태에서 통과해야 한다
- **GREEN** — `표본` 목록(`todo`)을 두고 그것이 `도는폴더` 에 있는지만 본다
- **REFACTOR** — 면제 사유 문장에 「서비스 폴더의 병합 근거는 에이전트 관문 3 기록(HOOKS 「가벼운 길」)」을 적는다

**files**: .claude/scripts/ci-covers-tests.test.mjs
**depends-on**: []
**검증**: `node --test .claude/scripts/ci-covers-tests.test.mjs`

### 할 일 5. 스킬 분리의 안전망 먼저 — 200줄 상한 검사 · 본문 읽기를 폴더 전체로

- **RED** — `chain-contract.test.mjs` 에 「`.claude/skills/*/` 의 모든 `.md` 가 200줄 이하(`playwright-cli` 제외)」. 지금 네 개가 빨강. `read(s)` 를 `SKILL.md` + `references/*.md` 이어 붙이기로 바꾼다 — 절을 옮겨도 기존 단언이 안 깨지게
- **GREEN** — 할 일 7~8 이 끝나면 초록 (이 할 일만으로는 빨강이 정상)
- **REFACTOR** — `cases-probe-contract.test.mjs` 가 §4 를 옮긴 파일에서 읽게 경로를 바꾼다

**files**: .claude/scripts/chain-contract.test.mjs, .claude/scripts/cases-probe-contract.test.mjs
**depends-on**: []
**검증**: `node --test .claude/scripts/chain-contract.test.mjs .claude/scripts/cases-probe-contract.test.mjs`

### 할 일 6. `tpx-cases` §4 에 「메뉴로 닿지 않는 화면」 규칙

- **RED** — `cases-probe-contract.test.mjs`: §4 안에 「메뉴로 닿지 않는 화면」 하위 절이 있고 「자료에 적힌 경로」·「지어내지 않는다」를 담는다
- **GREEN** — 규칙: ⒜ 자료(기획서·피그마)에 경로가 적혀 있으면 `page.goto('<상대 경로>')` 로 바로 간다(`baseURL` 기준, 주소를 박지 않는다) ⒝ 적혀 있지 않으면 **경로를 지어내지 않는다** — 표 `결과` 칸에 `판정 불가(화면 경로 없음)` ⒞ 메일·문자 링크처럼 화면 조작으로 못 만드는 진입은 R4 와 같이 `모킹 필요` 로 남긴다
- **REFACTOR** — 없음

**files**: .claude/skills/tpx-cases/SKILL.md (할 일 7 에서 옮길 자리), .claude/scripts/cases-probe-contract.test.mjs
**depends-on**: [5]
**검증**: `node --test .claude/scripts/cases-probe-contract.test.mjs`

### 할 일 7. `tpx-cases` 를 본문 + `references/` 로 나눈다 · 맥 표현 정리

- **RED** — 할 일 5 의 200줄 단언이 `tpx-cases` 에서 빨강
- **GREEN** — `SKILL.md` 에는 척추·절 목록·절마다 「언제 어느 파일을 연다」만 남긴다. 절(§1~§7)은 번호 그대로 `references/<§번호>-<이름>.md` 로 옮긴다 — 다른 문서가 `tpx-cases §4` 로 가리키므로 **번호는 영구 주소**. 「맥」은 기본 경로를 가리키는 곳만 「작성 에이전트」로
- **REFACTOR** — 옮기며 같은 말을 두 번 한 문단은 한 곳만 남긴다(내용 삭제 없음)

**files**: .claude/skills/tpx-cases/SKILL.md, .claude/skills/tpx-cases/references/*.md
**depends-on**: [5, 6]
**검증**: `wc -l .claude/skills/tpx-cases/*.md .claude/skills/tpx-cases/references/*.md` 전부 ≤200 · `node --test .claude/scripts/chain-contract.test.mjs .claude/scripts/cases-probe-contract.test.mjs`

### 할 일 8. `tpx` · `tpx-merge` · `spec-review` 를 나눈다 · `tpx-author` 맥 표현

- **RED** — 할 일 5 의 200줄 단언이 세 스킬에서 빨강
- **GREEN** — 같은 방식(본문 + `references/`). `tpx-author` 는 92줄이라 안 나누고 설명·본문의 「맥」을 「작성 에이전트(서버 `author` 가 기본, 맥은 대체)」로. 입력 「테스트 폴더」가 서비스 설정에서 온다고 적는다
- **REFACTOR** — 없음

**files**: .claude/skills/tpx/SKILL.md, .claude/skills/tpx/references/*.md, .claude/skills/tpx-merge/SKILL.md, .claude/skills/tpx-merge/references/*.md, .claude/skills/spec-review/SKILL.md, .claude/skills/spec-review/references/*.md, .claude/skills/tpx-author/SKILL.md
**depends-on**: [5]
**검증**: `node --test .claude/scripts/*.test.mjs` · `wc -l .claude/skills/*/SKILL.md .claude/skills/*/references/*.md` (playwright-cli 제외 ≤200)

### 할 일 9. 명세 · 안내 문서 동반 수정 (§2.7)

- **RED** — `npm run check:spec` 는 지금 초록. 이 할 일은 문장 대조라 RED 대신 아래 grep 결과를 0 으로 만든다
- **GREEN** — 아래 「SPEC 동반 수정」 표 전부
- **REFACTOR** — 없음

**files**: docs/spec/도메인/인증.md, docs/spec/도메인/작성.md, docs/HOOKS.md, CLAUDE.md, docs/SPEC.md
**depends-on**: [1, 2, 3, 4]
**검증**: `npm run check:spec` · `grep -rn "첫 케이스는 사람\|맥 경로는 전용 체인" docs .claude CLAUDE.md scripts` 가 0

## SPEC 동반 수정 (§2.7)

| 할 일 | 무엇 | 검증 |
|---|---|---|
| 같은 규칙 찾기 | `grep -rn "auth/me\|services\[\]" docs/spec` → 인증 §4(정본) · 실행 §8.2 · 카탈로그 · 화면공통. 정본은 인증 §4 한 곳, 나머지는 `envs` 만 말하므로 그대로. `grep -rn "첫 케이스\|케이스 폴더\|새 폴더" docs scripts .claude` → 작성 §3.6 · HOOKS 「가벼운 길」 · `authoring-chain.ts` 주석 | grep 결과를 PR 코멘트에 붙인다 |
| 인증 §7 | `services: [{ ..., testsDir }]` + 계약 블록 `상태: 반영 완료 (2026-09-25, 인증 §4)` | `npm run check:spec` |
| 작성 §3.6 | 제목 「맥 경로는 전용 체인이다」 → 「작성 에이전트는 전용 체인이다」(**절 번호 유지**). 기본 경로를 맥으로 읽히게 하는 문장만 「에이전트」로. 맥이 대체 경로라는 문장은 둔다. 「첫 케이스는 사람」 → 폴더는 서비스 설정 `testsDir` (②뒤집은 결정: 근거(응답에 없음)가 사실이 아니게 됐으므로 뺀다) | 눈 |
| 색인 네 곳 | 장 목록·절 번호·표 주인 변화 없음. 라우터 표에 「작성 에이전트」 항목 이름이 「맥」이면 고친다 | `npm run check:spec` |
| SPEC 밖 | `CLAUDE.md` §0(맥 → 작성 에이전트 · §3.6 새 제목) · `docs/HOOKS.md` 「가벼운 길」 새 폴더 줄 · `docs/WORKSTREAMS.md` 작성 킥오프(맥 표현 있으면) · `spec-review`·`check:*` 는 할 일 4·5 · SETUP §8 은 맥 표현만 · DESIGN·mockup 해당 없음 · **코드 상수**: `authoring-chain.ts` 의 폴더 정규식 제거(할 일 2) | 곳마다 눈 |
| 숫자 빼기 | 새로 적는 문장에 개수를 안 쓴다 | `check:spec` |

## 게이트 1 에서 볼 것

- **할 일 4 는 그물을 느슨하게 한다.** 지금은 `tests/` 에 새 폴더가 생기면 사람이 「CI 에 넣나 면제하나」를 정해야 한다. 바꾸면 `todo` 가 아닌 폴더는 전부 「서비스 폴더」로 자동 면제된다 — 앞으로 **플랫폼 자체 표본 폴더**를 새로 만들 때 CI 에 넣는 것을 잊어도 안 잡힌다. 서비스 폴더는 CI 가 원래 못 도는 자리라(BLOCKER 4) 다른 판정 방법이 없다
- **할 일 6 의 규칙 ⒝** — 경로가 없으면 판정 불가로 남긴다. 사람이 나중에 경로를 적어 다시 만든다

## Plan 메타
할 일 9개 · 예상 묶음 4개(1→2 · 3·4 · 5→6→7 · 8, 마지막 9) · 구현 규율: TDD · 추가 검증: `npm test` 1회 · `npm run check:workflow`

## 리뷰 결과
(계획 검토가 채운다)

**렌즈**: plan-eng-review · plan-ceo-review (3등급 = 2종) · 2026-09-25
**판정**: BLOCKER 1건 · 주의 11건 (eng 1+6 · ceo 5, 겹침 있음)

### BLOCKER 1 (eng) — 할 일 3 이 `lane.mjs` 를 빠뜨렸다
`lane.mjs:14,22-25` 가 `테스트만인가` 를 쓰고 `ls-tree` 도 돌린다. `lane.test.mjs:19` 가 「새 폴더는 full」 을 단언한다 → 할 일 3 을 하면 빨개지고 `detect-tier` 차선도 바뀐다.
고칠 것: files 에 `lane.mjs`·`lane.test.mjs`. **`lane.test.mjs:19` 기대값 변경은 게이트 1 승인 대상**(테스트 수정).

### 주의 1 (eng·ceo) — `testsDir` 모양 검사 없음
설정 API 는 `min(1)` 만(`settings/routes.ts:32`). `a/b` 면 `cases-only` 정규식에 안 맞아 다시 무거운 길 · `..` 는 자식 프롬프트에 그대로. 다른 서비스가 `todo` 를 쓰면 CI 가 도는 폴더에 섞인다.
고칠 것: 할 일 2 RED 에 `^[A-Za-z0-9._-]+$`(`.`·`..` 거부) · 칸 없음(옛 서버) 사유.

### 주의 2 (ceo) — 「서비스마다 다른 저장소(`tests_repo`)」 전제와 어긋남
에이전트는 플랫폼 저장소 사본에만 쓰고 PR 을 올린다. 고객사가 `tests_repo` 를 별도 저장소로 두면 finish 대조(`authoring/store.ts:159`)에서 매번 실패한다.
고칠 것: 계획 「왜」에 전제를 적고, 할 일 2 에서 `tests_repo` 가 origin 과 다르면 집기 전에 사유와 함께 거부.

### 주의 3 (ceo) — 할 일 4 면제가 이름으로만 갈린다
고칠 것: 「`<접두사>-NNN.spec.ts` 모양만 있는 폴더」만 서비스 폴더로 면제. 그 밖은 지금처럼 사람이.

### 주의 4 (eng) — `web/api.ts` 필수 칸을 더하면 화면 fixture 약 8개 타입이 깨진다
고칠 것: `api.ts` 는 안 건드리거나 `testsDir?`.

### 주의 5 (eng·ceo) — 스킬 분리가 계약 검사를 약하게 하고 옮기다 빠져도 모른다
`read()` 이어 붙이기는 「폴더 어딘가」만 증명. 계획은 §1~§7 만 적었지만 `§8 반환`·`용어 사전`·`막혔을 때` 도 있다. `spec-review` 체크리스트 한 절이 242줄.
고칠 것: 할 일 5 에 ⒜ `references/*.md` 는 전부 `SKILL.md` 가 이름으로 가리킨다 ⒝ 원본의 절 제목·R1~R16 식별자가 분리 뒤에도 전부 있다. 200줄 단언은 7·8 과 같은 커밋에.

### 주의 6 (eng·ceo) — 자식이 references 를 안 열 수 있다
고칠 것: `tpx-author` 절차 1~6 과 1:1 로 「이 단계에서 열 파일」을 적는다.

### 주의 7 (eng) — 밖에서 스킬 절을 가리키는 포인터
`CLAUDE.md:25,304` · `SETUP.md:358,491` · `docs/cases/TODO.md` · 작성 `:320` · `SPEC.md:90`. 할 일 9 depends-on 에 7·8.

### 주의 8 (ceo) — 할 일 9 검증 grep 이 이력 문서에 걸려 0 이 안 된다 · `공통/2-명세선언.md` 누락
고칠 것: `docs/progress`·`docs/plans`·`docs/reviews` 제외 · `2-명세선언.md` 를 files 에.

### 주의 9 (eng) — `나풀기` 는 켤 때 한 번 · 판정기 복사도 켤 때
`testsDir` 를 바꾸거나 할 일 3 배포 뒤에는 author 컨테이너 재시작. 작성 §3.6 에 한 줄.

### 통과한 것
- 할 일 1 자리(`auth/store.ts:57-62`) · viewer 노출 무해(케이스 목록에 이미 같은 정보)
- 폴더 기준: 사본=플랫폼 저장소, compose `./tests:/tests` → `tests/<testsDir>` 와 스캔 경로 일치
- `케이스폴더` 쓰는 곳 한 곳뿐 · `authoring-rules.ts:144` 주석도 같이
- CI 는 `tests/todo` 만 → 할 일 4 논리 성립 · 번호 영구 주소 · playwright-cli 제외 · 안전망 먼저
