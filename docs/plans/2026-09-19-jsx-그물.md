# JSX 단위 테스트 그물 — 화면 16개가 검사를 안 받고 있다

등급: 3 · 갈래: 없음 (하네스 + 명세) · 2026-09-19

## 도메인 정리

**제품 도메인을 하나도 건드리지 않는다.** 화면 코드도 안 고친다 — 검사 장치를 까는 일이다.

| 무엇 | 어디 | 판정 |
|---|---|---|
| 계약 넷 (`types.ts` · 러너 HTTP · DB 표·칸 · Admin API) | — | **건드리지 않음** |
| 승인받은 부품 목록 | 공통/6-인프라 §9.1 | **고친다 — 게이트 0 승인** |
| 단위 테스트 확장자 | 〃 기술 스택 표 · `CLAUDE.md` §3 | **고친다 — 게이트 0 승인** |
| 화면 기준 | `DESIGN.md` 모달 규칙 | 읽기만 — 검사의 기준이다 |

**§9.1 이 부품 목록의 정본이다.** 도메인/인증 §3.5 가 「승인받은 부품 전체 목록은 §9.1 이 정본이다」로
못 박았고, §9.1 자신도 「여기 없는 것을 쓰려면 CLAUDE.md §3 대로 먼저 묻는다」를 달고 있다.

## 왜

`vitest.config.ts` 의 `include` 가 `['apps/**/*.test.ts', 'packages/**/*.test.ts']` 라
**`.test.tsx` 를 아예 안 집는다.**

실측 — 화면 파일 **16개 2,790줄**이 자동 검사를 한 번도 안 받는다. 판단하는 순수 `.ts`
**16개 1,524줄**만 받는다.

지금까지의 대응은 「판단을 전부 `.ts` 로 빼내 그물에 넣고, 그리는 쪽은 사람이 브라우저로 본다」였다.
**그 방식이 실제로 사고를 놓쳤다** (`docs/LEARNINGS.md` — WS-E ③).

> 판단은 **맞았다.** 「임시 비밀번호를 한 번만 보여준다」가 정확히 계산됐고 검사도 초록이었다.
> 그런데 화면이 그 상자를 **화면 밖에** 그렸다. 계정이 열 명만 돼도 안 보이는데
> **그 사이 옛 비밀번호는 이미 죽어 있어** 그 사람은 영영 로그인을 못 한다.

**판단은 맞고 배치가 틀렸는데 그물이 배치를 안 본다.**

### 모달과 폼을 먼저 덮는 이유

**「특정 순간에만 드러나는」 자리**라서다. 목록 화면은 잘못 그려지면 눈에 바로 띄지만,
모달은 평소에 안 보이고 폼은 한 칸 비웠을 때·연타했을 때·도중에 닫았을 때가 각각 다르다.
사람이 눌러 확인하려면 그 상황을 하나하나 만들어야 하고, 빠뜨리기 쉽다.

### 나머지 13개는 이번에 안 덮는다

**「그 화면을 건드릴 때 같이 덮는다」로 넘긴다.** 근거 셋.

1. 이번 PR 의 값은 **그물 자체를 까는 것**이다. 한 번 깔면 다음부터는 검사 파일 하나를 더하는 일이다
2. 큰 화면(`RunResult` 408줄 · `RunSetup` 364줄 · `CaseList` 308줄)은 검사도 크다.
   한 PR 에 다 넣으면 **그물을 까는 변경과 검사를 쓰는 변경이 섞여** 무엇이 문제인지 가리기 어렵다
3. 그 셋은 **사람이 자주 눌러 보는 자리**라 사각지대가 상대적으로 작다

**게이트 1 이 이 판단을 뒤집을 수 있다.** 더 덮기로 하면 할 일 8~10 을 늘린다.

### 그물에 기존 버그가 걸리면

**고치는 것도 이번 범위다.** LEARNINGS 「기준을 재는 장치를 만들자마자 기존 값이 걸렸다」와 같은
유형이고, 그때도 고쳤다. 다만 **고친 것을 커밋으로 가른다** — 그물 까는 커밋과 섞지 않는다.
걸린 것이 화면 로직의 큰 결함이면 그 자리에서 멈추고 게이트 2 요약에 싣는다.

---

## Plan

### 할 일 1. 부품 둘을 들이고 `.test.tsx` 를 집게 한다

- **RED** — `apps/admin/src/web/그물.test.tsx` 에 **그물이 도는지만 보는 최소 검사** 하나를 둔다.
  `render(<div>그물</div>)` 이 문서에 붙는지 단언한다. 지금은 ① `include` 가 `.tsx` 를 안 집어
  **수집 자체가 안 되고** ② 부품이 없어 import 가 깨진다
- **GREEN** — `npm i -D jsdom @testing-library/react` · `vitest.config.ts` 의 `include` 에
  `apps/**/*.test.tsx` 를 더한다. 검사 파일 머리에 `// @vitest-environment jsdom`
- **REFACTOR** — 없음

**files**: `package.json`, `package-lock.json`, `vitest.config.ts`, `apps/admin/src/web/그물.test.tsx`
**depends-on**: []
**검증**: `npx vitest run apps/admin/src/web/그물.test.tsx` 종료코드 0

**jsdom 을 전역으로 켜지 않는다.** DB·파일시스템·러너 검사가 전부 node 환경을 전제한다.
`vitest` 3 에서 `environmentMatchGlobs` 는 비권장이고 `test.projects` 는 설정이 커진다.
**파일 머리 주석 한 줄**이 가장 작다 — 설정 파일은 `include` 한 줄만 바뀐다.

---

### 할 일 2. 기존 606건이 그대로인지 실측한다

- **RED** — 없다. 이건 **회귀 확인**이다. 앞 할 일이 `include` 와 환경을 건드렸으니
  기존 검사가 흔들렸는지 본다
- **GREEN** — 흔들렸으면 원인을 찾아 고친다. 안 흔들렸으면 그 사실을 계획에 적는다
- **REFACTOR** — 없음

**files**: 없음 (확인만)
**depends-on**: [1]
**검증**: `DATABASE_URL='postgres://platform:platform@localhost:5433/platform' npm test` —
**606건이 그대로 있고** 새 검사만 늘었는지 본다. 종료코드 0

---

### 할 일 3. 모달이 DESIGN.md 규칙을 지키는지 그물로 잡는다

- **RED** — `Modal.test.tsx` 가 DESIGN.md 의 규칙을 하나씩 단언한다.
  **닫는 길 셋** — 버튼 누르기 · `Escape` · 바깥 누르기가 각각 `onClose` 를 부른다.
  **상자 안에서 시작한 누르기는 안 닫는다** (드래그로 글자를 긁다 놓으면 닫히면 안 된다).
  **포커스를 가둔다** — 끝에서 `Tab` 이 처음으로, 처음에서 `Shift+Tab` 이 끝으로.
  **닫으면 열기 전 자리로 포커스가 돌아온다.**
  `role="dialog"` · `aria-modal="true"` · `aria-label` 이 제목이다.
  코드가 이미 지키고 있으므로 **처음부터 초록일 것으로 본다** — 초록이면 그물이 제 규칙을 본다는 뜻이다
- **GREEN** — 어긋나는 것이 나오면 `Modal.tsx` 를 고친다. **그때는 커밋을 가른다**
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/Modal.test.tsx`
**depends-on**: [1]
**검증**: `npx vitest run apps/admin/src/web/Modal.test.tsx`

---

### 할 일 4. 그물이 실제로 무는지 역검증한다

- **RED** — `Modal.tsx` 의 `Escape` 처리를 **일부러 지운다.** 할 일 3 의 검사가
  **그 줄을 이름째로 짚으며 빨간불**을 내야 한다. 안 나면 그물이 헛돈 것이다
- **GREEN** — **편집으로 되돌린다.** `git checkout` 을 쓰지 않는다 —
  이 세션이 앞 작업에서 그 방법으로 커밋 안 한 수정을 날렸다
- **REFACTOR** — 없음

**files**: 없음 (되돌린다)
**depends-on**: [3]
**검증**: 망가뜨렸을 때 종료코드 1, 되돌린 뒤 종료코드 0. **두 결과를 PR 에 적는다**

**이 할 일을 빼지 마라.** 검사가 초록인 것과 검사가 **무는** 것은 다르다.
앞 작업에서 「열두 경로를 표로 돌렸는데 축을 하나 빼먹어 전부 통과」한 일이 있었다.

---

### 할 일 5. 사고가 났던 자리를 그물로 잡는다

- **RED** — `SettingsPassword.test.tsx` 가 단언한다.
  아이디와 임시 비밀번호가 **화면에 보인다.**
  **화면 안으로 끌어온다** — `scrollIntoView` 가 불렸다.
  **포커스가 그 상자로 간다.**
  `role="alert"` `aria-live="assertive"` 라 낭독기에도 알린다.
  **값이 바뀌면 다시 끌어온다** (다른 계정으로 또 만들었을 때).
  「적었습니다」를 누르면 `onClose` 가 불린다
- **GREEN** — 어긋나면 고친다
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/SettingsPassword.test.tsx`
**depends-on**: [1]
**검증**: `npx vitest run apps/admin/src/web/SettingsPassword.test.tsx`

**이 자리가 사고가 난 곳이다.** `scrollIntoView` 는 jsdom 에 없어서 **직접 끼워 넣어야 한다** —
그 사실 자체가 「브라우저 밖에서는 이게 안 보인다」는 증거다.

---

### 할 일 6. 폼이 스키마대로 칸을 그리는지 그물로 잡는다

- **RED** — `Form.test.tsx` 가 단언한다.
  칸이 없으면 **「이 케이스는 입력값을 선언하지 않았습니다」** 가 뜬다.
  라벨이 `htmlFor` 로 칸에 묶여 있다 (라벨을 눌러도 칸이 잡힌다).
  **`idPrefix` 가 다르면 id 가 안 겹친다** — 입력값과 기대결과에 같은 이름 칸이 있을 수 있다.
  `enum` 은 드롭다운이고, 선택 칸이면 **「고르지 않음」이 맨 앞**에 있다.
  `optional` 이면 **「선택」 배지**가 붙는다.
  글자를 치면 `onChange` 가 그 키와 값으로 불린다.
  오류가 있으면 그 칸 아래에 뜬다
- **GREEN** — 어긋나면 고친다
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/Form.test.tsx`
**depends-on**: [1]
**검증**: `npx vitest run apps/admin/src/web/Form.test.tsx`

---

### 할 일 7. 임시 검사 파일을 치운다

- **RED** — 할 일 1 의 `그물.test.tsx` 는 **그물이 도는지만 보는 임시 자리**다.
  할 일 3·5·6 이 진짜 검사를 들고 있으면 그것이 같은 일을 대신한다
- **GREEN** — `그물.test.tsx` 를 지운다
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/그물.test.tsx` (삭제)
**depends-on**: [3, 5, 6]
**검증**: `npx vitest run apps/admin/src/web` 종료코드 0 · 그 파일이 목록에 없다

---

## SPEC 동반 수정 (§2.7)

### 할 일 8. §9.1 의 부품 목록과 확장자를 고친다 (게이트 0 승인분)

- **RED** — `npm run check:spec` 이 장 줄 수 불일치로 **실패**하는 것을 본다 (줄을 더한 직후)
- **GREEN** — `docs/spec/공통/6-인프라.md` §9.1
  1. 「승인받은 새 부품」 표에 두 줄. **왜 직접 만들지 않나** 칸을 채운다 —
     `jsdom` 은 브라우저의 DOM 을 Node 안에 다시 만드는 일이라 직접 짤 값이 없고,
     `@testing-library/react` 는 React 19 의 `act` 경계와 비동기 갱신을 다뤄 준다.
     승인 날짜 `2026-09-19`
  2. 기술 스택 표의 「각 앱 안 `*.test.ts`」 를 **`*.test.ts` · 화면은 `*.test.tsx`** 로 넓힌다.
     `tests/**` 는 Playwright 전용이라는 규칙은 **그대로 둔다**
  그다음 `docs/SPEC.md` 의 장 줄 수와 갈래 합계를 고친다
- **REFACTOR** — 없음

**files**: `docs/spec/공통/6-인프라.md`, `docs/SPEC.md`
**depends-on**: [1]
**검증**: `npm run check:spec` 종료코드 0

---

### 할 일 9. 같은 규칙이 적힌 다른 곳을 맞춘다 (§2.7 ①)

- **RED** — 핵심 낱말로 훑은 결과가 이렇다.
  ```
  CLAUDE.md:240                단위 테스트는 각 앱 안에 *.test.ts 로 둔다
  docs/WORKSTREAMS.md:338      .test.tsx 는 vitest 가 안 잡으므로 JSX 는 브라우저로 확인한다
  docs/WORKSTREAMS.md:346-347  넘긴 것 — JSX 단위 테스트 그물 하나
  docs/LEARNINGS.md:555-556    vitest.config.ts 로 승격. 단위는 *.test.ts, Playwright 는 *.spec.ts
  ```
- **GREEN**
  - `CLAUDE.md` §3 — 확장자를 넓힌다. **게이트 0 승인분이다** (기존 규칙 수정)
  - `WORKSTREAMS.md` 338 — **사실이 아니게 된다.** 고친다
  - `WORKSTREAMS.md` 346~347 — 「넘긴 것」의 마지막 하나를 **닫힘**으로. 이로써 WS-E 가 넘긴 것이 전부 닫힌다
  - `LEARNINGS.md` 555~556 — 승격 기록에 「2026-09-19 에 화면용 `*.test.tsx` 를 더했다」 한 줄.
    **원래 기록을 지우지 않는다** (승격 규칙)
  - **`docs/progress/WS-E.md` 61·116 은 안 건드린다** — 그때는 사실이었던 과거 기록이다
- **REFACTOR** — 없음

**files**: `CLAUDE.md`, `docs/WORKSTREAMS.md`, `docs/LEARNINGS.md`
**depends-on**: [8]
**검증**: `npm run check:workflow` · `grep -rn '\.test\.tsx' CLAUDE.md docs/WORKSTREAMS.md` 로 눈으로

---

### §2.7 절별 대응

| §2.7 | 무엇 | 어느 할 일 |
|---|---|---|
| ① 같은 규칙이 적힌 다른 절 | 낱말로 훑기 → 네 곳 | 9 |
| ② 뒤집은 결정 | **해당 없음** — 확장자를 넓힐 뿐 뒤집는 결정이 없다 | |
| ③ 색인 네 곳 | 새 절을 안 만들어 장 목록·절 번호·표 주인은 그대로. **분량 둘** | 8 |
| ③ 라우터 표 | **해당 없음** — 새 개념이 아니다. 「검사를 더한다」는 길잡이가 이미 없고, 이번에 만들 것도 아니다 | |
| ④ SPEC 밖 | `CLAUDE.md` · `WORKSTREAMS.md` · `LEARNINGS.md` | 9 |
| ④ 코드에 박힌 상수 | **`vitest.config.ts` 의 `include` 가 바로 그것이다** — §9.1 의 확장자 규칙을 코드가 복사해 둔 자리 | 1 |
| ⑤ 손으로 적은 숫자 | 색인 분량은 `check-spec-refs.mjs` 가 센다. 새로 손으로 적는 숫자를 만들지 않는다 | 8 |
| ⑥ 검사 | `check:spec` → `spec-review` H 절 → 게이트 2 | [6] 이 돈다 |

## Plan 메타

할 일 9개 · 예상 묶음 4개 · 구현 규율: TDD · 추가 검증: `npm run check:deps` · `typecheck` ·
`check:workflow` · `check:spec` · `check:tests` · `check:secret-names` · `vite build` ·
`DATABASE_URL=… npm test` **연속 3회**

묶음 — ①{1} ②{2, 3, 5, 6, 8} ③{4, 9} ④{7}

**미분류: `package-lock.json`** — 게이트 2 요약까지 들고 간다.

## 리뷰 결과

(계획 검토가 채운다)
