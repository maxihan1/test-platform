# 실행 상태를 보여준다 — 진행 모달과 리포트 비교

등급: 3 · 갈래: WS-E(주도) + WS-D · 2026-09-21

1차(PR #35 청사진 스킨)의 후속이다. 원래 불만 ①「수동 테스트 실행 시 status 가 직관적이지 않음」과
⑥「리포트가 케이스 내용과 결과만 보여 준다」를 닫는다.

---

## 도메인 정리

| BC | 무엇을 | 어디 |
|----|-------|------|
| **Execution** | 안 건드린다 | 진행 모달은 `GET /api/runs/:runId` 가 이미 주는 값만 읽는다 |
| **Reporting** | 조회 하나 신설 | `GET /api/runs/:runId/insights`. §3.3 의 「읽기 전용」을 지킨다 — write 가 없다 |
| **Catalog** | 안 건드린다 | |
| **Runner** | 안 건드린다 | 절차 단위 진행(A-2)은 게이트 0 에서 미뤘다 |

관련 절 — 도메인/실행 §8.3 · §8.9 · 공통/5-화면공통 §8 · 도메인/리포팅 §3.3 · §7 · §8.4 · DESIGN.md 모달

### 게이트 0 결과 (2026-09-21)

**A-1 + B 승인. A-2 는 이번 범위 밖.** 계약 블록 둘(`ExecuteRequest.progressUrl` · `POST/GET .../progress`)은
`docs/plans/2026-09-21-청사진스킨.md` 에 `상태: 대기` 로 그대로 남는다. 이번 PR 이 건드리지 않는다.

**승인된 계약 블록은 하나다.**

```
[계약 변경 필요]
대상:    Admin API — GET /api/runs/:runId/insights 신설
현재:    실행 결과 조회는 GET /api/runs/:runId 하나다. 직전 실행과 견줄 조회가 없다
제안:    조회 하나를 더한다. 기존 GET /api/runs/:runId 의 응답 모양은 한 글자도 바꾸지 않는다.
         돌려줄 것 — 직전 실행의 runId 와 시각, 케이스별 새로깨짐 | 계속깨짐 | 고쳐짐 | 그대로,
         같은 사유로 묶은 실패 덩어리(대표 문장 · 건수 · 속한 항목).
         「직전」은 같은 서비스 · 같은 대상 서버의 바로 앞 실행이다
이유:    지금 리포트는 이번 판정만 보여 준다. 기존 응답을 늘리면 그것을 읽는 화면 전부가 같이 흔들린다.
         DB 와 마이그레이션은 안 바뀐다
영향:    WS-D(조회를 만든다), WS-E(리포트가 그린다)
상태:    대기
```

이 블록은 **할 일 9** 에서 `docs/spec/도메인/리포팅.md` §7 에 실으면서
`상태: 반영 완료 (2026-09-21, 도메인/리포팅 §7)` 로 닫는다.
`check-spec-refs.mjs` 는 `docs/spec/**` 만 훑으므로 계획 파일의 이 블록은 기계가 안 본다 — 사람이 닫는다.

---

## 왜

### ① 진행 모달 — 실행을 걸고 나면 지금 무엇이 도는지 모른다

한 번 돌리면 최악 50분이다 (SPEC §8.9). 지금 있는 것은 셋이다.

| 어디 | 무엇을 말하나 | 부족한 것 |
|------|-------------|----------|
| 맨 위 알림 줄 | `RUN 2113 이 도는 중입니다 12/40` | **지금 무엇이 도는지 모른다** |
| 결과 화면 머리 | `도는 중 3건` + 통과·실패·미실행 숫자 | 같다 |
| 완료 모달 | 끝난 뒤에만 | 도는 동안은 아무것도 안 한다 |

**「12/40」은 이미 있다.** 2단계에서 `apps/admin/src/web/layout.ts:152` 를 열어 확인했다 —
옛 계획(청사진스킨.md 2차 절)이 「지금은 `▶ 도는 중` 글자뿐」이라 적은 것은 **사실이 아니었다.**
SPEC 공통/5-화면공통 §8 이 처음부터 `12/40` 을 요구했고 구현이 그것을 지켰다.
**그래서 그 할 일을 이 계획에 넣지 않는다.**

빠진 것은 **항목 이름**이다. 사람이 러너 로그를 직접 열어 보는 이유가 그것이다 —
「3건이 돌고 있다」는 「멈춘 것」과 구분되지 않는다. 원래 불만 ①이 요구한 「현재 진행 중인 테스트」가 이 자리다.

### ② 리포트 — 이번 판정만 보여 준다

「지난번엔 통과였나」를 알려면 옛 실행을 따로 찾아 눈으로 견줘야 한다.
실패 30건이 **같은 이유 하나**일 때도 30줄로 늘어선다.

**필요한 값이 전부 이미 저장돼 있다.** 2단계에서 마이그레이션을 열어 확인했다.

| 무엇 | 어디 |
|------|------|
| 직전 실행 찾기 | `test_run (service_id, env, started_at)` — 칸이 셋 다 있다 |
| 케이스별 앞뒤 판정 | `run_item (tc_id, platform, status)` |
| 실패 사유 | `run_item.error` (JSONB `{message}`) · `run_item_step.assertions` (`AssertionResult[]`) |

**`test_run (service_id, env, started_at)` 인덱스는 만들지 않는다.** 실행 한 번에 한 행이라
1만 건이어도 순차 훑기가 밀리초다. 인덱스를 만들면 마이그레이션이 생기고 표면에 `MIGRATION` 이 붙는다 —
값이 없는 비용이다. 실행 기록이 수십만이 되면 그때 만든다.

---

## 되돌리기

**A-1 은 화면에만 있다.** 통째로 되돌려도 서버가 그대로다.
**B 는 조회를 더하기만 한다.** 기존 `GET /api/runs/:runId` 를 한 글자도 안 바꾸므로
`insights` 를 빼면 그 칸만 사라지고 나머지 화면이 안 흔들린다.

---

## Plan

### 할 일 1. 진행 계산을 순수 함수로 뽑는다

`RunDetail` 하나를 넣으면 모달이 그릴 것이 전부 나오는 함수를 만든다.
화면 없이 단언할 수 있어야 막대 비율·경계 조건을 jsdom 없이 본다.

- **RED** — `runProgress.test.ts` 가 다섯을 단언한다. ① 항목 10건 중 6건이 끝났을 때
  막대 네 칸(`통과·실패·실행중·대기`)의 합이 `counts.total` 과 같다 ② `finishedAt === null` 인 **첫** 항목이
  「지금 도는 것」이다 ③ 방금 끝난 것은 `finishedAt` **내림차순 넷**이다 ④ 대기줄은 아직 시작 안 한 앞 셋이다
  ⑤ 전부 끝났으면 「지금 도는 것」이 `null` 이다. 파일이 없으니 전부 실패한다
- **GREEN** — `runProgress.ts` 에 `진행상황(data: RunDetail): 진행` 하나. 막대는 `counts` 를 그대로 쓴다 —
  **`counts.total` 은 `run_item` 건수지 케이스 수가 아니다** (SPEC §7). 대기 = `total - pass - fail - na - running`
  이 아니라 `total - (끝난 것) - running` 으로 센다. 끝난 것은 `finishedAt !== null` 인 항목 수다.
  **셈의 단위를 한 곳에서 정한다** — LEARNINGS 2026-09-21 「셈의 단위가 다른 상수를 가져다 썼다」
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/runProgress.ts` · `apps/admin/src/web/runProgress.test.ts`
**depends-on**: []
**검증**: `npx vitest run apps/admin/src/web/runProgress.test.ts`

---

### 할 일 2. 모달 하나가 「도는 중」과 「끝났다」를 둘 다 그린다

**완료 모달을 새로 만들지 않는다. 있는 것을 옮겨 와 상태 하나를 더한다.**

지금 완료 모달은 `RunResult.tsx:210~250` 에 JSX 로 박혀 있다.
진행 모달을 따로 만들면 **같은 화면에서 모달이 두 번 뜬다** — 도는 동안 하나, 끝나고 또 하나.
DESIGN.md 가 「모달은 하던 일을 가로막는 장치다. 아껴 쓴다」고 적은 것과 정면으로 부딪힌다.

**그래서 한 모달이 변신한다.** 도는 동안 진행을 그리다가, 끝나면 **같은 자리에서 내용만 바뀐다.**
§8.9 의 「한 번 닫은 모달은 그 실행에 대해 다시 뜨지 않는다」도 그대로 산다 — 닫는 것이 한 번이다.

- **RED** — `RunProgressModal.test.tsx` (첫 줄 `// @vitest-environment jsdom`) 가 넷을 단언한다.
  ① 도는 중 `RunDetail` 을 주면 제목이 `도는 중` 이고 지금 도는 항목의 **이름**이 보인다
  ② `12 / 34 완료` 글자가 보인다 ③ `status: 'FINISHED'` 를 주면 **같은 컴포넌트**가 완료 내용을 그리고
  실패한 케이스 이름이 보인다 ④ 판정을 **색만으로** 전달하지 않는다 — 숫자 옆에 글자가 있다 (SPEC §8.9)
- **GREEN** — `RunProgressModal.tsx`. `Modal` 을 그대로 쓴다 (바탕·테두리·닫는 길·포커스는 거기가 정본).
  `진행상황()` 결과를 받아 막대와 목록을 그린다. 폭은 480px 기본 그대로 — 넓힐 이유가 없다
- **REFACTOR** — `RunResult.tsx` 의 완료 모달 JSX 를 지운다. **그 파일이 353줄로 이미 300줄을 넘어 있다**
  (CLAUDE.md §3). 이 이동으로 줄어든다 — 줄어든 값을 게이트 2 요약에 적는다

**files**: `apps/admin/src/web/RunProgressModal.tsx` · `apps/admin/src/web/RunProgressModal.test.tsx`
**depends-on**: [1]
**검증**: `npx vitest run apps/admin/src/web/RunProgressModal.test.tsx`

---

### 할 일 3. 실행을 건 직후 모달이 뜬다 — `useState` 로 든다

- **RED** — `RunResult.test.tsx` 에 셋을 더한다. ① 도는 중 `RunDetail` 로 처음 그리면 모달이 **떠 있다**
  ② 닫으면 사라지고 **폴링은 계속 돈다** (2초 뒤 `api.run` 이 다시 불린다)
  ③ 닫은 뒤 실행이 끝나도 **다시 뜨지 않는다** (SPEC §8.9)
- **GREEN** — `RunResult.tsx` 에 `const [모달열림, set모달열림] = useState(true)` 하나.
  **`useRef` 로 들지 않는다** — LEARNINGS 2026-09-21 「`useRef` 로 든 진행 상태는 화면에 안 뜬다」.
  그 사고가 바로 이 화면 옆에서 났다. 지금 있는 `끝났다고알릴까말까` 와 합쳐 **상태 하나**로 만든다 —
  둘로 두면 「진행은 닫았는데 완료는 뜬다」가 생긴다
- **REFACTOR** — `앞선상태` `useRef` 는 그대로 둔다. **화면에 안 나오는 값**이라 그 교훈의 대상이 아니다.
  그 사실을 주석으로 적는다 — 다음 사람이 「여기도 `useState` 로 바꿔야 하나」를 다시 묻지 않게

**files**: `apps/admin/src/web/RunResult.tsx` · `apps/admin/src/web/RunResult.test.tsx`
**depends-on**: [2]
**검증**: `npx vitest run apps/admin/src/web/RunResult.test.tsx`

---

### 할 일 4. 직전 실행을 찾고 케이스별로 견준다

- **RED** — `insights.test.ts` 가 다섯을 단언한다. ① 앞 실행이 없으면 `previous: null` 이다
  ② 같은 서비스라도 **대상 서버(`env`)가 다르면 직전이 아니다** ③ 통과→실패가 `새로깨짐` 이다
  ④ 실패→실패가 `계속깨짐`, 실패→통과가 `고쳐짐`, 통과→통과가 `그대로` 다
  ⑤ 앞 실행에 없던 케이스는 **어느 칸에도 안 들어간다** — 새 케이스를 「고쳐짐」으로 세면 거짓이다.
  DB fixture 접두사는 **`XDG`** (WS-D 것. `ZZ` 로 시작하는 것을 쓰지 않는다)
- **GREEN** — `reporting/insights.ts` 에 조회 둘. ① `test_run` 에서 같은 `service_id` · 같은 `env` ·
  `started_at <` 인 가장 최근 하나 ② 두 실행의 `run_item` 을 `(tc_id, platform)` 로 맞춰 판정 넷으로 가른다.
  **`write` 를 하지 않는다** — §3.3 이 Reporting 의 write 를 `evidence_document` 하나로 막는다
- **REFACTOR** — 없음

**files**: `apps/admin/src/reporting/insights.ts` · `apps/admin/src/reporting/insights.test.ts`
**depends-on**: []
**검증**: `DATABASE_URL='postgres://platform:platform@localhost:5433/platform' npx vitest run apps/admin/src/reporting/insights.test.ts` — **연속 3회** (spec-review G3)

---

### 할 일 5. 같은 사유로 실패한 것을 한 덩어리로 묶는다

- **RED** — 같은 `insights.test.ts` 에 셋을 더한다. ① 실패 셋이 같은 검증 문장이면 **덩어리 하나**로
  `건수: 3` 이 나온다 ② 검증 문장이 있으면 그것이 대표 문장이고, 없으면 `run_item.error.message` 가 대표다
  ③ **비밀값이 대표 문장으로 새지 않는다** — `run_item.params` 를 대표 문장에 붙이지 않는다 (SPEC §4.1).
  LEARNINGS 2026-09-19 「실패 방향을 열린 쪽으로 뒀다 — 비밀값이 "모르면 보여준다"였다」
- **GREEN** — `insights.ts` 에 `사유로묶는다()`. 묶는 키는 **대표 문장 글자 그대로**다.
  정규화(숫자 빼기·앞뒤 자르기)를 하지 않는다 — 다른 사유를 같은 덩어리로 합치는 쪽이 더 나쁘다.
  덩어리는 건수 내림차순
- **REFACTOR** — `insights.ts` 가 200줄을 넘으면 묶는 쪽을 `insightsGroup.ts` 로 가른다

**files**: `apps/admin/src/reporting/insights.ts` · `apps/admin/src/reporting/insights.test.ts`
**depends-on**: [4]
**검증**: `DATABASE_URL='postgres://platform:platform@localhost:5433/platform' npx vitest run apps/admin/src/reporting/insights.test.ts` — **연속 3회**

---

### 할 일 6. 조회를 낸다 — 인증 표에 등록하는 것이 이 할 일의 절반이다

**`apps/admin/src/auth/scope.ts` 는 WS-F 소유 경로다** (`docs/WORKSTREAMS.md`).
이번 갈래(WS-E + WS-D) 밖이지만 **고치지 않으면 새 조회가 아예 안 열린다** —
그 파일이 「표에 없으면 문이 막는다」를 기본값으로 두고 있다 (`scope.ts:22`).
CLAUDE.md §1.1 대로 **이 자리에 이유를 적고 고친다.** 더하는 것은 표에 한 줄이고 로직을 안 바꾼다.

- **RED** — ① `reporting/routes.test.ts` 에 「없는 `runId` 면 404」와 「`runId` 가 `1e3` 이면 400」
  ② `auth/scope.test.ts` 에 **`/api/runs/:runId/insights` 가 `{ 종류: '실행', 칸: 'runId' }` 로 분류된다**.
  등록 전이라 둘 다 실패한다. WS-F fixture 접두사(`XFS5`)를 건드리지 않는다 — 표를 읽기만 하는 단언이다
- **GREEN** — `reporting/routes.ts` 에 `GET /runs/:runId/insights`. `정수()` 로 경로 숫자를 본다
  (다른 조회와 같은 규칙). `scope.ts` 의 표에 한 줄. `api.ts` 에 `insights(runId)` 와 응답 타입
- **REFACTOR** — 없음

**files**: `apps/admin/src/reporting/routes.ts` · `apps/admin/src/reporting/routes.test.ts` ·
`apps/admin/src/auth/scope.ts` · `apps/admin/src/auth/scope.test.ts` · `apps/admin/src/web/api.ts`
**depends-on**: [5]
**검증**: `npx vitest run apps/admin/src/auth/scope.test.ts` 그리고
`DATABASE_URL='postgres://platform:platform@localhost:5433/platform' npx vitest run apps/admin/src/reporting/routes.test.ts`

---

### 할 일 7. 리포트 화면에 비교 칸과 사유 묶음을 그린다

- **RED** — `RunInsights.test.tsx` (첫 줄 `// @vitest-environment jsdom`) 가 넷을 단언한다.
  ① `previous: null` 이면 **비교 칸을 아예 안 그린다** — 빈 칸을 자리만 잡지 않는다 (SPEC §8)
  ② `새로깨짐` 이 있으면 그 케이스 이름이 보인다 ③ 사유 덩어리에 건수가 글자로 적힌다
  ④ **도는 중인 실행에서는 안 부른다** — 아직 판정이 안 들어간 항목을 견주면 전부 「고쳐짐」으로 보인다
- **GREEN** — `RunInsights.tsx`. `RunResult.tsx` 가 실행이 끝났을 때만 부른다.
  판정 넷은 **색과 글자를 같이** 쓴다 (DESIGN.md 원칙 1). 새 색 토큰을 만들지 않는다 —
  `--pass` · `--fail` · `--na` 로 충분하고, `styles.test.ts` 가 토큰과 명암비를 기계로 잰다
- **REFACTOR** — 새 CSS 는 `styles.css` 에. `font-weight` 는 **600 이 상한**이다 (1차에서 800 을 부르던
  여덟 자리를 고쳤고 `styles.test.ts` 가 그 단언을 들고 있다)

**files**: `apps/admin/src/web/RunInsights.tsx` · `apps/admin/src/web/RunInsights.test.tsx` ·
`apps/admin/src/web/RunResult.tsx` · `apps/admin/src/web/styles.css`
**depends-on**: [6]
**검증**: `npx vitest run apps/admin/src/web/RunInsights.test.tsx apps/admin/src/web/styles.test.ts`

---

### 할 일 8. 1차가 넘긴 것 둘을 닫는다

**이 할 일 하나만 이번 PR 의 목적 밖이다.** 게이트 1 에서 잘라도 나머지 여덟이 그대로 선다.

- **RED** — ① `CaseListParts.test.tsx` 에 「객체 기본값을 넣으면 `[object Object]` 가 **안** 보인다」
  (1차 검사 경미 F1, `CaseListParts.tsx:118`) ② `RunSetup.test.tsx` · `ItemDetail.test.tsx` ·
  `SettingsService.test.tsx` 를 새로 만든다 (1차 검사 중대 F9). 셋 다 **그 화면의 규칙 하나씩**을 단언한다 —
  렌더만 하고 끝나는 검사를 쓰지 않는다 (spec-review G8 「항진명제가 아닌가」)
- **GREEN** — `CaseListParts.tsx` 의 `String(칸.default)` 를 객체·배열이면 `—` 로 접는 한 줄로 바꾼다.
  화면 셋에는 코드를 안 고친다 — 그물만 친다
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/CaseListParts.tsx` · `apps/admin/src/web/CaseListParts.test.tsx` ·
`apps/admin/src/web/RunSetup.test.tsx` · `apps/admin/src/web/ItemDetail.test.tsx` ·
`apps/admin/src/web/SettingsService.test.tsx`
**depends-on**: []
**검증**: `npx vitest run apps/admin/src/web/CaseListParts.test.tsx apps/admin/src/web/RunSetup.test.tsx apps/admin/src/web/ItemDetail.test.tsx apps/admin/src/web/SettingsService.test.tsx`

---

### 할 일 9. 문서 — §2.7 동반 수정 (아래 절이 목록의 정본)

- **RED** — `npm run check:spec` 이 절 참조·링크·분량을 본다. 절을 고치면 분량이 어긋나 빨개진다
- **GREEN** — 아래 「SPEC 동반 수정」 표의 여덟 곳
- **REFACTOR** — 없음

**files**: `docs/spec/도메인/리포팅.md` · `docs/spec/도메인/실행.md` · `docs/spec/공통/5-화면공통.md` ·
`docs/spec/공통/6-인프라.md` · `docs/spec/공통/7-데모와-완료.md` · `docs/SPEC.md` · `docs/DESIGN.md` ·
`docs/design-mockup.html` · `docs/plans/2026-09-21-청사진스킨.md`
**depends-on**: [3, 7]
**검증**: `npm run check:spec`

---

## SPEC 동반 수정 (§2.7)

2단계가 **고친 문장의 핵심 낱말로 12장을 훑어** 찾은 목록이다. 절 번호로 찾지 않았다.

```
grep -rn '모달' docs/spec/ docs/SPEC.md docs/DESIGN.md   → 13곳
grep -rn '껍데기' docs/spec/ docs/DESIGN.md docs/SPEC.md → 7곳
```

### ① 같은 규칙이 적힌 다른 절

| 어디 | 무엇 | 왜 |
|------|------|-----|
| `docs/DESIGN.md:208` | **「그래서 세 자리에만 쓴다」에서 개수를 뺀다** | 같은 절 `:228` 이 「**자리 개수를 적지 않는다**. 위 목록만 정본으로 둔다」고 적었다. **한 절 안에서 앞뒤가 반대다.** 1차가 이것을 피하려고 정해 놓고 윗줄을 안 고쳤다 (CLAUDE.md §2.7 ⑤) |
| `docs/spec/도메인/실행.md` §8.9 | 「닿는 길은 셋」 표가 **끝났을 때만** 다룬다. 도는 중의 자리를 더하고, **모달이 하나이고 변신한다**를 적는다 | 안 적으면 다음 사람이 완료 모달과 별개로 진행 모달을 또 만든다 |
| `docs/spec/공통/5-화면공통.md:54` · `docs/DESIGN.md:33` | **「껍데기는 넷뿐이다」에서 개수를 뺀다** | 양쪽에 손으로 적혀 있다. 자리가 다섯이 되는 날 한쪽만 고쳐진다 (1차 검사 경미 H2) |
| `docs/spec/공통/6-인프라.md:145` | 모달 그물이 무는 축 | 모달이 하나 늘었다 |

**개수를 세는 일은 하지 않는다.** 세 자리 모두 **목록만 정본으로 두고 숫자를 뺀다** (§2.7 ⑤).

### ② 뒤집은 결정

`docs/plans/2026-09-21-청사진스킨.md` 2차 절의 「알림 줄에도 진행률을 붙인다 — 지금은 `▶ 도는 중`
글자뿐이다」 — **근거가 사실이 아니었다** (`layout.ts:152` 가 이미 그린다).
§2.7 ② 의 「근거가 사실이 아니게 됐다 → **뺀다**」에 해당한다. 그 자리에 **왜 뺐는지 한 줄**을 남긴다.

### ③ 색인 네 곳 — 넷째를 가장 잘 빠뜨린다

1. 장 목록 — `도메인/리포팅` · `도메인/실행` · `공통/5-화면공통` 줄 수
2. 절 번호 → 장 표 — 새 절을 만들면
3. 표 주인
4. **「기능을 더하거나 고칠 때」 라우터 표** — **「실행이 도는 동안 무엇이 도는지 보여준다」** 와
   **「직전 실행과 견준다」** 두 줄을 더한다. 새 개념이라 이걸 하려는 사람이 여기서 길을 찾는다

### ④ SPEC 밖

| 어디 | 무엇 |
|------|------|
| `docs/spec/공통/7-데모와-완료.md:108` | 완료 기준 — 진행 모달 줄을 더한다 |
| `docs/design-mockup.html` | **1차에서 안 고쳐 토큰이 옛것이다** (1차 검사 경미). 이번에 같이 고친다 |
| `docs/WORKSTREAMS.md` 킥오프 | 기계가 안 본다 — 눈으로 대조한다 |
| **코드에 박힌 상수** | 없다. 진행 모달이 쓰는 값은 전부 `RunDetail` 에서 온다 |

### ⑤ 숫자를 손으로 적지 않는다

위 ①의 셋(`DESIGN.md:33` · `:208` · `5-화면공통.md:54`)이 전부 이 항목이다.

### ⑥ 검사

`npm run check:spec` → `spec-review` H 절 → 게이트 2 에서 사람이 「같은 규칙이 두 곳에서 다른 말을 하는지」를 본다.

---

## 확인 방법 — 브라우저로 눈으로 보는 것

**jsdom 은 이것을 원리적으로 못 본다** (레이아웃 엔진이 없다). [5] 구현 뒤에 반드시 한다.

1. **새로고침 없이** 실행을 걸고 모달이 뜨는지, 막대가 **저절로** 차는지
   — LEARNINGS 2026-09-19 「확인하는 방법이 버그를 가렸다 (4회)」 · spec-review G6.
   `location.reload()` 를 쓰면 폴링이 안 돌아도 맞게 보인다
2. 모달을 닫고 실행이 끝날 때까지 두었다가 **다시 안 뜨는지**
3. **375px `iframe`** 으로 열어 모달과 리포트 비교 칸을 본다
   — LEARNINGS 2026-09-17·19 「좁은 화면에서 제목이 한 글자씩 세로로 흘렀다」가 **두 번** 났다.
   **창 리사이즈로는 안 보인다**
4. 밝게·어둡게 양쪽

---

## 정말 이번 범위 밖

| 무엇 | 어디로 |
|------|-------|
| A-2 절차 단위 진행 (`progressUrl`) | 계약 블록 둘이 `청사진스킨.md` 에 `상태: 대기` 로 남아 있다. 다음 PR |
| 결과 흔들림(flaky) 표시 | 이번 B 가 깔아 주는 값 위에 얹는다. 그다음 |
| `test_run (service_id, env, started_at)` 인덱스 | 실행 기록이 수십만이 되면 |
| 키보드로 훑기 · production 경고 줄 | WS-E 킥오프 항목 |

---

## Plan 메타

할 일 9개 · 예상 묶음 4개 · 구현 규율: TDD

묶음 계산 (`files` 교집합이 있으면 자동 직렬화).

```
묶음 1   할 일 1 · 할 일 4 · 할 일 8      ← 진입 차수 0
묶음 2   할 일 2 · 할 일 5
묶음 3   할 일 3 · 할 일 6
묶음 4   할 일 7 → 할 일 9
```

할 일 3 과 7 은 `RunResult.tsx` 를 함께 고치므로 **번호 순으로 직렬화된다.**

추가 검증 — `npm run check:deps` · `npm run typecheck` · `npm run check:workflow` ·
`npm run check:spec` · `npm run check:tests` · `npm test`.
DB 를 타는 검사는 **연속 3회** (spec-review G3).

## 리뷰 결과

(계획 검토가 채운다)
