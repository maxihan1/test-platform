# 넘긴 것 셋 — 첫 계정 명령 · 서비스 경계 · 명세 충돌

등급: 3 · 갈래: 없음 (배포 · 인증 · 명세) · 2026-09-19

## 도메인 정리

세 갈래를 걸친다. 사용자가 한 PR 로 지시했다 (CLAUDE.md §1.1 — 막는 장치는 없고 범위를 밝히면 된다).

| 항목 | BC | 관련 절 | 판정 |
|---|---|---|---|
| ① `scripts/` 를 이미지에 싣는다 | 배포 | 공통/6-인프라 §9.2 | **명세 미구현** |
| ② 서비스 경계를 문에서 막는다 | 인증 | 도메인/인증 §7 · §3.5 | **명세 미구현** |
| ③ 서비스 색 예외를 적는다 | 화면 공통 | 공통/5-화면공통 §8 | **명세 변경 — 게이트 0 승인받음** |

계약 넷(공유 타입 · 러너 HTTP · DB 표·칸 이름 · Admin API 경로·응답 형태) 중 **건드리는 것은 없다.**
②는 응답 *내용*이 줄 뿐 경로도 모양도 그대로다. ③은 화면 규칙이라 계약 표에 없다.

## 왜

### ① 첫걸음이 막혀 있다

`apps/admin/Dockerfile` 이 `COPY tsconfig.json playwright.config.ts ./` · `COPY packages` ·
`COPY apps/admin` 셋만 한다. **`scripts/` 가 이미지에 없다.**

공통/6-인프라 §9.2 는 이미 이렇게 못 박았다 — 「이 스크립트를 admin 안 어느 경로에 둘지는 공용 골격을
맡은 갈래가 정한다. **2026-09-18에 저장소 루트의 `scripts/`로 정했고 확장자는 `.ts`다.** 컨테이너가
이미 `tsx`로 돌고 있어(`CMD ["npx","tsx",…]`) 명령도 같은 것으로 돈다」.

그래서 `docs/SETUP.md` 가 안내하는 맨 첫걸음이 실제로는 안 된다. **회원가입 화면이 없으므로
(§8.6) 처음 띄우는 사람은 로그인 자체를 못 한다.**

### ② 문이 번호를 안 본다

도메인/인증 §7 「인증 적용 범위」가 이미 규정했다 — 「**서비스 경계도 여기서 막는다.
배정받지 않은 서비스의 자원을 부르면 403 이다.**」

`gate.ts` 의 `닿는서비스()` 는 둘만 본다.

1. `?service=` 질의 문자열
2. `POST /api/runs` 본문 `items[].tcId` 의 접두사

**나머지 열두 경로가 그냥 지나간다.** 컨트롤러가 라우트를 전수 조사했다.

**A. 경로에 접두사가 박혀 있다 — DB 를 안 읽어도 된다**

| 경로 | 새는 것 |
|---|---|
| `GET /api/cases/:tcId/history` | 남의 케이스 이력 |
| `GET /api/cases/:tcId/param-sets` | 남의 저장된 입력값 |
| `POST /api/cases/:tcId/param-sets` | **쓰기** — 남의 케이스에 입력값을 만든다 |
| `GET /api/catalog/cases/:tcId` | 남의 케이스 상세 |
| `GET /api/catalog/cases/:tcId/source?line=` | **남의 테스트 소스 코드** |

`tcId` 는 `<접두사>-<3자리>` 라(§2) 앞을 자르면 곧 서비스다.
`닿는서비스()` 가 `POST /api/runs` 본문에서 이미 쓰는 바로 그 논리다.

**B. 번호로 부른다 — 그 번호가 어느 서비스인지 DB 를 읽어야 안다**

| 경로 | 새는 것 | 무엇으로 찾나 |
|---|---|---|
| `GET /api/runs/:runId` | 남의 실행 기록 | `test_run.service_id` |
| `GET /api/runs/:runId/items/:historyId` | 남의 항목 상세 (입력값·오류 원문) | 〃 |
| `GET /api/screenshots/:runId/:historyId/:seq.png` | 남의 화면 사진 | 〃 |
| `POST /api/runs/:runId/abort` | **쓰기 — 남의 실행을 멈춘다** | 〃 |
| `POST /api/runs/:runId/evidence` | **쓰기 — 남의 증적 문서를 만든다** | 〃 |
| `GET /api/evidence/:id` | 남의 증적 문서 | `evidence_document.run_id` → `test_run.service_id` |
| `DELETE /api/param-sets/:id` | **쓰기 — 남의 입력값 묶음을 지운다** | `param_set.tc_id` 접두사 |

#### 어디에 두나 — 근거

**문(gate)이 맞다.** §7 의 「서비스 경계도 **여기서** 막는다」의 '여기'가 인증 적용 범위이고
그것을 실행하는 자리가 문이다. 라우트마다 흩어 두면 새 라우트가 늘 때마다 빠뜨린다.

**§3.5 불변식과 어긋나지 않는다.** 그 불변식은 「누구인지 알아내는 부분」(`identify()`)과
「그 이름으로 무슨 일을 하는 부분」을 가르라고 한다. 경계 검사는 **확인이 끝난 뒤에 오는 별개의 일**이다.
SSO 로 갈아 끼울 때 바뀌는 곳은 여전히 `identify()` 하나다.

**새 파일 `apps/admin/src/auth/scope.ts` 하나.** 순수 판정(경로 → 무엇을 찾아야 하나)과
DB 질의를 한 파일에 두되 **순수 함수를 따로 내보낸다.** CI 에 postgres 가 없어
DB 검사는 `describe.skipIf` 로 건너뛰는데, 순수 부분은 CI 에서도 돈다.

#### 없는 번호는 어떻게 하나

**문을 지나보내고 라우트가 404 를 낸다.** 없는 자원은 아무것도 안 샌다.
SPEC §7 이 「404로 감추지 않는다 — 화면이 그 자리를 아예 안 보여주므로 여기까지 닿은 요청은
화면의 버그이거나 직접 찌른 것이고, 둘 다 감추는 편이 더 나쁘다」로 이미 정했다.

#### `service_id` 가 NULL 인 옛 실행은 — **403 으로 막는다**

서비스 통합 이전 행이다 (`CHECK ((service_id IS NULL) = (service_name = ''))`).

1. **이미 목록에 안 뜬다.** `listRuns()` 가 `WHERE r.service_id = (SELECT id FROM service WHERE prefix=$3)`
   라서 어느 서비스로 걸러도 안 나온다. 막아도 **화면에서 보이던 것이 사라지지 않는다**
2. §7 이 막으라 한 것은 「배정받지 않은 서비스의 자원」이고, 서비스가 없는 행은 어느 배정에도 안 든다
3. LEARNINGS 「실패 방향을 열린 쪽으로 뒀다 — 비밀값이 「모르면 보여준다」였다」가 fail-closed 를
   이미 교훈으로 남겼다

실측 — 개발 DB 의 `test_run` 12행 중 **11행이 NULL** 이다. 그래서 이 판단을 여기 적어 둔다.

### ③ 명세 두 절이 서로를 막는다

공통/5-화면공통 §8 — 「**서비스 색은 이 띠에서만 쓴다.** 본문의 색은 언제나 판정이다」
도메인/인증 §8.8 — 「이름 · 색 | **언제든 고친다**」

각각은 맞는데 한 화면에서 만나면 **색을 안 보여주고 색을 고르게 하라**가 된다.
설정 화면은 그 색을 **만드는** 자리라 띠 미리보기가 있어야 흰 글자가 읽히는지 볼 수 있고,
그것이 명암비 경고(DESIGN.md 4.5:1)와 짝이다.

WS-E ③ 의 코드 리뷰와 화면 QA **두 렌즈가 독립적으로 같은 제안을 냈다.**
게이트 0 에서 「승인 — 명세를 먼저 고친다」를 받았다.

**이미 코드는 예외대로 돌고 있다.** `SettingsService.tsx` 가 본문에서 서비스 색을 쓴다 —
**명세만 안 따라온 상태**라 이번에 명세를 맞춘다. 화면 코드는 안 바꾼다.

---

## Plan

### 할 일 1. Dockerfile 이 `scripts/` 를 이미지에 싣는다

- **RED** — 이미지를 새로 빌드하고 `docker compose exec admin ls scripts/add-user.ts` 가 **없다**고 답하는 것을 먼저 확인한다. 검사 코드가 아니라 실측이다 — 컨테이너 빌드는 vitest 가 못 돈다
- **GREEN** — `COPY apps/admin ./apps/admin` 다음 줄에 `COPY scripts ./scripts` 를 더한다. **왜** 넣는지 한국어 주석으로 §9.2 를 가리킨다
- **REFACTOR** — 없음

**files**: `apps/admin/Dockerfile`
**depends-on**: []
**검증**: `docker compose up -d --build admin` 뒤 `docker compose exec admin npx tsx scripts/add-user.ts zztest 확인용 admin` — 비밀번호 한 줄이 찍히면 통과. **「고쳤다」로 끝내지 않는다**

---

### 할 일 2. 경로에서 서비스를 읽는 순수 함수

- **RED** — `apps/admin/src/auth/scope.test.ts` 에 DB 없이 도는 검사를 쓴다.
  `경로접두사('/api/cases/DEMO-001/history')` → `['DEMO']` · `/api/catalog/cases/ZZA-007/source` → `['ZZA']` ·
  `/api/catalog/cases` (tcId 없음) → `[]` · 접두사 모양이 아닌 것(`/api/cases/없는거/history`) → `[]`.
  파일이 없어 실패한다
- **GREEN** — `apps/admin/src/auth/scope.ts` 를 만들고 `경로접두사(path)` 를 내보낸다.
  tcId 가 박히는 경로 다섯을 정규식 하나로 받는다. 접두사 모양은 §2 의 `^[A-Z][A-Z0-9]{0,11}$` 를 쓴다
- **REFACTOR** — 없음

**files**: `apps/admin/src/auth/scope.ts`, `apps/admin/src/auth/scope.test.ts`
**depends-on**: []
**검증**: `npx vitest run apps/admin/src/auth/scope.test.ts`

---

### 할 일 3. 번호가 어느 서비스인지 DB 에 묻는다

- **RED** — `scope.test.ts` 에 `describe.skipIf(DATABASE_URL 없음)` 블록을 더한다.
  `XFS3A` 서비스의 실행을 하나 넣고 `자원의서비스({종류:'run', 번호})` → `'XFS3A'` ·
  없는 번호 → `null` · `service_id` 가 NULL 인 행 → **`'서비스없음'`**(지나보내지 않는다는 표시).
  증적 번호와 입력값 묶음 번호도 같은 것을 단언한다
- **GREEN** — `scope.ts` 에 `번호로찾을것(path, method)` (순수) 과 `자원의서비스(찾을것)` (DB) 을 더한다.
  질의 셋 — `test_run` 직접 · `evidence_document JOIN test_run` · `param_set.tc_id` 접두사
- **REFACTOR** — 세 질의가 같은 모양이면 하나로 접는다

**files**: `apps/admin/src/auth/scope.ts`, `apps/admin/src/auth/scope.test.ts`
**depends-on**: [2]
**검증**: `DATABASE_URL='postgres://platform:platform@localhost:5433/platform' npx vitest run apps/admin/src/auth/scope.test.ts`

fixture 접두사는 `XFS3`(gate.test.ts 것)을 **쓰지 않는다** — 그 파일이 `XFS3%` 로 지운다.
**`XFS5` 를 새로 쓰고 `CLAUDE.md §3` 의 접두사 줄에 적는다** (할 일 9).

---

### 할 일 4. 문이 열두 경로를 막는다

- **RED** — `gate.test.ts` 에 「배정 안 받은 서비스의 자원은 403」을 더한다.
  `xfu3-operator` 는 `XFS3A` 만 배정받았다. `XFS3B` 의 실행을 하나 넣고 —
  `GET /api/runs/<그 번호>` · `POST /api/runs/<그 번호>/abort` · `GET /api/cases/XFS3B-001/history` 가
  **403 `SERVICE_FORBIDDEN`**. 반대로 `XFS3A` 것은 지나간다. **양방향으로 단언한다**
  (LEARNINGS 「반쪽만 고치고 고쳤다고 적었다」)
- **GREEN** — `gate.ts` 의 `닿는서비스()` 를 `async` 로 바꾸고 `scope.ts` 의 둘을 붙인다.
  `preHandler` 가 이미 async 라 `await` 만 더하면 된다
- **REFACTOR** — `gate.ts` 가 300줄을 넘으면 나눈다. 지금 약 100줄이라 안 넘을 것이다

**files**: `apps/admin/src/auth/gate.ts`, `apps/admin/src/auth/gate.test.ts`
**depends-on**: [3]
**검증**: `DATABASE_URL=… npx vitest run apps/admin/src/auth/gate.test.ts`

`gate.test.ts` 의 `afterAll` 이 `DELETE FROM service WHERE prefix LIKE 'XFS3%'` 를 하는데
`test_run` 이 그 서비스를 가리키면 외래키가 막는다. **실행 행을 먼저 지우는 줄을 더한다.**

---

### 할 일 5. 케이스 목록의 마지막 결과가 남의 서비스를 안 준다 — **게이트 1 에서 뺄 수 있다**

- **RED** — `GET /api/runs/last-by-case` 를 `XFS3A` 만 배정받은 계정으로 부르면
  `XFS3B` 케이스가 **안 들어 있다**를 단언한다. 지금은 들어 있다
- **GREEN** — `lastByCase(접두사들)` 로 인자를 받고 질의에 `WHERE split_part(tc_id,'-',1) = ANY($1)` 를 건다.
  라우트가 `req.user.services` 에서 넘긴다
- **REFACTOR** — 없음

**files**: `apps/admin/src/execution/history.ts`, `apps/admin/src/execution/routes.ts`, `apps/admin/src/auth/gate.test.ts`
**depends-on**: [4]
**검증**: `DATABASE_URL=… npx vitest run apps/admin/src/execution apps/admin/src/auth`

**왜 따로 떼어 뒀나** — 이건 번호로 부르는 것이 아니라 **「전부 주는」 질의**라 문에서 못 막는다.
질의 자체에 배정 필터를 걸어야 하고 고치는 자리가 `execution/` 이다.
응답 *모양*은 안 바뀌고 항목 수만 줄어 계약 변경은 아니다 (§7 의 「일괄 조회」 그대로다).
**범위를 넓히는 일이라 게이트 1 이 정한다.**

---

### 할 일 6. §8 정본에 예외를 적는다

- **RED** — `npm run check:spec` 이 장 줄 수 불일치로 **실패**하는 것을 본다 (줄을 더한 직후)
- **GREEN** — `docs/spec/공통/5-화면공통.md` 의 「서비스 색은 이 띠에서만 쓴다」 밑에 예외를 적는다 —
  **설정 화면(§8.8)의 색 고르개와 띠 미리보기는 예외다. 거기가 이 색을 만드는 자리라
  흰 글자가 읽히는지 볼 수 있어야 하고, 그것이 명암비 4.5:1 경고와 짝이다.**
  그다음 `docs/SPEC.md` 의 장 목록 줄 수와 갈래별 합계를 고친다
- **REFACTOR** — 없음

**files**: `docs/spec/공통/5-화면공통.md`, `docs/SPEC.md`
**depends-on**: []
**검증**: `npm run check:spec`

---

### 할 일 7. 같은 규칙이 흩어진 다섯 곳을 정본 가리키게 한다 (§2.7 ①)

- **RED** — 핵심 낱말로 12장을 훑은 결과를 붙인다.
  `grep -rn "띠에서만\|판정 외의 색\|띠 밖에서\|띠에만" docs/` 가 여섯 곳을 낸다
- **GREEN** — 정본(§8)을 가리키게 고친다. **내용을 옮겨 적지 않는다**
  - `docs/spec/공통/1-제품과-구조.md:150` (§2 용어표) — 「띠 밖에서는 쓰지 않는다」 → 예외는 §8 에 있다고 가리킨다
  - `docs/spec/공통/4-데이터모델.md:17` (SQL 주석) — 「맨 위 띠에만 쓴다」 → 〃
  - `docs/DESIGN.md:28` — 예외를 한 줄 적고 §8 을 가리킨다
  - `docs/DESIGN.md:223` — 28줄과 **이미 어긋나 있다.** 둘을 맞춘다
  - `docs/design-mockup.html:15·288·359` — 「**유일한** 예외다」가 틀려진다
- **REFACTOR** — 없음

**files**: `docs/spec/공통/1-제품과-구조.md`, `docs/spec/공통/4-데이터모델.md`, `docs/DESIGN.md`, `docs/design-mockup.html`, `docs/SPEC.md`
**depends-on**: [6]
**검증**: `npm run check:spec` · `grep -rn "띠에서만\|띠 밖에서\|띠에만\|유일한 예외" docs/` 로 남은 것이 없는지 눈으로

---

### 할 일 8. §9.2 의 서비스 만들기 명령이 SETUP.md 와 어긋난다

- **RED** — 지금 §9.2 는 `add-service.ts <접두사> <이름>` 인데 `docs/SETUP.md` 는
  「`--env` 를 **하나 이상 넣는다**」로 필수라고 적는다. 둘을 나란히 놓고 확인한다
- **GREEN** — §9.2 의 명령에 `--env <키>=<주소>` 를 적고 **왜 필수인지**를 한 줄로 —
  대상 서버가 없으면 실행 설정 드롭다운이 비고 실행 요청이 400 이다 (§8.2).
  **정본은 `docs/SETUP.md` 로 두고 §9.2 는 그것을 가리킨다**
- **REFACTOR** — 없음

**files**: `docs/spec/공통/6-인프라.md`, `docs/SPEC.md`
**depends-on**: [7]
**검증**: `npm run check:spec`

---

### 할 일 9. 검사가 이 규칙들을 사후에 보게 한다 (§2.7 ④)

- **RED** — `spec-review` 체크리스트에 색 규칙 항목이 없고, A3 이 `docker-compose.yml` 만 보고
  **`Dockerfile` 은 이름이 없다.** 둘 다 확인한다
- **GREEN** — 셋을 더한다
  1. `spec-review` F 절에 `F8 | 서비스 색이나 판정 색을 맨 위 띠와 판정 밖에서 쓰는가 (§8 · DESIGN.md 원칙 1). **설정 화면(§8.8)의 색 고르개·띠 미리보기는 예외** | 예외 밖에서 쓰지 않음`
  2. `spec-review` A3 을 **`docker-compose.yml` 과 `apps/*/Dockerfile`** 로 넓힌다 — 이번 ①이 그 사각지대에서 왔다
  3. `docs/SPEC.md` 라우터 표에 「**본문에서 서비스 색을 쓰려 한다** → 공통/5-화면공통 §8 이 정본 → 설정 화면은 도메인/인증 §8.8」 한 줄.
     두 렌즈가 빠진 함정이 정확히 이 자리다
  4. `CLAUDE.md §3` 의 fixture 접두사 줄에 `XFS5`(할 일 3) 를 적는다
- **REFACTOR** — 없음

**files**: `.claude/skills/spec-review/SKILL.md`, `docs/SPEC.md`, `CLAUDE.md`
**depends-on**: [8, 3]
**검증**: `npm run check:spec` · `grep -n "F8\|Dockerfile" .claude/skills/spec-review/SKILL.md`

CLAUDE.md §2.5 표가 **`spec-review` 체크리스트와 검사기는 직접 고쳐도 된다**고 했다.
CLAUDE.md 자체는 **규칙 추가는 직접** 이고, 4번은 기존 줄에 접두사를 더하는 추가다.

---

## SPEC 동반 수정 (§2.7)

할 일 6 · 7 · 8 · 9 가 그것이다. 절별 대응은 아래와 같다.

| §2.7 | 무엇 | 어느 할 일 |
|---|---|---|
| ① 같은 규칙이 적힌 다른 절 | 낱말로 12장 훑기 → 여섯 곳 발견 | 7 |
| ② 뒤집은 결정 | **해당 없음** — 예외를 더할 뿐 뒤집는 결정이 없다 |  |
| ③ 색인 네 곳 | 새 절을 안 만들어 장 목록·절 번호·표 주인은 그대로. **분량 둘**과 **라우터 표** | 6 · 7 · 8 · 9 |
| ④ SPEC 밖 여섯 곳 | DESIGN.md · design-mockup.html · spec-review · SETUP↔§9.2 · CLAUDE.md | 7 · 8 · 9 |
| ④ 코드에 박힌 상수 | **역방향이다** — 코드(`SettingsService.tsx`)가 이미 예외대로 돌고 명세만 안 따라왔다. 코드는 안 바꾼다 |  |
| ⑤ 손으로 적은 숫자 | 색인 분량은 `check-spec-refs.mjs` 가 센다. 새로 손으로 적는 숫자를 만들지 않는다 | 6 |
| ⑥ 검사 | `check:spec` → `spec-review` H 절 → 게이트 2 | [6] 이 돈다 |

**`docs/WORKSTREAMS.md` 킥오프는 안 건드린다** — 이번 변경이 어느 갈래의 할 일 목록도 바꾸지 않는다.
넘긴 것 셋이 닫혔다는 기록은 [7] 병합 단계가 진행 기록에 남긴다 (LEARNINGS 「정리를 구현 단계에 넣을 뻔했다」).

## Plan 메타

할 일 9개 · 예상 묶음 5개 · 구현 규율: TDD · 추가 검증: `npm run check:deps` · `typecheck` ·
`check:workflow` · `check:spec` · `check:tests` · `check:secret-names` · `vite build` ·
`DATABASE_URL=… npm test` **연속 3회** (DB 를 건드린다)

묶음 — ①{1, 2, 6} ②{3, 7} ③{4, 8} ④{5, 9} ⑤{}

## 리뷰 결과

(계획 검토가 채운다)
