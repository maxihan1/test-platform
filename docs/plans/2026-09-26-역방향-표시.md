# 역방향 WS-작성 ③-2 — 표시 (워드 메모 사본 · 피그마 댓글)

등급: 2 · 갈래: WS-작성 · 2026-09-26

## 도메인 정리

- BC: 작성 — 에이전트 스크립트(`scripts/authoring-*.ts`) · 자식 스킬(`.claude/skills/tpx-author/references/reverse.md`)
- 명세: 도메인/작성 §3.6 「★ 역방향」 표 「표시」(워드는 메모 단 사본 · 피그마는 그 화면에 댓글 · PDF 는 차이 목록만) · 「표시는 실패해도 요청을 실패시키지 않는다」 · §7 `outputs`(MARKED 는 `source` 필수, 원본과 같은 확장자) · `diffs[].marked`·`markError`
- SETUP §8 이 피그마 댓글 쓰기 권한(`file_comments:write`)과 전용 계정을 이미 안내한다
- **계약 변경 없음** — 서버로 가는 `diffs` 모양 그대로. 에이전트↔자식 `diffs.json` 에 표시 자리 칸(`asset`·`anchor`·`node`)만 더하고 서버로 보내기 전에 뺀다
- jszip 정식 의존성 — 2026-09-26 사용자 승인(이미 exceljs 를 따라 설치돼 있다)

| 자료 | 표시 | 못 하면 `markError` |
|---|---|---|
| `.docx` | 메모 단 사본 → `outputs` MARKED `source=<자료 번호>` · 이름 `<원래 이름>-표시.docx` | 문장을 못 찾음 · 파일을 못 읽음 · 못 올림 |
| `.doc` | 안 한다 | 「옛 워드(.doc)에는 표시하지 못한다」 — 서버 pandoc 이 못 읽는다 |
| `.pdf` | 안 한다 | 「PDF 표시는 아직 안 한다」 — 새 패키지 승인 뒤 |
| `.md`·`.txt` | 안 한다 | 「이 파일 종류에는 표시하지 않는다」 |
| 피그마 | 그 노드에 댓글 | 403 「피그마 토큰에 댓글 쓰기 권한이 없다」 · 404 · 그 밖 코드 |

## 왜

③-1 은 차이를 목록으로만 보고하고 `marked:false · 표시는 아직 안 한다` 를 박았다. 명세가 정한 「원본에 표시해 넘긴다」가 아직 없다.
기획자는 차이 목록 화면보다 **자기 기획서에 달린 메모**로 받는 편이 고치기 쉽다.

**원본은 서버에서 다시 받는다.** 자료 폴더의 사본은 자식이 바꿀 수 있다 — 거기서 만들면 자식이 넣은 내용이 「표시 사본」으로 올라간다.
**표시 글은 push 전에 비밀번호 검사를 이미 거친 차이 글에서만 만든다** — 자리 칸(`anchor` 등)도 `글모두` 가 같이 본다.

## Plan

### 할 일 1. jszip 정식 의존성

- **RED** — `npm ls jszip --depth=0` 이 빈 목록(지금은 exceljs 를 따라서만 있다)
- **GREEN** — `package.json` dependencies 에 `"jszip": "^3.10.1"` · `package-lock.json` 갱신(`npm install jszip@^3.10.1` — 새로 받는 것 없음)
- **REFACTOR** — 없음

**files**: package.json, package-lock.json
**depends-on**: []
**검증**: `npm ls jszip --depth=0 && npm run check:deps`

### 할 일 2. 차이 파일의 표시 자리 칸

- **RED** — `차이정리` 가 `asset`(양의 정수) · `anchor`(글, 300자로 자름) · `node`(`^\d+[:-]\d+$`)를 **표시 자리**로 받고 모양이 틀리면 버린다(사유 아님) · `보낼차이(diffs)` 가 그 칸을 빼고 서버 모양만 준다 · `글모두` 가 `anchor` 도 본다
- **GREEN** — `authoring-reverse.ts` 의 `차이` 에 `표시?: { asset: number | null; anchor: string | null; node: string | null }` · `보낼차이`
- **REFACTOR** — `authoring-upload.ts` 의 finish 에 `보낼차이`

**files**: scripts/authoring-reverse.ts, scripts/authoring-reverse.test.ts, scripts/authoring-upload.ts
**depends-on**: []
**검증**: `npx vitest run scripts/authoring-reverse.test.ts`

### 할 일 3. 표시 계획 — 차이를 자료별로 가른다

- **RED** — `표시계획(diffs, 입력자료)` 가 `.docx` 자료별 메모 묶음 · 피그마 자료별 댓글 묶음을 주고, 나머지 차이에는 위 표의 `markError` 를 준다 · `asset` 이 없거나 입력 자료가 아니면 「어느 자료의 차이인지 적혀 있지 않다」
- **GREEN** — 새 파일 `scripts/authoring-mark.ts`(순수)
- **REFACTOR** — 없음

**files**: scripts/authoring-mark.ts, scripts/authoring-mark.test.ts
**depends-on**: [2]
**검증**: `npx vitest run scripts/authoring-mark.test.ts`

### 할 일 4. 메모 글

- **RED** — `메모글(차이)` — DIFFERENT `[D1] 화면과 다름 — 화면: 확인 (PAY-012)` · SCREEN_ONLY `[D2] 문서에 없음 — 화면: 쿠폰 칸` · DOC_ONLY `[D3] 화면에 없음`
- **GREEN** — 같은 파일
- **REFACTOR** — 없음

**files**: scripts/authoring-mark.ts, scripts/authoring-mark.test.ts
**depends-on**: [3]
**검증**: `npx vitest run scripts/authoring-mark.test.ts`

### 할 일 5. 워드 메모 달기 (jszip)

- **RED** — 검사가 jszip 으로 작은 docx 를 지어 넣는다. `메모달기(바이트, 메모들)` 가 ⑴ 그 문장이 든 문단에 `commentRangeStart`·`commentRangeEnd`·`commentReference` ⑵ `word/comments.xml` · 관계 파일 · `[Content_Types].xml` 등록 ⑶ **기존 메모가 있으면 번호를 이어 붙인다** ⑷ 문장이 여러 글 조각(`w:t`)에 걸쳐도 찾는다 ⑸ `anchor` 가 없으면 마지막 문단 ⑹ 못 찾은 메모는 결과에 「못 찾음」 ⑺ 메모 글의 `<`·`&` 를 이스케이프 ⑻ 원본 문단 글은 그대로다(다시 풀어 확인) ⑼ `document.xml` 이 상한(10MB)을 넘으면 거절
- **GREEN** — 같은 파일에 jszip 을 쓰는 함수 하나(파일·네트워크 없이 바이트 → 바이트)
- **REFACTOR** — 파일 300줄 넘으면 `authoring-docx.ts` 로

**files**: scripts/authoring-mark.ts, scripts/authoring-mark.test.ts
**depends-on**: [1, 4]
**검증**: `npx vitest run scripts/authoring-mark.test.ts`

### 할 일 6. 피그마 댓글 요청

- **RED** — `피그마댓글요청(figmaUrl, node, 글)` 이 저장된 주소(`https://www.figma.com/design/<키>/?node-id=12-34`)에서 키를 뽑아 `https://api.figma.com/v1/files/<키>/comments` 와 `{ message, client_meta: { node_id: '12:34', node_offset: { x: 0, y: 0 } } }` 를 준다 · 차이의 `node` 가 자료의 노드보다 앞선다 · 노드가 없으면 `client_meta` 없이(파일 전체 댓글) · 키 모양이 아니면 null · `피그마실패사유(status)`
- **GREEN** — 같은 파일
- **REFACTOR** — 없음

**files**: scripts/authoring-mark.ts, scripts/authoring-mark.test.ts
**depends-on**: [3]
**검증**: `npx vitest run scripts/authoring-mark.test.ts`

### 할 일 7. 표시 껍데기 — 원본 다시 받기 · 메모 사본 올리기 · 댓글 달기

- **RED** — 껍데기는 검사를 안 붙인다. 부르는 쪽의 결과 합치기 `표시결과합치기(diffs, 결과들)` 를 순수로 RED(차이 번호별 `marked`·`markError`)
- **GREEN** — 새 껍데기 `scripts/authoring-marking.ts`: 자료 파일을 **서버에서 다시 받는다**(`GET …/assets/:id`) → `메모달기` → `outputs` MARKED(`산출물주소` 에 `source`) · 피그마는 `X-Figma-Token` 으로 POST(시간 제한). 모든 예외를 잡아 그 차이의 `markError` 로. `authoring-upload.ts` PR 뒤 try 블록에서 부른다. `authoring-run.ts` 가 입력 자료 목록과 피그마 토큰을 넘긴다
- **REFACTOR** — upload·run 300줄 확인

**files**: scripts/authoring-marking.ts, scripts/authoring-mark.ts, scripts/authoring-mark.test.ts, scripts/authoring-upload.ts, scripts/authoring-run.ts
**depends-on**: [5, 6]
**검증**: `npm run typecheck && npx vitest run scripts`

### 할 일 8. 자식에게 표시 자리를 적게 한다

- **RED** — `자료목록글` 이 파일마다 `(자료 번호 N)` 을 싣는다 · 피그마 줄에도
- **GREEN** — `authoring-assets.ts` · `reverse.md` §5 에 `asset`·`anchor`(기획서 문장을 **그대로** 20~120자)·`node` 칸과 예시
- **REFACTOR** — 없음

**files**: scripts/authoring-assets.ts, scripts/authoring-assets.test.ts, .claude/skills/tpx-author/references/reverse.md
**depends-on**: [2]
**검증**: `npx vitest run scripts/authoring-assets.test.ts && npm run check:workflow`

### 할 일 9. §2.7 동반 수정

- **RED** — `npm run check:spec`
- **GREEN** — WORKSTREAMS ③-2 반영 완료 · 진행 기록
- **REFACTOR** — 없음

**files**: docs/WORKSTREAMS.md, docs/progress/WS-작성.md
**depends-on**: [7, 8]
**검증**: `npm run check:spec`

## SPEC 동반 수정 (§2.7)

해당 없음 — SPEC 안 건드림(명세 §3.6 표가 이미 이 동작을 적었다). SETUP §8 은 댓글 권한을 이미 안내한다. SPEC 밖은 할 일 8·9.

## 결정할 것 (게이트 1)

- **`anchor` 없는 차이(주로 「화면에만 있다」)를 워드 어디에 다나** — 추천 **마지막 문단**에 단다. 자리가 없으면 표시 자체를 못 해 기획자가 워드만 보면 그 차이를 모른다. 대안은 표시 안 함(`markError`)
- **메모 작성자 이름** — 추천 `테스트 플랫폼`(고정). 요청자 이름을 쓰면 사람이 직접 단 것처럼 보인다

## Plan 메타

할 일 9개 · 예상 묶음 6개 · 구현 규율: TDD · 추가 검증: `npm run typecheck`

## 리뷰 결과

(계획 검토가 채운다)
