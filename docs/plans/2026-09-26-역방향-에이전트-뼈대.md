# 역방향 WS-작성 ③-1 — 작성 에이전트 뼈대

등급: 2 · 갈래: WS-작성 · 2026-09-26

## 도메인 정리

- BC: 작성 — 에이전트 스크립트(`scripts/authoring-*.ts`)와 자식 스킬(`.claude/skills/tpx-author`)
- 명세: 도메인/작성 §3.6 「★ 역방향」(로그인 · 출처 규칙 · 훑기 허용 목록 · 남는 한계) · §7(집기 `target` · `outputs` · `finish` 의 `diffs`)
- 서버(PR #79)와 화면(PR #80)은 끝났다. 이 PR 은 **에이전트가 그 통로를 실제로 쓰게** 한다
- 표시(워드 메모 사본 · 피그마 댓글)는 ③-2 다(사용자 2026-09-26 — jszip 정식 의존성 승인). PDF 스티커는 여전히 차이 목록만

| 무엇 | 어디서 |
|---|---|
| 집을 때 대상 다시 대조(줄 없음 · 계정 빠짐 · 시작 주소 출처 어긋남 → FAILED) | 순수 `authoring-reverse.ts` · 껍데기 `authoring-run.ts` |
| 계정·주소는 자식 **환경 변수**로만(`TARGET_*`) — 프롬프트에 값 없음 | 같음 |
| 화면만(입력 0 + 시작 주소) 돌기 · 재실행이 원본의 `INPUT` 만 읽기 | `authoring-assets.ts` |
| 자식이 `out/diffs.json` · `out/reverse-spec.md` 를 쓴다 → 에이전트가 읽고 비밀번호 원문 검사 → pandoc 으로 `.docx` → `outputs` → `finish` 의 `result.diffs` | 순수 `authoring-reverse.ts` · 껍데기 `authoring-upload.ts` |
| 자식 절차 | `tpx-author` 스킬 |

## 왜

역방향 요청이 서버와 화면에는 서지만 에이전트는 `target` 을 모르고, 입력 0 인 화면만 요청은 「자료도 본문도 없다」로 실패한다.
명세의 「남는 한계」가 에이전트에게 맡긴 비밀번호 원문 검사와 출처 재대조도 아직 없다.

**역기획서를 자식이 워드로 바로 쓰지 않고 마크다운으로 쓰는 이유** — 비밀번호 검사가 글자를 찾는데 `.docx` 는 압축이라
원문이 들어 있어도 안 보인다. 에이전트가 마크다운을 검사한 뒤 pandoc 으로 바꾼다(변환은 자식 uid · 토큰 없는 환경 — 기존 자료 변환과 같은 규율).

## Plan

### 할 일 1. 집을 때 대상 다시 대조 — `대상점검`

- **RED** — `authoring-reverse.test.ts`: `target` 없음 → null(정방향) · `baseUrl` null → 「대상 서버 줄이 없어졌다」 · 아이디나 비밀번호가 비었거나 null → 「테스트 계정이 빠졌다」 · `startUrl` 출처가 `baseUrl` 과 다르거나 http·https 아님 → 「시작 주소가 대상 서버와 어긋난다」 · 정상 → null. **사유 글자에 비밀번호·아이디 원문이 없다**
- **GREEN** — `scripts/authoring-reverse.ts` 에 `대상` 타입 · `대상점검(target)`
- **REFACTOR** — 없음

**files**: scripts/authoring-reverse.ts, scripts/authoring-reverse.test.ts
**depends-on**: []
**검증**: `npx vitest run scripts/authoring-reverse.test.ts`

### 할 일 2. 자식 환경 변수 — `대상환경`

- **RED** — `대상환경(target)` 이 `TARGET_ENV · TARGET_BASE_URL · TARGET_LOGIN_ID · TARGET_LOGIN_PASSWORD` 와 (있으면) `TARGET_START_URL` 을 준다 · 없는 값은 키를 안 싣는다
- **GREEN** — 같은 파일에 함수 하나
- **REFACTOR** — 없음

**files**: scripts/authoring-reverse.ts, scripts/authoring-reverse.test.ts
**depends-on**: [1]
**검증**: `npx vitest run scripts/authoring-reverse.test.ts`

### 할 일 3. 화면만 돌기 · 원본은 `INPUT` 만

- **RED** — `authoring-assets.test.ts`: `돌릴수있나` 가 대조 + 시작 주소 + 자료 0 이면 null · 대조인데 시작 주소 없이 자료 0 이면 지금처럼 막는다 · `입력만(자료들)` 이 `role` 이 `MARKED`·`REVERSE_SPEC` 인 것을 뺀다(없으면 입력)
- **GREEN** — `자료` 타입에 `role?` · `입력만` · `돌릴수있나` 에 `화면만?: boolean`
- **REFACTOR** — 없음

**files**: scripts/authoring-assets.ts, scripts/authoring-assets.test.ts
**depends-on**: []
**검증**: `npx vitest run scripts/authoring-assets.test.ts`

### 할 일 4. 줄 프롬프트 역방향 절

- **RED** — `authoring-rules`(기존 검사 파일): `target` 이 있으면 프롬프트에 「역방향」 · 대조/화면만 구분 · `TARGET_*` 이름 · 산출물 폴더 경로가 있고 **비밀번호·아이디 원문은 없다** · 화면만이면 「기획서 없음」 줄(빈 `--- 기획서 ---` 를 안 싣는다) · 정방향은 지금 글과 같다
- **GREEN** — `줄프롬프트` 에 역방향 인자 하나(`{ 대조: boolean; 화면만: boolean; 산출물폴더: string }`)
- **REFACTOR** — 없음

**files**: scripts/authoring-rules.ts, scripts/authoring-agent.test.ts
**depends-on**: [1]
**검증**: `npx vitest run scripts/authoring-agent.test.ts`

### 할 일 5. 산출물 읽기 — `차이파일풀기`

- **RED** — `차이파일풀기(글)`: JSON 배열이면 객체 줄만 · 배열이 아니거나 JSON 이 아니면 사유(「차이 목록 파일 모양이 틀렸다」) · 없으면(`null`) 빈 목록 · 줄마다 `no`·`kind` 가 없으면 사유
- **GREEN** — 같은 파일
- **REFACTOR** — 없음

**files**: scripts/authoring-reverse.ts, scripts/authoring-reverse.test.ts
**depends-on**: [2]
**검증**: `npx vitest run scripts/authoring-reverse.test.ts`

### 할 일 6. 비밀번호 원문 검사 — `계정섞였나`

- **RED** — 비밀번호가 PR 본문 · 케이스 파일 · `diffs.json` 글 · `reverse-spec.md` 중 하나에 있으면 참. 4자 미만 비밀번호도 본다(테스트 계정은 짧을 수 있다 — 피그마 토큰의 8자 문턱을 그대로 쓰지 않는다) · 빈 비밀번호는 거짓
- **GREEN** — `authoring-reverse.ts` 에 함수 하나 (`비밀섞였나` 는 토큰 전용 문턱이 있어 따로 둔다)
- **REFACTOR** — 없음

**files**: scripts/authoring-reverse.ts, scripts/authoring-reverse.test.ts
**depends-on**: [5]
**검증**: `npx vitest run scripts/authoring-reverse.test.ts`

### 할 일 7. 껍데기 연결 — 집기 대조 · 환경 · 올리기 앞 검사 · outputs · finish

- **RED** — 껍데기는 검사를 안 붙인다(CLAUDE.md §3 「얇은 껍데기」). 대신 `outputs` 부르는 주소·질의를 만드는 순수 함수 `산출물주소(id, 서비스, 이름)` 와 `끝내기몸(prUrl, diffs)` 를 할 일 5 파일에 두고 RED
- **GREEN** — `authoring-run.ts`: 집은 뒤 `대상점검` → FAILED · 자식 환경에 `대상환경` · 재실행 원본 자료는 `입력만` · 화면만 · 산출물 폴더 만들기(자식 소유). `authoring-upload.ts`: push 전에 `out/` 읽기(모양보기로 링크·비일반 거부) → `계정섞였나` → FAILED(값 없는 사유) · PR 뒤 역기획서 pandoc(자식 uid) → `outputs` → `finish({ status:'DONE', prUrl, result:{ diffs } })`
- **REFACTOR** — 두 파일 300줄 이하 확인

**files**: scripts/authoring-run.ts, scripts/authoring-upload.ts, scripts/authoring-reverse.ts, scripts/authoring-reverse.test.ts
**depends-on**: [3, 4, 6]
**검증**: `npm run typecheck && npx vitest run scripts`

### 할 일 8. `tpx-author` 역방향 절차

- **RED** — 스킬 줄 수 검사(`check:workflow` 또는 스킬 200줄 검사)가 넘지 않게. 절차는 `references/reverse.md` 로 떼고 SKILL.md 에는 가리키는 줄만
- **GREEN** — 훑기 허용 목록(링크 이동 · 탭 · 펼치기 · 팝업) · 폼 제출·저장·삭제·로그아웃 금지 · 계정 입력 **직전** 출처가 `TARGET_BASE_URL` 출처인지 확인 · 다른 출처 링크·리다이렉트 안 따라감 · 차이 처리 표(같다 → 정식 / 다르다·화면에만 → 미확정 `unconfirmed: '… 차이 Dn (작성 요청 N)'` / 문서에만 → 정식) · 계정은 `params` 비밀값 칸(K9) · `out/diffs.json` 모양 · 화면만은 전부 미확정 + `out/reverse-spec.md`(항목마다 「확인 필요」) · 결과 요약에 차이 수
- **REFACTOR** — 없음

**files**: .claude/skills/tpx-author/SKILL.md, .claude/skills/tpx-author/references/reverse.md
**depends-on**: [7]
**검증**: `npm run check:workflow`

### 할 일 9. §2.7 동반 수정

- **RED** — `npm run check:spec`
- **GREEN** — WORKSTREAMS 킥오프(③-1 반영 완료 · ③-2 남은 것) · 진행 기록
- **REFACTOR** — 없음

**files**: docs/WORKSTREAMS.md, docs/progress/WS-작성.md
**depends-on**: [8]
**검증**: `npm run check:spec`

## SPEC 동반 수정 (§2.7)

해당 없음 — SPEC 안 건드림. 명세 §3.6·§7 이 이미 이 동작을 적었다. SPEC 밖은 할 일 9(WORKSTREAMS · 진행 기록).

## 결정할 것 (게이트 1)

- **산출물 올리기가 실패하면** — 케이스 PR 은 이미 섰다. 추천 **요청은 DONE 으로 끝내되 `error` 에 「역기획서를 못 올렸다」를 남긴다** — 명세 「표시는 실패해도 요청을 실패시키지 않는다」와 같은 결. FAILED 로 하면 이미 선 PR 이 실패 요청에 매달린다
- **`diffs.json` 이 모양이 틀리면** — 추천 **FAILED 가 아니라 diffs 없이 DONE + `error` 에 사유.** 케이스는 멀쩡하다

## 게이트 1 반영 (2026-09-26 사용자 — 지적 반영하고 진행)

| 무엇 | 정한 것 | 어디에 |
|---|---|---|
| BLOCKER 1 | 컨테이너 pandoc 이 2.9.2.1 이라 `--sandbox` 가 없다(실측). 대신 둘을 겹친다 — ⒜ 원고에 그림 문법(`![`)이 있으면 거절(pandoc 마크다운이 바깥 파일을 끌어오는 길은 그림뿐, 그림이 없으면 원격 받기도 없다) ⒝ 바꾼 `.docx` 를 다시 `pandoc -t plain` 으로 글자로 돌려 **올릴 파일 자체**에서 비밀번호를 찾는다. 변환은 서버에서 자식 uid, 맥에서도 `env: 자식빈환경` 명시. 인자 배열은 순수 함수 | 할 일 6·7 |
| 주의 1 | 자리 `자리.자료/out` (트리 밖 · 자식 소유). 읽기 전 `lstat` 으로 일반 파일 · `nlink === 1` · 크기 상한(차이 1MB · 원고 1MB) | 할 일 7 |
| 주의 2 | 허용 키만(`no·kind·where·doc·screen·tcId`) · `kind` 셋만 · 글 길이 상한 · ③-1 은 `marked:false` 와 `markError: '표시는 아직 안 한다'` 를 에이전트가 강제 | 할 일 5 |
| 주의 3 | 역방향이면 `error` 로 나가는 모든 글을 `계정섞였나` 로 거르고, 걸리면 고정 글 | 할 일 6·7 |
| 주의 4 | `reverse.md` 에 「계정 값을 명령 인자·출력으로 펼치지 말고 스크립트 안에서 `process.env` 로 읽어라」 | 할 일 8 |
| 주의 5 | 「검사할 글 모음 → 사유」 순수 함수 `올리기전검사` RED · 재대조에 `baseUrl` http·https · **비밀번호 4자 미만은 FAILED**(새는지 검사할 수 없다 — 이 세션의 기본값) · 화면만인데 원고가 없으면 역기획서 없이 DONE + 이유 · `authoring-rules.ts` 는 줄 수 보며 필요하면 역방향 글을 `authoring-reverse.ts` 로 | 할 일 1·4·6·7 |
| 부분 실패 | **DONE + `error` 에 이유**(사용자 — 역기획서 못 올림 · 차이 파일 모양 틀림). 케이스 PR 은 머지할 수 있다 | 할 일 7 |

## Plan 메타

할 일 9개 · 예상 묶음 6개(1·3 병렬, 이후 직렬) · 구현 규율: TDD · 추가 검증: `npm run typecheck`

## 리뷰 결과

**렌즈**: plan-eng-review (2등급 = 1종) · 2026-09-26
**판정**: BLOCKER 1건 · 주의 5건

### BLOCKER 1 — 역기획서 `.docx` 가 비밀번호 검사를 비껴간다
pandoc 은 `![](../x.txt)` 같은 참조를 docx 에 끌어 넣는다(원격이면 받아 오기까지). md 만 검사하면 비밀번호를 적은 파일을 가리켜 docx 로 올릴 수 있다.
맥(자식 null)에서는 `친다(..., {})` 가 부모 환경(토큰 포함)을 그대로 물려준다.
고칠 것 — `pandoc --sandbox` · 맥에서도 `env: 자식빈환경` 명시. 인자 배열을 만드는 순수 함수로 빼서 RED.

### 주의 1 — out/ 자리 · 읽는 방식
트리 안이면 push 가 「테스트만 아님」으로 거부, 트리 밖이면 `파일거부사유` 기준이 다르다. 하드링크는 `lstat` 이 일반 파일이라 통과하고 root 가 읽는다. 크기 상한 없음.
고칠 것 — 자리를 `자리.자료/out` 으로 적고, 자식 uid 로 읽거나 링크 수(`nlink`)·크기 상한을 본다.

### 주의 2 — diffs 를 서버가 안 본다 · 자식이 `marked: true` 로 거짓말
고칠 것 — 허용 키만 남기고 `kind` 는 셋만, ③-1 은 `marked:false` · `markError` 를 에이전트가 강제(표시는 ③-2).

### 주의 3 — 사유 글에 원문이 섞일 수 있다
`claude 를 못 띄웠다: <stderr>` · pandoc·outputs 실패 사유가 `error` 로 나간다. 고칠 것 — 역방향이면 이 셋도 `계정섞였나` 를 거치거나 고정 글.

### 주의 4 — 자식이 비밀번호를 명령 인자로 펼치면 `ps`·`/proc/*/cmdline` 으로 다른 자리 자식이 읽는다
고칠 것 — `reverse.md` 에 「값을 인자·출력으로 펼치지 말고 스크립트 안에서 `process.env` 로 읽어라」.

### 주의 5 — 검사 순서가 껍데기에만 있다
고칠 것 — 「검사할 글 모음 → 사유」 순수 함수 RED. 곁가지: 재대조에 `baseUrl` http·https · 1~3자 비밀번호는 거의 모든 글에 걸린다 · 화면만인데 `reverse-spec.md` 가 없을 때 · `authoring-rules.ts` 255줄.

### 통과한 것
계정은 환경 변수로만 · 재대조 세 조건 · `입력만` 해 없음 · push 전 검사가 자식을 거둔 뒤 · pandoc 자식 uid 방향 · DONE+error 를 서버가 받는다 · run·upload 300줄 안
