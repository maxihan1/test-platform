# 역방향 WS-F — 대상 서버 줄의 테스트 계정

등급: 3 · 갈래: F · 2026-09-26

## 도메인 정리

- BC: 인증·설정. 칸은 PR #74 가 만든 `service_env.login_id` · `login_password`
- 정본: 도메인/인증 §7 설정 API 「envs[] 한 줄」 · 같은 자리 계약 블록 · §8.8 「대상 서버」
- 영향

| 자리 | 바뀌는 것 |
|---|---|
| `settings/routes.ts` 입력 스키마 | `envs[]` 줄에 `loginId?` · `loginPassword?` (문자열 또는 null) |
| `settings/store.ts` `대상서버넣기` · 목록 질의 | 비밀번호 유지 규칙 · 응답 `loginId` · `hasLoginPassword` (비밀번호 원문은 안 싣는다) |
| `/api/auth/me` envs | 안 바뀐다 — 계정을 싣지 않는다(인증 §7 101줄) · `hasLogin` 도 안 넣는다(사용자 결정) |
| `auth/gate.ts` · `auth/scope.ts` | **이번에 안 고친다** — `outputs` 세 줄은 WS-작성으로 넘긴다(사용자 결정) |
| 화면(WS-E) | 설정 > 서비스의 계정 칸. 이번에 안 고친다 |

## 왜

역방향 에이전트가 대상 서버 화면에 로그인하려면 계정이 필요하다. 저장은 대상 서버 줄을 **전부 지우고 다시 넣기** 때문에,
화면이 받은 적 없는 비밀번호를 되돌려 보내지 못하면 주소 하나만 고쳐도 모든 줄의 비밀번호가 사라진다 — 그래서 「키 없음 = 유지」다.

사용자 결정 (2026-09-26, [2])
- `outputs` 를 등급표·토큰 통로·라우트표에 넣는 일은 통로를 만드는 WS-작성으로 넘긴다. 표만 먼저 넣으면 `gate.test.ts` 「표에 있는데 소스에 없는 자리」 검사가 빨강이다
- `/api/auth/me` 에 `hasLogin` 을 넣지 않는다. 계정 없는 줄은 서버가 400 `BAD_ENV` 로 거절한다(작성 §3.6)

**게이트 1 에서 확인할 빈칸** — 명세는 `loginPassword` 만 「키 없음 = 유지」로 정했다. `loginId` 키가 없을 때는 적혀 있지 않다.
지금 설정 화면(WS-E 전)은 `loginId` 를 모르니 서비스를 저장할 때마다 아이디가 지워진다. **제안: `loginId` 도 같은 규칙(키 없음 = 유지, null·빈 글자 = 지움)**

## 게이트 1 반영 (2026-09-26, 사용자 — 지적 반영하고 진행 · 저장 규칙 둘 다 더함)

- `loginId` 도 키 없음 = 유지 · `loginId`·`loginPassword` 빈 글자(공백뿐 포함) = 지움(null 과 같다). 인증 §7 「화면이 그대로 되돌려 보낸다」 문장은 새 규칙으로 바꾼다
- 할 일 2 RED ① 은 화면 모양 `{ env, baseUrl }` 만 보내도 아이디·비밀번호가 남는지 · 비밀번호 판정은 `login_password IS NOT NULL AND login_password <> ''` · 기존 값은 같은 `client` 로 읽는다(왜 주석)
- 할 일 1 RED 에 누출 검사 둘 — `/api/auth/me` 응답과 틀린 입력(`baseUrl: ''`)의 400 본문에 원문이 없다
- 할 일 3 — 계약 블록을 둘로(설정 envs[] 반영 완료 · outputs 세 줄 대기) · 「env 이름을 바꾸면 새 줄이라 계정을 다시 넣는다」 한 줄 · WS-E 킥오프에 이름 바꾸기 안내와 BAD_ENV 문구

## Plan

### 할 일 1. 대상 서버 줄에 테스트 계정을 저장하고, 응답은 아이디와 「비밀번호 설정됨」만 준다

- **RED** — 새 파일 `settings/testAccount.test.ts`(접두사 `XFS7`): `envs: [{ env:'qa', baseUrl, loginId:'tester', loginPassword:'pw-원문' }]` 로 만들면 목록 줄이 `{ env, baseUrl, loginId:'tester', hasLoginPassword:true }` 이고 **응답 본문 어디에도 `pw-원문` 이 없다**. 계정 없는 줄은 `{ loginId:null, hasLoginPassword:false }`. 기존 `routes.test.ts:66` 의 `toEqual([{ env, baseUrl }])` 은 계약대로 두 칸이 붙는다. 지금은 스키마가 키를 버려 실패
- **GREEN** — zod `대상서버` 에 두 칸, `대상서버넣기` INSERT 에 두 칸, 목록 `json_build_object` 에 `loginId` · `hasLoginPassword`, 타입 `대상서버입력`(입력) / `대상서버`(응답) 분리
- **REFACTOR** — 없음

**files**: apps/admin/src/settings/routes.ts, apps/admin/src/settings/store.ts, apps/admin/src/settings/testAccount.test.ts, apps/admin/src/settings/routes.test.ts
**depends-on**: []
**검증**: DATABASE_URL=<검사용 DB> npx vitest run apps/admin/src/settings/

### 할 일 2. 다시 저장할 때 키가 없으면 유지하고 null·빈 글자면 지운다

- **RED** — 같은 파일: 계정 넣은 서비스를 ① `envs: [{ env:'qa', baseUrl:새주소 }]` 로 PATCH → 비밀번호·아이디 유지, 주소만 바뀜 ② `loginPassword: null` → `hasLoginPassword:false` ③ `loginPassword: ''` → 지움 ④ 다른 env 키 줄은 이어받지 않는다 ⑤ 새 비밀번호를 보내면 바뀐다(DB 값으로 확인). 지금은 지우고 다시 넣어 ① 이 실패
- **GREEN** — `대상서버넣기` 가 지우기 전에 같은 서비스의 `env → { login_id, login_password }` 를 읽고, 키가 없으면 그 값, null·빈 글자면 NULL, 문자열이면 그 값
- **REFACTOR** — 없음

**files**: apps/admin/src/settings/store.ts, apps/admin/src/settings/testAccount.test.ts
**depends-on**: [1]
**검증**: DATABASE_URL=<검사용 DB> npx vitest run apps/admin/src/settings/testAccount.test.ts

### 할 일 3. 명세·문서 동반 수정

- **RED** — 해당 없음(문서). `npm run check:spec`
- **GREEN** — 아래 「SPEC 동반 수정」
- **REFACTOR** — 없음

**files**: docs/spec/도메인/인증.md, docs/WORKSTREAMS.md, CLAUDE.md, docs/progress/WS-F.md, docs/SPEC.md
**depends-on**: [2]
**검증**: npm run check:spec

## SPEC 동반 수정 (§2.7)

- **같은 규칙 찾기** — `grep -rln "loginPassword\|hasLoginPassword\|login_password\|테스트 계정"` → 인증.md · 작성.md · 공통/4-데이터모델.md · 공통/2-명세선언.md · SPEC.md · WORKSTREAMS.md. 정본은 인증 §7 「envs[] 한 줄」(「이 규칙의 정본은 여기다」). 나머지는 가리키기만 하는지 눈으로 본다
- **인증 §7** — 빈 글자 = 지움(피그마 토큰과 같다) · (게이트 1 확정 시) `loginId` 도 같은 유지 규칙 한 줄 · 계약 블록 `상태: 반영 완료 (2026-09-26, PR #77 설정 envs[] — outputs 세 줄은 WS-작성)` 꼴로 남은 몫을 괄호에
- **색인** — 새 절 없음, 분량만. 라우터 표 90줄이 이미 인증 §7 · §8.8 을 가리킨다 — 해당 없음
- **SPEC 밖** — WORKSTREAMS WS-F 줄 → 반영 완료 · WS-작성 줄에 `outputs` 세 줄(등급표·토큰 통로·라우트표) · WS-E 줄에 「계정 없는 줄은 목록에서 안 거른다 — 서버 `BAD_ENV` 안내 문구」 · CLAUDE.md fixture 줄에 `XFS7` · SETUP·HOOKS·DESIGN·WORKFLOW·mockup 해당 없음(화면은 WS-E) · 코드 상수 해당 없음
- **숫자 빼기** — 해당 없음

## Plan 메타

할 일 3개 · 예상 묶음 3개(직렬) · 구현 규율: TDD · 추가 검증: DATABASE_URL 붙인 `npx vitest run apps/admin/src/settings/ apps/admin/src/auth/` · `npm run typecheck` · `npm run check:spec`

## 리뷰 결과

**렌즈**: plan-eng-review · plan-ceo-review (3등급 = 2종) · 2026-09-26
**판정**: BLOCKER 0건 · 주의 5건 (겹친 것은 하나로)

### 주의 1 — loginId 유지 규칙은 필요하고, 인증 §7 의 「화면이 그대로 되돌려 보낸다」는 사실이 아니다 (두 렌즈)
지금 설정 화면 `web/SettingsEnvs.tsx:26` `보낼모양` 이 줄을 `{ env, baseUrl }` 로 잘라 보낸다. 규칙 없이는 저장마다 아이디가 지워진다. 게이트 1 확정 뒤 그 문장을 새 규칙으로 바꾼다(§2.7 ②). RED ① 에 「화면 모양만 보내도 아이디가 남는다」

### 주의 2 — 「비밀번호 빈 글자 = 지움」도 명세에 없는 계약 변경이다 (ceo)
명세는 null = 지움만 적었다. 웹훅·피그마 토큰(`store.ts:190-195`)과 같은 꼴이라 일관되지만 게이트 1 확인 항목으로

### 주의 3 — env 이름을 바꾸면 계정이 조용히 사라진다 (두 렌즈)
동작은 명세대로 둔다. 인증 §7 에 「이름을 바꾸면 새 줄이라 계정을 다시 넣는다」 한 줄 · WS-E 킥오프에 안내 항목

### 주의 4 — 비밀번호 원문 누출 검사가 목록 응답 하나뿐 (두 렌즈)
`/api/auth/me` 응답과 틀린 입력의 400 본문(`detail`)에도 원문이 없는지 본다. 지금 새는 길은 없다(zod 4 는 입력값을 메시지에 안 싣는다 · Fastify 로거는 본문을 안 찍는다) — 나중에 바뀌는 것을 잡으려는 것

### 주의 5 — 계약 블록을 「반영 완료 (… outputs 는 WS-작성)」로 닫으면 끝난 것으로 읽힌다 (ceo)
블록을 둘로 나눈다 — 설정 envs[] 는 `반영 완료`, outputs 세 줄은 `대기`

### 통과한 것
- 두 사용자 결정(outputs 넘김 · hasLogin 안 넣음)은 빈 곳을 만들지 않는다 — 토큰 통로는 기본이 닫힘, `/auth/me`·실행 생성은 칸을 이름으로 골라 읽는다
- 트랜잭션: `서비스고치기` 가 서비스 행을 먼저 UPDATE 로 잠그고 `대상서버넣기` 를 부른다 — 같은 `client` 로 읽으면 흔들리지 않는다
- zod v4 `nullable().optional()` 은 키 없음과 null 을 가른다 · `XFS7` 비어 있음
