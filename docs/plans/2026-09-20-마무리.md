# 마무리 — 낡은 기록 · 명세 모순 셋 · 명세를 어긴 것 셋

등급: 3 · 갈래: 없음 (명세 + 리포팅 + 화면) · 2026-09-20

## 도메인 정리

| 계약 | 건드리나 |
|---|---|
| `packages/kit/src/types.ts` | **안 건드림** |
| 러너 HTTP 요청·응답 | **안 건드림** |
| DB 표·칸 이름 | **안 건드림** (읽기만) |
| **Admin API 경로·응답 형태** | **한 자리 건드린다** — `POST /api/runs/:runId/evidence` 에 상태 코드가 한 줄 는다. **게이트 0 승인분** |

관련 절 — 도메인/리포팅 §7·§8.4·§8.5 · 공통/6-인프라 §9.1·§9.2 · 공통/4-데이터모델 §6 · 도메인/러너 §5.2.

## 왜

갈래 일곱이 끝난 뒤 **「스펙 기능 구현이 다 됐나」**를 전수 조사했다. **코드에 미구현 표시 0건**이고
기능 뼈대는 전부 실물로 확인됐다. 그런데 세 묶음이 남았다.

### ① 기록이 거짓말하고 있다

명세 안에 `[계약 변경 필요]` 블록이 **넷** 있는데 **넷 다 이미 반영이 끝났다.** [2] 가 코드로 재확인했다.

| 블록 | 확인한 증거 |
|---|---|
| `docs/spec/도메인/러너.md:82` | `playwright.config.ts:15` `retries: 0` · `:18` `baseURL: process.env.PLATFORM_BASE_URL` |
| `docs/spec/공통/4-데이터모델.md:224` | 마이그레이션에 제안한 칸 **열넷 전부** + `grafana_ro` 기본 허용 해제 |
| `docs/spec/공통/6-인프라.md:53` | 포트 셋이 환경변수 · 러너 `ports` 없음 · `restart` 넷 · `SESSION_SECRET` · `vite build` |
| `docs/spec/공통/6-인프라.md:167` | `@fastify/secure-session` · `exceljs` |

**3등급 표면이라 아무도 안 건드렸다.** 지금 명세를 여는 사람은 **있지도 않은 할 일 넷**을 본다.
진행 기록에도 **실제로는 닫힌 항목 다섯**이 열린 채 남아 있다.

### ② 명세가 스스로 모순된 것 셋 — 게이트 0 이 정했다

| 모순 | 결정 |
|---|---|
| 엑셀 칸 목록에 「사유」가 없는데 같은 절이 「미실행과 사유로 남는다」고 한다 | **칸을 더한다** |
| 실행 중에 증적을 뽑으면 내용이 달라진다 (화면은 막지만 서버는 안 막는다) | **서버가 막는다** |
| 대시보드 패널 둘이 반복 회차를 안 접는다 | **판정 패널만 접는다** |

### ③ 명세를 어긴 것 셋 — 승인이 필요 없다

**증적 PDF 가 대체 글꼴로 그려진다.** 명세가 이미 금지하고 있던 상태다.

> 「화면 글꼴을 이미지 안에 담는다. 막히면 글꼴이 **시스템 기본으로 조용히 떨어지고**…」 (§9.1)
>
> 「**숫자 폭이 제각각인 글꼴로 바뀌면 TC ID·소요시간·응답 코드가 어긋난다**」 (DESIGN.md)

**검수처에 나가는 문서에서 숫자가 어긋난다.** 그리고 엑셀이 확장자 없는 이름으로 떨어지고,
Grafana 링크가 포트를 코드에 박고 있다 — 그 주석이 **스스로 「③ 덩이에서 정한다」고 적어 뒀고 그 덩이가 끝났다.**

---

## Plan

### 할 일 1. 「계약 변경 필요」 블록 넷을 닫는다

- **RED** — 지금 `grep -rn "계약 변경 필요" docs/` 가 **4건**을 낸다. 넷 다 반영이 끝났으므로
  이 숫자는 거짓이다. 닫은 뒤 다시 세어 **0건**이어야 한다
- **GREEN** — 네 블록을 각각 판정해 닫는다. **CLAUDE.md §2.7 ② 가 기준이다.**

  | 어떤 경우 | 어떻게 |
  |---|---|
  | 판단 기준이 아직 쓸모 있다 | 문단을 **남기고** 「반영 완료 (날짜·어디에)」를 덧붙인다 |
  | 근거가 사실이 아니게 됐다 | **뺀다** |

  **넷이 서로 다른 답일 수 있다.** 각 블록의 「이유」 칸을 읽고 정해라 —
  예를 들어 러너 블록의 「retries 는 지금 안 적혀 있어 누가 켜도 아무 표시가 남지 않는다」는
  **지금도 쓸모 있는 판단 기준**이고, 인프라 블록의 「현재: 포트가 박혀 있다」는 **사실이 아니게 됐다**
- **REFACTOR** — 없음

**files**: `docs/spec/도메인/러너.md`, `docs/spec/공통/4-데이터모델.md`, `docs/spec/공통/6-인프라.md`
**depends-on**: []
**검증**: `grep -rc "계약 변경 필요" docs/spec/` 가 **0** · `npm run check:spec` 종료코드 0

---

### 할 일 2. 진행 기록의 닫힌 다섯을 표시한다

- **RED** — `docs/progress/WS-A.md` 와 `WS-D.md` 의 「다른 갈래로 넘긴 것」이 **다섯을 열린 것으로 적고 있다.**
  전부 코드로 닫힌 것이 확인됐다. **다음 세션이 이걸 읽고 있지도 않은 일을 시작한다**
- **GREEN** — **§2.6 이 append-only 로 정했다. 덮어쓰지 말고 파일 끝에 날짜 항목을 더해** 다섯을 닫는다.

  | # | 어디 | 닫힌 증거 |
  |---|---|---|
  | 1 | `WS-D.md` 「`ItemDetail.tsx` 가 입력값을 안 가린다」 | `:7` 이 `fieldsOf` import, `:28` 이 사용 |
  | 2 | `WS-A.md` 「`api.ts` 의 `cases()` 가 서비스를 안 보낸다」 | `api.ts:317` 이 `service` 를 넣는다 |
  | 3 | `WS-A.md` 「케이스 목록에 '마지막 결과'가 없다」 | `GET /api/runs/last-by-case` + `catalogView.ts` |
  | 4 | `WS-A.md` 「403 판정이 `볼수있나()` 안」 | `auth/gate.ts`·`auth/scope.ts` (PR #28) |
  | 5 | `WS-D.md` 「글꼴 파일이 0개인데 Dockerfile 주석은 담긴다고 적는다」 | 파일이 생겨 주석이 사실이 됐다. **절반** — PDF 경로는 할 일 6 |

  **다섯째는 「절반 닫혔다」로 적고 할 일 6 을 가리켜라.** 다 닫힌 것처럼 적으면 그게 다음 거짓말이다
- **REFACTOR** — 없음

**files**: `docs/progress/WS-A.md`, `docs/progress/WS-D.md`
**depends-on**: []
**검증**: `grep -n "넘긴 것" docs/progress/WS-A.md docs/progress/WS-D.md` 로 눈으로 대조 · 기존 줄이 안 바뀌었는지 diff 로 확인

---

### 할 일 3. 증적 엑셀에 「사유」 칸을 더한다 (게이트 0 결정)

- **RED** — `apps/admin/src/reporting/xlsx.test.ts` 에 단언을 더한다.
  **중단된 실행의 미실행 항목에 사유가 실린다.** 지금 머리행은 24칸이고 사유가 없어 **빨간불**이다.
  (`xlsx.test.ts:68` 이 `length: 24` 로 머리행을 읽고 `:196` 이 24번째 칸을 단언한다 — **둘 다 따라 고쳐야 한다.**
  이건 「검사를 느슨하게 고치는 것」이 아니라 **칸이 하나 늘어난 사실을 반영하는 것**이다)
- **GREEN**
  1. `docs/spec/도메인/리포팅.md` 의 엑셀 표 모양(§8.4)에 **사유 칸 한 줄**. **어느 묶음에 넣을지**(검증 묶음 끝) 정하고,
     왜 거기인지 한 마디 적어라
  2. `apps/admin/src/reporting/xlsx.ts` 머리행 **24 → 25**, 해당 값을 채운다
  3. `xlsx.test.ts` 의 `length: 24` 와 24번째 칸 단언을 따라 고친다
- **REFACTOR** — 없음

**files**: `docs/spec/도메인/리포팅.md`, `apps/admin/src/reporting/xlsx.ts`, `apps/admin/src/reporting/xlsx.test.ts`
**depends-on**: []
**검증**: `npx vitest run apps/admin/src/reporting/xlsx.test.ts` 종료코드 0 · `npm run check:spec` 0 · `npm run typecheck` 0

**사유가 어디서 오나 먼저 확인해라** — HTML 은 이미 사유를 그린다(`apps/admin/src/reporting/html.ts`).
같은 모델(`collect.ts`)에서 나오는지 보고, **없으면 모델부터 봐야 한다** — 그때는 보고해라.

---

### 할 일 4. 실행 중에는 서버가 증적을 막는다 (게이트 0 결정 · 계약 변경분)

- **RED** — `apps/admin/src/reporting/routes.test.ts` 에 단언을 더한다.
  **`RUNNING` 인 실행에 `POST /api/runs/:runId/evidence` 를 치면 거절된다.** 지금은 그냥 만들어져 **빨간불**이다
- **GREEN**
  1. `apps/admin/src/reporting/routes.ts` 의 `POST /runs/:runId/evidence` 에서
     **`실행이있는가` 검사 바로 뒤, `claim` 앞에** 막는다
  2. **상태 코드는 `409`** 로 하고 오류 코드를 새로 준다 (예 `RUN_NOT_FINISHED`).
     **근거** — 같은 라우트가 이미 `409 EVIDENCE_BUSY` 를 쓰고 `GET` 이 `409 EVIDENCE_NOT_READY` 를 쓴다.
     둘 다 「지금 상태와 충돌한다」는 뜻이라 결이 같다. 오류 본문 모양(`{ error, detail }`)도 그대로 따른다
  3. **`ABORTED` 는 뽑을 수 있어야 한다** — 명세가 「중단된 실행도 만들 수 있다」로 못 박았다.
     막는 것은 **아직 안 끝난 것**(`QUEUED`·`RUNNING`)뿐이다
  4. `docs/spec/도메인/리포팅.md` §7 의 `POST /api/runs/:runId/evidence` 줄에 **한 줄** 적는다
- **REFACTOR** — 없음

**files**: `apps/admin/src/reporting/routes.ts`, `apps/admin/src/reporting/routes.test.ts`, `docs/spec/도메인/리포팅.md`
**depends-on**: [3]
**검증**: `npx vitest run apps/admin/src/reporting/routes.test.ts` 종료코드 0 · `npm run check:spec` 0

**★「도는중」 판단이 두 곳이 된다.** 화면은 `apps/admin/src/web/evidence.ts:31` 의 `도는중(status)` 이 이미 같은 판단을 한다.
**서버가 화면 코드를 import 하면 안 된다**(번들이 다르다). 그러니 사본이 하나 는다 —
**명세를 정본으로 삼고 양쪽 주석이 그 절을 가리키게 해라.** 비밀 이름 목록이 셋으로 갈린 것과 같은 모양이고,
그때는 `check:secret-names` 를 뒀다. **여기도 검사가 필요한지 계획 검토에 물어라** (판정 값이 둘뿐이라 과할 수 있다).

---

### 할 일 5. 대시보드의 「실패 TOP 10」만 회차를 접는다 (게이트 0 결정)

- **RED** — `apps/admin/src/reporting/dashboard.test.ts` 에 단언을 더한다.
  **「실패 TOP 10」 패널이 회차를 접는다.** 지금 패널 id 3 이 `count(*)` 로 행을 그대로 세어 **빨간불**이다.
  (`dashboard.test.ts` 가 이미 있다 — 무엇을 단언하는지 먼저 열어 보고 결을 맞춰라)
- **GREEN**
  1. `infra/grafana/provisioning/dashboards/test-platform.json` 패널 **id 3** 을 회차를 접는 모양으로.
     **패널 id 1(성공률 추이)이 이미 `bool_and` 로 접는다 — 그것을 본뜬다**
  2. **패널 id 2(평균 소요시간)는 건드리지 마라.** 게이트 0 이 「측정치라 접지 않는다」로 정했다
  3. `docs/spec/도메인/리포팅.md` §8.5 에 **그 구분을 적는다** — 「판정을 세는 패널은 회차를 접는다.
     소요시간은 측정치라 접지 않는다 — 표본이 줄면 평균이 덜 정확해진다」
- **REFACTOR** — 없음

**files**: `infra/grafana/provisioning/dashboards/test-platform.json`, `apps/admin/src/reporting/dashboard.test.ts`, `docs/spec/도메인/리포팅.md`
**depends-on**: [4]
**검증**: `npx vitest run apps/admin/src/reporting/dashboard.test.ts` 종료코드 0 · `npm run check:spec` 0

**`infra/**` 는 등급 판정에서 미분류로 나온다.** 게이트 2 요약까지 들고 간다.

---

### 할 일 6. 증적 PDF 가 제 글꼴로 그려지게 한다 (명세를 어긴 것)

- **RED** — `apps/admin/src/reporting/html.test.ts` 에 단언을 더한다.
  **증적 HTML 이 글꼴을 제 안에 싣는다** (`@font-face` 가 있고 글꼴 데이터가 박혀 있다).
  지금 `html.ts:128` 이 `font-family` 를 **이름으로만** 불러 **빨간불**이다
- **GREEN** — **방법을 먼저 견주고 근거를 적어라.**

  | 길 | 좋은 점 | 나쁜 점 |
  |---|---|---|
  | **글꼴을 base64 로 HTML 에 심는다** | 스크린샷도 이미 `data:` URI 로 심는다(`generate.ts:35` 의 `심는다()`) — 같은 결이다. HTML 증적을 따로 열어도 글꼴이 산다 | **파일이 2MB 라 증적 문서가 그만큼 커진다.** 형식 셋 모두에 붙는다 |
  | `page.goto('file://…')` 로 기준 주소를 준다 | 문서가 안 커진다 | **PDF 에만 듣는다.** HTML 증적을 따로 열면 여전히 대체 글꼴이다 |

  **HTML 증적도 같은 문제인지 먼저 확인해라** — 그 답이 길을 정한다.
  파일이 커지는 것이 문제면 **글꼴 부분집합(subset)** 은 이번 범위 밖으로 넘겨라
- **REFACTOR** — 없음

**files**: `apps/admin/src/reporting/html.ts`, `apps/admin/src/reporting/html.test.ts`, (필요하면) `apps/admin/src/reporting/generate.ts`
**depends-on**: []
**검증**: `npx vitest run apps/admin/src/reporting/html.test.ts` 종료코드 0 · `npm run typecheck` 0

**명세가 이미 금지하고 있다** — 승인이 필요 없다. 근거는 §9.1 과 DESIGN.md 다.

---

### 할 일 7. 내려받는 문서에 파일 이름이 붙고, 큰 숫자가 500 을 안 낸다

- **RED** — `apps/admin/src/reporting/routes.test.ts` 에 단언 둘을 더한다.
  ① **문서를 받으면 응답에 파일 이름이 붙는다** — 지금 `Content-Disposition` 이 저장소 전체에 0건이라 빨간불
  ② **`1e21` 같은 큰 수를 주면 400 이다** — 지금 `정수()` 가 `Number.isInteger(n) && n > 0` 뿐이라
  통과시키고 Postgres 가 `22003` 을 던져 **잡히지 않은 500** 이 된다
- **GREEN**
  1. `apps/admin/src/reporting/routes.ts:104` 의 `reply.type(...).send(파일)` 에 파일 이름을 붙인다.
     **이름에 무엇을 넣을지 정하고 근거를 적어라** — 실행 번호와 형식이면 충분하다.
     **★비밀값이나 사람 이름이 파일 이름에 새면 안 된다.** 케이스명·실행 제목을 넣지 마라
  2. `정수()` 에 상한을 건다. **상한 값을 어디서 가져올지 정해라** — Postgres `integer` 최대(2147483647)가 자연스럽다
  3. `apps/admin/src/execution/routes.ts:221` 도 같은 부류다. **같은 방식으로 막는다**
  4. `docs/spec/도메인/리포팅.md` §7 의 `GET /api/evidence/:id` 줄에 **파일 이름이 붙는다**를 한 마디
- **REFACTOR** — `정수()` 가 두 파일에 갈라져 있으면 합칠지 판단한다. 억지로 합치지 마라

**files**: `apps/admin/src/reporting/routes.ts`, `apps/admin/src/reporting/routes.test.ts`, `apps/admin/src/execution/routes.ts`, `docs/spec/도메인/리포팅.md`
**depends-on**: [4]
**검증**: `npx vitest run apps/admin/src/reporting/routes.test.ts` 종료코드 0 · `npm run check:spec` 0 · `npm run typecheck` 0

---

### 할 일 8. 화면이 Grafana 포트를 설정에서 받는다

- **RED** — `apps/admin/src/web/layout.test.ts:77·79` 가 **`3001` 을 단언한다.**
  설정으로 빼면 그 단언이 **빨간불**이 된다. 그 자리를 「설정값을 따른다」로 바꾸고,
  **설정을 안 준 경우 기본값 3001** 도 같이 단언한다
- **GREEN**
  1. `apps/admin/src/web/layout.ts:34` 의 박힌 `3001` 을 **빌드 시 설정값**으로 바꾼다
     (`import.meta.env.VITE_GRAFANA_PORT` 같은 자리, 비면 `3001`)
  2. `apps/admin/Dockerfile` 의 `vite build` 단계에 그 값을 넘긴다. `docker-compose.yml` 이 `GRAFANA_PORT` 를 이미 들고 있다
  3. `layout.ts:28~31` 의 주석에서 **「③ 덩이에서 정한다」를 지우고 정해진 결과를 적는다** —
     그 문장이 지금 거짓이다
  4. `docs/spec/공통/6-인프라.md` §9.2 에 **「화면이 Grafana 포트를 어떻게 아는가」**를 한 줄.
     **한계도 같이 적어라** — 빌드 시에 굽히므로 **같은 이미지를 다른 포트로 띄우면 링크가 틀린다**
  5. `docs/SETUP.md:200` 의 설명에 그 사실을 반영한다
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/layout.ts`, `apps/admin/src/web/layout.test.ts`, `apps/admin/Dockerfile`, `docs/spec/공통/6-인프라.md`, `docs/SETUP.md`
**depends-on**: [1]
**검증**: `npx vitest run apps/admin/src/web/layout.test.ts` 종료코드 0 · `npx vite build --config apps/admin/src/web/vite.config.ts` 0 · `npm run check:spec` 0

**★빌드 시에 굽는 길이 맞는지 계획 검토에 물어라.** 대안은 **서버가 내려주는 값**인데
그건 Admin API 응답에 칸을 더하는 것이라 **게이트 0 을 다시 거쳐야 한다.**
지금 길을 고른 이유 — 승인 범위 안에서 끝나고, `vite build` 가 이미 이미지 안에서 돈다.

---

## SPEC 동반 수정 (§2.7)

### 할 일 9. 색인 네 곳을 맞춘다 (§2.7 ③)

- **RED** — 위 할 일들이 `docs/spec/**` 의 줄 수를 바꾼다. `npm run check:spec` 이 **분량 불일치로 실패**한다
- **GREEN**
  1. `docs/SPEC.md` 의 장별 줄 수·갈래 합계·「12장 전부」 — **검사기가 내는 숫자를 그대로 넣는다. 손으로 추측하지 않는다**
  2. **장 목록 · 절 번호 표 · 표 주인** — 새 절을 안 만들었으면 그대로다. **확인만 하고 안 바뀌면 그렇게 적어라**
  3. **★라우터 표** — 「기능을 더하거나 고칠 때」에 행이 필요한지 판정해라. 후보 둘 —
     **「증적을 언제 뽑을 수 있나」**(할 일 4 가 새 규칙을 만든다) · **「대시보드가 회차를 어떻게 세나」**(할 일 5).
     **이 자리를 가장 잘 빠뜨린다**
- **REFACTOR** — 없음

**files**: `docs/SPEC.md`
**depends-on**: [1, 3, 4, 5, 7, 8]
**검증**: `npm run check:spec` 종료코드 0

---

### 할 일 10. SPEC 밖을 맞춘다 (§2.7 ④)

- **RED** — 핵심 낱말로 훑은 결과를 붙인다. **시작할 때 직접 다시 grep 해라** (앞 할 일들이 줄을 밀었다).
  ```
  docs/WORKSTREAMS.md   「PR #30 이 넘긴 것 여섯」 — 이번에 몇이 닫히나
  docs/WORKFLOW.md      현재 위치 — 「기능 구현이 다 됐나」의 답이 바뀐다
  docs/SETUP.md:200     GRAFANA_PORT 설명 (할 일 8 이 건드린다 — 겹치면 거기서 끝낸다)
  docs/progress/WS-E.md:104  「설정으로 뺄지는 ③ 에서 정한다」 ← 그 ③ 이 끝났다. §2.6 대로 덧붙인다
  ```
- **GREEN**
  1. `docs/WORKSTREAMS.md` 의 「넘긴 것 여섯」 중 **이번에 닫히는 것을 표시한다.**
     **「전부 닫혔다」로 적지 마라** — 화면 열둘 그물·비밀값 데모 케이스·`@fastify/static` 은 그대로 남는다
  2. `docs/WORKFLOW.md` 현재 위치 — **「기능 구현은 다 됐고 확인이 남았다」**가 이번 결론이다.
     완료 기준 상자 **59개 중 체크된 것이 0개**라는 사실을 적어라 — 그게 다음 단계([3] 전체 검사)의 입구다
  3. `docs/progress/WS-E.md` 에 날짜 항목을 **덧붙여** Grafana 포트 결정을 닫는다
  4. **`spec-review` 체크리스트 · `npm run check:*`** — 이번 변경으로 옛 기준이 된 항목이 있는지 본다.
     없으면 「없음」이라고 적어라
  5. **`docs/DESIGN.md` · `docs/HOOKS.md` · `docs/design-mockup.html`** — 화면 규칙이 안 바뀌므로
     **해당 없음으로 본다. 확인하고 그렇게 적어라**
- **REFACTOR** — 없음

**files**: `docs/WORKSTREAMS.md`, `docs/WORKFLOW.md`, `docs/progress/WS-E.md`
**depends-on**: [9]
**검증**: `npm run check:spec` 종료코드 0 · `grep -n "넘긴 것" docs/WORKSTREAMS.md` 로 눈으로

---

### §2.7 절별 대응

| §2.7 | 무엇 | 어느 할 일 |
|---|---|---|
| ① 같은 규칙이 적힌 다른 절 | `3001` 이 아홉 곳 · 「도는중」 판단이 두 곳 | 4 · 8 |
| ② 뒤집은 결정 | **「계약 변경 필요」 블록 넷.** 남길지 뺄지를 각각 판정 | 1 |
| ③ 색인 — 장 목록·절 번호·표 주인 | 새 절을 안 만든다. 확인만 | 9 |
| ③ 라우터 표 | **후보 둘** — 증적을 언제 뽑나 · 대시보드가 회차를 어떻게 세나 | 9 |
| ④ `WORKSTREAMS` 킥오프 | 넘긴 것 여섯 중 닫히는 것 | 10 |
| ④ `WORKFLOW` | 현재 위치 — 「구현은 됐고 확인이 남았다」 | 10 |
| ④ `SETUP` | `GRAFANA_PORT` 설명 | 8 |
| ④ `spec-review` · `check:*` | 확인만. 없으면 「없음」 | 10 |
| ④ `DESIGN`·`HOOKS`·`design-mockup` | **해당 없음** — 화면 규칙이 안 바뀐다 | 10 |
| ④ 코드에 박힌 상수 | **`layout.ts` 의 `3001`** · `xlsx.ts` 의 칸 수 | 8 · 3 |
| ⑤ 손으로 적은 숫자 | 색인 분량은 검사기가 센다. **새로 손 숫자를 만들지 마라** | 9 |
| ⑥ 검사 | `check:spec` → `spec-review` H 절 → 게이트 2 | [6] 이 돈다 |

## Plan 메타

할 일 10개 · 예상 묶음 6개 · 구현 규율: TDD · 추가 검증: `npm run check:deps` · `typecheck` ·
`check:workflow` · `check:spec` · `check:tests` · `check:secret-names` · `vite build` ·
`DATABASE_URL=… npm test` **연속 3회**

묶음 — ①{1, 2, 6} ②{3, 8} ③{4} ④{5, 7} ⑤{9} ⑥{10}

**미분류: `infra/grafana/provisioning/dashboards/test-platform.json`** — 게이트 2 요약까지 들고 간다.

**G8 을 따른다** — 새로 만드는 검사는 **한 자리 부숴 빨개지는 것을 보고 그 결과를 PR 에 적는다.**
할 일 8 은 `layout.test.ts` 가 이미 `3001` 을 단언하므로 자연스럽게 걸린다.

## 리뷰 결과
(계획 검토가 채운다)
