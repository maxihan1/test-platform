# 마무리 — 낡은 기록 · 명세 모순 셋 · 명세를 어긴 것 셋

등급: 3 · 갈래: 없음 (명세 + 리포팅 + 화면) · 2026-09-20

## 도메인 정리

| 계약 | 건드리나 |
|---|---|
| `packages/kit/src/types.ts` | **안 건드림** |
| 러너 HTTP 요청·응답 | **안 건드림** |
| DB 표·칸 이름 | **안 건드림** (읽기만) |
| **Admin API 경로·응답 형태** | **한 자리 건드린다** — `POST /api/runs/:runId/evidence` 에 상태 코드가 한 줄 는다. **게이트 0 승인분** |

관련 절 — 도메인/리포팅 §7·§8.4·§8.5 · 공통/6-인프라 §9.1·§9.2 · 공통/4-데이터모델 §6 · 도메인/러너 §5.2.

## 왜

갈래 일곱이 끝난 뒤 **「스펙 기능 구현이 다 됐나」**를 전수 조사했다. **코드에 미구현 표시 0건**이고
기능 뼈대는 전부 실물로 확인됐다. 그런데 세 묶음이 남았다.

### ① 기록이 거짓말하고 있다

명세 안에 `[계약 변경 필요]` 블록이 **넷** 있는데 **넷 다 이미 반영이 끝났다.** [2] 가 코드로 재확인했다.

| 블록 | 확인한 증거 |
|---|---|
| `docs/spec/도메인/러너.md:82` | `playwright.config.ts:15` `retries: 0` · `:18` `baseURL: process.env.PLATFORM_BASE_URL` |
| `docs/spec/공통/4-데이터모델.md:224` | 마이그레이션에 제안한 칸 **열넷 전부** + `grafana_ro` 기본 허용 해제 |
| `docs/spec/공통/6-인프라.md:53` | 포트 셋이 환경변수 · 러너 `ports` 없음 · `restart` 넷 · `SESSION_SECRET` · `vite build` |
| `docs/spec/공통/6-인프라.md:167` | `@fastify/secure-session` · `exceljs` |

**3등급 표면이라 아무도 안 건드렸다.** 지금 명세를 여는 사람은 **있지도 않은 할 일 넷**을 본다.
진행 기록에도 **실제로는 닫힌 항목 다섯**이 열린 채 남아 있다.

### ② 명세가 스스로 모순된 것 셋 — 게이트 0 이 정했다

| 모순 | 결정 |
|---|---|
| 엑셀 칸 목록에 「사유」가 없는데 같은 절이 「미실행과 사유로 남는다」고 한다 | **칸을 더한다** |
| 실행 중에 증적을 뽑으면 내용이 달라진다 (화면은 막지만 서버는 안 막는다) | **서버가 막는다** |
| 대시보드 패널 둘이 반복 회차를 안 접는다 | **판정 패널만 접는다** |

### ③ 명세를 어긴 것 셋 — 승인이 필요 없다

**증적 PDF 가 대체 글꼴로 그려진다.** 명세가 이미 금지하고 있던 상태다.

> 「화면 글꼴을 이미지 안에 담는다. 막히면 글꼴이 **시스템 기본으로 조용히 떨어지고**…」 (§9.1)
>
> 「**숫자 폭이 제각각인 글꼴로 바뀌면 TC ID·소요시간·응답 코드가 어긋난다**」 (DESIGN.md)

**검수처에 나가는 문서에서 숫자가 어긋난다.** 그리고 엑셀이 확장자 없는 이름으로 떨어지고,
Grafana 링크가 포트를 코드에 박고 있다 — 그 주석이 **스스로 「③ 덩이에서 정한다」고 적어 뒀고 그 덩이가 끝났다.**

---

## Plan

> **게이트 1 에서 계획이 커졌다** (2026-09-20). 검토가 낸 **BLOCKER 7 건을 전부 흡수**했고,
> 사용자가 넷을 정했다 — **지적 반영하고 진행** · **Grafana 포트는 배선까지** ·
> **「증적 문서를 눈으로 본다」 완료 기준 상자를 더한다**(게이트 0 에 없던 명세 변경) · **한 PR 로 간다**.
>
> **★색인 분량은 할 일 11 이 한 번에 맞춘다. 그때까지 `npm run check:spec` 은 빨간 채로 둔다** (BLOCKER 2).
> 그래서 아래 할 일들의 `검증` 에 `check:spec` 이 없다 — 할 일 11·12 에만 있다.

### 할 일 1. 「계약 변경 필요」 블록 넷을 닫는다

- **RED** — `grep -rn "계약 변경 필요" docs/spec/` 이 **4건**을 낸다. 넷 다 반영이 끝났으므로 거짓이다.
  **★`docs/` 전체로 세면 14건이다** — 나머지는 진행 기록·계획·검토 기록이고 **거기는 일부러 남는다**(append-only).
  **범위는 `docs/spec/` 뿐이다**
- **GREEN** — 네 블록을 아래 표대로 닫는다. **답을 미리 정해 뒀다** — 각 블록의 「이유」 칸을 읽고 판정한 결과다.

  | 블록 | 어떻게 | 왜 |
  |---|---|---|
  | `도메인/러너.md:82` | **남기고 닫는다** | 「retries 는 안 적혀 있어 누가 켜도 표시가 안 남는다」는 **지금도 쓸모 있는 판단 기준**이다. 다음에 재시도를 켜고 싶을 때 같은 질문을 다시 해야 한다 |
  | `공통/4-데이터모델.md:224` | **남기고 닫는다** | 「스캔 때마다 덮어쓰는 캐시에서 읽으면 나중에 뽑은 증적이 달라진다」가 **박제 규칙의 근거**다. 지우면 다음 세션이 왜 칸이 이렇게 많은지 모른다 |
  | `공통/6-인프라.md:53` | **뺀다** | 「현재: 포트가 박혀 있다 · restart 가 없다 · 화면 빌드가 없다」가 **전부 사실이 아니게 됐다.** 무너진 근거를 남기면 다음 세션이 그걸 읽고 또 막힌다 |
  | `공통/6-인프라.md:167` | **뺀다** | 「현재: 인증에 쓸 부품이 없다」가 사실이 아니다. 부품 목록은 같은 절의 「승인받은 새 부품」 표가 **정본**이라 이 블록이 중복이다 |

  **남기는 둘은 블록 바로 아래에 `상태:` 줄을 붙인다** — `상태: 반영 완료 (2026-09-17, <어디에>)`.
  그 형식은 할 일 10 의 검사기가 강제한다
- **REFACTOR** — 없음

**files**: `docs/spec/도메인/러너.md`, `docs/spec/공통/4-데이터모델.md`, `docs/spec/공통/6-인프라.md`
**depends-on**: []
**검증**: 남긴 둘이 `상태:` 줄을 갖고, 뺀 둘이 사라졌는지 **파일을 열어 눈으로**.
`grep -rn "계약 변경 필요" docs/spec/` 이 **2건**(남긴 둘)

---

### 할 일 2. 진행 기록의 닫힌 다섯을 표시한다 — **확인 절차 (RED 없음)**

**RED 가 없다.** 「이 기록이 낡았다」에는 기계가 읽을 안정적인 신호가 없다. 억지로 검사기를 만들지 않는다.

- **할 것** — **§2.6 이 append-only 로 정했다. 덮어쓰지 말고 파일 끝에 날짜 항목을 더해** 다섯을 닫는다.

  | # | 어디 | 닫힌 증거 |
  |---|---|---|
  | 1 | `WS-D.md` 「`ItemDetail.tsx` 가 입력값을 안 가린다」 | `:7` 이 `fieldsOf` import, `:28` 이 사용 |
  | 2 | `WS-A.md` 「`api.ts` 의 `cases()` 가 서비스를 안 보낸다」 | `api.ts:317` 이 `service` 를 넣는다 |
  | 3 | `WS-A.md` 「케이스 목록에 '마지막 결과'가 없다」 | `GET /api/runs/last-by-case` + `catalogView.ts` |
  | 4 | `WS-A.md` 「403 판정이 `볼수있나()` 안」 | `auth/gate.ts`·`auth/scope.ts` (PR #28) |
  | 5 | `WS-D.md` 「글꼴 파일이 0개인데 Dockerfile 주석은 담긴다고 적는다」 | 파일이 생겨 주석이 사실이 됐다. **절반** — PDF 경로는 할 일 6 |

  **다섯째는 「절반 닫혔다」로 적고 할 일 6 을 가리켜라.** 다 닫힌 것처럼 적으면 그게 다음 거짓말이다

**files**: `docs/progress/WS-A.md`, `docs/progress/WS-D.md`
**depends-on**: []
**검증**: 기존 줄이 **하나도 안 바뀌었는지** diff 로 확인 (추가만 있어야 한다)

---

### 할 일 3. 증적 엑셀에 「사유」 칸을 더한다 (게이트 0 결정)

- **RED** — `apps/admin/src/reporting/xlsx.test.ts` 에 단언을 더한다.
  **미실행 항목의 행에 사유가 실린다.** 지금 머리행이 24칸이고 사유가 없어 **빨간불**이다
- **GREEN**
  1. **자리는 케이스칸 묶음 끝**(판정·소요 옆)이다. **검증 묶음이 아니다.**
     **근거** — 사유(`collect.ts` 의 `notRunReason`)는 **항목 단위** 값이고 엑셀 행은 **검증 문장 단위**다.
     검증 묶음에 넣으면 「이 검증 문장이 왜 안 돌았나」로 읽힌다.
     **한 항목이 여러 행이면 같은 사유가 반복되는데 그것이 맞다** — 명세가
     「**상위 정보를 앞 칸에 반복한다.** 병합 셀을 쓰지 않는다」로 이미 정했다
  2. **칸 이름이 두 가지를 다 덮어야 한다** — `notRunReason` 은 **미실행 사유**와 **판정못낸 사유**(`NA`+error)를 같이 싣는다.
     한쪽만 덮는 이름을 쓰면 고치려던 문장이 **여전히 코드와 다른 말**을 한다
  3. `docs/spec/도메인/리포팅.md` §8.4 엑셀 표에 그 자리로 한 줄
  4. `apps/admin/src/reporting/xlsx.ts` 머리행 24 → 25. **케이스칸 끝에 넣으면 그 뒤 칸이 밀린다** —
     `xlsx.test.ts` 의 `length: 24` 와 자리로 세는 단언들이 따라 밀린다.
     **이건 검사를 느슨하게 고치는 것이 아니라 칸이 하나 는 사실을 반영하는 것**이다
  5. `xlsx.ts:111` 주석의 **「24칸을 가로로 훑는 표라」도 손 숫자다** — 같이 고친다
- **REFACTOR** — 없음

**files**: `docs/spec/도메인/리포팅.md`, `apps/admin/src/reporting/xlsx.ts`, `apps/admin/src/reporting/xlsx.test.ts`
**depends-on**: []
**검증**: `npx vitest run apps/admin/src/reporting/xlsx.test.ts` 종료코드 0 · `npm run typecheck` 0

**모델 작업은 없다** — 값이 이미 `collect.ts` 의 `EvidenceItem.notRunReason` 에 있고 `html.ts` 가 이미 그린다.

---

### 할 일 4. 실행 중에는 서버가 증적을 막는다 (게이트 0 결정 · 계약 변경분)

- **RED** — `apps/admin/src/reporting/routes.test.ts` 에 단언을 더한다.
  **`RUNNING` 인 실행에 `POST /api/runs/:runId/evidence` 를 치면 409 로 거절된다.** 지금은 그냥 만들어져 **빨간불**이다
- **GREEN**
  1. **★막는 것은 `RUNNING` 하나다.** `QUEUED` 는 **이 저장소에 없는 상태**다 —
     `grep -rn "QUEUED" apps packages db docs/spec` 이 **0건**이고 `db/migrations/20260916000001_init.sql:35` 주석이
     `RUNNING | FINISHED | ABORTED` 다. **없는 상태 이름을 명세에 적으면 새 모순이 생긴다**
  2. `실행이있는가` 를 **`실행상태(runId): Promise<string | null>`** 로 바꾼다 — `SELECT 1` 로는 상태를 못 봐서
     왕복이 하나 더 는다. `null` → 404 · `'RUNNING'` → 409 · 나머지 통과. **쿼리 하나, 함수 하나**
  3. 오류 코드는 새로 준다(예 `RUN_NOT_FINISHED`). **근거** — 같은 라우트가 이미 `409 EVIDENCE_BUSY` 를,
     `GET` 이 `409 EVIDENCE_NOT_READY` 를 쓴다. 둘 다 「지금 상태와 충돌한다」는 뜻이라 결이 같다
  4. **`ABORTED` 는 뽑을 수 있어야 한다** — 명세가 못 박았다
  5. `docs/spec/도메인/리포팅.md` §7 의 그 줄에 **한 줄**
- **REFACTOR** — 없음

**files**: `apps/admin/src/reporting/routes.ts`, `apps/admin/src/reporting/routes.test.ts`, `docs/spec/도메인/리포팅.md`
**depends-on**: [3]
**검증**: **`DATABASE_URL='postgres://platform:platform@localhost:5433/platform' npx vitest run apps/admin/src/reporting/routes.test.ts`**
종료코드 0 **이고 건너뛴 개수가 0** — 그 파일이 `describe.skipIf(연결 === undefined)` 라
**맨손으로 돌리면 새 단언이 안 돈 채 초록이 난다** (BLOCKER 3)

**「도는중」 정본은 `apps/admin/src/web/runState.ts:20` 의 `status === 'RUNNING'` 이다**(`evidence.ts` 는 부르는 자리).
서버가 화면 코드를 import 할 수 없어 사본이 하나 는다 — **명세를 정본으로 삼고 양쪽 주석이 그 절을 가리키게 한다.**
**검사기는 만들지 않는다** — 판정 값이 하나뿐이다. `check:secret-names` 는 **늘어나는 목록**이라 값을 했다.
상태값이 셋 이상으로 늘면 그때 다시 본다 — 그 조건을 주석에 적는다.

---

### 할 일 5. 대시보드의 「실패 TOP 10」만 회차를 접는다 (게이트 0 결정)

- **RED** — `apps/admin/src/reporting/dashboard.test.ts` 에 단언을 더한다.
  **「실패 TOP 10」이 회차를 접는다.** 지금 패널 id 3 이 `count(*)` 로 행을 세어 **빨간불**이다
- **GREEN**
  1. 패널 **id 3** 의 접는 키는 패널 1 과 같게 `(run_id, tc_id, platform)` 로 한다.
  2. **★세는 조건은 `bool_or(i.status = 'FAIL')` 이다. 패널 1 을 그대로 베끼면 안 된다** —
     패널 1 의 `bool_and(status='PASS')` 는 **`NA`(미실행)도 「통과 아님」**으로 본다.
     그대로 쓰면 **한 번도 안 돈 케이스가 실패 순위에 오르고**, 매일 아침 보는 대시보드가
     러너 장애를 케이스 탓으로 돌린다
  3. **패널 id 2(평균 소요시간)는 건드리지 마라.** 게이트 0 이 「측정치라 접지 않는다」로 정했다
  4. `docs/spec/도메인/리포팅.md` §8.5 에 그 구분을 적는다 — 「판정을 세는 패널은 회차를 접는다.
     소요시간은 측정치라 접지 않는다」
- **REFACTOR** — 없음

**files**: `infra/grafana/provisioning/dashboards/test-platform.json`, `apps/admin/src/reporting/dashboard.test.ts`, `docs/spec/도메인/리포팅.md`
**depends-on**: [4]
**검증**: **`DATABASE_URL=… npx vitest run apps/admin/src/reporting/dashboard.test.ts`** 종료코드 0 **이고 건너뛴 개수 0**
(`:34` 가 `describe.skipIf` 다)

**`infra/**` 는 등급 판정에서 미분류로 나온다.** 게이트 2 요약까지 들고 간다.

---

### 할 일 6. 증적 문서가 제 글꼴로 그려지게 한다 (명세를 어긴 것)

**★먼저 지금 증적을 한 부 뽑아 열어 본다.** 무엇이 깨지는지 **눈으로 보고 그 사실을 PR 에 적는다.**
**아무도 생성된 증적 문서를 열어본 기록이 없다** — 글꼴 문제는 코드를 **읽어서** 찾았다.
admin 이미지가 `playwright:v1.63.0-jammy` 인데 **한글 글꼴이 들었는지 아무도 확인 안 했다** —
숫자 폭이 아니라 **한글이 두부(□□□)로 나올 수도** 있다. **증상을 모르면 고쳤는지도 모른다.**

- **RED** — `apps/admin/src/reporting/html.test.ts` 에 **부술 수 있는** 단언을 더한다.
  「`@font-face` 가 있다」 같은 문자열 검사는 **base64 가 잘려도 초록**이라 항진명제다 (G8).
  대신 ① **`@font-face` 의 `font-family` 값과 `body` 의 첫 `font-family` 값이 같은 이름인가**
  ② **박힌 base64 길이가 실제 파일 크기와 맞는가**
- **GREEN** — **길은 base64 로 심는 것으로 확정한다.** 견줄 것이 없다 —
  `grep -n "font-face" apps/admin/src/reporting/html.ts` 가 **0건**이라 **HTML 증적도 같은 문제**이고,
  `generate.ts` 는 `goto` 가 아니라 `setContent` 를 써서 「기준 주소를 준다」는 길은 **HTML 을 임시 파일로 먼저 써야 하는 훨씬 큰 변경**이다.

  **비용을 정확히 적는다** — 글꼴은 **2,057,688 바이트**, base64 로 약 **2.74MB**.
  **형식 셋이 아니라 둘**이다(`XLSX` 는 `renderHtml` 을 안 탄다).
  **PDF 는 안 커진다** — Chromium 이 쓴 글자만 추려 담는다. **커지는 것은 디스크에 남는 HTML 증적 파일**이고
  문서마다 2.74MB 다.

  **정할 것 둘.**
  1. **글꼴 파일을 어디서 읽나** — 지금 `apps/admin/src/web/fonts/` 는 **화면 갈래 소유 경로**다.
     서버가 거기를 읽으면 갈래 사이 의존이 생긴다 — `generate.ts:24` 주석이 바로 그걸 경고한다.
     **공유 자리로 옮길지, 읽고 근거를 적을지 정한다**
  2. **동기를 지킬지** — `renderHtml` 은 지금 동기다. 글꼴을 읽으면 비동기가 되고 `generate.test.ts` 까지 번진다.
     **모듈 로드 때 한 번 읽어 기억하면**(2코어 서버에 2.74MB 상주) 동기를 지킨다. **골라서 적는다**

  **그리고 `docs/spec/공통/3-공유계약.md` 또는 §3.3 에 한 줄** — 「**글꼴 파일은 증적의 입력이다.
  바꾸면 과거 증적의 바이트가 달라진다**」. 완료 기준이 「오늘 뽑든 한 달 뒤에 뽑든 **글자 하나까지 같다**」를 요구한다

  **★완료 기준 상자를 더한다 (게이트 1 승인분)** — `docs/spec/공통/7-데모와-완료.md` 에
  「**증적 PDF 를 열면 한글이 깨지지 않고 TC ID·소요시간·응답 코드의 숫자가 줄 맞는다**」.
  59개 중 **증적 문서의 내용을 보는 상자가 0개**라 이 버그가 여기까지 살아남았다
- **REFACTOR** — 없음

**files**: `apps/admin/src/reporting/html.ts`, `apps/admin/src/reporting/html.test.ts`,
`apps/admin/src/reporting/generate.ts`(동기/비동기 선택에 따라), `docs/spec/도메인/리포팅.md`,
`docs/spec/공통/7-데모와-완료.md`, (글꼴을 옮기면) 그 경로
**depends-on**: []
**검증**: `npx vitest run apps/admin/src/reporting/html.test.ts` 종료코드 0 · `npm run typecheck` 0
(`html.test.ts` 는 `skipIf` 가 없다 — 확인했다)

**부분집합(subset)은 범위 밖이다** — 새 npm 부품이 필요해 물어야 한다.

---

### 할 일 7. 내려받는 문서에 파일 이름이 붙는다

- **RED** — `apps/admin/src/reporting/routes.test.ts` 에 단언을 더한다.
  **문서를 받으면 응답에 파일 이름이 붙는다.** `Content-Disposition` 이 저장소 전체에 **0건**이라 빨간불
- **GREEN** — `apps/admin/src/reporting/routes.ts:104` 의 `reply.type(...).send(파일)` 에 파일 이름을 붙인다.
  **실행 번호와 형식이면 충분하다.**
  **★비밀값이나 사람 이름이 파일 이름에 새면 안 된다** — 케이스명·실행 제목을 넣지 마라.
  `docs/spec/도메인/리포팅.md` §7 의 `GET /api/evidence/:id` 줄에 한 마디
- **REFACTOR** — 없음

**files**: `apps/admin/src/reporting/routes.ts`, `apps/admin/src/reporting/routes.test.ts`, `docs/spec/도메인/리포팅.md`
**depends-on**: [4]
**검증**: **`DATABASE_URL=… npx vitest run apps/admin/src/reporting/routes.test.ts`** 종료코드 0 **이고 건너뛴 개수 0**

---

### 할 일 8. 큰 숫자·음수가 500 을 내지 않는다 — **다섯 자리 전부**

- **RED** — 라우트마다 단언을 더한다. **지금 다섯 자리가 다 뚫려 있다.**

  | 어디 | 지금 | 뚫리는 값 |
  |---|---|---|
  | `reporting/routes.ts:24` 의 `정수()` | `Number.isInteger(n) && n > 0` | `1e21` |
  | `execution/routes.ts:220` | `Number.isInteger(id)` **만** | `1e21` · **`0` · `-1`** |
  | `execution/routes.ts:108` | **검사 없음** | `NaN` · `1e21` · 음수 |
  | `execution/routes.ts:152` | **검사 없음** | 〃 |
  | `execution/routes.ts:158` | **검사 없음** (`runId`·`historyId` 둘 다) | 〃 |

  Postgres 가 `22003` 을 던져 **잡히지 않은 500** 이 된다
- **GREEN** — **가드 하나를 다섯 자리 전부에 건다.** 호출자마다 막지 않는다.
  상한은 Postgres `integer` 최대(**2147483647**), 하한은 `> 0`.
  `정수()` 를 공유할 자리로 올릴지 각 모듈에 둘지 정하고 **근거를 적는다**
- **REFACTOR** — 없음

**files**: `apps/admin/src/reporting/routes.ts`, `apps/admin/src/execution/routes.ts`,
`apps/admin/src/reporting/routes.test.ts`, `apps/admin/src/execution/routes.test.ts`
**depends-on**: [7]
**검증**: **`DATABASE_URL=… npx vitest run apps/admin/src/reporting/routes.test.ts apps/admin/src/execution/routes.test.ts`**
종료코드 0 **이고 건너뛴 개수 0** · `grep -rn "Number(req.params" apps/` 로 **남은 자리가 없는지** 확인

---

### 할 일 9. 화면이 Grafana 포트를 설정에서 받는다 — **배선까지** (게이트 1 결정)

- **RED** — `apps/admin/src/web/layout.test.ts:77·79` 가 **`3001` 을 단언한다.**
  설정으로 빼면 빨간불이 된다. 그 자리를 「설정값을 따른다」로 바꾸고 **설정을 안 준 경우 기본값 3001** 도 단언한다
- **GREEN** — **세 토막을 다 잇는다.** 하나라도 빠지면 `ARG` 만 있고 아무도 안 채우는 헛수고가 된다.
  1. `apps/admin/src/web/layout.ts:34` 의 박힌 `3001` → 빌드 시 설정값(`import.meta.env.VITE_GRAFANA_PORT`, 비면 `3001`)
  2. `apps/admin/Dockerfile` 에 `ARG`/`ENV` 를 넣고 `vite build` 가 그 값을 보게 한다
  3. **`docker-compose.yml` 의 admin `build:` 에 `args:` 를 넣는다.**
     지금 `GRAFANA_PORT` 는 grafana 포트 매핑에만 있는 **실행 시점 값**이라 빌드로 안 넘어온다
  4. **★`docker-compose.yml` 은 §1.3 잠긴 파일이다.** 그래서 `docs/spec/공통/6-인프라.md` §9.1 의
     **compose 예시도 같이 고친다** — 명세에 없는 변경을 넣으면 안 된다 (게이트 0 승인 범위 안이다)
  5. `layout.ts:28~31` 주석의 **「설정으로 뺄지는 ③ 덩이에서 정한다」를 지운다** — 지금 거짓이다
  6. `docs/spec/공통/6-인프라.md` §9.2 에 한 줄 + **한계** — 값이 **빌드 때 구워지므로
     `GRAFANA_PORT` 를 바꾸면 admin 이미지를 다시 빌드해야 하고, 안 하면 오류 없이 링크만 틀린다**
  7. **`docs/SETUP.md:200`** 이 지금 그 포트를 「자유롭게 바꾸는 값」으로 안내한다 — **다시 빌드해야 한다**를 넣는다
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/layout.ts`, `apps/admin/src/web/layout.test.ts`, `apps/admin/Dockerfile`,
`docker-compose.yml`, `docs/spec/공통/6-인프라.md`, `docs/SETUP.md`
**depends-on**: [1]
**검증**: `npx vitest run apps/admin/src/web/layout.test.ts` 종료코드 0 ·
`npx vite build --config apps/admin/src/web/vite.config.ts` 0 ·
`docker compose config` 가 `args` 를 실제로 내는지 (안 되면 `grep` 으로 세 토막을 눈으로)

**셋째 길이 있었다** — admin 이 `index.html` 에 값을 심으면 다시 빌드가 필요 없다.
**게이트 1 이 「배선까지 제대로」를 골랐으므로 빌드 시 방식으로 간다.** 그 사실과 대안을 §9.2 에 한 줄 남긴다.

---

### 할 일 10. 기계가 잡게 한다 — 상태 줄 강제 · 완료 기준 세기

**§2.5 가 `.claude/scripts/` 와 `npm run check:*` 를 「직접 고친다」로 정했다.** 승인을 받지 않는다.

- **RED** — 둘 다 지금은 아무도 안 본다.
  1. 할 일 1 이 남기는 `[계약 변경 필요]` 블록에 `상태:` 줄이 **없어도 아무 일도 안 일어난다** →
     **시간이 지나면 또 「아직 안 했다」로 읽힌다.** 그게 이 PR 이 온 이유다
  2. `docs/WORKFLOW.md` 가 「구현이 다 됐다」를 **손으로** 적으려 한다(할 일 12) — **다음 거짓말 후보**다.
     §2.7 ⑤ 가 막으려던 모양이다
- **GREEN**
  1. **`docs/spec/**` 의 `[계약 변경 필요]` 블록 바로 아래에 `상태:` 줄이 없으면 실패**시킨다.
     값은 `대기` · `반영 완료 <날짜> <어디에>` · `철회됨` 셋. `check:spec` 에 붙이거나
     `.claude/scripts/*.test.mjs` 로 둔다 — **`npm test` 가 CI 에서 도는 자리를 고른다**
  2. **완료 기준 상자를 세게 한다** — `docs/spec/공통/7-데모와-완료.md` 의 `- [ ]` / `- [x]` 를 세어 찍는다.
     `docs/WORKFLOW.md` 는 **그 명령을 가리키기만** 한다
  3. **둘 다 부숴서 확인한다** (G8) — 상태 줄을 지워 빨개지는지, 상자 하나를 체크해 숫자가 바뀌는지
- **REFACTOR** — 없음

**files**: `.claude/scripts/check-spec-refs.mjs` (또는 새 검사 파일), `.claude/scripts/check-spec-refs.test.mjs`
**depends-on**: [1]
**검증**: `npm run check:workflow` 종료코드 0 · 부순 뒤 종료코드 1 · 되돌린 뒤 0

---

## SPEC 동반 수정 (§2.7)

### 할 일 11. 색인 네 곳을 맞춘다 (§2.7 ③)

- **RED** — 앞 할 일들이 `docs/spec/**` 의 줄 수를 바꿔 **`npm run check:spec` 이 분량 불일치로 실패한다.**
  **이 자리가 유일하게 `check:spec` 을 초록으로 만드는 곳이다** (BLOCKER 2)
- **GREEN**
  1. `docs/SPEC.md` 의 장별 줄 수 · 갈래 합계 · 「12장 전부」 — **검사기가 내는 숫자를 그대로 넣는다**
  2. 장 목록 · 절 번호 표 · 표 주인 — 새 절을 안 만들었으면 그대로다. **확인하고 안 바뀌면 그렇게 적는다**
  3. **★라우터 표** — 후보 셋. 「**증적을 언제 뽑을 수 있나**」(할 일 4) ·
     「**대시보드가 회차를 어떻게 세나**」(할 일 5) · 「**증적 문서의 글꼴은 어디서 오나**」(할 일 6).
     **이 자리를 가장 잘 빠뜨린다**
- **REFACTOR** — 없음

**files**: `docs/SPEC.md`
**depends-on**: [1, 3, 4, 5, 6, 7, 9]
**검증**: `npm run check:spec` 종료코드 **0**

---

### 할 일 12. SPEC 밖을 맞춘다 (§2.7 ④) — **확인 절차 + 기록**

- **할 것** — **시작할 때 직접 grep 해 줄 번호를 다시 잡는다** (앞 할 일들이 줄을 밀었다).
  1. `docs/WORKSTREAMS.md` 의 「넘긴 것 여섯」 중 **이번에 닫히는 것을 표시한다.**
     **「전부 닫혔다」로 적지 마라**
  2. **★「넘긴 것」에 ⑦ 을 더한다** — 「**증적 만들기가 실행 대기줄을 공유한다**
     (`generate.ts` 의 `찍는다()` → `dispatcher.ts` 의 `enqueue`). 큰 실행이 도는 동안 다른 실행의 증적을 누르면
     **PENDING 으로 굳고 오류 문장 없이 회복 수단이 admin 재기동뿐**이다. 고치려면 대기줄을 가르거나
     취소 통로를 만들어야 해 **게이트 0 이 필요하다** — 그래서 이번에 안 했다」.
     **할 일 4 가 같은 실행 경우는 없앤다**는 것도 적는다.
     **아는 것을 안 적는 것이 이 계획이 지우려는 거짓말의 원형이다**
  3. `docs/WORKFLOW.md` 현재 위치 — 「**기능 구현은 됐고 확인이 남았다**」.
     **완료 기준 상자 개수는 손으로 적지 말고 할 일 10 이 만든 명령을 가리킨다**
  4. `docs/progress/WS-E.md` 에 날짜 항목을 **덧붙여** Grafana 포트 결정을 닫는다
  5. `spec-review` 체크리스트 · `npm run check:*` — 옛 기준이 된 항목이 있는지 본다. 없으면 「없음」이라 적는다
  6. `docs/DESIGN.md` · `docs/HOOKS.md` · `docs/design-mockup.html` — 화면 규칙이 안 바뀐다. **확인하고 「해당 없음」이라 적는다**

**files**: `docs/WORKSTREAMS.md`, `docs/WORKFLOW.md`, `docs/progress/WS-E.md`
**depends-on**: [11]
**검증**: `npm run check:spec` 종료코드 0 · `grep -n "넘긴 것" docs/WORKSTREAMS.md` 로 눈으로

---

### §2.7 절별 대응

| §2.7 | 무엇 | 어느 할 일 |
|---|---|---|
| ① 같은 규칙이 적힌 다른 절 | `3001` 이 아홉 곳 · 「도는중」 정본은 `runState.ts:20` | 9 · 4 |
| ② 뒤집은 결정 | **「계약 변경 필요」 넷 — 둘은 남기고 둘은 뺀다.** 답을 계획이 미리 정했다 | 1 |
| ③ 색인 — 장 목록·절 번호·표 주인 | 새 절을 안 만든다. 확인만 | 11 |
| ③ 라우터 표 | **후보 셋** — 증적을 언제 뽑나 · 회차를 어떻게 세나 · 글꼴은 어디서 오나 | 11 |
| ④ `WORKSTREAMS` 킥오프 | 닫히는 것 + **⑦ 증적 대기줄 공유** | 12 |
| ④ `WORKFLOW` | 현재 위치. **숫자는 기계가 센다** | 12 · 10 |
| ④ `SETUP` | `GRAFANA_PORT` 를 바꾸면 **다시 빌드해야 한다** | 9 |
| ④ `spec-review` · `check:*` | **상태 줄 강제 · 완료 기준 세기** | 10 |
| ④ `DESIGN`·`HOOKS`·`design-mockup` | **해당 없음** — 확인하고 적는다 | 12 |
| ④ 코드에 박힌 상수 | `layout.ts` 의 `3001` · `xlsx.ts` 의 칸 수(주석 포함) | 9 · 3 |
| ⑤ 손으로 적은 숫자 | 색인 분량·완료 기준 개수 전부 **기계가 센다** | 11 · 10 |
| ⑥ 검사 | `check:spec` → `spec-review` H 절 → 게이트 2 | [6] 이 돈다 |

## Plan 메타

할 일 12개 · 예상 묶음 6개 · 구현 규율: TDD · 추가 검증: `npm run check:deps` · `typecheck` ·
`check:workflow` · `check:spec` · `check:tests` · `check:secret-names` · `vite build` ·
`DATABASE_URL=… npm test` **연속 3회**

묶음 — ①{1, 2, 6} ②{3, 10} ③{4, 9} ④{5, 7} ⑤{8} ⑥{11} ⑦{12}

**미분류: `infra/grafana/provisioning/dashboards/test-platform.json`** — 게이트 2 요약까지 들고 간다.

**의존의 이유는 대부분 파일 경합이다** — 할 일 3·4·5·6·7 이 `docs/spec/도메인/리포팅.md` 하나를 같이 쓴다.
구현자 하나·가지 하나라 직렬이 맞다.

**G8 을 따른다** — 새로 만드는 검사는 **한 자리 부숴 빨개지는 것을 보고 그 결과를 PR 에 적는다.**
할 일 9 는 `layout.test.ts` 가 이미 `3001` 을 단언하므로 자연스럽게 걸리고, 할 일 10 은 명시적으로 부순다.

### 게이트 1 이 계획을 어떻게 바꿨나

| | 전 | 후 |
|---|---|---|
| 할 일 | 10개 | **12개** (정수 가드 분리 · 검사기 승격 추가) |
| `QUEUED` | 명세에 적을 뻔했다 | **뺐다** — 저장소에 0건인 상태 (BLOCKER 1) |
| `check:spec` 검증 | 여섯 할 일에 달려 통과 불가 | **할 일 11·12 에만** (BLOCKER 2) |
| DB 검사 | 맨손 `vitest` → 건너뛴 채 초록 | **`DATABASE_URL=…` + 건너뛴 개수 0** (BLOCKER 3) |
| 정수 가드 | 두 자리 | **다섯 자리 전부** (BLOCKER 4) |
| 할 일 1 RED | 「4건」 · 검증이 GREEN 을 부숨 | **범위를 `docs/spec/` 으로 · 닫힘 표시를 센다** (BLOCKER 5) |
| 글꼴 | 방법을 구현자에게 위임 · 문자열 단언 | **base64 확정 · 부술 수 있는 단언 · 먼저 눈으로 본다 · 완료 기준 상자** (BLOCKER 6) |
| 여섯째 | 말없이 뺐다 | **`WORKSTREAMS` 에 ⑦ 로 적는다** (BLOCKER 7) |
| 사유 칸 | 검증 묶음 끝 | **케이스칸 끝** — 항목 단위 값이다 |
| 대시보드 | 「패널 1 을 본뜬다」 | **`bool_or(FAIL)`** — 안 그러면 미실행이 실패로 오른다 |
| Grafana 포트 | 배선이 빠짐 | **compose → Dockerfile → build 세 토막 + §9.1 예시** |

## 리뷰 결과

**렌즈**: `plan-eng-review` + `plan-ceo-review` (3등급 = 2종) · 2026-09-20 · 각각 독립 세션
**판정**: **BLOCKER 7건**(엔지니어링 4 · 제품 3) · **주의 19건**

`plan-design-review` 는 안 불렀다 — 화면 규칙이 안 바뀌고 링크 주소 한 줄만 바뀐다.

**컨트롤러가 직접 실측으로 확인한 것 넷** — `QUEUED` 0건 · `docs/` 전체 14건 vs `docs/spec/` 4건 ·
`routes.test.ts`·`dashboard.test.ts` 의 `skipIf` · `execution/routes.ts:220` 에 `> 0` 없음.

---

### BLOCKER 1 — `QUEUED` 는 이 저장소에 **없는 상태**다

할 일 4 가 「막는 것은 `QUEUED`·`RUNNING` 뿐」이라 적고 그 문장을 명세 §7 에 싣게 한다.
그런데 `grep -rn "QUEUED" apps packages db docs/spec` 이 **0건**이고
`db/migrations/20260916000001_init.sql:35` 주석이 `RUNNING | FINISHED | ABORTED` 다.

**나중에 무엇이 잘못되나** — 명세가 DB 가 절대 만들지 않는 상태 이름을 계약으로 적는다.
다음 세션이 그걸 읽고 죽은 갈래를 만든다. **모순을 닫으러 온 계획이 새 모순을 만든다.**

**고칠 것** — 막는 것은 `RUNNING` 하나. 계획과 명세에서 `QUEUED` 를 전부 뺀다.

### BLOCKER 2 — 여섯 할 일의 `check:spec 0` 검증이 **통과할 수 없다**

렌즈가 실증했다. `docs/spec/도메인/리포팅.md` 에 빈 줄 하나만 붙여도 색인 분량이 4건 어긋난다.
그런데 할 일 **1·3·4·5·7·8** 이 전부 `docs/spec/**` 를 고치면서 `check:spec 0` 을 검증에 달았고,
색인을 맞추는 것은 할 일 **9** 뿐인데 9 는 그 여섯에 `depends-on` 이 걸려 있다.

**나중에 무엇이 잘못되나** — 구현자가 할 일 1 에서 빨간 게이트를 만나 ① 멈추거나
② 손 숫자를 여섯 번 고치거나(§2.7 ⑤ 가 금지한 짓) ③ 게이트를 조용히 무시한다.

**고칠 것** — 그 여섯의 검증에서 `check:spec` 을 뺀다. 「색인 분량은 할 일 9 가 한 번에 맞춘다.
그때까지 `check:spec` 은 빨간 채로 둔다」를 계획에 적는다.

### BLOCKER 3 — 할 일 4·5·7 의 새 단언이 **아예 안 돈다** (초록 거짓말)

`apps/admin/src/reporting/routes.test.ts:24` 와 `dashboard.test.ts:34` 가
`describe.skipIf(연결 === undefined)` 다. 계획의 검증은 맨손 `npx vitest run …` 이라
**`DATABASE_URL` 이 없으면 새 단언이 SKIP 되고 종료코드 0 이 난다.**

**나중에 무엇이 잘못되나** — RED 가 한 번도 안 빨개지고 GREEN 도 공짜다.
409 가드 · 파일 이름 · 큰 숫자 수정 **셋 다 실행된 적 없이 「통과」로 PR 에 적힌다.** G8 이 성립 불가능해진다.

**고칠 것** — 그 셋의 검증을 `DATABASE_URL=… npx vitest run …` 으로 바꾸고 **건너뛴 개수가 0** 임을 PR 에 적는다.
(할 일 3·6·8 의 파일은 `skipIf` 가 없다 — 확인했다.)

### BLOCKER 4 — 할 일 7 이 같은 500 을 **문 셋에 그대로 남긴다**

`apps/admin/src/execution/routes.ts` 의 `:108`·`:152`·`:158` 이 `Number(req.params...)` 를
**검사 없이** 넘긴다. `NaN` 도 `1e21` 도 그대로 pg 로 간다.
그리고 `:220` 은 `Number.isInteger` 만 봐서 **`0`·`-1` 도 통과한다** — 계획이 적은 「같은 부류」가 아니라 **다른 구멍**이다.

**나중에 무엇이 잘못되나** — PR 에 「큰 숫자가 500 을 안 낸다」가 적히는데 `GET /api/runs/1e21` 은 여전히 500 이다.
**기록이 또 거짓말을 한다** — 이 계획이 닫으려는 바로 그 죄다.

**고칠 것** — 가드 하나를 **다섯 자리 전부**에 건다. 범위가 커지므로 정수 상한 쪽을 독립 할 일로 가른다.

### BLOCKER 5 — 할 일 1 이 **자기 안에서 모순**이고 첫 줄 숫자가 틀렸다

RED 가 「4건」이라 했는데 `docs/` 전체는 **14건**이다(`docs/spec/` 4 + 진행·계획·검토 기록 5 + 계획 자신 5).
검증 `grep -rc … docs/spec/` 은 **숫자 하나를 안 낸다**(파일마다 한 줄).
그리고 **GREEN 과 검증이 서로를 부순다** — GREEN 이 「남기고 덧붙인다」를 정상 결말로 허용하는데
남기면 그 낱말이 남아 **0건이 영원히 안 된다.**

**나중에 무엇이 잘못되나** — 구현자가 0건을 맞추려고 **남겨야 할 블록과 append-only 인 기록까지 지운다.**
거짓 기록을 지우겠다는 첫 할 일이 **거짓 숫자로 시작한다.**

**고칠 것** — 범위를 `docs/spec/` 으로 못 박고, 검증을 **닫힘 표시를 세는 쪽**으로 뒤집는다.

### BLOCKER 6 — 할 일 6 이 **본 적 없는 증상**을 고치고 단언이 항진명제다

`docs/reviews/**`·`LEARNINGS`·`progress/**` 어디에도 **사람이 생성된 증적 PDF·HTML 을 열어본 기록이 0건**이다.
글꼴 문제는 코드를 **읽어서** 찾았지 문서를 **열어서** 찾은 것이 아니다.
완료 기준 59상자 중 **PDF 의 글자 모양을 보는 상자가 0개**다.

RED 「`@font-face` 가 있고 글꼴 데이터가 박혀 있다」는 **문자열 검사**라 base64 가 잘려도,
글꼴 이름이 안 맞아도 **초록이 뜬다** — 직전 PR 이 **G8** 을 만든 이유가 정확히 이 모양이다.

그리고 증상이 과소평가일 수 있다 — admin 이미지가 `playwright:v1.63.0-jammy` 인데
**한글 글꼴이 들었는지 아무도 확인 안 했다.** 숫자 폭이 아니라 **한글이 두부(□□□)로 나올 수도** 있다.

**고칠 것** — ① **먼저 지금 PDF 를 한 부 뽑아 열고** 무엇이 깨지는지 눈으로 보고 계획에 적는다
② 단언을 부술 수 있는 모양으로(`@font-face` 의 이름과 `body` 의 `font-family` 가 같은가 · base64 길이가 파일 크기와 맞는가)
③ **완료 기준에 상자를 더한다** — 「증적 PDF 를 열면 한글이 안 깨지고 숫자가 줄 맞는다」.
**이건 명세 변경이라 게이트 0 에 없었다 — 사용자에게 물어야 한다.**

### BLOCKER 7 — 빠뜨린 여섯째를 **안 적었다**

증적 만들기가 실행 대기줄을 공유한다(`generate.ts` 의 `찍는다()` → `dispatcher.js` 의 `enqueue`, 취소 통로 없음).
큰 실행이 도는 동안 다른 실행의 증적을 누르면 **PENDING 으로 굳고 회복 수단이 admin 재기동뿐**이며
**오류 문장이 한 줄도 안 뜬다.**

**이번에 고치는 것은 범위 밖이 맞다**(다른 갈래 소유 경로 · 새 능력이라 게이트 0 · 드물다).
**틀린 것은 장부다** — 계획이 **말없이 뺐다.** 다음 세션이 「접었다」와 「깜빡했다」를 구분 못 한다.

**고칠 것** — 할 일 10 에서 `WORKSTREAMS.md` 「넘긴 것」에 한 줄. **아는 것을 안 적는 것이 이 계획이 지우려는 거짓말의 원형이다.**

---

### 주의 (요점만)

| # | 무엇 | 어디 |
|---|---|---|
| 1 | **사유 칸 자리가 틀렸다** — `notRunReason` 은 **항목 단위**인데 검증 묶음 끝에 넣으면 한 항목이 6행일 때 **같은 사유가 6번 반복**된다. 제자리는 케이스칸인데 그러면 13~24칸이 밀려 검사를 훨씬 많이 고친다 — **맞바꿈인데 계획이 안 적고 싼 쪽을 골랐다** | 할 일 3 |
| 2 | `notRunReason` 이 **두 가지**를 싣는다 — 미실행사유와 판정못낸사유(`NA`+error). 칸 이름이 둘 다 안 덮으면 **고치려던 문장이 여전히 코드와 다른 말** | 할 일 3 |
| 3 | **패널 1 을 맹목으로 베끼면 미실행이 실패로 집계된다.** `bool_and(PASS)` 는 `NA` 도 「통과 아님」으로 본다 → **한 번도 안 돈 케이스가 실패 순위에 오른다.** `bool_or(status='FAIL')` 이어야 한다 | 할 일 5 |
| 4 | 할 일 8 의 `files` 에 **`docker-compose.yml` 이 빠졌다.** `Dockerfile` 에 `ARG` 가 없고 compose 의 admin `build:` 에 `args:` 가 없다 — 세 토막 배선이 필요하다 (COMPOSE 는 3등급·§1.3 대상) | 할 일 8 |
| 5 | 할 일 8 의 한계 서술이 **진짜 고장을 안 적는다** — `SETUP.md` 는 지금 포트를 자유롭게 바꾸라고 안내한다. 바꾸고 다시 안 빌드하면 **오류 없이 링크만 틀린다** | 할 일 8 |
| 6 | **「대안은 게이트 0 뿐」이 사실이 아니다** — admin 이 `index.html` 에 값을 심는 셋째 길이 있다(API 응답 형태가 아니라 게이트 0 불필요, 다시 빌드도 불필요) | 할 일 8 |
| 7 | **제품 렌즈는 할 일 8 을 「주석 한 문단 수정」으로 줄이라고 한다** — 빌드 시에 구우면 상수를 한 자리에서 다른 자리로 옮길 뿐이고 계획이 스스로 한계를 인정했다. **엔지니어링 렌즈는 유지하되 배선을 채우라고 한다 — 두 렌즈가 갈린다** | 할 일 8 |
| 8 | 「도는중」의 정본은 `evidence.ts:31` 이 아니라 **`runState.ts:20`** 이다 (`status === 'RUNNING'`). `QUEUED` 를 빼면 양쪽이 **같은 비교 한 줄**로 수렴한다 | 할 일 4 |
| 9 | **검사기는 과하다** — 판정 값이 하나뿐이다. `check:secret-names` 는 **늘어나는 목록**이라 값을 했다. 주석이 같은 명세 절을 가리키면 충분 | 할 일 4 |
| 10 | `실행이있는가` 는 `SELECT 1` 이라 상태를 보려면 **왕복이 하나 더** 생긴다 → `실행상태(runId)` 로 바꿔 쿼리 하나로 | 할 일 4 |
| 11 | **할 일 6 의 견주기는 이미 답이 나와 있다** — `html.ts` 에 `@font-face` **0건**이므로 **HTML 증적도 같은 문제다.** `goto('file://…')` 길은 탈락(게다가 `setContent` 라 훨씬 큰 변경). **base64 로 확정**하고 비용만 적으면 된다 | 할 일 6 |
| 12 | 비용표가 틀렸다 — **2MB 가 아니라 2.74MB**(base64) · **형식 셋이 아니라 둘**(XLSX 는 `renderHtml` 을 안 탄다) · **PDF 는 안 커지고 HTML 파일만** 커진다 · `renderHtml` 이 **동기→비동기**가 되면 `generate.test.ts` 까지 번진다 | 할 일 6 |
| 13 | 글꼴 파일이 `web/fonts/` 에 있다 — **화면 갈래 소유 경로**다. 서버가 거기를 읽으면 갈래 사이 의존이 생긴다. `generate.ts:24` 주석이 바로 그걸 경고한다 | 할 일 6 |
| 14 | **글꼴이 「재현 불변식」의 입력이 된다** — 글꼴을 바꾸면 **과거 증적의 바이트가 달라진다**(§3.3 · 완료 기준 「글자 하나까지 같다」). 계획에 이 말이 없다 | 할 일 6 |
| 15 | **「계약 변경 필요」 넷 중 둘의 답만 적혀 있다** — 러너(남긴다)·인프라 §53(뺀다). 나머지 둘은 안 적혀 있어 **구현자가 예시를 베끼고 각자의 「이유」 칸을 안 읽는다.** 위험은 「다른 답」이 아니라 **「안 따져 본 답」** | 할 일 1 |
| 16 | 할 일 **2·10 의 RED 는 RED 가 아니다** — 눈 대조와 수집이다. **「확인 절차 (RED 없음)」로 이름 붙이고 왜 없는지 적어라.** 억지로 검사기를 만들지 마라 | 할 일 2·10 |
| 17 | `depends-on` 이 **파일 경합을 논리 의존으로 적었다.** 할 일 7 은 4 와 논리 관계가 없다 — 4 가 멈추면 7 까지 같이 멈춘다. **7 을 `[]` 로 풀어라** | 할 일 7 |
| 18 | `xlsx.ts:111` 주석의 「24칸을 가로로 훑는 표라」도 **손 숫자**다 | 할 일 3 |
| 19 | `머리행` 에 **`'판정'` 이라는 같은 이름 칸이 둘** 있다(11번·23번). 기존 문제라 범위 밖 — **보고만** | — |

### 제품 렌즈가 따로 낸 것 — 순서와 승격

- **①이 PR 을 가르라고 한다** — 할 일 6 만 떼어 먼저 병합하고 나머지 아홉을 한 PR 로.
  근거 — 검수처로 나가는 문서의 **바이트를 바꾸는 유일한 항목**이고, 문서마다 커지며,
  **사람이 PDF 를 열어봐야만** 통과를 안다. 문서 정리 아홉과 섞이면 **그 한 번이 안 일어난다**
- **④상자가 먼저다** — 완료 기준 상자 **최소 셋이 지금 거짓**이다(실행 중 증적·엑셀 받기·엑셀 한 행).
  제안 순서는 `6 → 4 → 7` → **사용자가 상자 59 한 바퀴** → `3·5` → `1·2` → `9·10`
- **⑤승격 셋** — ⓐ `[계약 변경 필요]` 블록에 `상태:` 줄을 강제하는 검사기(**BLOCKER 5 도 같이 풀린다**)
  ⓑ 완료 기준에 「증적 문서를 눈으로 본다」 상자(**명세 변경 — 게이트 0 에 없었다**)
  ⓒ **59/0 을 기계가 세게 한다** — 지금 할 일 10 이 「구현이 다 됐다」를 **손으로** 적으려 한다.
  그게 다음 거짓말 후보다

### 통과한 것

- **묶음 여섯은 과하지 않다** — 구현자 하나·가지 하나고, 211줄 명세 파일 하나를 쓰는 넷을 줄 세우는 것은 옳다.
  다만 **의존의 이유를 「파일 경합」이라고 적어라**
- **할 일 7 의 기능 둘은 커밋만 가르면 된다** — §2.2 는 「기능 하나 = 커밋 하나」지 「할 일 하나 = 커밋 하나」가 아니다.
  **단 BLOCKER 4 를 반영해 다섯 자리로 커지면 갈라야 한다**
- **할 일 1 의 위임은 맞다** — §2.7 ② 가 다른 답을 명시적으로 허용한다. 표만 채워 주면 된다
- **여섯째를 이번에 안 고치는 판단은 옳다** — 고치려면 게이트 0 이 필요하다
- 이미 있어 새로 안 만들어도 되는 것 — 사유 값(`collect.ts` 의 `notRunReason`) · 회차 접기 SQL 본(패널 1) ·
  정수 가드(`정수()`) · `@font-face` 본(`styles.css:7~16`) · `data:` 로 심는 본(`generate.ts:35`)
