# 작성 에이전트를 서버 컨테이너로

등급: 3 · 갈래: WS-작성 · 2026-09-23 · PR #68

## 도메인 정리

- BC: 작성(§3.6 · §7) · 배포(공통/6 §9) · 컨텍스트 지도(공통/1 §3) · 인증(§7 에이전트 토큰)
- 계약 변경(게이트 0 승인, 2026-09-23): compose 새 서비스 `author` · `.env` 새 값 셋(`CLAUDE_CODE_OAUTH_TOKEN`·`GH_TOKEN`·`AUTHORING_AGENT_TOKEN`) ·
  「맥에서 돈다」 → 「서버 컨테이너가 기본, 맥은 같은 스크립트의 대체 경로」 · 이미지에 claude CLI·gh
- DB·API 계약은 안 바뀐다. 에이전트는 지금처럼 admin 을 HTTP 로 부르고 에이전트 토큰으로 붙는다

## 왜

「맥 에이전트를 켜 두는 방식은 사용성이 너무 떨어진다」(사용자, 2026-09-23). 브라우저는 맥의 프로그램을 못 켜고,
로그인 시 자동 실행도 거절됐다. 서버 안에서 돌면 보내기 → 5초 안에 작성이 시작되고, 누가 어느 기계에서 보내든 같다.
`claude setup-token` 이 구독용 장기 토큰을 준다(`claude setup-token --help`: "requires Claude subscription") — 추가 비용 0 을 지킨다.

부수 효과: 서버와 에이전트가 같은 저장소 폴더를 보므로 병합 뒤 당기기가 서버에서도 먹는다 (PR #67 후속 하나가 닫힌다).

## Plan

### 할 일 1. SPEC 을 먼저 고친다

- **GREEN** — 작성 §3.6 「서버에서 도는 이유」와 결정표 새 행(맥 → 서버, 옛 판단 남김) · 공통/1 §3 컨텍스트 지도 ·
  공통/6 §9 compose 표·설정값 · 인증 §7 「토큰은 서버에서는 .env」 · SPEC.md 라우터 표
- RED·REFACTOR — 해당 없음

**files**: docs/spec/도메인/작성.md, docs/spec/공통/1-제품과-구조.md, docs/spec/공통/6-인프라.md, docs/spec/도메인/인증.md, docs/SPEC.md
**depends-on**: []
**검증**: `npm run check:spec`

### 할 일 2. 주소 검사 — compose 안 이름은 같은 기계다

- **RED** — `주소안전한가('http://admin:3000')` 가 `null` · `http://qa.example.com` 은 여전히 막는다 · `http://10.0.0.5` 도 막는다
- **GREEN** — 점이 없는 호스트 이름(컨테이너 네트워크 안)을 localhost 와 같이 친다

**files**: scripts/authoring-rules.ts, scripts/authoring-agent.test.ts
**depends-on**: []
**검증**: `npx vitest run scripts/authoring-agent.test.ts`

### 할 일 3. 토큰 — 서버에서는 환경값에서 읽는다

- **RED** — `토큰고르기({ AUTHORING_AGENT_TOKEN: 좋은토큰 }, 파일값)` 가 환경값 · 없으면 파일값 · 환경값 모양이 틀리면 던진다 ·
  env 가드 검사는 `AUTHORING_AGENT_TOKEN` 한 이름만 허용
- **GREEN** — `authoring-token.ts` 에 순수 함수, `authoring-agent.ts` 켜기에서 부른다. 환경값이면 파일에 저장하지 않는다

**files**: scripts/authoring-token.ts, scripts/authoring-token.test.ts, scripts/authoring-agent.ts, scripts/authoring-agent.test.ts
**depends-on**: []
**검증**: `npx vitest run scripts`

### 할 일 4. 이미지 — `apps/authoring/Dockerfile`

- **GREEN** — Playwright 이미지(러너와 같은 태그) + `gh`(apt) + `@anthropic-ai/claude-code`(전역) · `npm ci` 로 `/repo/node_modules`(리눅스용) ·
  시작 스크립트: `git config --global safe.directory '*'` · `gh auth setup-git` · `~/.claude/settings.json` 에 `Bash(*)` 허용 · `npx tsx scripts/authoring-agent.ts`
- **검증** — `docker build` 성공 · 컨테이너에서 `claude --version`·`gh --version` · 토큰 없이 켜면 [거부] 로 끝난다

**files**: apps/authoring/Dockerfile, apps/authoring/start.sh
**depends-on**: [3]
**검증**: `docker build -f apps/authoring/Dockerfile .` + 위 세 줄

### 할 일 5. compose — `author` 서비스 · `.env.example`

- **GREEN** — 저장소 뿌리 `./:/repo` 바인드 + `author_node_modules:/repo/node_modules` 볼륨 · env 넷 · `depends_on: admin` · `mem_limit` ·
  `.env.example` 세 값과 만드는 법
- **검증** — `docker compose config` 통과 · 토큰 없이 `up author` → 로그에 [거부] 사유

**files**: docker-compose.yml, .env.example, apps/admin/src/deploy.test.ts
**depends-on**: [4]
**검증**: `docker compose -p test_platform --env-file .env config >/dev/null` · `npx vitest run apps/admin/src/deploy.test.ts`

### 할 일 6. 안내 — SETUP §8 을 서버 기본으로

- **GREEN** — 서버 설치 세 값(setup-token · GitHub 토큰 권한 · 에이전트 토큰 발급) → `up -d author` → 로그 보기. 맥 실행은 「대체 경로」 절로.
  전용 OS 계정 절은 맥 경로 아래로
- **검증** — 적힌 순서를 처음부터 따라 해 본다(할 일 7)

**files**: docs/SETUP.md, docs/progress/WS-작성.md, docs/LEARNINGS.md
**depends-on**: [5]
**검증**: 눈으로

### 할 일 7. 실측 — 서버에서 한 바퀴

- 사용자가 `claude setup-token` 과 GitHub 토큰을 `.env` 에 넣은 뒤(사람만 할 수 있다) `up -d author` → DEMO 로 기획서 보내기 → PR 까지
- 과금이 구독으로 잡히는지 첫 실행에서 확인(`claude` 출력·계정 사용량)

**files**: (없음 — 결과를 progress 에)
**depends-on**: [6]
**검증**: 요청이 DONE 이고 PR 이 열린다

## SPEC 동반 수정 (§2.7)

- ① `grep -rn "맥에서\|컨테이너 밖\|맥 스크립트" docs/spec docs/SPEC.md` 로 맥을 전제한 문장을 찾아 할 일 1 이 본다
- ② 「서버에 Claude 토큰도 GitHub 토큰도 심을 필요가 없다」는 **근거가 사라졌다가 아니라 사용성과 맞바꿨다** — 남기고 왜 뒤집었는지 덧붙인다
- ③ 라우터 표 「맥 작성 경로를 바꿀 때」 줄을 서버 기본으로 고친다
- ④ SETUP §8 · WORKSTREAMS 킥오프(맥 전제 문장) · `deploy.test.ts`(compose 서비스 목록을 보는지) · 코드 상수 없음
- ⑤ 개수 적지 않는다 · ⑥ `check:spec`

## Plan 메타

할 일 7개 · 묶음 [1,2,3] → [4] → [5] → [6] → [7] · TDD(2·3) · 추가 검증: DB 붙인 `npm test` 연속 3회 · 이미지 빌드

## 리뷰 결과

(계획 검토가 채운다)
