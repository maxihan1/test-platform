# 어드민 화면 전면 리뉴얼 ② — 목록에서 값을 고치고 상세를 펼친다

등급: 3 · 갈래: WS-E · 2026-09-21

## 도메인 정리

| BC | 무엇을 건드리나 | 관련 절 |
|---|---|---|
| Execution | `GET /api/runs/last-by-case` 응답에 `recent` 를 더한다. 질의 하나 | 실행 §7 · 공유계약 §7.1 |
| Catalog | 목록 화면의 줄 구성이 바뀐다. **카탈로그 API 는 안 바뀐다** | 카탈로그 §8.1 |
| Reporting | 건드리지 않는다 | — |

**경계를 넘지 않는다.** 판정 흐름 데이터는 `run_item` 에서 오므로 **실행 쪽이 낸다** —
카탈로그가 `run_item` 을 읽으면 컨텍스트 경계가 깨진다 (카탈로그 §3.1).
지금 `last-by-case` 가 그 이유로 실행 쪽에 있고, 같은 자리에 얹는다.

## 왜

케이스 목록이 **읽는 자리**에 머물러 있다. 하나를 돌리려면 목록 → 실행 설정 화면으로 나가고,
그 케이스가 무슨 값을 쓰는지 보려면 또 한 번 들어간다. 값이 무엇인지 확인하는 데 화면 둘을 건넌다.

이 PR 이 목록을 **거기서 끝나는 자리**로 만든다.

### 착수 전에 코드를 읽어 알아낸 것 — 새로 만들 것이 생각보다 적다

| 하려는 것 | 필요한 데이터 | 이미 있나 |
|---|---|---|
| 파라미터 인라인 입력 | `paramSchema` · `expectedSchema` | **목록 응답에 이미 있다.** `schema.ts` 의 `schemaToFields` · `initialText` · `toValues` 가 그대로 쓰인다 |
| 고친 값을 실행으로 | `POST /api/runs` 의 `params` · `expected` | **이미 그렇게 돈다.** 모달이 쓰는 `글자표` 와 같은 모양이면 그대로 이어진다 (실행 §8.10) |
| 상세 — 사전조건 · 입력값 · 기대결과 | `precondition` · 스키마 둘 | **목록 응답에 이미 있다.** 부를 것이 없다 |
| 상세 — 시험 절차 | 마지막 실행의 절차 | `GET /api/runs/:runId/items/:historyId` (실행 §7) |
| 상세 — 실행 이력 | 최근 N회 | `GET /api/cases/:tcId/history` (실행 §7) |
| **줄의 판정 흐름 막대** | 케이스×디바이스마다 **최근 5회** | **없다** ← 계약 변경 하나. 게이트 0 승인 |

### 왜 흐름 막대만 통로가 필요한가

카탈로그 §8.1 이 못을 박아 뒀다 — 「`마지막 결과` 칸은 `GET /api/runs/last-by-case` **한 번**으로
전부 채운다. 케이스마다 이력을 따로 부르지 않는다」.

줄마다 막대를 그리려면 케이스 수만큼 이력을 불러야 해 그 규칙을 정면으로 어긴다.
그래서 **이미 한 번 부르는 그 응답에 얹는다.** 부르는 횟수는 그대로 1회다.

**상세 펼침은 이 규칙에 안 걸린다** — 목록이 부르는 것이 아니라 **사람이 한 줄을 폈을 때** 한 번 부른다.

## Plan

### 할 일 1. `last-by-case` 가 최근 5회 판정을 같이 준다

- **RED** — `runs.test.ts` 에 「`XBX-001` 의 `recent` 가 새 것부터 쌓이고 맨 앞이 `status` 와 같다」를 단언한다.
  지금 응답에 `recent` 칸이 아예 없어 `undefined` 로 실패한다
- **GREEN** — `lastByCase` 의 `DISTINCT ON` 을 `ROW_NUMBER() OVER (PARTITION BY tc_id, platform ORDER BY started_at DESC, history_id DESC)` 로 바꿔 5건까지 받고, 1번 행이 지금 주던 값 그대로가 되게 한다
- **REFACTOR** — 최근 몇 건인지를 상수 하나로 둔다 (`최근몇건 = 5`). 숫자를 질의와 화면 둘에 적지 않는다

**files**: `apps/admin/src/execution/history.ts` · `apps/admin/src/execution/runs.test.ts`
**depends-on**: []
**검증**: `DATABASE_URL='postgres://platform:platform@localhost:5433/platform' npx vitest run apps/admin/src/execution/runs.test.ts`

> fixture 접두사는 **그 파일이 이미 쓰는 `XBX`** 다. 새 접두사를 만들지 않는다 —
> 만들면 CLAUDE.md §3 의 접두사 표에 줄을 더해야 하고, 그 표가 길어질 이유가 없다.

### 할 일 2. 판정 흐름을 케이스 줄에 그린다

- **RED** — `Summary.test.tsx` 에 「막대 다섯이 판정마다 **다른 높이 클래스**를 갖고, 옆에 `통과 4 · 실패 1` 글자가 같이 나온다」.
  `판정흐름` 조각이 없어 실패한다. **색만으로 판정을 전달하지 않는다** (spec-review F6)
- **GREEN** — `Summary.tsx` 에 `판정흐름({ recent })` 를 더하고 `CaseListParts` 의 줄에 건다.
  `api.ts` 의 `LastResult` 에 `recent: ItemStatus[]` 를 더한다
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/Summary.tsx` · `apps/admin/src/web/Summary.test.tsx` · `apps/admin/src/web/CaseListParts.tsx` · `apps/admin/src/web/CaseListParts.test.tsx` · `apps/admin/src/web/api.ts`
**depends-on**: [1]
**검증**: `npx vitest run apps/admin/src/web/Summary.test.tsx apps/admin/src/web/CaseListParts.test.tsx`

### 할 일 3. 케이스 줄의 값이 **고칠 수 있는 칸**이 된다

- **RED** — `CaseListParts.test.tsx` 에 「입력칸에 기본값이 이미 채워져 있고, 고치면 `on값` 이 그 값으로 불린다」.
  지금은 읽기만 하는 `<span>` 이라 입력칸 자체가 없어 실패한다
- **GREEN** — `값칩들` 을 `값칸들` 로 바꾼다. `schemaToFields` 로 칸을 만들고 `initialText` 로 채운다.
  `enum` 은 고르개, `boolean` 은 예·아니오 고르개, 비밀값은 `type="password"` (판단은 `mask.ts` 그대로)
- **REFACTOR** — 칸이 여럿이면 **세로로 쌓는다.** 가로로 감으면 라벨과 값이 서로 다른 줄로 갈린다

**files**: `apps/admin/src/web/CaseListParts.tsx` · `apps/admin/src/web/CaseListParts.test.tsx`
**depends-on**: []
**검증**: `npx vitest run apps/admin/src/web/CaseListParts.test.tsx`

### 할 일 4. 목록에서 고친 값이 실행에 그대로 쓰인다

- **RED** — `CaseList.test.tsx` 에 「목록 칸의 값을 고치고 `전체 실행` 을 누르면 **모달에 그 값이 떠 있다**」.
  지금은 목록의 값이 아무 데도 안 가서 실패한다
- **GREEN** — `CaseList` 가 `글자표`(모달과 **같은 모양**)를 들고, `RunPickModal` 에 `초기글자` 로 넘긴다.
  모달의 `useState` 시작값을 그 값으로 바꾼다
- **REFACTOR** — `글자표` 타입을 `RunPickModal` 에서 꺼내 한 곳에 둔다. 두 벌이면 한쪽이 뒤처진다

**files**: `apps/admin/src/web/CaseList.tsx` · `apps/admin/src/web/CaseList.test.tsx` · `apps/admin/src/web/RunPickModal.tsx` · `apps/admin/src/web/RunPickModal.test.tsx` · `apps/admin/src/web/pickRun.ts`
**depends-on**: [3]
**검증**: `npx vitest run apps/admin/src/web/CaseList.test.tsx apps/admin/src/web/RunPickModal.test.tsx`

### 할 일 5. `상세` 버튼과 펼침 — 손에 이미 있는 것부터

- **RED** — `CaseDetail.test.tsx` 에 「처음엔 사전조건이 안 보이고, `상세` 를 누르면 보인다」.
  조각이 없어 실패한다
- **GREEN** — `CaseDetail.tsx` 를 새로 만든다. 사전조건 · 입력값 표 · 기대결과 표 셋을 `CaseRow` 만으로 그린다.
  줄에 `상세` 버튼을 더하고 `aria-expanded` 를 붙인다
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/CaseDetail.tsx` · `apps/admin/src/web/CaseDetail.test.tsx` · `apps/admin/src/web/CaseListParts.tsx` · `apps/admin/src/web/CaseListParts.test.tsx`
**depends-on**: [3]
**검증**: `npx vitest run apps/admin/src/web/CaseDetail.test.tsx apps/admin/src/web/CaseListParts.test.tsx`

### 할 일 6. 펼쳤을 때만 시험 절차와 실행 이력을 불러온다

- **RED** — `CaseDetail.test.tsx` 에 두 가지. ① 접혀 있는 동안 `api.caseHistory` 가 **한 번도 안 불린다**
  ② 펼치면 불리고 이력 다섯 줄이 뜬다. 지금은 부르는 코드가 없어 ②가 실패한다
- **GREEN** — `api.ts` 에 `caseHistory(tcId)` 를 더한다 (경로는 **이미 있는** `/cases/:tcId/history`).
  절차는 마지막 결과가 있을 때만 `api.item(runId, historyId)` 로 받고, 없으면 「아직 돌린 적이 없습니다」를 적는다
- **REFACTOR** — 없음

**files**: `apps/admin/src/web/CaseDetail.tsx` · `apps/admin/src/web/CaseDetail.test.tsx` · `apps/admin/src/web/api.ts` · `apps/admin/src/web/api.test.ts`
**depends-on**: [5]
**검증**: `npx vitest run apps/admin/src/web/CaseDetail.test.tsx apps/admin/src/web/api.test.ts`

### 할 일 7. 새 자리들의 화면 규칙을 CSS 로 못 박는다

- **RED** — `styles.test.ts` 에 넷. ① 입력칸 글자색이 바탕 대비 4.5 이상 ② 흐름 막대가 판정마다 **높이가 다르다**
  ③ 펼침 패널이 줄과 **다른 바탕**을 쓴다 ④ 좁은 화면(620px 미만)에서 입력칸이 가로로 안 넘친다.
  규칙이 없어 전부 실패한다
- **GREEN** — `styles.css` 에 `.pcell` · `.prow` · `.pinput` · `.spark` · `.sparktext` · `.detail` · `.dsec` 를 더한다.
  값은 `docs/design-mockup.html` 에 확정돼 있다 — **옮겨 적고 다르면 목업이 아니라 이 파일이 틀린 것이다**
- **REFACTOR** — 쓰는 토큰이 전부 `:root` 에 있는지 기존 검사가 본다. 새 토큰을 만들지 않는다

**files**: `apps/admin/src/web/styles.css` · `apps/admin/src/web/styles.test.ts`
**depends-on**: [2, 3, 5]
**검증**: `npx vitest run apps/admin/src/web/styles.test.ts`

### 할 일 8. 옛 껍데기 이름을 정리한다

PR① 이 **일부러 안 지웠다** — 그 이름으로 단언하는 검사가 있어 한꺼번에 깨지기 때문이다.
이제 같이 고친다. **이름만 바뀌고 화면은 한 픽셀도 안 바뀐다.**

- **RED** — `Shell.test.tsx:55` 의 `.band` 와 `styles.test.ts:116·135` 의 `.nav` 를 새 이름으로 바꾼다.
  구현이 아직 옛 이름이라 실패한다
- **GREEN** — `Shell.tsx` · `styles.css` 에서 `band*` → `side-*`, `nav` → `side-nav`,
  `notice*` → `toast*`, `svc-dot` → `side-dot`
- **REFACTOR** — `Shell.tsx:45` 의 「옛 가로 띠가 이 안으로 들어왔다」 주석을 뺀다. 더 이상 사실이 아니다

**files**: `apps/admin/src/web/Shell.tsx` · `apps/admin/src/web/Shell.test.tsx` · `apps/admin/src/web/styles.css` · `apps/admin/src/web/styles.test.ts`
**depends-on**: [7]
**검증**: `npx vitest run apps/admin/src/web/Shell.test.tsx apps/admin/src/web/styles.test.ts`

## SPEC 동반 수정 (§2.7)

### 할 일 9. 명세 정본과 계약 표를 같이 고친다

「같은 규칙이 적힌 다른 절」을 **낱말로** 훑어 찾았다. 절 번호로는 못 찾는 자리가 셋이었다.

```
$ grep -rn "last-by-case" docs/ apps/ --include='*.md' --include='*.ts' --include='*.tsx'
docs/spec/도메인/카탈로그.md:58    ← 「한 번으로 전부 채운다」
docs/spec/도메인/카탈로그.md:92    ← 「쪽 없이 전체를 한 번에」
docs/spec/도메인/카탈로그.md:108   ← 검색 조건 표 5행
docs/spec/도메인/실행.md:92        ← API 한 줄
docs/spec/공통/3-공유계약.md:121   ← ★ 계약 표의 주인. 「케이스×디바이스마다 최신 1건」
apps/admin/src/auth/scope.ts:42    ← 인증 적용 범위 (바뀌지 않는다)
apps/admin/src/web/CaseList.tsx:2  ← 코드에 박힌 문장
apps/admin/src/execution/routes.ts:176 ← 코드에 박힌 문장
```

- **정본은 `공통/3-공유계약.md:121`** — 「최신 1건」을 「최신 1건 + 최근 5회 판정」으로 고친다.
  **「승패를 요약하지 않는다」는 그대로 남긴다** — 배지는 여전히 마지막 회차 하나다.
  흐름 막대는 요약이 아니라 **다섯 개를 그대로 늘어놓는 것**이라 그 규칙과 부딪히지 않는다
- `도메인/실행.md:92` 는 응답 설명 한 줄만 고친다
- `도메인/카탈로그.md §8.1` — 줄 구성에 **입력칸 · `상세` 버튼 · 판정 흐름** 을 더한다.
  58·59줄의 「한 번으로 전부 채운다 · 케이스마다 이력을 따로 부르지 않는다」는 **그대로 둔다** —
  이번 변경이 그 규칙을 지키려고 한 것이므로 근거가 살아 있다 (§2.7 ②)
- **상세 펼침이 그 규칙의 예외임을 그 자리에 적는다.** 안 적으면 다음 세션이 「이력을 부르네」로 읽고 되돌린다

**files**: `docs/spec/공통/3-공유계약.md` · `docs/spec/도메인/실행.md` · `docs/spec/도메인/카탈로그.md`
**depends-on**: []
**검증**: `npm run check:spec`

### 할 일 10. SPEC 밖 여섯 곳

| 어디 | 무엇 |
|---|---|
| `docs/SPEC.md` | 절 번호 표 · 표 주인 · **「기능을 더하거나 고칠 때」 라우터 표**에 「케이스 상세를 보고 싶다」 줄 · 장별 줄 수 |
| `docs/WORKSTREAMS.md` | WS-E 킥오프(602~603줄 부근) — 목록 줄 구성 |
| `docs/DESIGN.md` | 인라인 입력칸 · 펼침 패널 · 흐름 막대. **판정을 색만으로 전하지 않는다**를 막대 규칙에 명시 |
| `docs/spec/공통/7-데모와-완료.md` | 완료 기준에 「목록에서 값을 고쳐 바로 돌릴 수 있다」 |
| `docs/WORKFLOW.md` | 눌러볼 길 — 화면리뉴얼 ② |
| **코드에 박힌 문장 둘** | `CaseList.tsx:2` · `execution/routes.ts:176` 의 머리 주석 |

`docs/design-mockup.html` 은 **이미 이 판이다** (확정 목업이 그대로 들어가 있다). 눈으로 대조만 하고 안 고친다.

**files**: `docs/SPEC.md` · `docs/WORKSTREAMS.md` · `docs/DESIGN.md` · `docs/spec/공통/7-데모와-완료.md` · `docs/WORKFLOW.md` · `apps/admin/src/web/CaseList.tsx` · `apps/admin/src/execution/routes.ts`
**depends-on**: [9]
**검증**: `npm run check:spec && npm run check:workflow`

## Plan 메타

할 일 10개 · 예상 묶음 5개 · 구현 규율: TDD
추가 검증: `npm run check:deps && npm run typecheck && npm run check:tests` ·
DB 를 건드리므로 `DATABASE_URL` 붙여 **연속 3회** (spec-review G3)

## 리뷰 결과

**렌즈**: `gstack:/plan-eng-review` (3등급 = 렌즈 셋 중 첫째) · 2026-09-21 · `[자기검사]` — 계획을 쓴 세션이 돌렸다
**판정**: BLOCKER 2건 · 주의 7건

### BLOCKER 1 — 입력칸을 새로 짜면 **비밀번호 관리자 함정**을 다시 밟는다

`Form.tsx:76-81` 이 이미 그 함정을 막고 있고, 왜 막는지를 주석으로 적어 뒀다.

```
type={field.secret ? 'password' : 'text'}
// 브라우저 비밀번호 관리자가 이 관리 화면의 로그인 비밀번호(Login.tsx 의
// current-password)를 후보로 내민다. 잘못 고르면 그 값이 테스트 입력값으로 서버에 가
// run_item.params 에 평문으로 남는데, 화면과 증적에는 ******** 로 가려져 들어간 줄도 모른다
autoComplete={field.secret ? 'new-password' : 'off'}
```

**할 일 3 의 GREEN 은 `type="password"` 만 적고 `autoComplete` 을 안 적는다.**
새로 그리면 이 한 줄이 빠지고, 빠진 것이 **증적에 평문으로 남는데 화면에는 안 보인다.**

**고칠 것** — 새로 그리지 말고 `Form` 을 쓴다. `Form` 은 `Field[]` 를 받아 enum·boolean·숫자·비밀값을
전부 그리고 기본값 줄(`기본값 … 에서 바꿈`)과 사유 줄까지 낸다. 줄에서 좁아 보이는 것은 CSS 로 한다.
(confidence 9/10)

### BLOCKER 2 — 판정 흐름을 「줄에 하나」로 그릴 수 없다. **데이터가 디바이스마다다**

- `history.ts` 의 `LastResult` 는 `(tcId, platform)` 단위다. `recent` 도 그 단위로 붙는다
- `CaseListParts.tsx` 의 `케이스줄` 은 `row.platforms.map(...)` 으로 **디바이스마다** `<Verdict>` 를 그린다
- 그런데 목업(`design-mockup.html:496-498`)의 그 칸은 배지 **하나**에 막대 **한 줄**이다 — **제품과 구조가 다르다**

**할 일 2 는 「케이스 줄에 그린다」고만 적고, 할 일 7 은 「목업과 다르면 목업이 아니라 이 파일이 틀린 것」이라고 적는다.
두 문장이 부딪힌다.** 그대로 두면 구현자가 목업을 따라 제품 구조를 부수거나, 배지 둘에 막대 하나를 붙인다.

**고칠 것** — ① 할 일 2 에 「흐름 막대는 **디바이스마다** 둔다」를 못 박는다 ② 할 일 7 의 「목업이 정본」을
**값(색·크기·간격)에만** 한정한다. **자리는 제품 구조를 따른다.** (confidence 9/10)

### 주의 1 — 파일 둘이 300줄 규칙을 넘는다

`CaseListParts.tsx` 233줄 · `CaseList.tsx` 221줄. 할 일 3·5 가 둘 다 앞엣것에 붙고 할 일 4 가 뒤엣것에 상태를 더한다.
CLAUDE.md §3 은 300줄에서 분리하라고 적는다. **할 일 3 의 REFACTOR 를 「값 칸 조각을 파일로 뺀다」로 적는다** —
나중이 아니라 그 사이클 안에서. (9/10)

### 주의 2 — 비밀값 가림에 RED 가 없다

할 일 3 의 RED 는 「기본값이 채워져 있고 고치면 `on값`」뿐이다. 비밀값은 GREEN 에만 나온다.
**규칙이 검사에 안 걸리면 다음 사람이 조용히 깬다.** RED 에 「비밀값 칸은 `type=password` 이고
`autoComplete=new-password` 다」를 더한다. (9/10)

### 주의 3 — 고친 값이 **요청까지** 가는 것을 아무도 안 본다

할 일 4 의 RED 는 「모달에 그 값이 떠 있다」에서 끝난다. 모달 → `POST /api/runs` 의 `params` 한 홉이 남는다.
그 홉이 끊기면 **사람은 고친 값으로 돈 줄 알고 기본값으로 돈 결과를 증적으로 제출한다.**
이 PR 에서 가장 비싼 조용한 실패다. RED 에 「`onRun` 이 받은 `items[0].params` 가 고친 값이다」를 더한다. (9/10)

### 주의 4 — 여러 줄을 펼치면 통로를 그만큼 부른다

할 일 6 은 「펼쳤을 때만 부른다」까지만 정한다. **접었다 다시 펴면 또 부르는지**가 안 적혀 있다.
`ItemDetail.tsx:190` 이 `opened ? api.source(...) : Promise.resolve(null)` 로 같은 모양을 쓰는데,
**그 자리를 그대로 따르면 접을 때마다 버리고 펼 때마다 다시 부른다.** 캐시할지 다시 부를지를 정해 적는다. (8/10)

### 주의 5 — 옛 이름 정리가 **화면을 안 보고** 끝난다

할 일 8 은 검사 두 줄(`Shell.test.tsx:55` · `styles.test.ts:116·135`)만 바꾼다.
그런데 손으로 고칠 곳은 `styles.css` 42곳 · `Shell.tsx` 11곳이고 `.band-repo` · `.band-who` · `.notice-x` 처럼
**검사가 안 붙은 이름이 대부분이다.** 하나만 놓쳐도 그 자리가 스타일 없이 남는데 검사는 초록이다.

**검증 칸에 `grep -c` 가 0 인 것을 더한다.** 기계가 세게 한다. (9/10)

### 주의 6 — 질의 하나가 다섯 배를 읽는다

`lastByCase` 는 배정받은 서비스 전체를 훑는다. `DISTINCT ON` 은 그룹마다 한 줄에서 멈추지만
`ROW_NUMBER() <= 5` 는 다섯 줄을 읽고 응답도 다섯 배가 된다.
**지금 규모(데모 26건)에서는 문제가 아니다.** 바꾸지 말고 할 일 1 에 「왜 5인가 · 어디서 다시 볼 것인가」를 한 줄 적는다. (7/10)

### 주의 7 — 할 일 8 은 이번 기능과 **아무 관련이 없다**

이름 바꾸기는 기능 넷과 선행·후행 관계가 없다. `depends-on: [7]` 은 「CSS 를 두 번 만지지 않으려고」일 뿐이다.
빼면 이 PR 이 **기능만 남는다.** 다만 PR① 이 「PR② 몫」이라 적어 뒀고, 미루면 이름이 한 판 더 거짓말을 한다.
**결정은 게이트 1 이 받는다.** (해당 없음 — 범위 판단)

### 통과한 것

- **경계** — 판정 흐름 데이터를 실행 쪽에 얹었다. 카탈로그가 `run_item` 을 읽으면 경계가 깨진다 (카탈로그 §3.1)
- **계약** — 새 엔드포인트를 안 만들고 **이미 한 번 부르는 응답에 얹어** §8.1 「한 번으로」를 지켰다
- **§2.7** — 정본을 `공통/3-공유계약.md:121` 로 짚었고 **코드에 박힌 문장 둘**까지 찾았다. 가장 잘 빠뜨리는 자리다
- **`api.item` 은 실재한다** (`api.ts:382`) — 계획의 참조가 맞다. 없다고 지적할 뻔했고 grep 으로 막았다
- **fixture 접두사를 새로 안 만들었다** — `runs.test.ts` 가 이미 쓰는 `XBX` 다. CLAUDE.md §3 의 접두사 표가 안 길어진다

### 검사 덮임 그림

```
코드 갈래                                   사람이 하는 일
[+] execution/history.ts                    [+] 목록에서 값을 고쳐 돌린다
  └── lastByCase()                            ├── [★★ 할일3] 기본값이 채워져 있다
      ├── [★★ 할일1] recent 가 새 것부터        ├── [빈틈] 비밀값이 가려진다      ← 주의 2
      └── [빈틈]     5회 미만일 때              └── [빈틈] 고친 값이 요청에 실린다  ← 주의 3
[+] web/CaseDetail.tsx                      [+] 상세를 펼친다
  ├── [★★ 할일5] 접힘·펼침                      ├── [★★ 할일5] 사전조건이 보인다
  └── [★  할일6] 펼쳐야 부른다                  └── [빈틈] 접었다 펴면 또 부르나   ← 주의 4
[+] web/Shell.tsx (이름 바꾸기)              [+] 껍데기
  └── [빈틈] 42+11곳 중 검사가 보는 것 3곳      └── [빈틈] 이름 하나 놓쳐도 초록   ← 주의 5

덮임: 할 일 10개 중 RED 가 갈래를 무는 것 7  |  빈틈 5 (전부 지적에 있음)
```

---

**렌즈**: `gstack:/plan-design-review` (둘째) · 2026-09-21 · `[자기검사]`
**판정**: BLOCKER 1건 · 주의 6건 · 7패스 평균 5.3/10

**목업은 새로 안 뽑았다.** 확정 목업(`docs/design-mockup.html`)을 **사용자가 PR① 에서 이미 골랐다.**
새 변형을 뽑으면 끝난 결정을 다시 여는 것이다. 목업을 **기준으로 삼아** 대조만 했다.

### 패스별 점수

| # | 패스 | 점수 | 10 이 되려면 |
|---|---|---|---|
| 1 | 정보 구조 | 6/10 | 줄의 **읽는 순서**와 입력칸이 어느 칸에 들어가는지를 적는다 |
| 2 | 상태 덮임 | **3/10** | 아래 표의 빈칸을 채운다 — 이번 계획의 가장 큰 구멍 |
| 3 | 사용자 여정 | 5/10 | 「내가 고친 값이 실제로 쓰였나」를 볼 자리를 준다 |
| 4 | 뻔한 디자인 위험 | 8/10 | 낮다. 확정 목업이 구체값을 들고 있다 |
| 5 | 디자인 기준 정합 | 5/10 | DESIGN.md 의 **132px·8개·판정 표기 표**와 맞춘다 |
| 6 | 반응형·접근성 | **4/10** | 탭 순서 · 44px · `aria-controls` · 낡은 반응형 절 |
| 7 | 안 정한 것 | — | 다섯 개를 아래에 적었다 |

### BLOCKER 3 — 「내가 고친 값이 쓰였나」를 볼 자리가 사라진다

`Form.tsx:89-96` 이 칸마다 아래 줄을 그린다.

```
기본값 <s>4000-...-0002</s> 에서 바꿈     ← 바꿨을 때
기본값 4000-...-0002                     ← 안 바꿨을 때
```

주석이 왜인지 적어 뒀다 — 「값을 고칠 수는 있는데 **원래 값이 무엇이었나**를 볼 자리가 없었다.
코드를 열어야 알 수 있으면 화면에서 값을 고치라고 해 놓고 **판단할 근거를 안 준 것**이다」.

**이 제품에서 가장 비싼 순간이 거기다.** 고친 값으로 돈 결과가 그대로 제출 문서가 된다.
할 일 3 이 `값칸들` 을 새로 그리면 이 줄이 없어지고, 사람은 **고친 줄 알고 기본값 결과를 제출한다.**

**고칠 것** — 엔지니어 렌즈의 BLOCKER 1 과 **같은 답이다.** `Form` 을 쓴다. (confidence 9/10)

### 주의 8 — 상태 표가 거의 비어 있다 (패스 2)

| 기능 | 부르는 중 | 빈 것 | 실패 | 일부 |
|---|---|---|---|---|
| 인라인 입력 | 해당 없음 | 칸이 없는 케이스 → **계획에 없음** | **검증 실패를 어디 적나 → 계획에 없음** | 비밀값 → GREEN 에만 |
| 상세 펼침 | **계획에 없음** | 돌린 적 없음 → 할 일 6 에 한 줄 있다 ✅ | **통로 둘 중 하나만 실패 → 계획에 없음** | — |
| 판정 흐름 | 해당 없음 | **5회 미만 · 0회 → 계획에 없음** | — | — |

특히 둘.

- **검증 실패 자리** — DESIGN.md 「폼 자동 생성 규칙」은 「칸 아래 `--fail` 12px 한 줄. 버튼을 비활성화하지 말고
  누르면 오류를 보여준다」고 정한다. 그런데 `useRunPick.실행걸기` 의 주석이 적고 있듯
  **`POST /api/runs` 는 칸별 사유를 내지 않는다.** 목록에서 틀린 값을 넣으면 모달에 한 줄이 뜨고
  **어느 줄의 어느 칸인지 아무도 모른다.** 목록에 칸을 열면서 이 구멍이 커진다
- **5회 미만** — 목업은 `<i class="e">`(테두리만 있는 빈 칸)으로 그린다(`design-mockup.html:139`).
  할 일 2 의 RED 는 막대 다섯만 단언한다. 빈 배열이면 칸이 무너진다

### 주의 9 — DESIGN.md 의 폼 규칙 둘과 부딪힌다 (패스 5)

- 「**라벨 열 너비 132px 고정**, 값 열이 나머지」 — 계획은 「세로로 쌓는다」만 적는다. 목록 줄에서 132px 가 유효한가
- 「**파라미터가 8개를 넘으면 구획을 나눈다. 접기는 쓰지 않는다**」 — **목록 줄에서는 구획을 나눌 수 없다.**
  파라미터 9개짜리 케이스의 줄은 얼마나 높아지나. 계획에 답이 없다
- **「판정 표기」 표에 흐름 막대 줄이 없다.** 배지와 거터만 적혀 있다. 할 일 10 의 DESIGN.md 항목이 이걸 안 짚는다

### 주의 10 — DESIGN.md 「반응형」 절이 **이미 낡았다** (패스 6)

```
620px 미만에서 목록 행은 2단으로 접힌다 (거터 + 내용).
```

PR① 이 그것을 **3단**으로 바꿨다 (`styles.css` 의 `grid-template-rows: auto auto auto`).
문서가 이미 사실이 아니고, 이번에 입력칸이 들어가면 더 벌어진다.
**할 일 10 의 DESIGN.md 항목에 이 줄을 명시한다.** (9/10)

### 주의 11 — 목록을 키보드로 훑을 수 없게 된다 (패스 6)

케이스 한 줄에 입력칸이 3~9개 생긴다. 20건이면 탭이 **60~180번** 멈춘다.
지금은 줄마다 체크박스 하나·링크 두셋이다.

**고칠 것** — 입력칸을 기본으로 `tabindex="-1"` 로 두고 줄에 들어갈 때만 잡히게 하거나,
상세를 펼쳤을 때만 칸이 탭 대상이 되게 한다. **어느 쪽이든 계획이 정해야 한다.** (8/10)

### 주의 12 — 펼친 패널과 버튼이 안 이어져 있다 (패스 6)

할 일 5 는 `aria-expanded` 를 적는다. **`aria-controls` 가 없다.**
화면을 안 보는 사람에게는 「펼쳤다」는 말만 들리고 **무엇이 펼쳐졌는지**가 안 잡힌다.
44px 터치 타깃도 계획에 없다 — `styles.test.ts` 가 이미 그 검사를 들고 있다(PR①). (8/10)

### 주의 13 — 목업을 1:1 로 베낄 수 없다 (패스 1·4)

목업의 그 칸은 `<table>` 안의 `<td>` 이고 `.pcell > .prow > .plabel + .pinput` 이다.
제품의 줄은 **CSS 격자**(`.row > .gutter .pick .tcid .title .right`)다.
할 일 7 의 「옮겨 적는다」를 그대로 하면 안 붙는다.

**값(색·크기·간격)은 목업이 정본, 자리는 제품 구조가 정본**임을 할 일 7 에 적는다.
엔지니어 렌즈 BLOCKER 2 와 같은 줄에서 고치면 된다. (9/10)

### 안 정한 것 다섯 (패스 7)

| 정해야 할 것 | 안 정하면 |
|---|---|
| 파라미터 9개짜리 줄의 높이 | 줄 하나가 화면을 채운다. **목록이 목록이 아니게 된다** |
| 인라인 칸의 검증 실패 자리 | 서버가 거절하는데 **어느 줄인지 아무도 모른다** |
| 흐름 막대 5회 미만·0회 | 빈 배열에 막대 0개를 그려 칸이 무너진다 |
| 목록의 탭 순서 | 키보드로 20건을 지나가려면 탭 180번 |
| DESIGN.md 반응형 2단 ↔ 실제 3단 | 문서가 이미 거짓말 중이고 더 벌어진다 |

### 잘 된 것

- **색 규칙을 안 깬다** — 흐름 막대는 판정색이고 원칙 1 의 「행 안에서 색이 보이면 판정」에 맞는다
- **JSON 원문 금지를 안 깬다** — 상세의 기대결과는 스키마가 만든 라벨·타입·값이지 원문이 아니다
- **확정 목업을 기준으로 삼았다** — 뻔한 디자인 위험이 낮다 (8/10)
- **색만으로 판정을 전하지 않기**를 할 일 2 RED 가 이미 단언한다

---

**렌즈**: `gstack:/plan-ceo-review` (셋째) · 2026-09-21 · `[자기검사]`
**모드**: 기존 기능의 개선이므로 **선택적 확장**. 다만 확장 후보를 고르는 일은 게이트 1 이 한다
**판정**: BLOCKER 1건 · 주의 4건

### BLOCKER 4 — 고친 값의 **수명**이 어디에도 안 적혀 있다

`CaseList.tsx:47-53` 이 서비스를 바꿀 때 하는 일이다.

```
if (본서비스 !== service) {
  set본서비스(service); setPage(1); setQ(''); setTyped('');
  뽑기.비우기();          // 고른 것을 버린다
}
```

고친 값(글자표)을 여기서 **같이 안 버리면** 다른 서비스로 옮겼을 때 `XBX-001` 에 적어 둔 값이
`ZZA-001` 에 그대로 붙는다. 케이스가 달라도 칸 이름이 같으면(`env` · `userId` 같은 흔한 이름) **붙는다.**

같은 질문이 셋 더 있고 **계획에 하나도 없다.**

| 언제 | 고친 값이 어떻게 되나 | 안 정하면 |
|---|---|---|
| 서비스를 바꾼다 | **버려야 한다** | 남의 서비스 값으로 돈다 |
| 쪽을 넘겼다 돌아온다 | **남아야 한다** (`고른` 이 Map 으로 남는 것과 같이) | 1쪽에서 고치고 2쪽 보고 오면 조용히 기본값으로 돈다 |
| `다시 스캔` 을 눌러 스키마가 바뀐다 | 없어진 칸의 값은 **버린다** | 선언에 없는 칸이 요청에 실려 400 이 난다 |
| 모달에서 다시 고친다 | 목록 값이 **따라 바뀌나** | 두 자리가 서로 다른 값을 보여준다 |

**넷 다 「조용히 틀린 값으로 돈다」로 끝난다.** 그리고 그 결과가 제출 문서가 된다.
할 일 4 에 이 표를 그대로 박고 RED 로 만든다. (confidence 9/10)

### 주의 14 — **예외를 기본 화면에 올리고 있다**

실행 명세가 못 박아 둔 사실이다 (`도메인/실행.md:466`).

> 기본은 전부 접혀 있고 케이스 코드에 적힌 기본값으로 돈다. 규칙 K10(§4)이
> 「사람이 값을 채워야만 도는 케이스」를 막고 있어 **모든 케이스가 값 없이 돈다.**

즉 **값을 고치는 것은 흔한 일이 아니라 예외**다. 그런데 이 계획은 입력칸을 **모든 줄에 항상** 놓는다.
그 대가를 다른 렌즈가 이미 셋 셌다 — 줄 높이(주의 9) · 탭 순서(주의 11) · 검증 실패 자리(주의 8).

**사용자가 이 모양을 이미 골랐다** (PR① 에서 목업을 확정했다). 되돌리자는 말이 아니라 **값을 보이게 하는 것**이다.

| 길 | 품 | 위험 | 무엇이 풀리나 |
|---|---|---|---|
| A. 계획 그대로 — 줄에 늘 보인다 | M | 중 | 목업 그대로. 대가 셋이 남는다 |
| B. 입력칸을 **상세 안으로** — 줄은 읽기 칩 유지 | S | 낮음 | 설계 지적 넷이 한꺼번에 풀린다. **목업과 다르다** |
| **C. 줄에 상한 4칸, 넘으면 `+N개 더` 가 상세를 편다** | M | 낮음 | 흔한 경우(3칸)는 목업 그대로이고 **꼬리만 자른다** |

**추천은 C다.** 목업이 그린 것은 3칸짜리 케이스이고, 9칸짜리는 목업이 그린 적이 없다.
「목업을 지킨다」와 「9칸짜리 줄이 화면을 채운다」는 부딪히지 않는다 — **목업이 답한 적 없는 질문**이다. (8/10)

### 주의 15 — 이 PR 이 다음 PR 의 바닥이 된다

`main` 이 들여온 `docs/plans/2026-09-21-테스트작성-화면-설계.md` 가 「어드민 화면 전면 리뉴얼이 끝난 뒤에 착수한다」고 적는다.
**케이스 줄은 그 화면이 또 얹힐 자리다.** 이번에 무겁게 만들면 다음이 그 위에 얹는다.

```
지금                    이 PR                         12개월 뒤
줄 = 읽는 것     →     줄 = 고치고 · 펼치고 · 훑는다   →   줄 = 위 + 작성·편집?
```

**정할 것 하나** — 줄은 앞으로도 「고르고 훑는 자리」인가, 「일하는 자리」인가.
이 PR 이 사실상 뒤엣것을 고르고 있는데 계획에 그 문장이 없다. (7/10)

### 주의 16 — 「왜 5회인가」에 답이 없다

흐름 막대의 길이를 5로 정했다. 목업이 5칸이라서다. 근거가 그것뿐이면 다음 사람이 7 로 바꾸고 아무도 못 막는다.
할 일 1 의 REFACTOR 가 상수로 빼는 것까지는 적었다. **그 상수 옆에 한 줄**을 적는다 —
「반복 실행이 기본 1회이므로 5회면 최근 다섯 번의 실행을 덮는다. 바꾸려면 이 줄을 고친다」. (7/10)

### 주의 17 — 되돌릴 수 있는 문인가

| 바꾸는 것 | 문 | 되돌리는 값 |
|---|---|---|
| `last-by-case` 응답에 `recent` 추가 | **양방향** | 칸을 더하는 것이라 안 쓰면 그만이다. 옛 화면도 그대로 돈다 |
| 줄 구성 변경 | **양방향** | CSS 와 조각이다 |
| 옛 클래스 이름 바꾸기 | **양방향이지만 되돌릴 이유가 없는 문** | 이름만이다 |
| 명세 §8.1 개정 | **양방향** | 절 번호를 안 바꾸고 뒤에 더한다 |

**되돌리기 어려운 문이 하나도 없다.** 그래서 빠르게 가도 된다 — 이 PR 에서 오래 고민할 것은
위 BLOCKER 4(값의 수명)뿐이고 나머지는 틀리면 고치면 된다. (9/10)

### 잘 된 것

- **새 부품을 안 들인다.** npm 패키지 0개, 새 엔드포인트 0개, 새 표 0개
- **관측은 이미 구조가 보장한다.** 고친 값으로 돌았는지가 `run_item.params` 스냅샷에 남는다 (§3.2 불변식) —
  로그를 더할 이유가 없다
- **명세를 먼저 고치고 코드를 고치는 순서**를 계획이 지킨다 (할 일 9·10)

## GSTACK REVIEW REPORT

| 항목 | 값 |
|---|---|
| Runs | 렌즈 셋 · 각 1회 · 비대화형 (`tpx-plan-review` 규약) |
| | `plan-eng-review` · `plan-design-review` · `plan-ceo-review` |
| Status | 셋 다 완료. 건너뛴 렌즈 없음 |
| Findings | **BLOCKER 4 · 주의 17** |
| Outside voice | 안 돌렸다 — 체인이 렌즈 종수를 등급으로 정한다 (3등급 = 이 셋) |
| Scope check | 20파일 · 새 조각 2개. 복잡도 문턱(8파일)을 넘는다 → 게이트 1 |
| Mockups | 새로 안 뽑았다 — 확정 목업을 사용자가 PR① 에서 이미 골랐다 |

### BLOCKER 넷을 한 자리에

| # | 렌즈 | 한 줄 | 고치는 곳 |
|---|---|---|---|
| 1 | eng | 입력칸을 새로 짜면 **비밀번호 관리자 함정**을 다시 밟는다 | 할 일 3 — `Form` 을 쓴다 |
| 2 | eng | 판정 흐름 데이터가 **디바이스마다**인데 계획은 「줄에 하나」다 | 할 일 2·7 |
| 3 | design | 「내가 고친 값이 쓰였나」를 볼 자리가 사라진다 | 할 일 3 — **1 과 같은 답** |
| 4 | ceo | 고친 값의 **수명**(서비스 바뀜·쪽 넘김·재스캔·모달)이 어디에도 없다 | 할 일 4 |

**넷 중 셋이 한 줄로 풀린다** — 할 일 3 이 `값칸들` 을 새로 그리는 대신 이미 있는 `Form` 을 쓰면
1·3 이 동시에 사라지고, 2 는 할 일 2·7 의 문장 한 줄씩이다. **되돌릴 구조 문제가 하나도 없다.**

VERDICT: BLOCKER 4건 — 전부 계획 문장 수정으로 풀린다. 구현을 되돌릴 것은 없다.

**UNRESOLVED DECISIONS:**
- 할 일 8(옛 이름 정리)을 이번 PR 에 둘 것인가, 다음으로 미룰 것인가
- 파라미터가 많은 케이스 — 줄에 다 보일 것인가(A), 상세로 옮길 것인가(B), 상한 4칸(C, 추천)
