# Grafana 작성 현황 — 가장 오래된 미확정 · 작성에 걸린 시간

등급: 3 · 갈래: D · 2026-09-26

## 도메인 정리

- BC: Reporting (대시보드). 읽기만 한다 — 카탈로그(`test_case`)·작성(`authoring_request`)·설정(`service`)의 칸을 **칸 단위 권한**으로 빌려 본다
- 영향: DB 권한(`grafana_ro`) · Grafana 대시보드 JSON · 명세 리포팅 §8.5·§8.4·§7 · 데이터모델 §6
- 화면·Admin API·러너·Kit 타입은 그대로다

| 패널 | 읽는 칸 | 규칙 |
|---|---|---|
| 가장 오래된 미확정 | `test_case(tc_id, is_active, unconfirmed_since)` · `service(id, prefix, name)` | 케이스 목록 머리(`catalog/store.ts`)와 같다 — 활성 케이스 · `tc_id LIKE prefix || '-%'` · `min(unconfirmed_since)` |
| 작성에 걸린 시간 (최근 30일) | `authoring_request(id, service_id, kind, status, compare, created_at, finished_at)` · `service(id, name)` | `kind='AUTHOR' AND status='DONE'` · 걸린 시간 = `finished_at - created_at` · 서비스 × 방식(보통 / 화면과 대조)으로 묶어 건수·중간값·최대 |

## 왜

사용자 2026-09-26 — 미확정 케이스가 기획 답을 며칠째 못 받는지, 기획서를 넣고 초안 PR 이 나오기까지 얼마나 걸리는지를 Grafana 에서 본다.
대시보드 계정은 `test_run`·`run_item` 만 읽는다(§8.5). 표를 통째로 열면 **기획서 본문(`spec_text`)·테스트 소스·`slack_webhook`·`figma_token`** 이 대시보드로 샌다 — 그래서 칸 단위로만 연다 (게이트 0 승인 2026-09-26).

대조 요청은 화면을 훑느라 훨씬 오래 걸린다 — 섞으면 보통 요청이 느려진 것처럼 보여 방식으로 가른다.

## Plan

### 할 일 1. `grafana_ro` 칸 단위 권한 마이그레이션

- **RED** — 새 파일 `dashboard-authoring.test.ts`: grafana_ro 로 허용 칸은 읽히고, `authoring_request.spec_text` · `service.slack_webhook` · `service.figma_token` · `test_case.name` 은 `permission denied` 로 거부된다. 지금은 표 자체가 거부라 허용 칸 단언이 실패
- **GREEN** — `db/migrations/20260926000001_grafana_authoring.sql` — 칸 단위 `GRANT SELECT (…) ON … TO grafana_ro` 셋, down 은 같은 칸 `REVOKE`
- **REFACTOR** — 없음

**files**: db/migrations/20260926000001_grafana_authoring.sql, apps/admin/src/reporting/dashboard-authoring.test.ts
**depends-on**: []
**검증**: `DATABASE_URL=postgres://platform:platform@localhost:5433/platform_wsg npx vitest run apps/admin/src/reporting/dashboard-authoring.test.ts`

### 할 일 2. 패널 「가장 오래된 미확정」

- **RED** — fixture 서비스 `XDH` + 케이스 `XDH-001`(미확정 10일 전) · `XDH-002`(미확정 3일 전) · `XDH-003`(미확정인데 비활성) · `XDH-004`(확정). 패널 SQL 을 grafana_ro 로 돌리면 `XDH` 줄이 건수 2 · 가장 오래된 10일이다. 패널이 없어 실패
- **GREEN** — 대시보드 JSON 에 table 패널 하나 (y=16)
- **REFACTOR** — 없음

**files**: infra/grafana/provisioning/dashboards/test-platform.json, apps/admin/src/reporting/dashboard-authoring.test.ts
**depends-on**: [1]
**검증**: 할 일 1 과 같다

### 할 일 3. 패널 「작성에 걸린 시간」

- **RED** — fixture `authoring_request`(서비스 `XDH`): 보통 DONE 30분·60분·90분 → 건수 3 · 중간값 60 · 최대 90 / 대조 DONE 240분 → 따로 한 줄 / FAILED · RERUN · 40일 전 DONE 은 안 센다
- **GREEN** — 대시보드 JSON 에 table 패널 하나 (y=16, 오른쪽). `percentile_cont(0.5)` 로 중간값
- **REFACTOR** — 없음

**files**: infra/grafana/provisioning/dashboards/test-platform.json, apps/admin/src/reporting/dashboard-authoring.test.ts
**depends-on**: [2]
**검증**: 할 일 1 과 같다

### 할 일 4. 패널 목록 검사를 여섯으로

- **RED** — `dashboard.test.ts` 의 제목 목록 단언을 여섯으로 늘린다 → 할 일 2·3 전이면 실패 (순서상 뒤라 곧바로 초록일 수 있다 — 목록이 명세 §8.5 와 같은지 보는 검사다)
- **GREEN** — 할 일 2·3 으로 충족. 기존 `it.each` 가 새 패널 SQL 도 grafana_ro 로 돌린다
- **REFACTOR** — 없음

**files**: apps/admin/src/reporting/dashboard.test.ts
**depends-on**: [3]
**검증**: `DATABASE_URL=… npx vitest run apps/admin/src/reporting/dashboard.test.ts`

### 할 일 5. 명세 — 리포팅

- §8.5 패널 목록 여섯 · 「읽는 표는 둘뿐」 문단을 「판정 패널은 둘, 작성 현황은 칸 단위 권한」으로 · 새 소절 「작성 현황 (2026-09-26)」(두 패널 규칙 · 칸 목록은 데이터모델 가리킴) · 계약 블록 `상태: 반영 완료`
- §8.4 「증적 문서 머리말」에 판정 한 줄(확정 칸 0 도 적음 · 항목 행 단위 · 묶음 꼴은 실행 §3.2) (PR #83 spec-review)
- §7 insights 줄에 「직전 실행에서만 미확정이던 케이스는 새 케이스처럼 견주지 않는다」 (PR #83 spec-review)

**files**: docs/spec/도메인/리포팅.md
**depends-on**: []
**검증**: `npm run check:spec`

### 할 일 6. 명세 — 데이터모델 · 완료 점검

- 데이터모델 §6 끝에 소절 「대시보드 칸 권한 (2026-09-26)」 — 칸 목록 정본 · 뺀 칸과 이유. `authoring_request` 절의 「grafana_ro 에 아무것도 주지 않는다」 줄에 「2026-09-26 부터 칸 일부 — 아래 소절」
- 7-데모와-완료: 「대시보드 계정으로 `authoring_request.spec_text` 를 읽으려 하면 거부된다」 한 줄

**files**: docs/spec/공통/4-데이터모델.md, docs/spec/공통/7-데모와-완료.md
**depends-on**: [5]
**검증**: `npm run check:spec`

### 할 일 7. 명세 밖 동반 수정 · 기록

- WORKSTREAMS 「Grafana 작성 현황」 → 반영 완료 · WS-D 킥오프 패널 목록에 둘 더함
- CLAUDE.md 접두사 줄에 `XDH`(`reporting/dashboard-authoring.test.ts` — `service.prefix = 'XDH'` 의 id 로 `authoring_request` 를 지우고 `test_case` 는 `tc_id LIKE 'XDH-%'`)
- `docs/progress/WS-D.md` 추가

**files**: docs/WORKSTREAMS.md, CLAUDE.md, docs/progress/WS-D.md
**depends-on**: [6]
**검증**: `npm run check:spec`

## SPEC 동반 수정 (§2.7)

- **같은 규칙 찾기** — `grep -rn "grafana_ro\|대시보드 계정\|둘뿐\|성공률 추이" docs/` 결과: 리포팅 §8.5(250·252) · 데이터모델 326·501~514 · 7-데모와-완료 115 · WORKSTREAMS 67·451. WORKFLOW 404 는 과거 기록이라 그대로 → 할 일 5·6·7
- **색인 네 곳** — 라우터 표 76 줄 「대시보드에 패널을 더하거나 질의를 고친다 → 리포팅 §8.5」가 이미 길을 준다. 새 절 번호 없음(소절만). 분량 줄은 `check:spec` 이 본다
- **SPEC 밖 여섯 곳** — WORKSTREAMS(할 일 7) · spec-review·check:* 해당 없음 · SETUP/HOOKS/DESIGN 해당 없음 · WORKFLOW 해당 없음 · 목업 해당 없음 · 코드 상수: 대시보드 SQL 이 `store.ts` 의 미확정 요약 규칙을 복사한다 → 명세 소절에 「같은 규칙」으로 적고 검사가 같은 값을 본다
- **숫자 빼기** — 「패널 넷」을 「여섯」으로 바꾸지 않고 목록을 적는다. 칸 목록 정본은 데이터모델 소절 한 곳

## Plan 메타

할 일 7개 · 예상 묶음 7개(직렬) · 구현 규율: TDD · 추가 검증: `npm run check:spec` · 검사용 DB `platform_wsg`

## 리뷰 결과

(계획 검토가 채운다)
