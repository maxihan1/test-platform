# WS-F 인증 — 진행 기록

## 2026-09-18

`contracts` · `WS-A` · `WS-B` 를 끝낸 세션이 이어서 돌렸다. 브랜치는 `contracts`.

### 완료 — 킥오프 네 가지 전부

| 무엇 | 어디 |
|------|------|
| 확인 함수 하나 (§3.5) | `apps/admin/src/auth/identify.ts` 의 `확인(req)` |
| 로그인·로그아웃·나 (§7 Auth) | `apps/admin/src/auth/routes.ts` |
| 인증 미들웨어 — 로그인·등급·서비스 (§7) | `apps/admin/src/auth/gate.ts` |
| 명령 셋 (§9.2) | `scripts/add-user.ts` · `add-service.ts` · `run-scheduled.ts` |
| 설정 API (§7 · §8.8) | `apps/admin/src/settings/routes.ts` · `store.ts` |

곁들여 나온 것 둘 — `app.ts` 에 문을 등록했고(계약 반영 단위가 열어 두기로 한 자리가 비어 있었다),
실행 검사 둘이 부딪히던 fixture 충돌을 고쳤다 (`XBQ` → `XBR`).

### 다음 세션이 알아야 할 것

**1. 갈아 끼울 자리는 `auth/identify.ts` 하나다.**
SSO 로 바꾸면 이 파일만 바뀐다. 그래서 `확인()` 은 `FastifyRequest` 를 통째로 받지 않고
`{ session: { get('username') } }` 모양만 받는다. 무엇을 대신 채워야 하는지가 한눈에 보이게 하려는 것이다.

**2. 문을 지난 사람은 `req.user` 로 실린다.**
`{ username, displayName, role, services }` 다. 타입은 `auth/gate.ts` 가 전역에 실어 두므로
다른 갈래는 import 없이 읽는다. **비밀번호도 세션도 다른 갈래에 가지 않는다** (§3.5).

**3. 실행자는 이제 로그인한 사람에게서 온다.**
`POST /api/runs` 의 본문에 `triggeredBy` 를 실어 보내도 **버린다.** 핸들러가 `req.user` 에서 읽고
`triggered_by_name` 까지 박제한다. 사용자 승인을 받아 `execution/**` 를 고쳤다 (소유 경로 밖).
확인 — 본문에 `사장님` 을 실어도 DB 에는 로그인한 사람이 남는다.
**본문에 실행자를 싣는 화면 코드가 있으면 지워야 한다** (WS-E).

**4. 서비스 경계는 `?service=` 와 실행 요청까지만 막는다.**
`GET /api/runs/:runId` 처럼 번호만 들고 오는 요청은 문이 어느 서비스 것인지 모른다.
병합 단계에서 각 갈래가 자기 조회에 `req.user.services` 를 한 줄 거는 것이 맞다.

**5. WS-E 가 이어받을 것** — 로그인 화면(§8.6)과 설정 화면(§8.8).
API 는 다 섰다. 화면이 `GET /api/auth/me` 로 시작해 401 이면 로그인 화면으로 보내면 된다.
등급이 모자란 자리는 **흐리게가 아니라 아예 안 보이게** 한다 (§3.5).

**6. 검사용 fixture 접두사** — `xfu1`~`xfu4`(계정) · `XFS1`~`XFS4`(서비스). 파일마다 갈랐다.

### 확인한 것

- 전체 301건이 **연속 6회** 통과 (`DATABASE_URL` 을 붙이고)
- `npm run typecheck` · `check:tests` · `check:spec` 전부 통과
- 앱을 실제로 띄워 `/api/**` 다섯 자리가 401, `/health` 와 로그인만 열리는 것을 확인
- 명령 셋을 실제 DB 에 돌려 봄. 정기 실행이 실행 1건을 만들어 끝냈고
  `test_run` 에 서비스 이름·대상 주소가 박제되고 `notify_slack` 이 켜졌다 (검사 행은 치웠다)

## 2026-09-19 — 넘겨받은 IDOR 를 닫았다 (PR #28, 다른 작업이 처리)

- **완료**: WS-E 가 넘긴 `gate.ts` 의 IDOR. 보고받은 건 `GET /api/runs/:runId` 하나였는데
  라우트를 세어 보니 **열둘**이었다. 읽기만이 아니라 **남의 실행 멈추기 · 증적 만들기 ·
  입력값 묶음 지우기**까지 열려 있었다. `GET /api/runs/last-by-case` 도 전 서비스의
  케이스 번호와 판정을 통째로 주고 있었다 — 그건 문이 아니라 질의에 배정 필터를 걸었다.
- **완료(추가)**: 검사 렌즈가 **인증 우회 둘**을 더 찾았다. 원래부터 있던 것이다.
  `/%61pi/**` 는 문이 통째로 비켜서 **로그인 없이** 전 계정 목록·관리자 비밀번호 재발급이 됐고,
  `/api/%73ettings/**` 는 등급 검사만 비켜서 실행까지 등급이 운영 API 를 열었다.
- **미완**: 없음.
- **막힌 것**: 없음.

### 다음 세션이 알아야 할 것

**진입점은 `apps/admin/src/auth/scope.ts` 의 `라우트표` 다.**
등록된 모든 `/api` 라우트가 거기 있어야 하고, **없으면 문이 막는다.**
새 라우트를 더하면 `scope.test.ts` 가 소스의 `routes.ts` 들을 훑어 빠진 것을 이름째로 잡는다.

**문은 `req.url` 을 판정에 안 쓴다.** 등록된 틀(`req.routeOptions.url`)과 `req.params` 만 본다.
라우터는 주소를 디코딩한 뒤 라우트를 찾으므로 글자를 읽으면 둘이 같은 주소를 다르게 본다 —
그 틈이 이번 우회 둘의 뿌리였다. **이 원칙을 깨면 같은 구멍이 다시 열린다.**

**`app.inject` 로는 이 부류를 못 본다** (주소를 정규화해 버린다).
`gate.test.ts` 의 「퍼센트로 감싼 번호도 막는다」가 **진짜 소켓**으로 거는 이유다.

fixture 접두사에 `XFS5`(`auth/scope.test.ts`)가 늘었다. `gate.test.ts` 는 `XFS3%` 로 지운다.

**남긴 경미 하나** — `/api/없는주소` 는 404, 있는 주소는 401 이라 로그인 없이 라우트 존재 여부가
드러난다. 라우트 목록은 이미 화면 번들에 있어 새로 새는 비밀이 아니고, 404 를 403 으로 덮으면
「없는 것과 막힌 것이 뭉개진다」가 되돌아온다 (§7). **알고 두는 것이다.**

## 2026-09-26 — 역방향: 대상 서버 줄의 테스트 계정 (PR #77)

- 완료: 설정 `envs[]` 에 `loginId`·`loginPassword` 저장 · 응답은 `loginId`·`hasLoginPassword` · 두 칸 다 키 없음 = 유지, null·빈 글자 = 지움 ·
  비밀번호 원문이 목록·`/auth/me`·400 본문에 없다는 검사
- 미완: 없음. `outputs` 권한 세 줄은 WS-작성으로 넘겼다(통로가 생길 때 같이). 화면 칸은 WS-E
- 막힌 것: 없음

### 다음 세션이 알아야 할 것

- 진입점은 `settings/store.ts` 의 `대상서버넣기` — 지우기 전에 같은 `client` 로 옛 계정을 읽어 env 이름으로 이어받는다. `계정칸()` 이 키 없음·null·빈 글자를 가른다
- 입력 타입 `대상서버입력` 과 응답 타입 `대상서버` 가 갈렸다 — 응답에는 비밀번호 원문 칸이 없다
- `/auth/me` 의 envs 는 칸을 이름으로 골라 만든다(`auth/store.ts`). `row_to_json` 같은 것으로 바꾸면 원문이 샌다 — `testAccount.test.ts` 가 잡는다
- DB 검사 접두사는 `XFS7`(서비스) · `xfu7`(계정)
