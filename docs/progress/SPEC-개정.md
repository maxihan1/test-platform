# SPEC 개정 진행 기록 — 발표 스크립트 대조에서 시작된 결정 반영

## 2026-09-17

- 완료: 토스페이먼츠 발표 스크립트를 SPEC과 대조(49건) → 추가 후보 정리 →
  대화로 결정 10건 확정 → **SPEC.md 반영 완료 (691줄 → 1496줄)**
- 산출물:
  - `docs/reviews/2026-09-16-script-gap.md` — 49건 대조 결과
  - `docs/reviews/2026-09-17-SPEC.md` — SPEC 검사 결과 (치명 0 · 중대 0 · 경미 0)
  - `docs/SPEC.md` — 신설 절 §1.1 · §3.5 · §4.1 · §8.6 · §9.2 · §11 Phase 2
- 브랜치: `worktree-script-gap-review` (원격에 푸시됨, main 병합 전)
- 미완: 문서 정합 4건, 코드 반영
- 막힌 것: 없음

### 확정된 결정 10건

| # | 무엇 | 어디에 |
|---|------|--------|
| 1 | **증적 재현** — 같은 실행은 언제 뽑아도 같은 문서 | §3.3 불변식 · §6 박제 칸 |
| 2 | **비밀값** — 문서에서만 가림. DB엔 평문. 꼬리표 + 검사기(K9) | §4.1 |
| 3 | **디바이스 / 대상 서버** 로 축 이름 분리 | §2 · 문서 전체 |
| 4 | **반복 실행**(회차 기록) · 자동 재시도 금지 · 상한 없음 | §3.2 · §5.2 · §8.2 |
| 5 | **즉시 중단** — 대기분과 진행분 둘 다 끊는다 | §3.2 · §5.2 · §8.3 |
| 6 | **검색 조건 다섯** + 검색엔진 이음새 조건 | §8.1 · §5.3 |
| 7 | **인증 최소** — 아이디·비밀번호. 갈아 끼울 수 있게 분리 | §3.5 · §8.6 |
| 8 | **인스턴스 구분** — 이름·색·저장소를 설정으로 | §8 · §9 · §9.2 |
| 9 | **정기 실행** — 데모 대상에서도 켠다. 실행자는 `스케줄러` | §9.2 · §8.5 |
| 10 | **기본값 강제**(K10) — 값 없이도 돌아가야 정기 실행이 된다 | §4 |

### 승인받았으나 아직 코드에 안 닿은 것 — 여기서부터가 다음 일

사용자 승인 완료. **잠긴 파일이라 §1.2 절차는 이미 밟았다.**

| 대상 | 무엇을 | 보고문 위치 |
|------|-------|-----------|
| `db/migrations/` | `test_run`에 인스턴스·저장소·주소·실행자 이름, `run_item`에 라벨·회차·제한시간·파일경로·묶음ID, 유일성을 회차까지, `app_user` 신설, `env` 기본값 제거 | §6 본문 |
| `packages/kit/src/types.ts` | `ExecuteRequest.baseUrl` 추가. `ItemStatus`는 3종 유지 | §5.1 아래 |
| `playwright.config.ts` | `use.baseURL`을 환경변수에서, `retries: 0` 명시 | §5.2 |
| `docker-compose.yml` · `apps/admin/Dockerfile` | 포트 설정화, 러너 포트 닫기, 상시 4개 재시작 정책, 인스턴스 설정 전달, 화면 빌드 단계 | §9 |
| `package.json` | 쿠키·세션 부품 **1개**. 비밀번호 해시는 Node 내장 `crypto.scrypt`로 되는 것을 실제로 돌려 확인해 추가 설치 없음 | §9.1 |

### 남은 문서 정합 4건

1. **`docs/reviews/2026-09-16-script-gap.md`** — 틀린 항목 3건.
   검색창·진행 표시·재실행을 "없다"고 적었는데 **WS-E가 이미 만들어 뒀다.** 고쳐야 한다
2. **`docs/WORKSTREAMS.md`** WS-E 킥오프 — "만들 것" 목록에 오늘 결정이 없다.
   소유 경로 표에 §3.5 인증과 §8.6 로그인 담당도 아직 안 정해졌다
3. **`docs/design-mockup.html`** — 검색 조건 · 대상 서버 · 반복 횟수 · 멈춤 버튼 ·
   회차 요약 · 결과 목록의 파라미터 · 로그인 화면 · 인스턴스 띠
4. **`docs/SETUP.md`** — 새 설정값(`ADMIN_PORT`·`PLATFORM_INSTANCE_NAME`·`PLATFORM_INSTANCE_COLOR`·
   `PLATFORM_TESTS_REPO`·`PLATFORM_ENV_URLS`), 계정 만들기, 인스턴스 복제, 러너 포트가 닫혀
   직접 찔러볼 수 없게 된 것. §6 "아직 안 정한 것"에서 마스킹·`ABORTED` 두 건을 뺀다

### 다음 세션이 알아야 할 것

- **화면은 이미 만들어져 있다.** `apps/admin/src/web/` 21개 파일. main에 병합까지 끝났다.
  **검색창·진행 표시(2초 자동 갱신)·"값을 바꿔 다시 실행" 버튼이 이미 있다.**
  §8은 "새로 만들 명세"가 아니라 "무엇을 더하고 무엇을 고치는가"다
- **이 브랜치는 원격 main 위에 얹혀 있다.** 화면 코드와 SPEC 개정이 같은 자리에 있어
  화면을 고칠 때 브랜치를 옮겨 다닐 필요가 없다
- **데모 케이스 10건이 K10을 이미 전부 통과한다.** 기본값이 다 들어가 있어 지금 규칙을 켜도 안 깨진다
- **`zod-to-json-schema`는 아무도 안 쓴다.** `package.json`에 선언만 있고 실제로는 zod 내장을 쓴다.
  지우기로 결정했다 (§9.1 표에 적어 뒀다)
- **미결 하나** — 증적 문서 **엑셀 양식**. 검수처가 주는 양식을 받아 봐야 표 모양을 정할 수 있다 (§1.1 L4)

---

## 2026-09-17 (2차) — 문서 정합

- 완료: 개정 SPEC 을 `worktree-spec-merge` 브랜치에서 main 과 합치고 **문서 정합 5건을 끝냈다**
- 브랜치: `worktree-spec-merge` (main + `worktree-script-gap-review` 병합, 충돌 0건)
- SPEC 검사: 치명 0 · 중대 0 · 경미 0 (`docs/reviews/2026-09-17-문서정합.md`, 자기검사)
- 미완: **코드 반영 0건 그대로**. 어느 기능부터 만들지 안 정했다
- 막힌 것: 없음

### 무엇을 맞췄나

| 문서 | 무엇을 |
|------|-------|
| `reviews/2026-09-16-script-gap.md` | 틀린 판정 3건(§2-1 검색창·§2-3 진행 표시·§2-6 다시 실행)에 정정을 덧붙였다. 지우지 않았다 |
| `WORKSTREAMS.md` | WS-D 킥오프를 개정 §8.4 로(PDF 단일·머리말·`test_case` 를 읽지 않는 불변식), WS-E 킥오프를 "이미 만든 화면에 더하고 고칠 것"으로 다시 썼다 |
| `DESIGN.md` | `환경 표기` → `디바이스 표기`. 대상 서버와 다른 축임을 적었다 |
| `WORKFLOW.md` | 현재 위치에 SPEC 개정 사실과 "Phase 1 기준이 K1~K10 을 요구한다"를 남겼다 |
| `SETUP.md` | 결정된 미결 3건을 내리고(엑셀 양식만 남음), 새 `.env` 설정값·러너 포트 닫힘·계정 만들기를 7절로 예고했다 |
| `design-mockup.html` | 인스턴스 띠·로그인 화면·케이스 목록(검색 조건 다섯)을 그리고, 기존 화면에 증적/멈춤 버튼·회차 요약·대상 서버·비밀번호 가리기를 넣었다 |
| `HOOKS.md` · `spec-review` 스킬 | 검사 항목 E3·F2 의 `환경` 을 `디바이스` 로 |

### 다음 세션이 알아야 할 것

- **`SETUP.md` 7절은 "아직 안 되는 것"이다.** 코드 반영 전이라고 절 머리에 못박아 뒀다.
  `docker-compose.yml` 을 고칠 때 이 절의 경고 문구를 지우는 것까지가 그 작업이다
- **`check:tests` 는 아직 K1~K8 만 본다** (`catalog/rules.ts:8`). 개정 §11 Phase 1 완료 기준이
  `K1~K10` 을 요구하므로(`SPEC.md:1471`) WS-A 가 K9·K10 을 넣기 전에는 그 칸에 체크할 수 없다
- **인증은 새 갈래 `WS-F` 가 맡는다** (2026-09-17 결정). 소유 경로는 `apps/admin/src/auth/**` 와 `scripts/**` 다.
  `.claude/scripts/guard.mjs` 의 `OWNED` 에도 같이 넣었으니 훅이 바로 강제한다.
  로그인 **화면**(§8.6)은 WS-E 소유 그대로고, 미들웨어를 다는 `app.ts` 는 공용 골격이라 계약 단위가 연다
- **`orchestration.yaml` 의 WS-D 는 `blocked` 그대로다.** 스펙은 확정됐지만 증적 문서 본체가
  `run_item.param_schema`·`attempt` 를 먼저 필요로 한다. Grafana 패널만 떼면 지금도 돌릴 수 있다
- 승격 제안 1건 — `spec-review` 체크리스트에 `H1 SPEC이 참조하는 §번호가 실제로 있는가`.
  제안만 했고 고치지 않았다 (CLAUDE.md §2.5) → **3차에서 승격 완료**

---

## 2026-09-17 (3차) — SPEC 을 12장으로 가름

- 완료: 1496줄짜리 `docs/SPEC.md` 를 **도메인 5장 · 공통 7장**으로 가르고, `docs/SPEC.md` 자체는 **색인**으로 바꿨다
- 브랜치: `worktree-spec-split-domain`
- 검사: `npm run check:spec` 통과(없는 절 0 · 깨진 링크 0) · `typecheck` 통과 · `npm test` 141건 통과 · `check:tests` 통과
- 미완: **코드 반영 0건 그대로.** 이번 작업은 문서 구조만 바꿨다
- 막힌 것: 없음

### 왜 이렇게 갈랐나

사용자 요구는 "유지보수할 때 도메인 단위로 관리되어야 기능 추가·변경에 유리하다" 였다.

| 축 | 어디로 |
|----|-------|
| 도메인 고유 (§3.x · 도메인별 API · 그 도메인 화면) | `docs/spec/도메인/` 5장 |
| 여러 갈래 공유 (§1 §2 §4 §5.1 §6 §9 §10 §11) | `docs/spec/공통/` 7장 |
| 읽기 동선 | `docs/SPEC.md` 색인의 세 표 |

**§6 데이터 모델만 도메인으로 가르지 않았다.** 표는 주인이 하나씩이지만(`test_case`→카탈로그 등)
뒤따르는 박제 원칙과 계약 변경 보고문이 표를 가로지르며 설명한다. 쪼개면 관계가 안 보인다.
**§7 Admin API 는 갈랐다.** 엔드포인트가 서로 독립이고 `# Auth`·`# Catalog` 로 이미 경계가 있었다.

### 내용이 새지 않았다는 증거

절 번호 참조가 코드 주석·훅·스킬·킥오프에 **221건** 있어서 번호를 바꾸면 전부 깨진다.
**번호는 영구 주소로 두고 파일만 옮겼다.** 한 장 안에서 §3.2 다음에 §7 이 오는 것은 의도다.

검사 두 개를 돌려 확인했다.
1. 배정 검사 — 원본 1496줄 중 배정 1478줄 · 색인 9줄 · 나머지는 빈 줄과 코드펜스. **샌 줄 0**
2. 무손실 검사 — 원본 내용 1188줄과 분할본 내용 1188줄을 줄 단위로 대조. **잃음 0 · 더함 0**

### 같이 고친 것

`CLAUDE.md` §0 · `WORKFLOW.md` 3곳 · `WORKSTREAMS.md` 킥오프 6개(갈래마다 읽을 장을 직접 지정) ·
`spec-review` 스킬(절차 + **H 섹션 신설**) · `orchestration.yaml` 주석 · `package.json`(`check:spec`)

### 다음 세션이 알아야 할 것

- **전문을 읽지 않는다.** `docs/SPEC.md` 색인에서 자기 갈래 줄을 찾아 그 장만 읽는다.
  WS-D 면 537줄, WS-F 면 527줄이다 (전체 1644줄)
- **절 번호를 바꾸지 않는다.** 새 내용은 뒤에 번호를 더한다 (§8.7, §9.3 처럼).
  절을 옮기거나 더하면 색인의 세 표를 같이 고치고 `npm run check:spec` 을 돌린다
- **WS-F 킥오프 프롬프트가 아직 없다.** 소유 경로 표에는 WS-F 인증이 들어갔는데
  `WORKSTREAMS.md` 의 Phase 1 킥오프는 WS-A~E 다섯 개뿐이다. WS-F 를 돌리기 전에 써야 한다
- **오케스트레이터가 장 단위로 문서를 지정하지 못한다.** `orchestrator/src/manifest.ts` 의
  `Docs` 스키마가 고정 키(`spec`·`workflow`·…)만 받아 `docs:` 에 장을 등록해도 zod 가 버린다.
  지금은 `spec` 이 색인을 가리키고 세션이 색인을 읽어 스스로 장을 고르는 방식으로 돈다.
  `Docs` 를 `.catchall(z.string())` 으로 열면 단계별 `required_docs` 에 장을 직접 적을 수 있다
- `final_check: "spec#Phase 1"` 이 깨질 뻔했다. `extractSection` 은 절을 못 찾으면 예외를 던지는데
  색인에는 §11 본문이 없다. 색인에 `## Phase 1 완료 기준 (G4)` 절을 두어 그 장으로 안내한다
