# SPEC 개정 진행 기록 — 발표 스크립트 대조에서 시작된 결정 반영

## 2026-09-17

- 완료: 토스페이먼츠 발표 스크립트를 SPEC과 대조(49건) → 추가 후보 정리 →
  대화로 결정 10건 확정 → **SPEC.md 반영 완료 (691줄 → 1496줄)**
- 산출물:
  - `docs/reviews/2026-09-16-script-gap.md` — 49건 대조 결과
  - `docs/reviews/2026-09-17-SPEC.md` — SPEC 검사 결과 (치명 0 · 중대 0 · 경미 0)
  - `docs/SPEC.md` — 신설 절 §1.1 · §3.5 · §4.1 · §8.6 · §9.2 · §11 Phase 2
- 브랜치: `worktree-script-gap-review` (원격에 푸시됨, main 병합 전)
- 미완: 문서 정합 4건, 코드 반영
- 막힌 것: 없음

### 확정된 결정 10건

| # | 무엇 | 어디에 |
|---|------|--------|
| 1 | **증적 재현** — 같은 실행은 언제 뽑아도 같은 문서 | §3.3 불변식 · §6 박제 칸 |
| 2 | **비밀값** — 문서에서만 가림. DB엔 평문. 꼬리표 + 검사기(K9) | §4.1 |
| 3 | **디바이스 / 대상 서버** 로 축 이름 분리 | §2 · 문서 전체 |
| 4 | **반복 실행**(회차 기록) · 자동 재시도 금지 · 상한 없음 | §3.2 · §5.2 · §8.2 |
| 5 | **즉시 중단** — 대기분과 진행분 둘 다 끊는다 | §3.2 · §5.2 · §8.3 |
| 6 | **검색 조건 다섯** + 검색엔진 이음새 조건 | §8.1 · §5.3 |
| 7 | **인증 최소** — 아이디·비밀번호. 갈아 끼울 수 있게 분리 | §3.5 · §8.6 |
| 8 | **인스턴스 구분** — 이름·색·저장소를 설정으로 | §8 · §9 · §9.2 |
| 9 | **정기 실행** — 데모 대상에서도 켠다. 실행자는 `스케줄러` | §9.2 · §8.5 |
| 10 | **기본값 강제**(K10) — 값 없이도 돌아가야 정기 실행이 된다 | §4 |

### 승인받았으나 아직 코드에 안 닿은 것 — 여기서부터가 다음 일

사용자 승인 완료. **잠긴 파일이라 §1.2 절차는 이미 밟았다.**

| 대상 | 무엇을 | 보고문 위치 |
|------|-------|-----------|
| `db/migrations/` | `test_run`에 인스턴스·저장소·주소·실행자 이름, `run_item`에 라벨·회차·제한시간·파일경로·묶음ID, 유일성을 회차까지, `app_user` 신설, `env` 기본값 제거 | §6 본문 |
| `packages/kit/src/types.ts` | `ExecuteRequest.baseUrl` 추가. `ItemStatus`는 3종 유지 | §5.1 아래 |
| `playwright.config.ts` | `use.baseURL`을 환경변수에서, `retries: 0` 명시 | §5.2 |
| `docker-compose.yml` · `apps/admin/Dockerfile` | 포트 설정화, 러너 포트 닫기, 상시 4개 재시작 정책, 인스턴스 설정 전달, 화면 빌드 단계 | §9 |
| `package.json` | 쿠키·세션 부품 **1개**. 비밀번호 해시는 Node 내장 `crypto.scrypt`로 되는 것을 실제로 돌려 확인해 추가 설치 없음 | §9.1 |

### 남은 문서 정합 4건

1. **`docs/reviews/2026-09-16-script-gap.md`** — 틀린 항목 3건.
   검색창·진행 표시·재실행을 "없다"고 적었는데 **WS-E가 이미 만들어 뒀다.** 고쳐야 한다
2. **`docs/WORKSTREAMS.md`** WS-E 킥오프 — "만들 것" 목록에 오늘 결정이 없다.
   소유 경로 표에 §3.5 인증과 §8.6 로그인 담당도 아직 안 정해졌다
3. **`docs/design-mockup.html`** — 검색 조건 · 대상 서버 · 반복 횟수 · 멈춤 버튼 ·
   회차 요약 · 결과 목록의 파라미터 · 로그인 화면 · 인스턴스 띠
4. **`docs/SETUP.md`** — 새 설정값(`ADMIN_PORT`·`PLATFORM_INSTANCE_NAME`·`PLATFORM_INSTANCE_COLOR`·
   `PLATFORM_TESTS_REPO`·`PLATFORM_ENV_URLS`), 계정 만들기, 인스턴스 복제, 러너 포트가 닫혀
   직접 찔러볼 수 없게 된 것. §6 "아직 안 정한 것"에서 마스킹·`ABORTED` 두 건을 뺀다

### 다음 세션이 알아야 할 것

- **화면은 이미 만들어져 있다.** `apps/admin/src/web/` 21개 파일. main에 병합까지 끝났다.
  **검색창·진행 표시(2초 자동 갱신)·"값을 바꿔 다시 실행" 버튼이 이미 있다.**
  §8은 "새로 만들 명세"가 아니라 "무엇을 더하고 무엇을 고치는가"다
- **이 브랜치는 원격 main 위에 얹혀 있다.** 화면 코드와 SPEC 개정이 같은 자리에 있어
  화면을 고칠 때 브랜치를 옮겨 다닐 필요가 없다
- **데모 케이스 10건이 K10을 이미 전부 통과한다.** 기본값이 다 들어가 있어 지금 규칙을 켜도 안 깨진다
- **`zod-to-json-schema`는 아무도 안 쓴다.** `package.json`에 선언만 있고 실제로는 zod 내장을 쓴다.
  지우기로 결정했다 (§9.1 표에 적어 뒀다)
- **미결 하나** — 증적 문서 **엑셀 양식**. 검수처가 주는 양식을 받아 봐야 표 모양을 정할 수 있다 (§1.1 L4)

---

## 2026-09-17 (2차) — 문서 정합

- 완료: 개정 SPEC 을 `worktree-spec-merge` 브랜치에서 main 과 합치고 **문서 정합 5건을 끝냈다**
- 브랜치: `worktree-spec-merge` (main + `worktree-script-gap-review` 병합, 충돌 0건)
- SPEC 검사: 치명 0 · 중대 0 · 경미 0 (`docs/reviews/2026-09-17-문서정합.md`, 자기검사)
- 미완: **코드 반영 0건 그대로**. 어느 기능부터 만들지 안 정했다
- 막힌 것: 없음

### 무엇을 맞췄나

| 문서 | 무엇을 |
|------|-------|
| `reviews/2026-09-16-script-gap.md` | 틀린 판정 3건(§2-1 검색창·§2-3 진행 표시·§2-6 다시 실행)에 정정을 덧붙였다. 지우지 않았다 |
| `WORKSTREAMS.md` | WS-D 킥오프를 개정 §8.4 로(PDF 단일·머리말·`test_case` 를 읽지 않는 불변식), WS-E 킥오프를 "이미 만든 화면에 더하고 고칠 것"으로 다시 썼다 |
| `DESIGN.md` | `환경 표기` → `디바이스 표기`. 대상 서버와 다른 축임을 적었다 |
| `WORKFLOW.md` | 현재 위치에 SPEC 개정 사실과 "Phase 1 기준이 K1~K10 을 요구한다"를 남겼다 |
| `SETUP.md` | 결정된 미결 3건을 내리고(엑셀 양식만 남음), 새 `.env` 설정값·러너 포트 닫힘·계정 만들기를 7절로 예고했다 |
| `design-mockup.html` | 인스턴스 띠·로그인 화면·케이스 목록(검색 조건 다섯)을 그리고, 기존 화면에 증적/멈춤 버튼·회차 요약·대상 서버·비밀번호 가리기를 넣었다 |
| `HOOKS.md` · `spec-review` 스킬 | 검사 항목 E3·F2 의 `환경` 을 `디바이스` 로 |

### 다음 세션이 알아야 할 것

- **`SETUP.md` 7절은 "아직 안 되는 것"이다.** 코드 반영 전이라고 절 머리에 못박아 뒀다.
  `docker-compose.yml` 을 고칠 때 이 절의 경고 문구를 지우는 것까지가 그 작업이다
- **`check:tests` 는 아직 K1~K8 만 본다** (`catalog/rules.ts:8`). 개정 §11 Phase 1 완료 기준이
  `K1~K10` 을 요구하므로(`SPEC.md:1471`) WS-A 가 K9·K10 을 넣기 전에는 그 칸에 체크할 수 없다
- **인증은 새 갈래 `WS-F` 가 맡는다** (2026-09-17 결정). 소유 경로는 `apps/admin/src/auth/**` 와 `scripts/**` 다.
  `.claude/scripts/guard.mjs` 의 `OWNED` 에도 같이 넣었으니 훅이 바로 강제한다.
  로그인 **화면**(§8.6)은 WS-E 소유 그대로고, 미들웨어를 다는 `app.ts` 는 공용 골격이라 계약 단위가 연다
- **`orchestration.yaml` 의 WS-D 는 `blocked` 그대로다.** 스펙은 확정됐지만 증적 문서 본체가
  `run_item.param_schema`·`attempt` 를 먼저 필요로 한다. Grafana 패널만 떼면 지금도 돌릴 수 있다
- 승격 제안 1건 — `spec-review` 체크리스트에 `H1 SPEC이 참조하는 §번호가 실제로 있는가`.
  제안만 했고 고치지 않았다 (CLAUDE.md §2.5) → **3차에서 승격 완료**

---

## 2026-09-17 (3차) — SPEC 을 12장으로 가름

- 완료: 1496줄짜리 `docs/SPEC.md` 를 **도메인 5장 · 공통 7장**으로 가르고, `docs/SPEC.md` 자체는 **색인**으로 바꿨다
- 브랜치: `worktree-spec-split-domain`
- 검사: `npm run check:spec` 통과(없는 절 0 · 깨진 링크 0) · `typecheck` 통과 · `npm test` 141건 통과 · `check:tests` 통과
- 미완: **코드 반영 0건 그대로.** 이번 작업은 문서 구조만 바꿨다
- 막힌 것: 없음

### 왜 이렇게 갈랐나

사용자 요구는 "유지보수할 때 도메인 단위로 관리되어야 기능 추가·변경에 유리하다" 였다.

| 축 | 어디로 |
|----|-------|
| 도메인 고유 (§3.x · 도메인별 API · 그 도메인 화면) | `docs/spec/도메인/` 5장 |
| 여러 갈래 공유 (§1 §2 §4 §5.1 §6 §9 §10 §11) | `docs/spec/공통/` 7장 |
| 읽기 동선 | `docs/SPEC.md` 색인의 세 표 |

**§6 데이터 모델만 도메인으로 가르지 않았다.** 표는 주인이 하나씩이지만(`test_case`→카탈로그 등)
뒤따르는 박제 원칙과 계약 변경 보고문이 표를 가로지르며 설명한다. 쪼개면 관계가 안 보인다.
**§7 Admin API 는 갈랐다.** 엔드포인트가 서로 독립이고 `# Auth`·`# Catalog` 로 이미 경계가 있었다.

### 내용이 새지 않았다는 증거

절 번호 참조가 코드 주석·훅·스킬·킥오프에 **221건** 있어서 번호를 바꾸면 전부 깨진다.
**번호는 영구 주소로 두고 파일만 옮겼다.** 한 장 안에서 §3.2 다음에 §7 이 오는 것은 의도다.

검사 두 개를 돌려 확인했다.
1. 배정 검사 — 원본 1496줄 중 배정 1478줄 · 색인 9줄 · 나머지는 빈 줄과 코드펜스. **샌 줄 0**
2. 무손실 검사 — 원본 내용 1188줄과 분할본 내용 1188줄을 줄 단위로 대조. **잃음 0 · 더함 0**

### 같이 고친 것

`CLAUDE.md` §0 · `WORKFLOW.md` 3곳 · `WORKSTREAMS.md` 킥오프 6개(갈래마다 읽을 장을 직접 지정) ·
`spec-review` 스킬(절차 + **H 섹션 신설**) · `orchestration.yaml` 주석 · `package.json`(`check:spec`)

### 다음 세션이 알아야 할 것

- **전문을 읽지 않는다.** `docs/SPEC.md` 색인에서 자기 갈래 줄을 찾아 그 장만 읽는다.
  WS-D 면 537줄, WS-F 면 527줄이다 (전체 1644줄)
- **절 번호를 바꾸지 않는다.** 새 내용은 뒤에 번호를 더한다 (§8.7, §9.3 처럼).
  절을 옮기거나 더하면 색인의 세 표를 같이 고치고 `npm run check:spec` 을 돌린다
- **WS-F 킥오프 프롬프트가 아직 없다.** 소유 경로 표에는 WS-F 인증이 들어갔는데
  `WORKSTREAMS.md` 의 Phase 1 킥오프는 WS-A~E 다섯 개뿐이다. WS-F 를 돌리기 전에 써야 한다
- ~~오케스트레이터가 장 단위로 문서를 지정하지 못한다~~ → **해결 (같은 날 4차).** 아래 참조
- `final_check: "spec#Phase 1"` 이 깨질 뻔했다. `extractSection` 은 절을 못 찾으면 예외를 던지는데
  색인에는 §11 본문이 없다. 색인에 `## Phase 1 완료 기준 (G4)` 절을 두어 그 장으로 안내한다

---

## 2026-09-17 (4차) — 오케스트레이터가 색인을 읽고 장을 싣는다

- 완료: 세션을 띄울 때 **그 갈래가 읽을 장만** 자동으로 실린다. `docs/SPEC.md` 의 "갈래별 읽을 장" 표가 그 정본이다
- 고친 곳: **오케스트레이터** (`~/Projects/orchestrator`) `src/manifest.ts` · `src/graphs/pipeline.ts`,
  **이 저장소** `docs/SPEC.md` 표와 `docs/orchestration.yaml` 주석
- 검사: orchestrator `npm test` 87건 통과 · `typecheck` 통과. 이 저장소 `check:spec`·`typecheck`·`test` 141건·`check:tests` 통과
- 막힌 것: 없음

### 어떻게 도는가

`manifest.ts` 의 **`specChapters(projectDir, m, unitId)`** 가 색인을 한 줄씩 보며
`**<단위 id>**` 가 있는 표 행을 찾고, 그 행의 **백틱 안 경로**를 장으로 읽는다.
경로는 색인 파일 기준 상대경로다 (`spec/도메인/리포팅` → `docs/spec/도메인/리포팅.md`).
`pipeline.ts` 의 `stageDocs()` 가 `spec` 참조를 만나면 **색인 뒤에 그 장들을 붙인다.**

실측 — WS-A 4장 · WS-B 5장 · WS-C 4장 · WS-D 4장 · WS-E 6장 · WS-F 4장,
`skeleton` 과 `integration` 은 0장(색인만. 전 갈래를 건드리므로 직접 고른다).

### 규약 세 가지

- 읽을 장을 바꾸려면 **매니페스트가 아니라 `docs/SPEC.md` 의 표**를 고친다. 한 곳에만 적는다
- 장은 **백틱 안에** 적는다. `DESIGN.md` 처럼 `spec/` 밖 문서는 백틱 없이 적어야 장으로 오해받지 않는다
- 표에 없는 단위나 백틱 없는 행은 **색인 한 장만** 실린다. SPEC 이 한 파일인 다른 프로젝트도 그대로 돈다 (하위 호환)

### 다음 세션이 알아야 할 것

- **장을 옮기거나 더하면 색인의 갈래별 표를 반드시 같이 고친다.** 안 고치면 세션이 옛 장을 읽거나
  새 장을 못 받는다. `npm run check:spec` 은 링크는 보지만 이 표의 최신성은 못 본다 — 사람이 본다
- 오케스트레이터 테스트는 `test/manifest.test.ts`(단위 5건)와 `test/pipeline.test.ts`(통합 1건)에 있다.
  통합 테스트는 확장 코드를 끄면 실제로 실패하는 것을 확인했다 (거짓 초록불이 아니다)

---

## 2026-09-17 (5차) — LEARNINGS 를 자가학습 루프로 되돌리고, 안 쓰는 의존성을 지웠다

- 완료: 에이전트가 검사기·훅·안내 문서를 **직접 고칠 수 있게** 규칙을 정하고, 밀려 있던 승격 3건을 실제로 반영했다.
  `zod-to-json-schema` 의존성도 지웠다
- 검사: `check:spec` · `typecheck` · `npm test` 141건 · `check:tests` 전부 통과
- 미완: `packages/kit/src/types.ts` 주석 2줄에 `zod-to-json-schema` 이름이 남았다 (아래 참조)
- 막힌 것: 없음

### 무엇이 문제였나

LEARNINGS 27건을 자가학습 관점(`겪음 → 기록 → 반복 감지 → 규칙으로 승격 → 안 읽어도 안 틀림`)으로 재보니
**승격 고리가 끊겨 있었다.** 2회 반복된 3건이 전부 이틀째 `제안:` 상태로 파일 한가운데 묻혀 있었다.
원인은 `CLAUDE.md` §2.5 의 "제안만 하고 직접 고치지 않는다" 였다 — 검사기 한 줄도 사람 승인을 기다렸다.

### 무엇을 바꿨나

- **`CLAUDE.md` §2.5** — 직접 고쳐도 되는 범위를 표로. 검사기·훅·안내 문서는 직접, SPEC·잠긴 파일은 승인.
  기준은 "잘못 고쳤을 때 무엇이 깨지나" 다. 검사기는 깐깐해질 뿐 제품이 안 깨지고 바로 드러난다
- **밀린 3건 반영** — DB fixture LIKE 패턴(→ `CLAUDE.md` §3 · spec-review G3) ·
  워크트리 WORKSTREAM 전달 경로(→ HOOKS.md) · 문서 규정이 코드에 안 걸림(→ `CLAUDE.md` §1.2)
- **굳은 8건 축약** — 실물 파일을 열어 정말 고쳐졌는지 확인하고 한 줄로 줄였다
- **분량 관리 규칙** — 분량은 목표가 아니라 승격의 결과다. 늘기만 하면 밀린 승격이 문제다.
  세는 법은 `grep -cE "^(제안|미완):" docs/LEARNINGS.md`

기록 부분 173줄 → 147줄. 밀린 것 3건 → **0건** (zod 처리 후).

### zod-to-json-schema 제거

SPEC §9.1 이 이미 "의존성에서 지운다 (2026-09-16 결정)" 라고 적어 두고 있었다. 즉 **지우는 것이 SPEC 을 따르는 것**이라
§1.2 절차 대상이 아니었다. `package.json` · `package-lock.json` · WORKSTREAMS 킥오프 목록 · SPEC 스택 표에서 지웠다.
import 하는 코드는 0곳이었고 제거 후 테스트 141건 그대로 통과한다.

**`packages/kit/src/types.ts` 주석 2줄은 못 지웠다.** 잠긴 파일이라 훅이 막는다.
주석이라 동작에는 영향이 없다. §5.1 에 `ExecuteRequest.baseUrl` 을 더하려고 그 파일을 열 때 같이 고친다.

### 다음 세션이 알아야 할 것

- **두 번째 실수는 기계로 옮긴다.** 검사기·훅·안내 문서는 승인을 기다리지 말고 직접 고치고,
  고친 뒤 그 LEARNINGS 항목을 한 줄로 줄이며 `→ <어디>로 승격 (날짜)` 를 붙인다 (`CLAUDE.md` §2.5 표)
- **LEARNINGS 를 갈래별로 가르지 않는다.** 태그는 "누가 겪었나"이지 "누가 읽어야 하나"가 아니다.
  `[WS-A]` 항목이 본문에 "전부 같은 함정이다" 라고 적고 있다

---

## 2026-09-17 (6차) — 계약 파일 잠금을 풀었다

- 완료: `guard.mjs` 의 잠금 목록(`LOCKED`)을 비우고, 그에 맞춰 검사 기준과 문서 네 곳을 고쳤다
- 검사: `check:spec` · `typecheck` · `npm test` 141건 · `check:tests` 통과
- 미완: `packages/kit/src/types.ts` 주석 2줄 (아래 참조)
- 막힌 것: 없음

### 왜 풀었나

잠금은 **"말없이 바꾸지 마라"** 는 장치인데, 잠긴 세 파일의 변경이 **이미 전부 승인돼 있었다**
(위 1차 기록 "승인받았으나 아직 코드에 안 닿은 것"). 즉 승인된 일을 막고 있었다.
오늘 주석 두 줄을 고치려다 막힌 것이 그 증상이다.

`ALLOW_PROTECTED=1` 로 켜는 방식은 안 썼다. **스위치 하나가 잠금 6종을 한꺼번에 열기** 때문이다
(계약 파일 · 담당 폴더 · 테스트 규칙 · 검사 기록 요구). 필요한 것만 여는 쪽을 골랐다.

### 지금 무엇이 막히고 무엇이 안 막히나

| 막는 것 | 안 막는 것 |
|---------|-----------|
| 담당 폴더 밖 수정 (`ownership`) | **계약 파일 3개 수정** |
| 파괴적 명령 4개 (강제 push · 브랜치 강제 삭제 · 마이그레이션 되돌리기 · `rm -rf /`) | |
| `tests/**` 주석·`expect` | |
| 테스트 실패·오늘 검사 기록 없이 푸시 | |

파괴적 명령 4개는 `ALLOW_PROTECTED=1` 로도 안 풀린다 (`guard.mjs` bash 모드에서 ESCAPE 검사보다 앞에 있다).

### 같이 고친 것

- `spec-review` **A1~A3** — "Phase 0 이후 변경 없음"에서 **"SPEC 대로 바뀌었나"** 로.
  잠금이 사라져 이 검사가 유일한 방어선이다. `git diff main...HEAD` 와 SPEC §5.1·§6·§9 를 한 줄씩 대조한다
- `CLAUDE.md` §1.3 · `docs/HOOKS.md` · `docs/WORKSTREAMS.md` — "아무도 건드리지 않는 곳" 문구를 실제와 맞췄다.
  잠금이 없는데 있다고 적혀 있으면 다음 세션이 또 오판한다 (오늘 C1 오보고와 같은 유형)

### 다음 세션이 알아야 할 것

- **계약 파일 3개를 고치기 전에 SPEC 해당 절을 먼저 본다.** 막는 장치가 없다.
  SPEC 에 없는 변경은 `spec-review` A1~A3 이 사후에 치명으로 잡는다
- **`packages/kit/src/types.ts` 주석 2줄이 남았다.** `zod-to-json-schema` 옛 이름이 있다.
  워크트리에서 잠금을 비웠지만 훅은 메인 체크아웃 쪽 `guard.mjs` 를 읽어 이 세션에서는 계속 막혔다.
  **이 브랜치가 main 에 병합된 뒤에는 풀린다.** 그때 고친다
- **`.claude/` 아래를 고쳐 지금 세션에 먹이려면 메인 체크아웃 쪽을 고쳐야 한다** —
  `$CLAUDE_PROJECT_DIR` 이 워크트리가 아니다. LEARNINGS 에 3회차로 적었다

---

## 2026-09-17 (7차) — 작업 단위를 채워 파이프라인을 다시 돌게 한다

- 완료: 오케스트레이터가 **돌릴 수 있는 단위가 0개**였다. `contracts`(계약 반영) 단위를 신설하고
  WS-F 를 등록하고 WS-D·WS-E 를 `pending` 으로 되돌려 사슬을 이었다
- 검사: 오케스트레이터로 매니페스트를 실제로 읽어 확인 — **돌릴 수 있는 단위 `contracts` 1개**,
  킥오프 5개 전부 추출, 갈래별 장 적재 정상. 이 저장소 `check:spec`·`typecheck`·`test` 141건·`check:tests` 통과
- 미완: 코드 반영은 여전히 0건. 이 매니페스트로 `contracts` 부터 돌리면 시작된다
- 막힌 것: 없음

### 왜 멈춰 있었나

단위가 전부 `done` 이거나 `blocked`/의존 대기였다. 그리고 **승인된 계약 변경을 맡을 단위가 아예 없었다** —
WS-D 주석이 "잠긴 파일(계약) 단위가 먼저 서면" 이라고 그 단위를 전제하고 있었는데 만들어지지 않았다.

### 새 사슬

```
contracts ──┬──▶ WS-D 리포팅 ──┐
            ├──▶ WS-F 인증   ──┼──▶ WS-E 화면 ──▶ integration
            └────────────────┘
```

- **`contracts` 는 담당 갈래가 없다**(`WORKSTREAM` 없음). 어느 갈래 소유도 아닌 파일들이고 여러 폴더를 걸친다.
  `integration` 과 같은 방식이다. 2026-09-17 잠금 해제로 이 단위가 계약 파일을 직접 고칠 수 있다
- steps 5개 — `db/migrations` · `types.ts` · `playwright.config.ts` · `docker-compose.yml`·`Dockerfile` · `package.json`.
  각 step 에 사람 확인을 붙였다 (마이그레이션 적용·러너 포트 닫힘 등)
- **WS-E 를 `done` 에서 되돌렸다.** 킥오프에 개정 대응 11건이 적혀 있는데 상태가 `done` 이었다.
  4번(param_schema 박제)·6번(대상 서버)은 `contracts` 의 DB 칸이, 9번(증적 버튼)은 WS-D 가,
  10번(로그인 화면)은 WS-F 의 API 가 먼저 서야 해서 의존을 셋 다 걸었다

### 다음 세션이 알아야 할 것

- **`orchestrate run` 을 돌리면 `contracts` 부터 잡는다.** 그게 끝나야 나머지가 풀린다
- **WS-A 는 `done` 으로 뒀다.** K9·K10 검사기(`apps/admin/src/catalog/rules.ts`)가 남아 있어
  개정 §11 Phase 1 기준(`K1~K10`)을 아직 못 채우는데, **킥오프가 옛 내용 그대로**라 되돌리지 않았다.
  킥오프를 개정본으로 고친 뒤 `pending` 으로 바꾸면 된다. 매니페스트에 그 이유를 주석으로 남겼다
- **로그인 화면은 WS-E 가 만든다** (§8.6 은 WS-E 소유). WS-F 는 확인 함수·미들웨어·계정 만들기 명령까지다
- 미결 그대로 — 증적 문서 **엑셀 양식**. 검수처 양식을 받아야 표 모양을 정한다 (§1.1 L4)

---

## 2026-09-17 (8차) — 기술·화면 검토 결정 13건을 SPEC 에 반영

- 완료: `docs/reviews/2026-09-17-eng-review.md`(D1~D5)와 `-design-review.md`(D1~D8)를
  12장에 전부 반영했다. **절 하나마다 커밋했다**
- 검사: `check:spec` 통과(없는 절 0 · 깨진 링크 0 · 틀린 분량 0) · `typecheck` ·
  `npm test` 141건 · `check:tests` 통과. 오케스트레이터로 색인을 실제로 읽어 갈래별 장 적재 확인
- 미완: **코드 반영 0건 그대로.** 이번 작업도 문서만 바꿨다
- 막힌 것: 없음

### 가장 큰 것 — 서비스를 한 벌로 합쳤다 (eng D5)

「서비스마다 인스턴스를 따로 띄운다」를 뒤집었다. 서비스 둘을 쓰는 회사에서 **사람마다 계정이 둘**이
되기 때문이다. 충돌은 **케이스 번호 접두사 배정**으로 막는다 — 이 한 가지가 번호 충돌·서비스 칸·권한 표
세 문제를 없앤다. 접두사는 자유 형식이고 플랫폼은 그 뜻을 모른다.

이 결정 하나가 §1·§2·§6·§7·§8·§9·§9.2·§11 을 전부 건드렸다.

### 뒤집은 결정을 다루는 방식

**지우지 않고 왜 뒤집었는지를 덧붙였다.** 세 곳이다.

| 어디 | 무엇이 남아 있나 |
|------|----------------|
| §6 「권한 칸을 만들지 않는다」 | 문단 그대로 + 인용 블록으로 뒤집은 이유. 판단 기준이 살아 있어야 다음에 또 판단한다 |
| §1 「인스턴스를 고르는 화면」 | 영구 금지 표에서는 **뺐다**. 근거(DB 가 여럿)가 사라진 금지를 표에 남기면 다음 세션이 또 막힌다 |
| §8 「인스턴스 띠」 | 서비스 띠로 바뀐 사실과, 막으려던 사고(엉뚱한 쪽에서 실행)는 그대로임을 적었다 |

기준은 하나다 — **판단 기준이 아직 쓸모 있으면 남기고, 근거가 사실이 아니게 됐으면 뺀다.**

### 리뷰에 없던 것을 더한 세 건

1. **마지막 운영 계정 잠금 방지** (§7) — 막지 않으면 설정 화면에 아무도 못 들어간다
2. **`SESSION_SECRET` 없으면 기동 거부** (§9) — 임시 키를 지어내면 재기동마다 전원 로그아웃되고
   아무도 이유를 모른 채 「가끔 로그인이 풀린다」로 겪는다
3. **`test_run` 서비스 칸 CHECK** (§6) — 반만 채운 INSERT 를 막는다. eng D4 가 지적한
   「조용히 비는 칸」과 같은 유형이라 같은 방식으로 막았다

### 리뷰와 다르게 판단한 것 두 건

- **검색 조건에서 「기능 영역」을 뺐다.** eng D5 는 「접두사로 이름만 바꿔라」였는데,
  나중에 나온 design D1(한 번에 한 서비스만)이 이것을 덮는다 — 한 서비스로 좁힌 목록은
  접두사가 전부 같아 거를 것이 없다. 검색 조건 다섯 → 넷
- **`test_run` 에만 서비스 칸을 뒀다.** eng D5 는 「모든 표에 필요 없다」였다. 그 말은 그대로 두되
  `test_run` 만 예외로 하고 이유를 적었다 — 「한 실행은 한 서비스」 불변식과 증적 머리말 박제

### 다음 세션이 알아야 할 것

- **테스트 저장소는 서비스마다 설정에서 넣는다** (§9.2 · §8.8). 처음에 「한 저장소의 접두사별
  하위 폴더에 모은다」로 썼다가 사용자가 바로잡았다 — **이미 정해져 있던 결정**이다.
  서비스마다 저장소가 다르므로 `service.tests_dir`(읽을 폴더)과 `tests_repo`(적어 두는 주소)를 따로 둔다.
  **없는 규칙을 지어내 「잠정」이라 적은 것이 잘못이었다** — 모르면 묻거나 비워 두는 것이 맞다 (CLAUDE.md §1.4)
- **작업 단위를 다시 나누는 일이 남았다** (eng-review 할 일 2). 이번에는 `contracts` steps 와
  WS-E·WS-F 킥오프만 개정 SPEC 에 맞췄다. 설정 API 는 WS-F, 설정 화면은 WS-E 로 갈랐다
- **`docs/design-mockup.html` 은 안 고쳤다.** 서비스 띠·자리 넷·설정 화면·빈 목록 세 갈래가 목업에 없다
- 증적 문서 **엑셀 양식**은 여전히 미결 (§1.1 L4)

### 같은 날 (9차) — 화면 검토가 미뤄 둔 남은것 4·5 를 정리했다

화면 검토의 「남은 것 6건」 중 1·2·6 은 8차에서 정했고, **3·4·5 는 범위 밖이었다.**
사용자가 4·5 를 지금 정리하라고 해서 처리했다. 3(실행 완료 알림)은 여전히 §1.1 L8 이다.

**남은것 4 — 글자 대비율 수치.** 기준이 없어서 **지금 색이 미달인 줄도 모르고 있었다.**
실측해 보니 `--ink-faint` 2.96, 미실행 배지 3.68 로 본문 기준(4.5:1)에 못 미쳤다.
색조는 그대로 두고 밝기만 내려 셋을 고쳤다 — `--ink-muted` 5.16→8.23 · `--ink-faint` 2.96→5.29 ·
`--na` 배지 3.68→4.53. 세 단계(15.78·8.23·5.29)가 여전히 벌어져 있어 위계는 안 무너졌다.
DESIGN.md 에 **실측표**를 넣었다 — 토큰을 바꾸면 다시 재라고 적어 뒀다.

**남은것 5 — 글꼴이 외부 주소에서 온다.** 제품 안에 담기로 했다.
막히면 시스템 기본 글꼴로 조용히 떨어지고 **숫자 정렬 규칙이 같이 깨진다.**
`font-feature-settings: "tnum"` 도 표준 속성 `font-variant-numeric: tabular-nums` 로 바꾼다.
**목업은 예외** — 저장소에서 바로 열어 보는 참고 문서라 CDN 을 써도 된다.

닿아야 할 곳까지 이었다 — SPEC §9(이미지에 담기) · `contracts` Dockerfile step(사람 확인
「인터넷을 끊고 띄워도 글꼴이 그대로다」) · **WS-E 킥오프 19·20번**.
`apps/admin/src/web/styles.css` 와 `index.html` 은 WS-E 소유라 직접 안 고쳤다 (CLAUDE.md §1.1).
