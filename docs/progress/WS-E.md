# WS-E 진행 기록 — 화면

## 2026-09-22 — 표 영역 되찾기 5건 (PR #55)

- 완료: 3등급 체인 일곱 단계 전부. 계획은 `docs/plans/2026-09-22-표영역되찾기.md`,
  검사 결과는 `docs/reviews/2026-09-22-표영역되찾기.md`
  1. **표머리와 줄이 같은 격자 한 벌**(`--list-cols`·`--run-cols`)을 쓴다.
     「입력값」 머리가 실제 칸보다 121px 오른쪽에 있던 제품 버그를 고쳤다
  2. 좌우 폭 1204 → 1600px. 늘어난 396px 은 전부 케이스명 칸으로 간다
  3. 목록 세로 — body 여백 · 화면 머리 · 집계띠 · 바닥 줄 · 스캔 줄 · 필터 두 줄
  4. 모달 880×80vh → 1400 × `calc(100vh - 32px)`. 증적 정보를 머리 줄로, 견줌을 접기로
  5. 사이드바 언어 고르개 CSS 신설 — **규칙이 한 줄도 없어 OS 가 흰 상자를 그리고 있었다**
  - 실측 결과: 목록 1.5줄 → 4.7줄 · 실행 기록 3.9줄 → 6.9줄 · 모달 0.3줄 → 4줄
- 미완: 아래 「다음 세션이 알아야 할 것」 넷
- 막힌 것: **DB 포함 전체 검사가 흔들려 G3(연속 3회)를 판정할 수 없다.** 아래 ④
- **다음 세션이 알아야 할 것**
  - **① `RunPickModal` 이 `넓게` 를 안 준다.** `docs/DESIGN.md` 는 넓은 상자가 **셋**이라
    적는데 코드는 **둘**이다(케이스 상세·실행 결과). 여러 건 실행 모달은 지금 480px 이다.
    둘 중 하나여야 한다 — ⓐ `RunPickModal` 에 `넓게` 를 준다 ⓑ DESIGN.md 를 고친다.
    ⓑ 로 간다면 **손으로 센 숫자를 빼고 정본을 가리키게** 한다 (CLAUDE.md §2.7 ⑤)
  - **② `EvidenceSection` 의 `한줄로` 갈래가 블록 갈래를 거의 베꼈다.**
    「… 증적을 만들지 못했습니다 —」 문장이 두 곳에 있고 **둘 다 `t()` 를 안 탄다**(원래도 그랬다).
    영어로 바꾸면 한국어가 그대로 뜬다. 합치려면 렌더 경로를 하나로 다시 짜야 한다
  - **③ 좁은 화면 실행 기록 표머리.** `docs/spec/도메인/실행.md:302` 가
    「좁은 화면에서 표머리를 감추지 않는다 … 줄이 세로로 접히면 **표머리 대신 줄마다 라벨**」이라
    적는데 `styles.css` 는 `.rowhead { display:none }` 하나로 둘을 같게 만든다.
    **바른 고침은 `RunList.tsx` 에 줄마다 라벨을 다는 것**이고 다국어 표까지 걸린다
    (CSS `content:` 로는 번역이 안 된다). 이번에 CSS 한 줄로 때우지 않고 남겼다
  - **④ ★DB 검사가 원래 흔들린다.** `origin/main` 을 별도 작업방에 꺼내 돌려도 7건 깨진다.
    `reporting/generate.test.ts` 가 쓰는 중인 `test_run` 을 다른 검사 파일의 넓은
    `DELETE ... LIKE` 가 지운다 — `evidence_document_run_id_fkey` 위반.
    vitest 가 지난 실행 시간으로 파일 순서를 정해 **실행마다 깨지는 파일이 다르다.**
    **이것이 남아 있는 한 어느 PR 도 G3 을 만족시킬 수 없다.** 고치려면 검사 파일 여럿의
    정리 구문을 다시 갈라야 한다 (CLAUDE.md §3 의 접두사 표가 출발점이다)
  - **격자는 `--list-cols`·`--run-cols` 가 정본이다.** 표머리와 줄이 **같은 값**을 써야 하고
    **마지막 칸은 고정 폭**이어야 한다 — `auto` 면 머리(글자)와 줄(버튼)의 내용 폭이 달라
    영영 안 맞는다. 1164px 미만은 비율(`fr`)로 바뀐다. `styles.test.ts` 가 둘 다 본다
  - **브라우저 실측 없이 화면을 다 고쳤다고 하지 마라.** 이번에 jsdom 이 원리적으로 못 잡는
    버그를 넷 잡았다 — 표머리 여백 24px · 영어에서 칸 넘침 · 버튼이 한 글자씩 세로로 흐름 ·
    1024px 창 가로 넘침 139px. 방법은 제품 CSS 원본을 `<link>` 로 불러 DOM 을 재현하는 것이다

## 2026-09-22 — 화면 후속 다듬기 10건 (PR #54)

- 완료: 1등급 체인(1 · 5 인라인 · 6 · 7). 계획 파일 없음
  1. 케이스 상세를 넓은 모달로 — `Modal.tsx` 에 `넓게` 프롭 추가, `.modal.wide` 880px 신설.
     DESIGN.md 가 880px 을 적어 두고 있었는데 **구현이 없었다**
  2. 실행 결과 모달에서 「화면으로 열기」 제거. 그 정보를 모달 안으로 합쳤다
  3. 설정의 「더하기」를 `+` 아이콘 버튼으로 (`.btn.icon`). 서비스·계정 둘 다
  4·5. 사이드바 언어·로그아웃을 창 높이에 고정 — `.wrap` 에
     `height: calc(100vh - var(--body-pad-top) - var(--body-pad-bottom))` + `align-items: stretch`
  6. 사이드바 접었을 때 입력값 칸 공백 — `.row.pickable` 의 입력 칸을 `1fr` → `minmax(0, 300px)`
  7. 「1개 더」 를 누르면 케이스 상세 모달에서 입력값을 **고칠 수 있게** 했다 (`Form` 재사용)
  8·9. 결과 표시 축소 · 정렬
  10. 목록 표만 스크롤 — `.screen.list-screen` + `.rows-scroll` + `position: sticky` 표머리.
      CaseList · RunList · 모달 안 RunResult 가 같은 장치를 쓴다
- 미완: 없음
- 막힌 것: 없음
- **다음 세션이 알아야 할 것**
  - **`.device` 는 `width` 가 아니라 `min-width` 다.** 고정 폭으로 바꿨더니
    `.device-none` 의 「실행 이력 없음」 이 한두 글자씩 네 줄로 흘렀다. 브라우저로 잡았다.
    `white-space: nowrap` 이 같이 지킨다
  - **`schema.ts` 의 `initialText()` 가 객체·배열 기본값을 `String()` 으로 찍어
    `"[object Object]"` 를 만들고 있었다.** 원래 있던 버그인데 이번에 케이스 상세가
    편집 가능해지며 **실제로 저장될 수 있는 값**이 됐다. `JSON.stringify` 로 고쳤다 —
    `RunPickModal` 의 같은 버그도 같이 나았다. 자기검사(`/code-review`)가 잡았다
  - **`--body-pad-top` · `--body-pad-bottom` 이 생겼다.** `body` 여백과 `.wrap` 높이가
    같은 숫자를 봐야 해서다. 한쪽만 고치면 사이드바가 창보다 길거나 짧아진다
  - 좁은 화면(620px 미만)은 `@media` 안에서 **전부 되돌렸다**. 거기는 페이지 전체가 스크롤한다
  - 「화면으로 열기」 를 없앤 것은 되돌린 결정이라 `docs/LEARNINGS.md` 맨 위에 적어 뒀다.
    `#/runs/:runId` 경로 자체는 살아 있다 — Slack 알림과 사이드바가 쓴다

## 2026-09-21 — 제도 청사진 스킨 (PR #35)

- 완료: 일곱 단계 전부. 계획은 `docs/plans/2026-09-21-청사진스킨.md`
  1. 글꼴을 IBM Plex Sans KR 400·600 + IBM Plex Mono 400·500 으로. Pretendard 를 뺐다
  2. 토큰을 청회색으로, **28px 방안지 격자 제거**
  3. 띠를 `--chrome` 고정색으로, 서비스 색을 자리 넷 줄의 네모·경계선으로
  4. 조건 칩을 세그먼트로, 버튼 문구 12개를 명사형으로
  5. 케이스 줄에 값 칩, `RunPickModal` 접개 제거, 기본값 상시 표시
  6. 증적 문서 토큰을 화면과 일치
  7. DESIGN.md · SPEC 두 장 · 색인
- 미완: 없음
- 막힌 것: 없음
- **다음 세션이 알아야 할 것**
  - **명암비를 손으로 재지 않는다.** `apps/admin/src/web/styles.test.ts` 가 DESIGN.md 표를
    다시 재고, 증적 문서(`reporting/html.ts`)의 토큰 복사본까지 화면과 대조한다.
    토큰을 고치면 여기가 먼저 빨개진다
  - **서비스 색 기준이 4.5 에서 3 으로 바뀌었다** (`settingsView.ts`). 글자가 아니라 UI 요소다.
    기존 색은 안 깨진다 — 기준이 같은 방향으로 느슨해졌을 뿐이다
  - **글꼴은 굵기 400·600 둘뿐이다.** 500 을 쓰지 않는다. 벌을 늘리면 증적 문서가 그대로 무거워진다
    (`html.test.ts` 의 4MB 상한이 지킨다)
  - 등폭 글꼴은 TSX 가 아니라 `styles.css` 가 자리 이름(`.tcid`·`.runid`·`.ms` 등)으로 건다.
    새 자리를 만들면 그 목록에 더한다

## 2026-09-16
- 완료: 화면 4종 (케이스 목록 · 실행 설정 · 실행 결과 목록 · 항목 상세) + 실행 묶음 목록.
  React + Vite, 소유 경로 `apps/admin/src/web/**` 안에서만 만들었다. 단위 테스트 34건
- 확인함 (전부 **진짜 API**로. 목 데이터를 만들지 않았다):
  - `paramSchema`로 입력 폼이 자동 생성된다 — DEMO-004 enum→셀렉트(albums·photos·todos),
    DEMO-005 optional→라벨 옆 `선택` + 빈 칸, DEMO-003 파라미터 3개, boolean→`예`/`아니오` 셀렉트.
    라벨은 전부 zod `.describe()` 값이고 `default`가 있으면 칸이 미리 채워진다
  - 작성자 번호에 `아홉`을 넣고 실행하기를 누르니 **버튼은 그대로 살아 있고** 그 칸 아래에
    `작성자 번호(userId)은 숫자여야 한다`가 붙었다 (SPEC §8.2)
  - 케이스 목록이 `cases` + `last-by-case` **각 한 번**으로 다 채워진다. 케이스마다 이력을 부르지 않는다
  - DEMO-008을 눌러 실행 → `POST /api/runs`가 바로 돌아오고 결과 화면이 `도는 중 2건`으로 떴다가
    **스스로 갱신되어** PC 통과 0.60초 · 모바일 통과 0.80초가 됐다 (2초마다 다시 물음, FINISHED가 되면 멈춤)
  - RUN 123에서 DEMO-008이 한 행에 PC·모바일 판정을 나란히, DEMO-010은 PC 칸이 `—`로 비었다
  - 항목 상세(123/161)에 절차 2건 · 검증 문장 · 실패 문장 아래 스크린샷 · 그 아래 접힌 `실패 지점 코드`.
    펼치면 21~26행이 나오고 26행이 강조된다
  - `npx vite build`가 `apps/admin/src/web/dist`에 산출물을 떨어뜨린다 (app.ts의 정적 서빙 자리)
- 미완: 없음
- 막힌 것: 없음

### 컨테이너가 화면을 서빙하지 못한다 — 내 소유 경로 밖이라 고치지 않았다

`app.ts`는 `src/web/dist`가 있으면 서빙하도록 이미 돼 있다. 그런데

- `.dockerignore`에 `**/dist`가 있어 호스트에서 빌드한 산출물이 이미지에 안 들어간다
- `apps/admin/Dockerfile`에 vite 빌드 단계가 없다

둘 다 `apps/admin/src/web/**` 밖이다 (CLAUDE.md §1.1). **병합 담당이 Dockerfile에
`RUN npx vite build --config apps/admin/src/web/vite.config.ts` 한 줄을 넣으면 된다.**
`.dockerignore`는 `**/dist`를 그대로 둬도 된다 — 이미지 안에서 빌드하면 COPY를 타지 않는다.

지금 확인하는 방법은 개발 서버다. API는 진짜 admin(3000)이 받는다.

```
docker compose up -d
npx vite --config apps/admin/src/web/vite.config.ts
# → http://localhost:5173
```

### SPEC에 안 적혀 있어 판단한 것 3가지

| 무엇 | 정한 것 | 왜 |
|------|--------|-----|
| 실행 제목 | 실행 설정에 `실행 제목` 칸을 두고 `<tcId> 실행`으로 미리 채운다 | `POST /api/runs`의 `title`이 필수인데 §8.2 화면에 그 칸이 없다. 실행 목록이 제목으로 실행을 가린다 |
| §8.3의 `소요시간` 칸 | 환경별 판정 배지 **아래**에 각각 적는다 | 환경이 둘인 행에서 소요시간이 하나면 어느 환경 것인지 알 수 없다 |
| 아직 안 끝난 항목 | 판정 배지 대신 `도는 중` | `run_item`은 생성 시 `NA`다. 끝나야 판정이 들어간다 (SPEC §3.2). 미실행 배지를 붙이면 끝난 것처럼 보인다 |

### 다음 세션이 알아야 할 것

- **진입점**: 폼 자동 생성은 `schema.ts`의 `schemaToFields`/`toValues`,
  실행 전 검증은 `validation.ts`, 환경별 묶기는 `group.ts`, 주소 해시는 `route.ts`.
  화면은 `CaseList`·`RunSetup`·`RunList`·`RunResult`·`ItemDetail` 다섯 개, 공용 조각은 `ui.tsx`
- **`validation.ts`는 서버의 `../execution/validate.ts`를 그대로 부른다.** 검증기를 두 벌 두면
  '실행하기'와 '입력값 세트로 저장'이 같은 값에 다른 사유를 낸다. WS-B가 그 파일에
  DB나 fastify를 import 하면 화면 빌드가 깨진다 — 순수 함수로 남겨 둬야 한다
- **Vite 프록시 규칙은 `'^/api/'`다.** `'/api'`로 적으면 소스 파일 `/api.ts` 요청까지
  admin으로 넘어가 화면이 통째로 빈다 (LEARNINGS)
- **개발 서버 명령에 `--config`가 꼭 필요하다.** 화면 폴더에 package.json을 만들지 않았다 —
  루트·admin의 package.json은 Phase 0 공용 골격이라 스크립트를 넣을 수 없다
- **단위 테스트는 `*.test.ts`만이다.** vitest include가 `apps/**/*.test.ts`라 `.test.tsx`는 안 잡힌다.
  그래서 JSX가 아니라 순수 함수(스키마→칸, 묶기, 주소)를 테스트했다. DB를 쓰지 않으므로 fixture 접두사도 없다
- **좁은 창(620px 미만) 레이아웃은 눈으로 확인하지 못했다.** 목업의 미디어 쿼리를 그대로 옮기고
  `.err`·`.after-assert` 두 개를 더했다. 브라우저 창을 620px 아래로 줄이지 못했다
- `docs/LEARNINGS.md`가 202줄이다. 자체 규칙(100줄)을 넘었으니 정리 대상이다 — 내 갈래 소유가 아니라 건드리지 않았다

---

## 2026-09-19 — ① 뼈대 (PR #25)

킥오프 23개 중 **아홉**을 했다 — 1(용어) · 3(ABORTED 갱신) · 4(라벨 박제) · 7(비밀값) ·
10(로그인) · 11(서비스 띠) · 12(자리 넷 + 알림 줄) · 13(등급 숨김) · 19(색 토큰).
나머지 열넷은 사용자 승인으로 **②·③ PR 로 나눴다** (계획 맨 아래 표가 정본).

### 왜 이것부터였나 — 화면이 아예 안 떴다

2026-09-17 이후 `WS-F`(인증)와 `WS-A`(카탈로그)가 전제를 바꿨는데 화면이 몰랐다.

| 무엇이 막나 | 어디 |
|---|---|
| 모든 `/api/**` 에 로그인 문 | `auth/gate.ts:62` — 401 을 받고 전부 빈 화면 |
| `GET /api/catalog/cases` 가 `?service=` 필수 | `catalog/routes.ts:122` — 400 |
| `GET /api/runs` 가 `?service=` 필수 | `execution/routes.ts:138` — 400 |

### 버그 둘을 같이 닫았다

- `RunResult.tsx` 의 정지 조건이 `status !== 'FINISHED'` 여서 **중단된 실행이 2초마다 영영 다시 물었다.**
  확인: `ABORTED` 로 바꾸고 10초 동안 `/api/runs/:id` 요청이 **0건 늘었다**
- `ItemDetail.tsx` 가 `Object.entries(item.params)` 를 그대로 그려 **박제 이전 행의 비밀번호가 평문이었다.**
  `20260917000001` 이 과거 `run_item` 을 `param_schema = '{}'` 로 메워서 `secret` 표시가 아예 없다.
  확인: 그 모양의 행을 넣었더니 `password`·`apiToken` 이 `********`, `아이디` 는 그대로

### 소유 경로 밖을 다섯 건드렸다 (CLAUDE.md §1.1)

`apps/admin/src/execution/queries.ts`(계획 `files`) · `.claude/scripts/check-secret-names.mjs` ·
`package.json` · `.github/workflows/ci.yml` · `docs/LEARNINGS.md`.
앞 둘은 계획에, 뒤 셋은 게이트 2 요약에 이유를 적었다.

### SPEC 이 안 정해 판단한 것 둘

| 무엇 | 정한 것 | 왜 |
|---|---|---|
| 고른 서비스를 어디에 두나 | `localStorage` 에 접두사 한 줄 | 주소 해시에 넣으면 모든 화면 주소가 서비스를 끌고 다녀야 하고, 서버에 두면 설정 API 가 는다 |
| Grafana 주소 | 같은 호스트의 `3001` (`docs/SETUP.md` 의 `GRAFANA_PORT` 기본값) | SPEC §8 은 「바깥으로 나간다」까지만 적었다. **배포가 포트를 바꾸면 틀린다** — 설정으로 뺄지는 ③ 에서 정한다 |

### 다음 세션이 알아야 할 것

- **진입점이 바뀌었다.** `main.tsx` 가 로그인 여부를 먼저 가르고 `Shell.tsx` 안에 화면 하나를 그린다.
  띠·자리·알림 줄의 판단은 `layout.ts`(순수 함수), 그림은 `Shell.tsx`
- **`shell.ts` 가 아니라 `layout.ts` 다.** `Shell.tsx` 와 대소문자만 다르면 macOS 에서 충돌한다
- **비밀값·라벨은 `mask.ts` 한 곳에서 끝낸다.** `fieldsOf(values, schema)` 가 라벨을 붙이고 가린다.
  `SECRET_NAMES` 정본은 `catalog/rules.ts` 이고 세 곳이 갈라지는지는 **`npm run check:secret-names`** 가 본다
- **실행 상태는 `runState.ts` 하나가 판단한다.** 갱신을 멈출지도 무슨 글자를 쓸지도 같은 값을 본다 (SPEC §8.7)
- **등급은 `role.ts` 의 `할수있나(role, 무엇)`.** 서버 `auth/gate.ts` 가 같은 것을 다시 막는다 —
  화면 쪽은 자리를 안 그리기 위한 것이지 방어가 아니다
- **JSX 는 단위 테스트가 없다.** vitest `include` 가 `apps/**/*.test.ts` 다.
  `Login.tsx`·`Shell.tsx`·`main.tsx` 는 브라우저로 눌러 확인했다.
  테스트를 넣으려면 React Testing Library + jsdom 승인이 필요하다 (CLAUDE.md §3)
- **좁은 화면은 375px `iframe` 으로 본다.** 창 리사이즈가 안 먹는다 (2026-09-16 에도 같은 벽).
  페이지에 `<iframe style="width:375px">` 를 띄우면 미디어 쿼리가 그 폭에 반응한다.
  **이 방법으로 제목이 한 글자씩 세로로 흐르는 재발 버그를 처음 실제로 봤다**
- **컨테이너 admin 이 낡은 이미지를 물고 있으면** 인증 라우트가 404 다.
  `PLATFORM_ADMIN_URL=http://localhost:3200 npx vite --config ...` 로 소스 admin 을 보게 할 수 있다.
  **병합 뒤 `docker compose up -d --build admin` 을 한 번 돌려야 한다**
- ② 가 이어받을 자리 — 알림 줄이 지금은 「도는 중」만 말한다. 「끝났습니다」로 바뀌는 것과
  완료 모달(§8.9)은 ② 몫이다. `layout.ts` 의 `알림줄()` 에 얹으면 된다

---

## 2026-09-19 — ② 화면 고치기 (PR #26)

킥오프 열 항목 — 2 · 5 · 6 · 8 · 9 · 14 · 16 · 17 · 21 · 22.
**23개 중 열아홉이 끝났고 넷이 남았다** (③ — 15 설정 화면 · 18 좁은 화면 · 20 글꼴 · 23 Slack 웹훅 칸).

### 왜 이것부터였나 — 실행 버튼이 깨져 있었다

`RunSetup` 이 실행 요청에 `env`(대상 서버)를 안 실어 서버가 400 을 냈다.
① 에서 케이스 목록까지만 눌러 보고 **실행은 안 눌러 봤다.**

그 칸을 만들려면 통로가 필요했고 **게이트 0 을 거쳤다** — `/auth/me` 의 `services` 에
`envs`·`hasSlackWebhook` 을 더했다. SPEC §7 이 그 응답 필드를 **전부 나열**해 둔 닫힌 자리라
① 에서 `run`·`items` 에 필드를 더한 것과 성질이 다르다.

**통로만으로는 부족했다** — `service_env` 표에 행이 0개였다. 넣는 화면은 ③ 이라
`add-service.ts` 에 `--env` 를 더했다.

### 계획 검토가 잡은 것 (BLOCKER 2 · 주의 6)

| 무엇 | 안 고쳤으면 |
|---|---|
| **할 일 7 의 전제가 틀렸다** — 「이미 있는 값으로 그린다」인데 그 값이 없었다 | 구현이 거기서 멈추고 급히 고르는 세 갈래가 전부 나빴다. **답은 SPEC §8.3 에 이미 있었다** |
| **§8.9 의 셋 중 하나만 했다** — 알림 줄이 「끝났습니다」로 바뀌는 것이 빠졌다 | 한 번 돌리면 최악 50분이라 **사람은 그 화면에 안 있는 것이 보통**이다. 가장 흔한 상황에서 아무도 안 알린다 |

### 실측으로 확정한 것 둘

- **미실행 사유는 서버가 이미 한국어로 넣는다** (`러너에 닿지 못했습니다`).
  화면이 바꾸는 것은 `ABORTED` → `사용자가 멈춤` 하나뿐이다
- **정기 실행은 따로 가를 것이 없다.** `run-scheduled.ts:48` 이 이름을 `'스케줄러'` 로 박는다

### 검사와 게이트 2 — 렌즈 셋, 바깥 렌즈가 아홉을 잡았다

| 렌즈 | 결과 |
|---|---|
| `spec-review` **[자기검사]** | 치명 0 · 중대 1(게이트 2 에서 닫음) · 경미 2 |
| `/code-review high` | **9건 — 전부 고쳤다** (`3c9f4c1`). 둘이 심각했다 |
| `/security-review` | 취약점 0 (확신도 9~10) |

심각 둘 — ① 실패한 테스트에 「러너에 닿지 못했습니다」가 붙었다(사유를 고를 때 `status` 를
안 가렸는데 러너는 `FAIL` 이 예외로 끝나도 `error` 를 채운다) ② 증적이 「만드는 중입니다」에서
영영 굳었다(폴링이 **실행이 도는 중일 때만** 돌았다).

**② 를 자기검사가 못 본 이유가 「내가 `location.reload()` 로 확인했기 때문」이다.**
① 덩이에서도 같은 유형이 HIGH 를 낳았다 (로그인 **전** 경로만 눌러 봐서 세션 끊김을 못 봄).

**게이트 2 에서 「고치고 재검사」를 받아 셋을 기계로 옮겼다** — 제품 코드는 안 건드렸다.

| 무엇 | 어디 |
|---|---|
| 게이트 0 직후 등급 재측정 | `.claude/skills/tpx/SKILL.md` 등급 판정 표 + 게이트 절 |
| 「어떻게 확인했는가」 | `spec-review` **G6** 신설 |
| 승격 표시 | `docs/LEARNINGS.md` 두 항목 |

선언 등급을 **3 으로 올렸다** (실측과 일치). 착수 때는 2 였는데 게이트 0 에서 `docs/spec/**` 가
확정됐을 때 다시 재지 않은 것이 원인이었다 — 그 자리를 이제 체인이 막는다.

**안 고친 둘** — JSX 단위 테스트(새 npm 부품이라 ③ 게이트로) ·
`gate.ts` 의 IDOR(`origin/main` 의 기존 문제라 **WS-F**로. 로그인한 사람이 번호만 바꿔
남의 서비스 실행 기록을 읽을 수 있다).

### 다음 세션이 알아야 할 것

- **순수 함수가 늘었다** — `runPlan`(항목 수·1000건 상한) · `paging`(다음 쪽) ·
  `evidence`(증적 버튼·받는 법) · `catalogView`(마지막 결과 거르기·빈 이유).
  판단은 전부 여기 있고 화면은 그리기만 한다
- **`catalogView.ts` 는 `CaseList.ts` 가 아니다.** macOS 에서 대소문자만 다르면 같은 파일로 친다 —
  ① 의 `shell.ts` → `layout.ts` 와 같은 자리다. **순수 함수 파일 이름을 화면과 다르게 짓는다**
- **`Modal.tsx` 는 두 자리에만 쓴다** (DESIGN.md) — 멈춤 확인과 완료 알림.
  닫는 길 셋·포커스 가두기·애니메이션 없음이 들어 있다. 새 알림은 화면 안의 줄로 적는다
- **완료 모달과 알림 줄이 같은 자리를 본다** (`runState` 의 `본것키`).
  그 실행 화면에 있던 사람은 모달로 이미 알았으므로 줄이 또 뜨면 두 번 알리는 것이다
- **`group.ts` 의 칸이 배열이다.** 회차가 여럿이면 `회차요약()` 이 `3/5 통과` 를 만든다.
  하나라도 통과가 아니면 통과 색으로 칠하지 않는다
- **총건수로 페이지를 세지 않는다.** `paging.ts` 의 인자에 `total` 자리가 아예 없다
- ③ 이 이어받을 것 — 설정 화면(§8.8)이 서비스·계정·대상 서버·Slack 웹훅을 다 받는다.
  그때 `add-service.ts` 의 `--env` 와 `SETUP.md` 의 배정 SQL 은 첫 서비스용으로 남긴다.
  **서비스 색을 만들 때 흰 글자 대비 4.5:1 을 재는 장치**도 그 화면 몫이다 (① 에서 넘겼다)

## 2026-09-19 — ③ 마감 (PR #27)

킥오프 마지막 넷 — 15 설정 화면 · 18 좁은 화면 · 20 글꼴 번들 · 23 Slack 웹훅 칸.
**23개가 전부 끝났고 WS-E(화면)가 닫혔다.**

### 왜 1등급이었나 — 서버를 안 건드렸다

①②는 화면이 읽을 통로가 없어 각각 2·3등급이었다. 이번엔 `settings/routes.ts` 에
필요한 API 가 전부 있었다 — 서비스·계정·대상 서버·Slack 웹훅·비밀번호 다시 만들기.
접두사 불변(`PREFIX_IMMUTABLE`)도 서버가 이미 막는다. **화면만 만들면 됐다.**

### 브라우저로 눌러 보다가 찾은 것 셋

| 무엇 | 안 고쳤으면 |
|---|---|
| **배정이 없으면 설정 화면까지 덮였다** | 안내가 「설정에서 자기 자신을 배정하세요」로 보내 놓고 그 자리를 막았다. **서비스가 0개인 첫 운영자는 영영 못 빠져나온다** |
| **자기 자신을 배정해도 띠가 그대로** | 시킨 대로 했는데 아무 일도 안 일어난다. 새로고침하면 되지만 그러라고 알려 주는 것이 없다 |
| **기본 서비스 색이 명암비 미달** | `add-service.ts` 의 `#5B7FDE` 가 3.79 였다. DEMO 하나가 아니라 **새로 만드는 모든 서비스**가 미달 색을 받고 있었다 |

앞의 둘은 **검사 셋과 타입 검사를 다 통과한 채로** 있었다. 눌러 보지 않으면 안 나온다.
세 번째는 이번에 만든 명암비 함수가 잡았다 — 장치를 만들자마자 그 장치가 기존 값을 걸렀다.

### SPEC 이 정한 것 그대로

- 설정 화면은 **좁은 화면 대응을 하지 않는다** (§8.8 「책상에서만 쓰는 화면」).
  킥오프 18 이 고를 둘(실행 기록 목록·실행 결과)과도 맞는다
- 임시 비밀번호는 **모달이 아니라 화면 안의 줄**이다. 모달은 두 자리뿐이다 (DESIGN.md)
- 마지막 운영 계정은 등급 드롭다운과 내리기 버튼을 **아예 안 그린다** (§3.5)

### 다음 세션이 알아야 할 것

- **설정 화면의 판단은 `settingsView.ts` 에 있다** — 접두사 모양 · 명암비 · 웹훅 칸 ·
  마지막 운영 계정 · 서버 오류를 사람 말로. 화면(`Settings.tsx`)은 그리기만 한다
- **`빈띠사유()` 가 지금 자리를 받는다.** 설정만 예외로 두는 자리다 — 새 화면을 더할 때
  「배정이 없어도 열려야 하는가」를 물어본다
- **접두사 정규식이 두 곳에 있다** — 서버 `settings/routes.ts` 와 화면 `settingsView.ts`.
  화면은 서버 모듈을 import 할 수 없다(fastify 를 끌고 와 번들이 깨진다). SPEC §2 를 고치면 둘 다 고친다
- **글꼴은 `web/fonts/PretendardVariable.woff2` 하나다.** 굵기 셋을 한 파일로 낸다.
  `font-feature-settings` 대신 `font-variant-numeric` 을 쓴다 — 앞엣것은 폴백에서 조용히 무효다
- **`scripts/` 가 admin 이미지에 안 들어간다** (아래 「넘기는 것」)

### 넘기는 것

| 무엇 | 어디로 | 왜 |
|---|---|---|
| `apps/admin/Dockerfile` 에 `COPY scripts` 가 없다 | **배포(§9.2)** | `docker compose exec admin npx tsx scripts/add-user.ts` 가 `ERR_MODULE_NOT_FOUND` 로 죽는다. **SETUP.md 가 안내하는 첫 계정 만들기가 실제로는 안 된다.** Dockerfile 은 3등급(COMPOSE) 표면이라 이번 범위에서 고치면 등급이 올라간다 (CLAUDE.md §1.1 — 보고만 한다) |
| `gate.ts` 의 IDOR | **WS-F(인증)** | 로그인한 사람이 실행 번호만 바꿔 남의 서비스 기록을 읽는다. `origin/main` 의 기존 문제다 |
| JSX 단위 테스트 | 별도 작업 | 사용자가 ③ 게이트에서 「지금처럼 간다」를 골랐다. 그물을 씌우려면 ①②의 화면까지 한꺼번에 덮는 작업이 맞다 |

### 검사 렌즈 셋 — 자기검사가 혼자서는 못 잡았을 것들

실측 2등급이라 렌즈 셋을 병렬로 돌렸다. **셋이 독립적으로 겹쳐 지적한 것이 다섯**이었다.

| 렌즈 | 결과 |
|---|---|
| `spec-review` [자기검사] | 치명 0 · 중대 1 · 경미 2 |
| 코드 리뷰 | 심각 4 · 보통 9 · 경미 7 |
| 화면 QA | 심각 3 · 보통 7 · 경미 7 |

**전부 검사 일곱과 타입 검사를 통과한 채로 있었다.** 대표적인 것 셋 —

- **계정 목록이 실패하면 영영 「불러오는 중입니다」** (`users.error` 를 보는 자리가 없었다)
- **임시 비밀번호가 화면 밖에 떴다.** 계정이 열 명만 돼도 안 보이는데 **그 사이 옛 비밀번호는
  이미 죽어 있다** — 본인은 영문도 모르고 로그인이 막힌다
- **입력칸 아홉 개가 브라우저 기본 모양이었다.** 스타일이 `input[type='text']` 에 걸려 있는데
  React 는 기본 `type` 을 DOM 에 안 써 준다. **속성 선택자는 속성이 실제로 있어야 맞는다**

전말과 고친 목록은 `docs/reviews/2026-09-19-WS-E-3.md`.

**남은 판단 하나** — SPEC §8.8(「색을 언제든 고친다」)과 §8(「서비스 색은 띠에서만 쓴다」)이
부딪힌다. 색을 안 보여주고 색을 고르게 할 수 없다. 두 렌즈가 §8 에 예외 한 줄을 더하자고 제안했고
그건 SPEC 변경이라 게이트 0 이 필요하다.

---

## 2026-09-19 — JSX 단위 테스트 그물 (PR #30)

- 완료: `vitest.config.ts` 의 `include` 에 `apps/**/*.test.tsx` 를 더해 화면 파일이
  자동 검사 범위에 들어오게 했고, `Modal` · `SettingsPassword` · `Form` 셋에 그물을 씌웠다.
  `docs/spec/공통/6-인프라.md` §9.1 과 이 갈래의 킥오프·규칙 문서를 거기에 맞췄다
- 미완: **위 61줄·116줄의 「단위 테스트는 `*.test.ts` 만이다」·「JSX 는 단위 테스트가 없다」는
  이제 부분적으로만 참이다.** 셋은 덮였고 나머지 화면은 그대로 그물 밖이다 —
  그 화면을 건드릴 때 같이 덮는다. 몇 대 몇인지는 세어서 본다 —
  `ls apps/admin/src/web/*.tsx | grep -v '\.test\.tsx$' | wc -l` 와
  `ls apps/admin/src/web/*.test.tsx | wc -l`
- 막힌 것: 없음
- 다음 세션이 알아야 할 것: 화면 검사 파일은 **첫 줄에 `// @vitest-environment jsdom`** 이 있어야 한다.
  빠뜨리면 `document is not defined` 로 죽는다. 본을 뜰 자리는 `apps/admin/src/web/Modal.test.tsx` 다.
  화면을 고치면서 그물을 빠뜨리는 것은 `spec-review` 의 `F9` 가 잡는다.
  **jsdom 에는 레이아웃 엔진이 없다** — 「화면 밖에 그려졌다」·간격·색은 여전히 브라우저로 본다

---

## 2026-09-20 — Grafana 포트를 「빌드 시 설정값」으로 정했다 (마무리 PR)

- 완료: 위 2026-09-19 기록이 **「설정으로 뺄지는 ③ 덩이에서 정한다」고 미뤄 둔 판단을 닫았다.**
  그 덩이가 끝났는데 아무도 안 정해서 `layout.ts` 가 `3001` 을 박은 채로 남아 있었다.
  **빌드 시 설정값으로 정했고 세 토막을 이었다** — `docker-compose.yml` 의 admin `build.args`
  → `apps/admin/Dockerfile` 의 `ARG`/`ENV` → `vite build` 가 `VITE_GRAFANA_PORT` 를 번들에 굽는다.
  `layout.ts` 는 `import.meta.env.VITE_GRAFANA_PORT` 를 읽고 비면 `3001` 로 떨어진다
- 미완: 없음
- 막힌 것: 없음
- 다음 세션이 알아야 할 것: **한계가 있다 — 값이 번들에 구워진다.**
  실행 시점 환경변수로는 못 바꾼다. `GRAFANA_PORT` 를 바꾸면 **admin 이미지를 다시 빌드해야 하고**
  (`docker compose up -d --build admin`), 안 하면 **오류 한 줄 없이 「그래프 ↗」 링크만 틀린 포트를 가리킨다.**
  같은 문장을 `docs/SETUP.md` 설정값 표와 SPEC §9.2 에도 적어 뒀다.
  런타임에 바꾸고 싶어지면 admin 이 `index.html` 에 값을 심는 길이 따로 있다 — 그때 다시 본다

---

## 2026-09-20 — 증적 문서 형식 고르기 (「형식고르기」 PR)

- 완료: **넘긴 것 ⑧ 과 ⑤ 를 닫았다.** ⑧ — `apps/admin/src/web/evidence.ts` 에 `형식들` **한 표**를 두고
  `증적버튼들()` 이 그 표를 훑어 **형식마다 버튼 하나**를 낸다. `RunResult.tsx` 는
  `makeEvidence(runId, 버튼.format)` 으로 **누른 그 형식**을 보낸다. 그물은 `RunResult.test.tsx` 이고,
  형식 이름이 네 곳에서 갈라지면 `npm run check:evidence-formats` 가 CI 에서 잡는다.
  ⑤ — `tests/demo/DEMO-011.spec.ts` 와 명세 §10 「데모 케이스」 표의 새 줄.
  그 밖에 명세 셋(개수 줄 · 상태·버튼 표 · §8.4 내부 모순 · ASCII 상자) · 목업 맞춤 ·
  `DESIGN.md` 의 「비활성 버튼 이유는 화면 줄로」 · 색인 라우터 표 한 줄 ·
  `spec-review` E2 를 §10 표로 돌린 것까지 같이 들어갔다.
  **검사 기록은 `docs/reviews/2026-09-20-형식고르기.md` 다** — 여기에 옮겨 적지 않는다
- 미완: WS-E 가 넘긴 것 중 **① 드롭다운 비밀값 · ② 화면 그물 · ③ `aria-describedby` ·
  ④ 모달 규칙 다섯 · ⑥ `@fastify/static` 취약점 · ⑦ 증적 대기줄**은 그대로 열려 있다.
  ② 는 `RunResult.test.tsx` 만큼 한 칸 좁혔다 — 몇 대 몇인지는 위 두 `ls … | wc -l` 이 센다
- 막힌 것: 없음
- 다음 세션이 알아야 할 것: **「기능 구현은 됐다」의 근거로 「코드에 미구현 표시 0건」을 쓰지 않는다.**
  형식 고르는 버튼은 쓰다 만 코드가 아니라 **아예 없던 코드**여서 TODO 가 안 붙어 있었고,
  그 조사 방식으로는 원리적으로 안 걸렸다. **SPEC §8 이 요구하는 화면 자리를 열어서 본다** —
  `spec-review` 의 **F10** 이 그 약속을 붙든다.
  **사용자가 눌러볼 길은 `docs/WORKFLOW.md` 의 「눌러볼 길」 절에 있다.**
  핵심은 **`docker compose up -d --build admin` 을 먼저 돌리는 것**이다 —
  안 하면 버튼이 하나 그대로여서 「PR 이 실패했다」로 읽힌다.
  그다음 케이스 다시 불러오기, 그다음 `DEMO-011` 로 실행 하나.
  **형식은 실행이 다 끝난 뒤에 누르고 셋을 동시에 누르지 않는다** — 넘긴 것 ⑦ 에 걸리면
  오류 문장 없이 굳고 푸는 길은 admin 재기동뿐이다

---

## 2026-09-21 — 여러 건 골라 실행하기 (「여러건실행」 PR)

- 완료: **케이스 목록에서 여러 건을 골라 한 번에 실행하는 길을 냈다** (SPEC §8.1 · §8.10 · §8.2).
  목록 줄마다 고르는 칸이 생겼고, 목록 위 버튼 하나가 글자만 바꿔
  `전체 실행하기` ↔ `고른 N건 실행하기` 로 동작한다. 누르면 확인 모달(`RunPickModal.tsx`)이 떠서
  고른 케이스와 만들어질 항목 수를 보여주고 대상 서버·반복·Slack 칸을 받는다.
  케이스마다 접개를 펴면 입력값과 기댓값을 그 자리에서 고칠 수 있다.
  고른 것을 실행 항목으로 바꾸는 순수 로직은 `pickRun.ts`, 건수 셈과 상한은 `runPlan.ts` 한 곳이 정본이다.
  목록 줄 조각을 `CaseRow.tsx` 에서 `CaseListParts.tsx` 로 옮겨 `CaseList.tsx` 를 300줄 안에 맞췄다.
  **계획 검토가 낸 치명 셋을 전부 닫았다** — ① 항목 수 식이 곱셈이라 틀렸던 것
  (데모 11건이 실행 단위로는 13개다) ② 「전체 실행」이 한 쪽만 돌던 것 ③ 「마지막 결과」로 거르면
  한 쪽에 갇히던 것. 명세 다섯 자리와 목업의 곱셈 식도 같이 합으로 맞췄다.
  **검사 기록은 `docs/reviews/2026-09-21-여러건실행.md` 다** — 여기에 옮겨 적지 않는다
- 미완: **칸별 사유를 못 짚는다.** 여러 건 실행이 서버에서 거절당하면
  어느 케이스의 어느 칸이 문제인지 못 짚고 요청 전체에 대한 한 줄만 보여준다.
  `POST /api/runs` 가 `violations` 를 안 내기 때문이고, 내게 하려면 계약 변경이다 —
  `docs/WORKSTREAMS.md` 넘긴 것 ⑬ 으로 넘겼다.
  그 밖에 이번에 새로 넘긴 것 넷(목업 상한 문구 어순 · 좁은 화면 거터 색 띠 ·
  `CaseList.tsx` 300줄 포화 · 증적 문서 결함 셋)도 같은 자리에 있다.
  **WS-E 가 앞서 넘긴 것 중 ① 드롭다운 비밀값 · ② 화면 그물 · ③ `aria-describedby` ·
  ④ 모달 규칙 다섯 · ⑥ `@fastify/static` 취약점 · ⑦ 증적 대기줄 · ⑨ 완료 모달 버튼**은 그대로 열려 있다
- 막힌 것: 없음
- 다음 세션이 알아야 할 것: **진입점은 `apps/admin/src/web/CaseList.tsx` 의 `모으기()` 다.**
  거기서 쪽을 끝까지 돌아 모으고 → `pickRun.ts` 의 `담을것()` 이 비활성과 결과 조건으로 거르고 →
  `RunPickModal.tsx` 가 뜨고 → 다시 `CaseList.tsx` 의 `실행걸기()` 가 실제로 건다.
  **건수 셈을 여기저기 적지 않는다** — `runPlan.ts` 의 `항목수()` 가 정본이고
  `pickRun.ts` 의 `몇건()` 은 그것을 부르기만 한다. 곱셈으로 되돌리면 검사 6개가 4파일에서 깨진다.
  **모달 아래 한 줄은 넷을 겸한다** — 방금 누른 것에 대한 답 · 버튼이 죽은 이유 ·
  걸었다 거절당한 사유 · 건수 안내. **사유가 무조건 이기게 만들면 안 된다** —
  버튼이 죽은 이유와 대상 서버 안내가 가려져 「눌렀는데 아무 일도 안 일어난다」가 되살아난다.
  사유는 **건수 안내만** 덮는다.
  **눌러볼 길은 `docs/WORKFLOW.md` 의 「눌러볼 길」 절에 있다.**
  핵심은 **`docker compose up -d --build admin` 을 먼저 돌리는 것**이다 —
  화면은 번들로 구워져 이미지 안에 들어가므로 안 하면 **고르는 칸이 아예 안 보인다**

### 2026-09-21 — 검사와 고치기 (같은 「여러건실행」 PR)

- 완료: **독립 렌즈 다섯을 돌려 심각 2 · 중대 7 · 보안 높음 0 을 받았고 아홉을 고쳤다** (커밋 일곱).
  심각 둘은 화면 QA 가 브라우저로 실측해 잡았다 — **모달 바닥 버튼이 세로 글자탑**이 된 것(`.modal-foot` 에
  `align-items`·`flex-wrap` 이 없었다. 같은 파일 `.actions` 에는 이미 있던 두 줄이다)과
  **실행하기를 누른 뒤 화면이 침묵**한 것(`거는중` 을 `useRef` 로 들어 다시 그리지 않았다).
  코드 검토가 낸 중대 넷은 **뿌리가 하나**였다 — 고른 것을 `Set<string>`(tcId 만)으로 들어
  실체를 찾으러 목록을 쪽 단위로 다시 받는 구조. **`Map<string, CaseRow>` 로 바꾸니 셋이 같이 닫혔다.**
  명세 셋(§1.1 L12 의 곱셈 · §8.10 의 손으로 센 칸 개수 · §8.1 의 낱말)도 고쳤다.
  **검사 기록은 `docs/reviews/2026-09-21-여러건실행.md` 의 「검사 라운드」 이후 절이다** — 여기에 옮겨 적지 않는다
- 미완: **CI 에 DB 가 없어 DB 를 타는 검사가 통째로 건너뛴다.** 병합을 막는 초록불이 보는 것은
  전체가 아니다 — 몇 대 몇인지는 `npm test` 를 DB 없이 한 번, `DATABASE_URL=… npm test` 로 한 번
  돌리면 그 자리에서 갈린다. `ci.yml` 은 `GUARD` 표면이라 이번 범위 밖이고
  `docs/WORKSTREAMS.md` 넘긴 것 ⑮ 로 넘겼다. 그 밖에 이번에 새로 넘긴 것 다섯(⑯~⑳)이 같은 자리에 있다.
  **넘긴 것 ⑫(`CaseList.tsx` 300줄 포화)는 풀렸다** — 206줄이 됐다
- 막힌 것: 없음
- 다음 세션이 알아야 할 것: **진입점이 `apps/admin/src/web/useRunPick.ts` 로 옮겼다.**
  위 항목이 적은 `CaseList.tsx` 의 `모으기()` 는 이제 거기 있다 — `CaseList.tsx` 는 `뽑기.모으기()` 를 부르기만 한다.
  길은 그대로다 — `useRunPick.ts` 의 `쪽모으기()` 가 쪽을 끝까지 돌아 모으고 →
  `pickRun.ts` 의 `담을것()` 이 거르고 → `RunPickModal.tsx` 가 뜨고 → `실행걸기()` 가 실제로 건다.
  **고른 것은 `Map<tcId, CaseRow>` 다** — tcId 만 들면 실체를 찾으러 쪽을 되돌아야 하고
  그 길에서 결과 칩과 검색 조건이 고른 것을 말없이 버린다. 되돌리지 마라.
  **쪽을 끊는 수는 `쪽상한`(40, 쪽 수)이지 `runPlan` 의 `상한`(1000, 실행 항목 수)이 아니다** —
  둘은 세는 단위가 다르다. 빌려 쓰면 목록이 길 때 앞부분만 조용히 걸린다.
  **화면에 보여야 하는 상태는 `useRef` 로 들지 않는다** — 값은 맞는데 화면이 안 따라온다

## 2026-09-21 (2차 — 실행 상태를 보여준다, PR #36)

- 완료: **진행 상자**와 **직전 실행 비교**. 1차(PR #35)가 닫은 불만 ②③④⑤에 이어 ①⑥을 닫았다.
  - `runProgress.ts` — `RunDetail` 하나에서 막대·진행 중인 항목·방금 끝난 것을 낸다 (순수 함수, 53줄)
  - `RunProgressModal.tsx` — **한 상자가 「도는 중」과 「끝났다」를 갈라 그린다.**
    완료 모달을 새로 만들지 않고 `RunResult.tsx` 에 박혀 있던 JSX 를 옮겨 왔다
  - `reporting/insights.ts` — 직전 실행 비교 + 실패 사유 묶기 (173줄).
    `GET /api/runs/:runId/insights` 신설이 이번 PR 의 유일한 계약 변경이다
  - `RunInsights.tsx` — 비교 칸과 사유 묶음. `RunResultRow.tsx` 로 줄 그리기를 떼어
    `RunResult.tsx` 를 **353 → 246줄**로 내렸다 (CLAUDE.md §3 의 300줄)
  - 1차가 넘긴 것 둘도 닫았다 — 값 칩의 객체 기본값(`[object Object]`)과
    검사 그물이 없던 화면 셋(`RunSetup` · `ItemDetail` · `SettingsService`)
- 미완: **A-2(절차 단위 진행)는 게이트 0 에서 미뤘다.** 계약 블록 둘이
  `docs/plans/2026-09-21-청사진스킨.md` 에 `상태: 대기` 로 그대로 있다.
  `packages/kit/src/runtime/step.ts` 를 건드려야 해서 — 모든 케이스가 지나는 자리다
- 막힌 것: 없음
- 다음 세션이 알아야 할 것:
  **① 「실행 중」과 「대기」를 가르려 하지 마라.** `counts.running` 은 「안 끝난 것 전부」이고
  `run_item.started_at` 은 실행을 걸 때 한꺼번에 박힌 뒤 갱신되지 않는다. 가를 신호가 DB 에 없다.
  「진행 중인 항목」은 근사치이고 `runProgress.ts` 의 그 자리 주석에 이유가 셋 적혀 있다.
  정확해지려면 러너 계약(`progressUrl`)이 와야 한다.
  **② 상자는 하나지만 여는 이유는 둘이다** — `진행열림 || 끝났다고알릴까말까`.
  `onClose` 에서 `본것으로적는다()` 를 무조건 부르면 **진행 상자를 닫은 사람이 완료 알림을 영영 못 받는다.**
  `RunResult.test.tsx` 의 「진행 상자를 닫은 뒤 실행이 끝나면 완료 상자가 뜬다」가 그 회귀 그물이다.
  **③ 회차 접기는 갈래가 셋이다** — `bool_or(FAIL)` → FAIL, `bool_and(PASS)` → PASS, 나머지 NA.
  두 갈래로 접으면 미실행이 실패로 둔갑한다. 화면 쪽 같은 규칙은 `web/group.ts` 의 `회차요약()` 이고
  **둘이 다른 말을 하면 한 화면이 스스로를 부정한다.**
  **④ 진입점은 `apps/admin/src/web/RunResult.tsx`** — 거기서 `RunProgressModal`(띄울지 판단)과
  `RunInsights`(끝났을 때만 조회)와 `RunResultRow`(줄 그리기)로 갈라진다.

### 브라우저 확인 (2026-09-21, 같은 PR)

**결함 하나를 찾았다** — 진행 막대가 모달 안에서 높이 0 으로 줄어 통째로 안 보였다.
색도 비율도 맞아서 단위 검사는 초록이었다. `.modal-body` 가 세로 flex 라 `flex-shrink` 가 6px 를 먹었다.
`styles.test.ts` 에 그 규칙이 있는지 보는 단언을 더했고, 규칙을 빼서 빨개지는 것을 확인했다.

**확인한 것과 방법.**

| 무엇 | 어떻게 | 결과 |
|------|-------|------|
| 진행 상자 · 완료 상자 · 리포트 비교 칸 | 컴포넌트를 고정 데이터로 띄우는 임시 페이지 (커밋 안 함) | 계획대로 |
| 375px | **iframe 셋** (창 리사이즈는 뷰포트를 안 바꾼다 — LEARNINGS 가 적은 그대로다) | 가로 넘침 0 · **세로로 흐른 글자 0** |
| 판정 색 | `getComputedStyle` 로 실측 | 새로깨짐·계속깨짐 `--fail` · 고쳐짐 `--pass` · **그대로(이번 미실행) `--na`** |
| 어두운 테마 | `styles.css` 를 훑었다 | **이 제품에 어두운 테마가 없다.** 확인 항목이 아니다 |

**못 한 것 하나** — 로그인해서 실제 실행을 거는 끝에서 끝까지의 흐름은 안 봤다.
비밀번호를 치지 않기 때문이다. 폴링이 도는지는 `RunResult.test.tsx` 의 가짜 타이머 검사가 대신 본다
(닫은 뒤 2초에 `api.run` 이 다시 불리는 것을 단언한다). **화면을 여는 자리와 폴링 자리는 갈라져 있다.**

## 2026-09-21 — 화면 리뉴얼 ① (껍데기와 디자인 언어)

- 완료: 껍데기를 **사이드바 + 헤더 + 본문 + 푸터**로 바꿨다. 토큰을 따뜻한 아이보리 ·
  짙은 사이드바 · 테라코타 악센트로 갈았다. 집계 띠를 케이스 목록에 달았다.
  명세 정본(공통/5-화면공통 §8)과 색인 네 곳, SPEC 밖 문서 다섯을 같이 고쳤다.
- 미완: **PR② 가 곧바로 따라온다** — 목록 파라미터 인라인 입력 · 고르는 칸 · 상세 펼침 ·
  `GET /cases/:tcId/history` · 옛 클래스(`.band` · `.nav`) 정리 · **최근 판정 흐름(`.spark`)**.
  판정 흐름은 만들었다가 **게이트 2 에서 다시 뺐다** — 최근 N회 데이터가 PR② 의 API 에서 오는데
  조각과 CSS 만 먼저 있으면 검사는 초록인데 화면에는 안 뜬다.
  되살릴 때의 규칙(판정마다 높이를 다르게 + 글자 동반)은 `docs/design-mockup.html` 에 그대로 있다.
- 막힌 것: 없음. 다만 하위 작업자 둘이 워치독(600초 무응답)으로 죽어 남긴 작업을 직접 마무리했다.

### 다음 세션이 알아야 할 것

- **옛 클래스를 일부러 안 지웠다.** `.band` · `.nav` · `.notice` · `.svc-dot` 을 남기고
  `.side` · `.head` · `.foot` 을 더했다. 그 이름으로 단언하는 검사 파일이 **일곱**이라
  지우면 한꺼번에 깨진다. 지우는 것은 PR② 몫이고, 그때 검사 일곱을 같이 고쳐야 한다.
- **`styles.test.ts` 가 이제 토큰 정의까지 본다.** `var(--x)` 를 전부 긁어 `:root` 와 대조한다.
  새 토큰을 쓰면 `:root` 에 넣어야 통과한다. `--svc` 만 예외다.
- **제품 CSS 실측 하네스가 값을 한다.** 정적 HTML 에서 `<link>` 로 `styles.css` 원본을 불러
  `getComputedStyle` 로 재면 jsdom 이 못 보는 것이 보인다. 이번에 `select` 의 흰 상자를
  그렇게 잡았다. **목업으로 재면 안 된다** — 목업 CSS 는 사본이다.
- **화면이 못 받는 값 둘** — 푸터에 넣고 싶던 **저장소 주소**와 **러너 상태**는 통로가 없다.
  `ServiceRow` 에 저장소 칸이 없고 러너 상태를 주는 API 도 없다. 지어내지 않고 비워 뒀다.
  넣으려면 SPEC §7 을 먼저 고쳐야 한다.
- 진입점은 `apps/admin/src/web/Shell.tsx` 다. `header?: React.ReactNode` props 를 받아
  화면이 제목과 주 행동을 넘긴다 — 껍데기가 제목을 지어내지 않는다.

## 2026-09-21 — 화면 리뉴얼 ② (목록에서 값을 고치고 상세를 펼친다)

- 완료: 케이스 목록 줄을 **일하는 자리**로 바꿨다. 고칠 수 있는 입력 칸(넷까지) ·
  `상세` 펼침 · 디바이스마다의 최근 판정 흐름. 서버는 `last-by-case` 응답에 `recent` 를 더한 것 하나다.
  옛 껍데기 이름(`band`·`nav`·`notice`·`svc-dot`)을 걷어냈고 사이드바 첫 자리 이름을
  `테스트케이스 목록` 으로 바꿨다 (사용자 요청).
- 미완: 없음
- 막힌 것: 없음

### 다음 세션이 알아야 할 것

- **입력 칸을 새로 그리지 않는다.** `CaseRowParams` 는 `Form` 을 감싸기만 한다.
  `Form` 이 비밀값 가림과 `autoComplete="new-password"` 를 들고 있고, 그 한 줄이 빠지면
  **이 관리 화면의 로그인 비밀번호가 테스트 입력값으로 서버에 가** `run_item.params` 에 평문으로 남는다.
  화면과 증적에는 `********` 로 가려져 **들어간 줄도 모른다.**
- **줄에 보이는 칸은 넷까지.** 상한의 정본은 `CaseRowParams.tsx` 의 `줄에보일칸수` 다.
  객체·배열 기본값은 줄에서 뺀다 — `Form` 이 그것을 `JSON.stringify` 로 펴는데 목록에 원문은 금지다.
- **고친 값의 수명이 명세에 있다** (카탈로그 §8.1). 쪽을 넘기면 남고 **서비스를 바꾸면 버린다.**
  목록과 실행 모달이 **같은 표**(`글자표`, `pickRun.ts`)를 쓴다.
- **상세는 폈을 때 한 번만 부르고 접어도 안 버린다.** `ItemDetail.tsx:190` 의
  `opened ? … : Promise.resolve(null)` 모양을 그대로 따르면 접을 때마다 다시 부른다 — 그러면
  §8.1 의 예외가 위반으로 바뀐다.
- **흐름 막대는 디바이스마다다.** 목업의 그 칸은 배지가 하나라 구조가 다르다.
  **값은 목업이 정본, 자리는 제품 구조가 정본**이다.
- 진입점은 `apps/admin/src/web/CaseListParts.tsx` 의 `케이스줄` 이다.

## 2026-09-22 — 화면 리뉴얼 ③ (헤더 자리와 남은 화면 넷) · **리뉴얼 끝**

- 완료: 화면마다 **머리**(제목·부제·주 행동)가 본문 면 바깥에 선다. 실행 기록에 집계 띠와
  검색·거르개가 붙었고 거르는 일은 서버가 한다. 실행 결과·설정·로그인이 목업 수준이 됐다.
  `Shell` 의 죽은 `header` 통로를 걷어냈다. 서버는 `GET /api/runs` 에 `q`·`state`·`env` 와
  `summary` 를 더한 것 하나다.
- 미완: 없음. **확정 목업의 일곱 화면이 전부 옮겨졌다** — 완료 기준(§11)에 그 줄을 넣었다
- 막힌 것: 없음

### 다음 세션이 알아야 할 것

- **화면 머리는 껍데기가 아니라 화면이 그린다** (`Head.tsx`). 껍데기에 통로를 뚫으면
  부제(동적)와 행동(상태를 탐)을 위로 올려 보내야 해서 배선만 는다.
  **PR① 이 실제로 그 통로를 뚫었고 부르는 곳이 하나도 없었다** — 그것이 근거다.
- **실행 목록을 화면이 거르면 안 된다.** 쪽으로 나뉘어 오므로 받은 쪽만 거르면 뒤쪽이 빠진다.
  **집계도 같은 조건으로 좁아진다** — `queries.ts` 의 `거르는조건` 을 목록과 집계가 같이 쓴다.
- **`state=failed` 는 `test_run.status` 가 아니다.** 집계에서 나오므로 `HAVING` 으로 걸린다.
  `runs.test.ts` fixture 에 **실패 없는 실행**이 한 건 있고, 그것이 없으면
  그 검사가 안 걸러도 통과하는 항진명제가 된다.
- **띠의 색은 `판정` 이 있을 때만 나온다** (`칸띠`). 라벨에 색을 매면 `평균 소요` 같은 칸에도
  판정 색을 칠할 수 있게 된다 — 타입으로 막아 뒀다.
- 진입점은 `apps/admin/src/web/Head.tsx` 와 `RunList.tsx` 다.

## 2026-09-22 ②

- 완료: 목록 화면 다듬기 12건 + 전 화면 다국어(한국어/English). PR #53.
  케이스 목록 표머리와 입력 칸 분리, 케이스 상세·실행 결과를 모달로,
  사이드바 언어 고르개, 서비스 색을 화면에서 걷음, 워딩 6건 교정.
- 미완: 없음 (계획한 할 일 15개 전부 완료)
- 막힌 것: 없음
- 다음 세션이 알아야 할 것:
  - **다국어 키는 한국어 원문이다.** 영어 표(`messages/*.ts`) 한 벌뿐이고 한국어는 표를
    안 거친다. `t()` 로 감싸지 않은 한국어가 소스에 남으면 `messages.test.ts` 가 빨개진다.
  - **표에 키가 있다 ≠ 그 자리가 옮겨진다.** `mask.ts` 의 `'예'/'아니오'`가 `t()`를 안 타는데도
    같은 낱말이 *다른 파일*의 표 키로 있어서 처음엔 검사를 통과했다. 자기검사에서 손으로 찾았고,
    「화면 글자가 있는 파일은 전부 t()를 부른다」검사를 추가해 기계가 보게 했다.
  - **RunResult 의 `상자안` 깃발**이 모달 안에서 진행·완료 모달을 다시 여는 것을 막는다.
    이 가드를 지키는 검사가 처음엔 fixture 가 끝난 실행이라 항진명제였다 — 부숴서 확인하는 습관이
    구현 중에 진짜로 값을 냈다.
  - **서비스 색은 화면 어디에도 없다** (SPEC §8, 2026-09-22). `service.color` DB 칸은 남아 있다.
  - 진입점은 `apps/admin/src/web/i18n.ts`(번역 규칙)와 `messages.test.ts`(그물)다.

## 2026-09-26 — 역방향 ①: 미확정 칸·배지·나이 · 설정 계정 칸 (PR #78)

- 완료: 실행 결과·실행 기록·진행 막대·완료 모달·알림 줄의 미확정 묶음 · 미확정만 돌린 실행은 판정 색 없음 · 결과 행 박제 사유 ·
  케이스 목록 배지와 머리 나이 · 설정 대상 서버 줄 테스트 계정 칸 · DESIGN.md 「판정 표기」 미확정 표 · 목업 예시
- 미완: ② 작성 화면(대조 체크박스·대상 서버·시작 주소·차이 목록·「표시 못 함」) — WS-작성 서버와 같이. Grafana 나이는 WS-D 로 넘겼다
- 막힌 것: 없음

### 다음 세션이 알아야 할 것

- **미확정 글자는 `web/unconfirmed.ts` 한 곳이다** — `미확정글자()`·`끝난미확정()`·`판정없음()`·`미확정나이()`. 자리마다 따로 만들지 않는다
- 새 칸(`counts.unconfirmed`·`item.unconfirmed`·`CaseRow.unconfirmed`·`EnvRow.loginId`)은 **선택 칸**이다 — 기존 화면 검사의 가짜 응답을 안 고치려고. 없으면 미확정 0 · 계정 없음으로 읽는다
- 설정 편집기 `줄` 에 `원래키`·`새비밀번호` 가 늘었다. `새비밀번호 === null` 이면 비밀번호를 안 보내 서버가 유지한다
- 화면을 실제로 띄워 눈으로 보지는 못했다 — 미확정 데이터가 있는 실행이 아직 없다. jsdom 화면 검사로 대신했다. WS-작성이 첫 미확정 케이스를 만들면 밝게·어둡게로 한 번 본다
